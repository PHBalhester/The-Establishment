# Finding: H130 - No Feature Flags for Mainnet Switch Points

## Status: POTENTIAL
## Confidence Score: 7

## Rationale
Searching the codebase for "MAINNET TODO" comments reveals only one active instance in source code: `app/providers/providers.tsx:34` which marks the RPC URL switch point. However, the code already has a runtime override via `process.env.NEXT_PUBLIC_RPC_URL` (line 35), meaning the mainnet switch can be done via environment variable without code changes. The project maintains `Docs/mainnet-checklist.md` which tracks all devnet-to-mainnet switch points. The concern is that scattered TODO comments instead of a centralized feature flag system makes the mainnet migration error-prone -- missing one switch point could cause mainnet transactions to hit devnet RPCs or vice versa. That said, the current approach (env vars + checklist document) is a reasonable pattern for a pre-mainnet project, and the single remaining `MAINNET TODO` in active code is minimal.

## Code Evidence
```typescript
// app/providers/providers.tsx:34-35
// MAINNET TODO: Switch to WalletAdapterNetwork.MainnetBeta + mainnet RPC URL.
const endpoint = process.env.NEXT_PUBLIC_RPC_URL ?? DEVNET_RPC_URL;
```

## Severity Estimate
LOW -- The project uses environment variables for runtime configuration and maintains a mainnet checklist document. The single `MAINNET TODO` in active source code already has an env var override. Risk is operational (human error during migration), not a code vulnerability.
