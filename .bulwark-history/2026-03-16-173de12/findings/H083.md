# Finding: H083 - IDL Name Parameter Not Validated

## Status: NOT VULNERABLE
## Confidence Score: 8

## Rationale
The `loadIdl` function at `scripts/crank/crank-provider.ts:113-122` takes a `name` string and constructs a file path: `path.join(idlDir, \`${name}.json\`)`. This is only called with 5 hardcoded string literals at lines 125-129: `"epoch_program"`, `"amm"`, `"tax_program"`, `"staking"`, `"transfer_hook"`. It is a private inner function (not exported), called only from `loadCrankPrograms()`. No user or network input reaches this function.

## Code Evidence
```typescript
// crank-provider.ts:113 -- private inner function
function loadIdl(name: string): any {
  const idlPath = path.join(idlDir, `${name}.json`);
  // ...
}

// lines 125-129 -- all callers are hardcoded
epochProgram: new anchor.Program(loadIdl("epoch_program"), provider),
amm: new anchor.Program(loadIdl("amm"), provider),
```

No path traversal risk -- all inputs are hardcoded string literals.
