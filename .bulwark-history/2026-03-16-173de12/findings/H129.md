# Finding: H129 - Vault Conversion Rate Hardcoded at 100

## Status: NOT VULNERABLE
## Confidence Score: 10

## Rationale
The hypothesis asks whether the client-side `VAULT_CONVERSION_RATE = 100` in `shared/constants.ts:70` matches the on-chain value. The on-chain constant at `programs/conversion-vault/src/constants.rs:7` is `pub const CONVERSION_RATE: u64 = 100`. The conversion logic at `programs/conversion-vault/src/instructions/convert.rs:103` uses `let out = amount_in / CONVERSION_RATE` for faction->PROFIT and `checked_mul(CONVERSION_RATE)` at line 109 for PROFIT->faction. The client-side `quoteVaultConvert` in `app/lib/swap/quote-engine.ts:241-243` uses `Math.floor(inputAmount / conversionRate)` and `inputAmount * conversionRate` respectively. Both sides use the same rate (100) and the same arithmetic (integer division with floor for faction->PROFIT, multiplication for PROFIT->faction). The on-chain test at `programs/conversion-vault/tests/test_vault.rs:275-276` explicitly asserts `CONVERSION_RATE == 100`.

## Code Evidence
```rust
// programs/conversion-vault/src/constants.rs:7
pub const CONVERSION_RATE: u64 = 100;
```

```typescript
// shared/constants.ts:70
export const VAULT_CONVERSION_RATE = 100;
```

Exact match. Both use integer 100 with identical division/multiplication semantics.
