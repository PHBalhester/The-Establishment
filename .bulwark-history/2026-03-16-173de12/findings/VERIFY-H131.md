# Verification: H131
**Status:** NOT_FIXED
**Evidence:** Webhook URL path (`/api/webhooks/helius`) is standard and discoverable. The auth secret (HELIUS_WEBHOOK_SECRET) protects against unauthorized access. URL discoverability alone is not a vulnerability when auth is enforced.
