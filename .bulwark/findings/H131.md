# H131: npm Supply Chain Guard (H003)

**Severity**: Tier 3 (Informational)
**Status**: NOT VULNERABLE
**Category**: Supply Chain Security (npm)
**Verified**: 2026-03-21

---

## Finding

Supply chain attacks via transitive dependency manipulation are mitigated by committing lock files to version control.

## Target Files

- `.gitignore` — verified no exclusions for lock files
- `railway.toml` — build/deploy configuration
- `Cargo.lock` — Rust dependency lock
- `package-lock.json` (root + docs-site/) — npm dependency locks

## Verification Results

### Lock Files Committed ✓

All lock files are tracked in git and committed:

```
Cargo.lock                    171 KB  committed Mar  9 21:15
package-lock.json           664 KB  committed Mar 15 09:26
docs-site/package-lock.json  245 KB  committed Mar 12 20:34
```

**Latest commit**: `3f2a35c feat(97-02): create Squads 2-of-3 multisig on devnet`

### .gitignore Check ✓

No `.gitignore` rules exclude `Cargo.lock` or `package-lock.json`. Both files are fully tracked.

### Git Status ✓

All lock files are clean (no uncommitted changes, no staging state).

## Verdict

**NOT VULNERABLE**

Lock files are properly committed, preventing:
- Dependency injection via version mismatch (CI reproducibility)
- Transitive supply chain attacks
- Accidental breaking updates in CI/CD

This satisfies the H003 requirement: "Commit Cargo.lock and package-lock.json to prevent supply chain attacks."

## Related Controls

- **H003**: npm Supply Chain
- **Section**: CRYPTO-04 (Dependency Management)

---

*Bulwark Inspector*
Devnet Phase 102
