---
phase: 71-curve-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - programs/bonding_curve/Cargo.toml
  - programs/bonding_curve/src/lib.rs
  - programs/bonding_curve/src/constants.rs
  - programs/bonding_curve/src/state.rs
  - programs/bonding_curve/src/error.rs
  - programs/bonding_curve/src/instructions/mod.rs
autonomous: true

must_haves:
  truths:
    - "The bonding_curve program compiles with `cargo check` (no instruction logic yet, just types)"
    - "CurveState struct serializes to exactly 199 bytes (191 data + 8 discriminator)"
    - "All curve constants match Bonding_Curve_Spec.md Section 3.2 and 7.1 exactly"
    - "Feature-gated mint addresses resolve correctly for devnet and localnet features"
  artifacts:
    - path: "programs/bonding_curve/Cargo.toml"
      provides: "Anchor 0.32.1 + Token-2022 dependencies, proptest/litesvm dev-deps, feature flags"
    - path: "programs/bonding_curve/src/lib.rs"
      provides: "Program entrypoint with declare_id!, module declarations, instruction dispatch stubs"
    - path: "programs/bonding_curve/src/constants.rs"
      provides: "All curve constants (P_START, P_END, TOTAL_FOR_SALE, etc.), PDA seeds, feature-gated mint addresses"
    - path: "programs/bonding_curve/src/state.rs"
      provides: "CurveState, CurveStatus, Token enum with LEN constant and size validation test"
    - path: "programs/bonding_curve/src/error.rs"
      provides: "CurveError enum with all error variants needed for Phase 71-72"
    - path: "programs/bonding_curve/src/instructions/mod.rs"
      provides: "Empty re-export module (populated by subsequent plans)"
  key_links:
    - from: "programs/bonding_curve/src/state.rs"
      to: "programs/bonding_curve/src/constants.rs"
      via: "CurveState uses constants for seed references"
      pattern: "use crate::constants"
    - from: "programs/bonding_curve/src/lib.rs"
      to: "programs/bonding_curve/src/instructions/mod.rs"
      via: "Module declaration and instruction dispatch"
      pattern: "pub mod instructions"
---

<objective>
Create the bonding curve program scaffold -- Cargo.toml, entrypoint, all type definitions (state, constants, errors), and empty instruction module. This is the foundation that all subsequent plans build on.

Purpose: Every subsequent plan (math, instructions, tests) imports types from this scaffold. Getting the type definitions, constants, and account sizes exactly right from the start prevents cascading errors. The CurveState struct size is critical -- an incorrect `space` parameter causes runtime deserialization failures.

Output: A compiling (but non-functional) Anchor program with all types matching the Bonding_Curve_Spec.md exactly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-curve-foundation/71-CONTEXT.md
@.planning/phases/71-curve-foundation/71-RESEARCH.md

# Existing program patterns to follow:
@programs/conversion-vault/Cargo.toml
@programs/conversion-vault/src/lib.rs
@programs/conversion-vault/src/constants.rs
@programs/conversion-vault/src/state.rs
@programs/conversion-vault/src/error.rs
@programs/amm/Cargo.toml

# Spec (source of truth for all constants and types):
@docs/Bonding_Curve_Spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create program directory with Cargo.toml and lib.rs</name>
  <files>
    programs/bonding_curve/Cargo.toml
    programs/bonding_curve/src/lib.rs
    programs/bonding_curve/src/instructions/mod.rs
  </files>
  <action>
Create the program directory structure: `programs/bonding_curve/src/` and `programs/bonding_curve/src/instructions/`.

**Cargo.toml** -- Follow the AMM Cargo.toml pattern exactly:
```toml
[package]
name = "bonding-curve"
version = "0.1.0"
description = "Dr Fraudsworth Bonding Curve - Linear price discovery for CRIME and FRAUD"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
anchor-lang = { version = "0.32.1", features = ["init-if-needed"] }
anchor-spl = { version = "0.32.1", features = ["token_2022"] }

[dev-dependencies]
proptest = "1.9"
litesvm = "0.9.1"
solana-sdk = "2.2"
solana-program = "2.2"
spl-token-2022 = "8.0"
spl-associated-token-account = "7.0"
solana-address = "2.0"
solana-keypair = "~3.1"
solana-signer = "~3.0"
solana-message = "~3.0"
solana-transaction = { version = "~3.0", features = ["verify"] }
solana-account = "3.3"
solana-instruction = "~3.1"
sha2 = "0.10"

[features]
default = []
devnet = []
localnet = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]
```

**lib.rs** -- Anchor program entrypoint with all module declarations. Use a placeholder program ID (will be replaced after first `anchor build` generates a keypair). Declare all modules: `constants`, `error`, `state`, `math` (stub), `instructions`. Add instruction dispatch stubs for: `initialize_curve`, `fund_curve`, `start_curve`, `purchase`. Each stub just calls `Ok(())` for now -- subsequent plans fill in the real logic.

**instructions/mod.rs** -- Empty module file (just a comment explaining it will be populated by Plans 03 and 04).

IMPORTANT: The program ID in `declare_id!` should use a placeholder. After `anchor build` generates the keypair, the ID will be updated. Follow existing convention -- use `Pubkey::default()` style placeholder like `"11111111111111111111111111111111"` temporarily.

Actually, use a real-looking random ID. It will be overwritten after first build. The important thing is that the file compiles.
  </action>
  <verify>
Run `source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && cargo check -p bonding-curve 2>&1 | tail -5` -- should show "Checking bonding-curve" with no errors (warnings OK).
  </verify>
  <done>
Cargo.toml exists with correct dependencies matching AMM pattern. lib.rs compiles with module declarations and stub instruction dispatch. instructions/mod.rs exists as empty module.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create constants.rs, state.rs, and error.rs with spec-exact types</name>
  <files>
    programs/bonding_curve/src/constants.rs
    programs/bonding_curve/src/state.rs
    programs/bonding_curve/src/error.rs
  </files>
  <action>
**constants.rs** -- All curve constants from Bonding_Curve_Spec.md Sections 3.2, 6.1, 6.2, 7.1. Follow the conversion-vault constants.rs pattern exactly for feature-gated mints.

Constants to define (use the EXACT values from the spec -- do not recalculate):
- `PRECISION: u128 = 1_000_000_000_000` (1e12, Section 4.4)
- `P_START: u128 = 900` (lamports-per-million-tokens scaling, Section 4.1)
- `P_END: u128 = 3_450` (Section 4.1)
- `TOTAL_FOR_SALE: u128 = 460_000_000_000_000` (460M with 6 decimals, Section 3.2)
- `TARGET_TOKENS: u64 = 460_000_000_000_000` (same as u64 for state comparison, Section 7.1)
- `TARGET_SOL: u64 = 1_000_000_000_000` (1000 SOL in lamports, Section 7.1)
- `MAX_TOKENS_PER_WALLET: u64 = 20_000_000_000_000` (20M with 6 decimals, Section 6.1)
- `MIN_PURCHASE_SOL: u64 = 50_000_000` (0.05 SOL in lamports, Section 6.2)
- `DEADLINE_SLOTS: u64 = 432_000` (48 hours at 400ms/slot, Section 7.1)
- `TOKEN_DECIMALS: u8 = 6`
- `SELL_TAX_BPS: u64 = 1500` (15%, Section 4.5 -- needed for Phase 72 but define now)
- `BPS_DENOMINATOR: u64 = 10_000`

PDA seeds (Section 5.3 + 5.7):
- `CURVE_SEED: &[u8] = b"curve"`
- `CURVE_TOKEN_VAULT_SEED: &[u8] = b"curve_token_vault"`
- `CURVE_SOL_VAULT_SEED: &[u8] = b"curve_sol_vault"`
- `TAX_ESCROW_SEED: &[u8] = b"tax_escrow"`

Feature-gated mint addresses: Use the EXACT same pattern as conversion-vault/src/constants.rs. Use the same devnet mint addresses as the existing programs (CRIME=`42WFgfkXy4M5bzcReUCuyQDUmNUjrPpw8DY2r7DwTqAr`, FRAUD=`B73svJGjh78qNpcQtKXcL2PAS2FgPZdbFkY8j3jHBfj6`). Mainnet placeholders as `Pubkey::default()`.

**state.rs** -- CurveState, CurveStatus, Token exactly matching Bonding_Curve_Spec.md Section 5.1-5.2. Include `CurveState::LEN` constant calculated as `8 + 191 = 199` bytes. Add a `#[cfg(test)]` unit test that constructs a CurveState, serializes it with Borsh, and asserts the length matches `LEN - 8` (191 bytes, minus discriminator since Borsh serialize doesn't include it).

Token enum:
```rust
#[derive(AnchorSerialize, AnchorDeserialize, Clone, Copy, PartialEq, Eq)]
pub enum Token {
    Crime,
    Fraud,
}
```

CurveStatus enum: exactly as spec Section 5.2 (Initialized, Active, Filled, Failed, Graduated).

CurveState struct: exactly as spec Section 5.1 with all 15 fields. Include the `is_refund_eligible` helper method from spec Section 5.2.

**error.rs** -- CurveError enum with all error variants needed for Phase 71 (buy) and Phase 72 (sell). Define them all now to avoid modifying the file later:
- `Overflow` -- arithmetic overflow
- `CurveNotActive` -- curve not in Active status for buys
- `CurveNotActiveForSell` -- curve not in Active status for sells (Phase 72)
- `DeadlinePassed` -- deadline slot exceeded
- `BelowMinimum` -- below MIN_PURCHASE_SOL
- `WalletCapExceeded` -- ATA balance + tokens > 20M
- `SlippageExceeded` -- minimum_tokens_out / minimum_sol_out not met
- `InvalidStatus` -- wrong status for operation
- `CurveNotFunded` -- vault not funded before start
- `ZeroAmount` -- zero token amount (Phase 72)
- `InsufficientTokenBalance` -- not enough tokens to sell (Phase 72)
- `EscrowNotConsolidated` -- escrow not merged before refund (Phase 73)
- `NotRefundEligible` -- curve not in refundable state (Phase 73)
- `CurveAlreadyFilled` -- curve already at target (Phase 73)
- `InsufficientTokensOut` -- calculated tokens out is zero (dust buy)

Follow the conversion-vault error.rs pattern with `#[error_code]` and `#[msg("...")]` on each variant.
  </action>
  <verify>
Run `source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && cargo test -p bonding-curve -- --nocapture 2>&1 | tail -20` -- the CurveState serialization size test should pass, confirming LEN is correct. Also `cargo check -p bonding-curve` should succeed.
  </verify>
  <done>
constants.rs has all spec constants matching Bonding_Curve_Spec.md exactly. state.rs has CurveState (199 bytes confirmed by test), CurveStatus, Token enums. error.rs has all error variants for Phases 71-73. `cargo check -p bonding-curve` succeeds. Serialization size test passes.
  </done>
</task>

</tasks>

<verification>
1. `cargo check -p bonding-curve` succeeds (program compiles with all type definitions)
2. `cargo test -p bonding-curve` passes the CurveState size verification test
3. All constants match Bonding_Curve_Spec.md (spot-check P_START=900, P_END=3450, TARGET_TOKENS=460_000_000_000_000, TARGET_SOL=1_000_000_000_000)
4. Feature-gated mint addresses compile under both `--features devnet` and default
5. CurveState::LEN == 199 (verified by test)
</verification>

<success_criteria>
The bonding_curve program scaffold compiles cleanly. CurveState serializes to exactly 199 bytes. All constants match the spec. Subsequent plans can import types, constants, and errors without modification.
</success_criteria>

<output>
After completion, create `.planning/phases/71-curve-foundation/71-01-SUMMARY.md`
</output>
