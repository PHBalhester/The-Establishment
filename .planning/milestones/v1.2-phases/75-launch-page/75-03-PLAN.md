---
phase: 75-launch-page
plan: 03
type: execute
wave: 2
depends_on: [75-01]
requirements: [PAGE-01, PAGE-02, PAGE-04, PAGE-05, PAGE-06, PAGE-08]
files_modified:
  - app/app/launch/page.tsx
  - app/components/launch/LaunchScene.tsx
  - app/components/launch/PressureGauge.tsx
  - app/components/launch/CountdownTimer.tsx
  - app/components/launch/CurveStats.tsx
  - app/components/launch/LaunchWalletButton.tsx
  - app/components/launch/DocsModal.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "/launch route renders a full-bleed steampunk scene page"
    - "Two pressure gauges show % progress based on solRaised / 1000 SOL"
    - "Countdown timer displays approximate time remaining from deadline_slot"
    - "CurveStats shows SOL raised, market cap (USD), current price, tax escrow per curve"
    - "Wallet connect button is accessible as floating overlay"
    - "Docs button opens iframe modal to Nextra documentation"
    - "Page layout is mobile responsive (stacked on small screens)"
  artifacts:
    - path: "app/app/launch/page.tsx"
      provides: "/launch route entry point"
    - path: "app/components/launch/LaunchScene.tsx"
      provides: "Background image + overlay container (FactoryBackground pattern)"
    - path: "app/components/launch/PressureGauge.tsx"
      provides: "Needle rotation driven by CSS transform based on fill %"
    - path: "app/components/launch/CountdownTimer.tsx"
      provides: "Slot-based countdown display (~Xh Ym format)"
    - path: "app/components/launch/CurveStats.tsx"
      provides: "SOL raised, market cap, current price, tax escrow counters"
    - path: "app/components/launch/LaunchWalletButton.tsx"
      provides: "Floating wallet connection button"
    - path: "app/components/launch/DocsModal.tsx"
      provides: "Docs iframe modal triggered by button"
  key_links:
    - from: "app/app/launch/page.tsx"
      to: "app/hooks/useCurveState.ts"
      via: "useCurveState() hook providing real-time data"
      pattern: "useCurveState"
    - from: "app/components/launch/PressureGauge.tsx"
      to: "useCurveState"
      via: "solRaised prop driving needle rotation"
      pattern: "rotate|transform|solRaised"
    - from: "app/components/launch/CountdownTimer.tsx"
      to: "app/hooks/useCurrentSlot.ts"
      via: "deadlineSlot - currentSlot for remaining time"
      pattern: "useCurrentSlot|deadlineSlot"
---

<objective>
Build the launch page shell: /launch route, full-bleed scene layout with background image, two pressure gauges with CSS-driven needle rotation, countdown timer, curve statistics display, wallet connection button, and docs modal.

Purpose: This plan creates the visible page structure that users see. It consumes useCurveState data (Plan 01) to drive all real-time displays. The buy/sell panel (Plan 04) and state machine UI (Plan 05) will be composed into this layout.

Output: A functional /launch page showing real-time curve progress, countdown, stats, with wallet connection and docs access.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-launch-page/75-CONTEXT.md
@.planning/phases/75-launch-page/75-RESEARCH.md
@.planning/phases/75-launch-page/75-01-SUMMARY.md

@app/app/page.tsx
@app/components/scene/FactoryBackground.tsx
@app/hooks/useCurrentSlot.ts
@app/hooks/useSolPrice.ts
@app/hooks/useTokenBalances.ts
@app/components/wallet/
@app/components/modal/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Launch page route + scene layout + pressure gauges</name>
  <files>
    app/app/launch/page.tsx
    app/components/launch/LaunchScene.tsx
    app/components/launch/PressureGauge.tsx
  </files>
  <action>
  1. Create `app/components/launch/LaunchScene.tsx`:
     Follow FactoryBackground.tsx pattern (full-viewport background + overlay container), but simpler:
     - Full-bleed viewport div: `relative w-full h-screen overflow-hidden` with dark steampunk background color (bg-[#1a0f0a] or similar warm dark brown)
     - Inner scene container: centered with contain-fit scaling. Use the same pattern as FactoryBackground (absolute centered, min() sizing) but with a different aspect ratio that will be tuned when art is provided. Default to 16:9 (1.78:1) for now.
     - Background image: Use Next.js `<Image>` with `fill` and `object-cover`. For now, use a placeholder approach -- check if the image exists, otherwise render a dark gradient background. The user will provide the WebP later.
     - Accept `children` prop for overlay elements (gauges, timer, panels, etc.)
     - No header, no nav chrome -- full immersive page per CONTEXT.md

  2. Create `app/components/launch/PressureGauge.tsx`:
     Props: `{ solRaised: bigint, label: string, className?: string }`
     - Calculate fill percentage: `Number(solRaised * 100n / TARGET_SOL_BIGINT)` where TARGET_SOL = 1_000_000_000_000n (1000 SOL in lamports). Cap at 100.
     - Needle rotation via CSS transform: configurable NEEDLE_MIN_DEG (-120) and NEEDLE_MAX_DEG (120) constants at top of file. Rotation = MIN + (pct/100) * (MAX - MIN).
     - Render structure:
       - Outer container (positioned by parent via className)
       - Gauge background (placeholder div with circular shape, styled as brass steampunk gauge with CSS -- border-radius, gradient background, inner shadow). When user provides art, this becomes an <Image>.
       - Needle overlay: absolute positioned, transform-origin at bottom center, CSS `transform: rotate(${deg}deg)` with `transition: transform 0.5s ease-out` for smooth animation. Placeholder as a styled div (thin vertical line). When user provides needle image, this becomes an <Image>.
       - Label text below gauge: "{label}" and "{pct}% filled"
     - Steampunk styling: brass colors (#b8860b, #8b6914), dark backgrounds, subtle shadows

  3. Create `app/app/launch/page.tsx`:
     - "use client" directive
     - Import and call useCurveState() hook (from Plan 01)
     - Import useCurrentSlot() for countdown
     - Import useSolPrice() for market cap
     - Loading state: if useCurveState.loading, show a centered loading spinner (import from scene/LoadingSpinner or create simple CSS spinner)
     - Error state: if useCurveState.error and no data, show error message with retry hint
     - Compose:
       ```
       <LaunchScene>
         {/* Pressure gauges -- positioned as overlays */}
         <PressureGauge solRaised={crime.solRaised} label="CRIME" className="absolute left-[10%] top-[15%] w-[20%]" />
         <PressureGauge solRaised={fraud.solRaised} label="FRAUD" className="absolute right-[10%] top-[15%] w-[20%]" />

         {/* Countdown timer -- centered top */}
         <CountdownTimer deadlineSlot={deadlineSlot} />

         {/* Stats panels */}
         <CurveStats crime={crime} fraud={fraud} solPrice={solPrice} />

         {/* Buy/Sell panel placeholder -- will be added in Plan 04 */}

         {/* Wallet button -- floating */}
         <LaunchWalletButton />

         {/* Docs button */}
         <DocsButton />
       </LaunchScene>
       ```
     - Determine deadlineSlot: use crime.deadlineSlot (both curves share the same deadline once started; if not started yet, show "Not started")
     - Mobile layout: Use `@media (max-width: 1024px)` / Tailwind `lg:` breakpoints to stack elements vertically on small screens. Gauges side-by-side on desktop, stacked on mobile. Stats in a scrollable column on mobile.
  </action>
  <verify>
  `cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && npx tsc --noEmit` passes. Start dev server (`npx next dev`) and navigate to http://localhost:3000/launch -- page renders with dark background, two gauge placeholders, and no console errors.
  </verify>
  <done>
  /launch route renders a full-bleed immersive page with LaunchScene container, two PressureGauge components showing fill % with CSS needle rotation, positioned as overlays. Mobile responsive with stacked layout under 1024px.
  </done>
</task>

<task type="auto">
  <name>Task 2: Countdown timer, curve stats, wallet button, docs modal</name>
  <files>
    app/components/launch/CountdownTimer.tsx
    app/components/launch/CurveStats.tsx
    app/components/launch/LaunchWalletButton.tsx
    app/components/launch/DocsModal.tsx
  </files>
  <action>
  1. Create `app/components/launch/CountdownTimer.tsx`:
     Props: `{ deadlineSlot: number | null, className?: string }`
     - Use useCurrentSlot() hook to get current slot
     - If deadlineSlot is null or 0: display "Awaiting launch..."
     - Calculate remaining: `remainingSlots = deadlineSlot - currentSlot`
     - If remainingSlots <= 0: display "EXPIRED" in red/warning style
     - Convert to time: `remainingMs = remainingSlots * MS_PER_SLOT` (import MS_PER_SLOT from shared/constants)
     - Display format: "~Xh Ym remaining" (no seconds -- slot timing is approximate per RESEARCH.md Pitfall 4)
     - Steampunk styling: brass-framed timer look, monospace digits, warm amber text color on dark background
     - Position: `absolute` with className prop for parent positioning (centered top area of scene)

  2. Create `app/components/launch/CurveStats.tsx`:
     Props: `{ crime: CurveStateData | null, fraud: CurveStateData | null, solPrice: number | null, className?: string }`
     - Two-column layout (side by side on desktop, stacked on mobile)
     - Per curve, display:
       a) SOL Raised: `Number(solRaised) / 1e9` formatted to 2 decimals + " SOL" + " / 1,000 SOL" target
       b) Market Cap: `(Number(solRaised) / 1e9) * (solPrice ?? 0)` formatted as USD
       c) Current Price: Use getCurrentPrice(tokensSold) from curve-math.ts. Display as "X.XXXXXXX SOL/token" and USD equivalent
       d) Tax Escrow: `Number(taxCollected) / 1e9` formatted as SOL -- this is the total 15% sell tax accumulated
     - Use Kit Card component for each stat group if available, otherwise simple styled divs
     - Steampunk brass aesthetic: warm colors, subtle borders, monospace for numbers

  3. Create `app/components/launch/LaunchWalletButton.tsx`:
     - Wrap the existing wallet adapter connection pattern from useProtocolWallet
     - Render a floating button: `fixed bottom-4 right-4 z-50` (or absolute within scene)
     - If connected: show abbreviated address (first 4...last 4 chars) with disconnect option
     - If not connected: show "Connect Wallet" button
     - Use Kit Button component with steampunk styling override (brass/amber colors)
     - This is the ONLY way to connect wallet on the launch page (no header)

  4. Create `app/components/launch/DocsModal.tsx`:
     - Renders a button ("Docs" or book icon) positioned as overlay
     - On click: opens a dialog/modal containing an iframe to the Nextra docs site
     - Follow the project's existing modal pattern (check app/components/modal/ for patterns)
     - iframe src: the project's docs URL (Railway deployment docs route, or placeholder URL)
     - Modal is closable via X button or clicking backdrop
     - Steampunk styled button trigger, clean modal container
     - CONTEXT.md: "Only modal on this page: docs link (iframe to Nextra)"
  </action>
  <verify>
  `cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && npx tsc --noEmit` passes. Dev server shows countdown timer (even if "Awaiting launch..." when no curve data), stats panels with 0/placeholder values, wallet button in bottom-right corner, and docs button that opens a modal.
  </verify>
  <done>
  CountdownTimer shows slot-based countdown in ~Xh Ym format with EXPIRED state. CurveStats displays SOL raised, market cap, current price, tax escrow per curve. LaunchWalletButton provides floating wallet connection. DocsModal opens iframe to Nextra docs. All steampunk-themed and mobile responsive.
  </done>
</task>

</tasks>

<verification>
1. `/launch` route renders without errors
2. PressureGauge needles rotate based on solRaised prop
3. CountdownTimer shows correct approximate time from slot difference
4. CurveStats shows all 4 metrics per curve (SOL raised, market cap, price, tax escrow)
5. LaunchWalletButton connects/disconnects wallet
6. DocsModal opens iframe in a closable modal
7. Mobile responsive: elements stack vertically below 1024px
8. `npx tsc --noEmit` passes
</verification>

<success_criteria>
The /launch page is visually functional: users see two pressure gauges with progress, a countdown timer, real-time curve statistics, can connect their wallet, and can open documentation. The page is ready for the buy/sell panel (Plan 04) and state machine UI (Plan 05) to be composed in.
</success_criteria>

<output>
After completion, create `.planning/phases/75-launch-page/75-03-SUMMARY.md`
</output>
