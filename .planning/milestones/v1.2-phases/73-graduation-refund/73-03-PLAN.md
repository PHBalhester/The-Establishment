---
phase: 73-graduation-refund
plan: 03
type: tdd
wave: 3
depends_on: ["73-02"]
files_modified:
  - programs/bonding_curve/src/math.rs
autonomous: true

must_haves:
  truths:
    - "Refund order independence: 3 users claiming in any order receive the same amounts"
    - "Refund vault exhaustion: after all users claim, remaining vault balance is dust only (0 to N lamports)"
    - "Refund solvency: no individual claim ever exceeds the refund pool available at claim time"
    - "Refund floor rounding: each claim amount <= proportional share (protocol-favored)"
    - "Multi-user refund sequences with 2-8 users preserve all invariants at 1M+ iterations"
    - "All 5 Phase 73 instruction status gate tests pass (deterministic edge cases)"
  artifacts:
    - path: "programs/bonding_curve/src/math.rs"
      provides: "Refund property tests (1M+ iterations) + deterministic instruction logic tests"
      contains: "refund_order_independent"
  key_links:
    - from: "programs/bonding_curve/src/math.rs refund tests"
      to: "claim_refund.rs proportional formula"
      via: "identical floor(balance * pool / total) calculation"
      pattern: "user_balance.*refund_pool.*total_outstanding"
---

<objective>
Add comprehensive property-based tests proving the refund math is correct, order-independent, and solvent across all possible user distributions. Also add deterministic tests for the status gate logic of all 5 Phase 73 instructions.

Purpose: The refund math is the most critical correctness property of Phase 73 -- if it's wrong, users lose real SOL. Property tests at 1M+ iterations provide mathematical confidence that the floor-rounded proportional refund formula is solvent, order-independent, and exhaustive.

Output: New proptest block and deterministic test functions in math.rs. All tests pass via `cargo test -p bonding_curve`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/73-graduation-refund/73-CONTEXT.md
@.planning/phases/73-graduation-refund/73-RESEARCH.md
@.planning/phases/73-graduation-refund/73-01-SUMMARY.md
@.planning/phases/73-graduation-refund/73-02-SUMMARY.md
@programs/bonding_curve/src/math.rs
@programs/bonding_curve/src/constants.rs
@programs/bonding_curve/src/state.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write refund property tests and deterministic instruction tests</name>
  <files>
    programs/bonding_curve/src/math.rs
  </files>
  <action>
    Add a new section at the end of the `#[cfg(test)] mod tests` block in math.rs, AFTER the existing sell proptest block. Use a separate `proptest!` macro invocation with 1M iterations.

    **Pure helper function (outside tests module, in math.rs):**

    Add a public helper function that encapsulates the on-chain refund formula so tests can use it directly:

    ```rust
    /// Compute proportional refund amount using floor rounding (protocol-favored).
    ///
    /// Formula: floor(user_balance * refund_pool / total_outstanding)
    /// Uses u128 intermediates to prevent overflow.
    ///
    /// Returns None if total_outstanding is zero (defense-in-depth).
    pub fn calculate_refund(user_balance: u64, refund_pool: u64, total_outstanding: u64) -> Option<u64> {
        if total_outstanding == 0 {
            return None;
        }
        let result = (user_balance as u128)
            .checked_mul(refund_pool as u128)?
            / (total_outstanding as u128);
        Some(result as u64)
    }
    ```

    **Deterministic refund tests:**

    ```
    // =========================================================================
    // Refund Math: Deterministic Tests
    // =========================================================================
    ```

    1. `refund_single_user_gets_full_pool` -- One user holding all tokens gets the entire refund pool:
       - `calculate_refund(1_000_000, 500_000_000, 1_000_000)` == `Some(500_000_000)`

    2. `refund_two_equal_users` -- Two users with equal balances each get half:
       - User A: `calculate_refund(500, 1_000_000, 1_000)` == `Some(500_000)`
       - User B: same result

    3. `refund_zero_balance_gets_nothing` -- User with 0 tokens gets 0:
       - `calculate_refund(0, 1_000_000, 1_000)` == `Some(0)`

    4. `refund_zero_pool_gets_nothing` -- Empty vault returns 0:
       - `calculate_refund(500, 0, 1_000)` == `Some(0)`

    5. `refund_zero_outstanding_returns_none` -- Division by zero guard:
       - `calculate_refund(500, 1_000_000, 0)` == `None`

    6. `refund_floor_rounding_favors_protocol` -- When the division doesn't divide evenly:
       - 3 users each holding 1 token, pool = 10 lamports, total = 3
       - Each gets floor(10/3) = 3, total refunded = 9, dust = 1 lamport

    7. `refund_realistic_curve_values` -- With realistic curve amounts:
       - pool = 1_000_000_000_000 (1000 SOL), total = 460_000_000_000_000 (460M tokens)
       - User with 20_000_000_000_000 (20M tokens): refund should be ~43.478 SOL
       - Verify: `floor(20e12 * 1e12 / 460e12)` = floor(43_478_260_869.5...) = 43_478_260_869

    **Simulate Claims helper (inside tests module):**

    ```rust
    /// Simulate sequential refund claims for N users.
    /// Returns (per-user refund amounts, final remaining vault).
    fn simulate_claims(initial_pool: u64, total_tokens: u64, user_balances: &[u64]) -> (Vec<u64>, u64) {
        let mut pool = initial_pool;
        let mut outstanding = total_tokens;
        let mut refunds = Vec::with_capacity(user_balances.len());

        for &balance in user_balances {
            let refund = (balance as u128 * pool as u128 / outstanding as u128) as u64;
            pool -= refund;
            outstanding -= balance;
            refunds.push(refund);
        }

        (refunds, pool)
    }
    ```

    **Proptest block (1M iterations):**

    ```
    // =========================================================================
    // Proptest: Refund Property Tests (1M iterations)
    //
    // These tests prove the proportional refund formula is correct:
    // - Order-independent (claim ordering doesn't affect individual payouts)
    // - Solvent (no individual claim exceeds available pool)
    // - Exhaustive (total refunded approaches pool, dust only remains)
    // - Protocol-favored (floor rounding never overpays)
    // =========================================================================

    proptest! {
        #![proptest_config(ProptestConfig::with_cases(1_000_000))]
        ...
    }
    ```

    **PROPERTY R1: Refund order independence (3 users).**
    Generate 3 users with random balances summing to a total_outstanding. Random pool.
    Simulate claims in ABC order and CBA order. Assert each user's refund is identical regardless of order.

    Strategy:
    - `vault_sol in 1_000_000u64..=1_000_000_000_000u64` (realistic range: 0.001 SOL to 1000 SOL)
    - `pct_a in 1u64..=500_000u64`, `pct_b in 1u64..=500_000u64` (percentage-based derivation)
    - Derive `total = 460_000_000_000_000u64` (realistic tokens_sold)
    - `balance_a = floor(total * pct_a / 1_000_000)`, `balance_b = floor(total * pct_b / 1_000_000)`
    - `balance_c = total - balance_a - balance_b`
    - Skip if any balance is 0
    - Simulate ABC and CBA orders using `simulate_claims`
    - Assert: `refund_a_abc == refund_a_cba`, same for B and C
    - Assert: dust (vault_sol - sum_of_refunds) <= 3 (max 3 lamports for 3 users)

    **PROPERTY R2: Refund solvency per-claim (variable users).**
    Generate 2-6 users with random balances. For each claim in sequence, assert the refund amount does not exceed the current pool.

    Strategy:
    - `vault_sol in 1_000_000u64..=1_000_000_000_000u64`
    - `num_users in 2u32..=6u32`
    - `pcts in proptest::collection::vec(1u64..=1_000_000u64, 2..=6)`
    - Derive balances from percentages (normalize to sum = TARGET_TOKENS)
    - For each user: compute refund, assert refund <= current pool, deduct

    **PROPERTY R3: Refund vault exhaustion (total dust bounded).**
    After all users claim (same setup as R2), verify:
    - `remaining_pool <= num_users as u64` (max 1 lamport dust per user from floor rounding)
    - `sum_of_refunds + remaining_pool == initial_vault_sol`

    **PROPERTY R4: Floor rounding protocol-favored.**
    For each user's claim, verify: `refund <= user_balance * initial_pool / initial_total` computed with f64.
    This ensures floor rounding never overpays compared to the exact fractional answer.

    Strategy:
    - Same user generation as R2
    - For the FIRST claimer (before pool shrinks), compare:
      `floor_refund <= (user_balance as f64 * vault_sol as f64 / total as f64) + 1.0`
    - The +1.0 accounts for f64 imprecision at large values

    **PROPERTY R5: Refund with varied pool sizes (buy/sell scenarios).**
    Test refund correctness when the pool contains SOL from both buys and consolidated tax escrow.
    Strategy:
    - `buy_sol in MIN_PURCHASE_SOL..=TARGET_SOL`
    - `tax_portion in 0u64..=buy_sol / 7` (up to ~15% of buy amount)
    - `total_pool = buy_sol + tax_portion` (simulates consolidation)
    - `num_users in 2u32..=4u32`
    - Run refund claims, verify all R1-R4 properties hold

    **Deterministic instruction status gate tests:**

    Add a new section after the proptest block:

    ```
    // =========================================================================
    // Phase 73 Instruction Logic Tests (Deterministic)
    // =========================================================================
    ```

    These test the pure logic that each instruction enforces, without LiteSVM:

    8. `mark_failed_grace_buffer_exact_boundary` -- At slot `deadline + FAILURE_GRACE_SLOTS` the check should fail (not strictly greater). At slot `deadline + FAILURE_GRACE_SLOTS + 1` it should pass.
       - Use the formula: `clock.slot > curve.deadline_slot + FAILURE_GRACE_SLOTS`
       - Assert: slot = deadline + 150 -> NOT eligible (require > not >=)
       - Assert: slot = deadline + 151 -> eligible

    9. `prepare_transition_requires_both_filled` -- Test all status combinations:
       - (Filled, Filled) -> OK
       - (Filled, Active) -> CRIMECurveNotFilled or FRAUDCurveNotFilled
       - (Active, Filled) -> error
       - (Graduated, Filled) -> error
       Test the is_refund_eligible helper for cross-curve combinations.

    10. `consolidate_idempotency_flag` -- Test that `escrow_consolidated` starts false, can be set to true, and a second set would be caught by the `EscrowAlreadyConsolidated` check.

    11. `refund_denominator_shrinks_correctly` -- After each claim, tokens_sold decreases by user_balance:
        - Start with tokens_sold = 1000, user burns 300 -> tokens_sold = 700
        - Next user burns 700 -> tokens_sold = 0
        - Verify the pool is fully drained (minus dust)
  </action>
  <verify>
    Run `source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH" && cargo test -p bonding_curve -- --nocapture 2>&1 | tail -30`. All tests should pass including the new refund property tests. The 1M-iteration block will take 30-90 seconds.

    Count total tests: should be ~30+ (existing ~24 + new ~11).
  </verify>
  <done>
    All refund property tests pass at 1M+ iterations: order independence (R1), per-claim solvency (R2), vault exhaustion bounded dust (R3), floor rounding protocol-favored (R4), varied pool sizes (R5). All deterministic instruction logic tests pass. The `calculate_refund` helper function is available for reuse. Total test iterations for the bonding curve: ~9.5M+ (500K buy + 1M sell + 1M refund + deterministic).
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p bonding_curve` -- all tests pass (0 failures)
2. New proptest block runs 1M iterations without failures
3. `calculate_refund` helper function exists and is public
4. R1 (order independence) passes for 3-user claim sequences
5. R2 (solvency) passes for 2-6 user claim sequences
6. R3 (exhaustion) proves dust <= num_users lamports
7. R4 (floor rounding) proves no overpayment
8. R5 (varied pools) proves correctness with consolidated escrow
9. Deterministic tests cover grace buffer boundaries, status gates, consolidation flag, denominator shrinkage
10. No compilation warnings in the test module
</verification>

<success_criteria>
The refund formula `floor(user_balance * refund_pool / total_outstanding)` is proven correct at 1M+ iterations across all relevant properties: order-independent, solvent, exhaustive, and protocol-favored. Deterministic tests confirm the slot-boundary and status-gate logic for all 5 Phase 73 instructions. The bonding curve program has 9.5M+ total property test iterations covering buy, sell, and refund paths.
</success_criteria>

<output>
After completion, create `.planning/phases/73-graduation-refund/73-03-SUMMARY.md`
</output>
