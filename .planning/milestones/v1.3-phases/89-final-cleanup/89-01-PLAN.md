---
phase: 89-final-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/api/webhooks/helius/route.ts
  - shared/constants.ts
  - .npmrc
  - scripts/e2e/lib/swap-flow.ts
  - app/next.config.ts
  - Cargo.toml
  - tests/cross-crate/Cargo.toml
  - tests/cross-crate/src/lib.rs
autonomous: true
requirements: [H001, H002, H003, H035, H011, H026, H050, S007]

must_haves:
  truths:
    - "Webhook secret comparison uses constant-time equality — timing side-channel eliminated"
    - "HELIUS_API_KEY string does not appear in any shipped source file"
    - ".npmrc blocks all npm lifecycle scripts by default"
    - "All 75/24/1 tax split comments are corrected to 71/24/5"
    - "HSTS header is served on all responses"
    - "Database connections use TLS"
    - "Webhook endpoint rejects bodies larger than 1MB"
    - "Cross-crate EpochState serialization round-trip test passes"
  artifacts:
    - path: "app/app/api/webhooks/helius/route.ts"
      provides: "timingSafeEqual webhook auth + body size limit"
      contains: "timingSafeEqual"
    - path: ".npmrc"
      provides: "npm security lockdown"
      contains: "ignore-scripts=true"
    - path: "tests/cross-crate/src/lib.rs"
      provides: "EpochState cross-crate serialization test"
      contains: "epoch_state"
  key_links:
    - from: "app/app/api/webhooks/helius/route.ts"
      to: "crypto.timingSafeEqual"
      via: "Node.js crypto import"
      pattern: "timingSafeEqual"
    - from: "tests/cross-crate/Cargo.toml"
      to: "epoch-program, tax-program"
      via: "workspace dependencies"
      pattern: "epoch.program|tax.program"
---

<objective>
Apply all quick security fixes from audit findings and create the cross-crate EpochState serialization test.

Purpose: Close 8 audit findings (H001, H002, H003, H011, H026, H035, H050, S007) that are each 1-10 line changes but collectively eliminate timing attacks, key exposure, supply chain risks, TLS gaps, and cross-crate deserialization drift.

Output: Hardened webhook auth, removed dead API key, .npmrc lockdown, HSTS header, DB TLS, corrected comments, webhook body size limit, and a workspace-level cross-crate test.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-final-cleanup/89-CONTEXT.md
@shared/constants.ts
@app/app/api/webhooks/helius/route.ts
@app/next.config.ts
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Webhook timing-safe auth + body size limit + HELIUS_API_KEY removal</name>
  <files>
    app/app/api/webhooks/helius/route.ts
    shared/constants.ts
  </files>
  <action>
    **H001 — timingSafeEqual:**
    In `app/app/api/webhooks/helius/route.ts`, replace the `authHeader !== webhookSecret` string comparison (line ~227) with `crypto.timingSafeEqual`. Import `timingSafeEqual` from `node:crypto`. Both strings must be converted to Buffer before comparison. If lengths differ, still call timingSafeEqual on padded buffers (or use a constant-time length check) to avoid leaking length info.

    **H050 — Webhook body size limit:**
    In the same route.ts POST handler, before parsing `await request.json()`, read `request.headers.get('content-length')`. If content-length exceeds 1_048_576 (1MB), return 413 immediately. Also wrap the json() parse in a try-catch to handle malformed payloads.

    **H002 — Delete HELIUS_API_KEY:**
    In `shared/constants.ts`, delete the `HELIUS_API_KEY` export (line ~474) and the `heliusApiKey` reference (line ~517) in the config object. Search all files that import HELIUS_API_KEY and remove those imports — they should all be dead code since RPC goes through the proxy now. If any live reference remains, flag it rather than breaking the build.
  </action>
  <verify>
    `grep -r "HELIUS_API_KEY" shared/ app/ scripts/ --include="*.ts" --include="*.tsx"` returns zero matches.
    `grep -r "timingSafeEqual" app/app/api/webhooks/helius/route.ts` returns a match.
    `grep -r "content-length.*1048576\|1_048_576\|bodyLimit\|413" app/app/api/webhooks/helius/route.ts` returns a match.
    TypeScript compilation passes: `cd app && npx tsc --noEmit` (no new errors).
  </verify>
  <done>
    Webhook auth uses constant-time comparison, HELIUS_API_KEY is fully removed from source, and webhook rejects oversized bodies with 413.
  </done>
</task>

<task type="auto">
  <name>Task 2: .npmrc + HSTS + DB TLS + stale comment fix</name>
  <files>
    .npmrc
    app/next.config.ts
    scripts/e2e/lib/swap-flow.ts
  </files>
  <action>
    **H003 — .npmrc:**
    Create `.npmrc` in project root with:
    ```
    ignore-scripts=true
    ```
    This blocks all npm lifecycle scripts (preinstall, postinstall, etc.) to prevent supply chain attacks. When a package legitimately needs scripts (e.g., native modules), run `npm rebuild <package>` manually.

    **H026 — HSTS header:**
    In `app/next.config.ts`, add a `Strict-Transport-Security` header to the existing headers() config:
    ```
    { key: 'Strict-Transport-Security', value: 'max-age=63072000; includeSubDomains; preload' }
    ```
    Add it alongside the existing CSP and other security headers.

    **H011 — DB TLS:**
    This is a runtime configuration change. In `app/lib/db/` or wherever the Drizzle client is configured, ensure the DATABASE_URL connection string appends `?sslmode=require` if not already present. If the connection string is purely env-var driven, document in `.env.example` that DATABASE_URL must include `?sslmode=require` for production (Railway Postgres supports TLS). If there's a drizzle config that constructs the connection, add sslmode programmatically.

    **H035 — Stale 75/24/1 comment:**
    In `scripts/e2e/lib/swap-flow.ts` line 112, change "75/24/1" to "71/24/5". Search for any other 75/24/1 references in programs/ and app/ (there should be none remaining based on grep results).
  </action>
  <verify>
    `cat .npmrc` shows `ignore-scripts=true`.
    `grep "Strict-Transport-Security" app/next.config.ts` returns a match.
    `grep -r "75/24/1" scripts/ programs/ app/ --include="*.ts" --include="*.rs"` returns zero matches.
    `cd app && npx tsc --noEmit` passes.
  </verify>
  <done>
    .npmrc blocks install scripts, HSTS header configured with 2-year max-age + preload, DB TLS documented/enforced, all stale tax split comments corrected to 71/24/5.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cross-crate EpochState serialization test</name>
  <files>
    Cargo.toml
    tests/cross-crate/Cargo.toml
    tests/cross-crate/src/lib.rs
  </files>
  <action>
    **S007 — Cross-crate serialization test:**

    1. Create `tests/cross-crate/` directory with `Cargo.toml` and `src/lib.rs`.

    2. `tests/cross-crate/Cargo.toml`:
    ```toml
    [package]
    name = "cross-crate-tests"
    version = "0.1.0"
    edition = "2021"

    [dev-dependencies]
    epoch-program = { path = "../../programs/epoch-program" }
    tax-program = { path = "../../programs/tax-program" }
    borsh = "0.10"
    anchor-lang = "0.30"
    ```
    Adjust the borsh/anchor versions to match what the workspace already uses (check epoch-program's Cargo.toml).

    3. `tests/cross-crate/src/lib.rs`:
    Write a test that:
    - Creates an EpochState struct from epoch-program with known field values (epoch_number=42, current_tax_bps values, etc.)
    - Serializes it to bytes using Borsh
    - Deserializes those bytes using the tax-program's mirror struct (the EpochState copy in tax-program)
    - Asserts all field values match
    - Also tests the reverse direction (serialize from tax-program mirror, deserialize in epoch-program)

    The test proves byte-level compatibility between the two crates' EpochState definitions. If either crate changes field order or types, this test fails.

    4. Update root `Cargo.toml` workspace members:
    Change `members = ["programs/*"]` to `members = ["programs/*", "tests/cross-crate"]`.

    5. Run `cargo test -p cross-crate-tests` to verify the test passes.

    **Important:** Check the actual struct names and module paths in both programs. The tax-program mirror might be named differently (e.g., `EpochStateData` or in a specific module). Read both program's source to find the exact type paths.
  </action>
  <verify>
    `source "$HOME/.cargo/env" && cd "/Users/mlbob/Projects/Dr Fraudsworth" && cargo test -p cross-crate-tests` passes.
    `grep "cross-crate" Cargo.toml` shows the workspace member.
  </verify>
  <done>
    Workspace-level test crate proves EpochState serialization is byte-compatible across epoch-program and tax-program. Any future field reordering or type change in either crate will fail this test.
  </done>
</task>

</tasks>

<verification>
- `grep -r "HELIUS_API_KEY" shared/ app/ scripts/ --include="*.ts"` returns 0 matches
- `grep "timingSafeEqual" app/app/api/webhooks/helius/route.ts` finds the import/usage
- `.npmrc` exists with `ignore-scripts=true`
- `grep "Strict-Transport-Security" app/next.config.ts` finds HSTS header
- `grep -r "75/24/1" scripts/ programs/ app/` returns 0 matches
- `cargo test -p cross-crate-tests` passes
- `cd app && npx tsc --noEmit` passes (no type errors from changes)
</verification>

<success_criteria>
All 8 audit findings (H001, H002, H003, H011, H026, H035, H050, S007) are closed with verified fixes. No regressions in TypeScript compilation or Rust tests.
</success_criteria>

<output>
After completion, create `.planning/phases/89-final-cleanup/89-01-SUMMARY.md`
</output>
