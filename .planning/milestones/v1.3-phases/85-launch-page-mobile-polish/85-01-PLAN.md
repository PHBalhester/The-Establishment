---
phase: 85-launch-page-mobile-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/launch/PressureGauge.tsx
  - app/components/launch/LaunchScene.tsx
  - app/public/scene/launch/curve-overlay.png
  - app/public/scene/launch/needle.png
autonomous: false
requirements: [LP-01, LP-02, LP-03]

must_haves:
  truths:
    - "Gauge needles rotate smoothly from 0% (7 o'clock) to 100% (5 o'clock) tracking SOL raised"
    - "New curve overlay renders without baked-in needles — CSS needles are sole needle display"
    - "Needle position matches fill percentage in real-time as curve state updates"
  artifacts:
    - path: "app/public/scene/launch/needle.png"
      provides: "Separate needle PNG asset for CSS rotation"
    - path: "app/public/scene/launch/curve-overlay.png"
      provides: "Clean gauge face overlay without baked-in needles"
    - path: "app/components/launch/PressureGauge.tsx"
      provides: "CSS-rotated needle Image element with smooth transition"
      contains: "transform.*rotate"
  key_links:
    - from: "app/components/launch/PressureGauge.tsx"
      to: "app/public/scene/launch/needle.png"
      via: "Next.js Image src"
      pattern: "needle\\.png"
    - from: "app/app/launch/page.tsx"
      to: "app/components/launch/PressureGauge.tsx"
      via: "JSX composition"
      pattern: "<PressureGauge"
---

<objective>
Receive user-provided visual assets (needle PNG, clean curve overlay) and implement CSS-rotated gauge needles on the launch page pressure gauges.

Purpose: The launch page gauges currently have needles baked into the overlay image. Separate needle PNGs allow real-time CSS rotation to show bonding curve fill progress.
Output: Working animated gauge needles that rotate based on SOL raised, using the new clean overlay.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/85-launch-page-mobile-polish/85-CONTEXT.md
@app/components/launch/PressureGauge.tsx
@app/components/launch/LaunchScene.tsx
@app/app/launch/page.tsx
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Asset Handoff</name>
  <action>
    Ask user to provide:
    1. **needle.png** — The needle asset (transparent PNG, pointing upward at 12 o'clock / 0 degrees as rest position). Ask user where the file is on disk.
    2. **curve-overlay.png** — The clean overlay with gauge faces but NO baked-in needles. Ask user where the file is.

    For each asset, ask:
    - What is the image dimensions / resolution?
    - Is the needle pivot point at the center-bottom of the image, or center?
    - Any specific notes about positioning?

    Place assets:
    - `app/public/scene/launch/needle.png`
    - Replace `app/public/scene/launch/curve-overlay.png` with the new clean version

    If new overlay is large (>500KB), convert to WebP or compress with sharp/squoosh CLI if available. Keep PNG if user prefers.
  </action>
  <resume-signal>User provides asset files and placement instructions. Type "assets ready" after files are in place.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement CSS-Rotated Gauge Needles</name>
  <files>app/components/launch/PressureGauge.tsx, app/app/launch/page.tsx</files>
  <action>
    Update PressureGauge.tsx to render the needle PNG with CSS rotation:

    1. Import Next.js Image component
    2. Add needle Image element inside the gauge div:
       - `src="/scene/launch/needle.png"`
       - Position absolutely within the gauge area, centered on the gauge pivot point
       - Apply `transform: rotate(${needleDeg}deg)` using the existing `needleDeg` calculation
       - Set `transform-origin` to match the needle's pivot point (likely `center bottom` or `50% 50%` depending on asset)
       - Add smooth CSS transition: `transition: transform 0.8s cubic-bezier(0.4, 0, 0.2, 1)` for animated rotation when value changes
    3. The needle element should be `pointer-events-none` and `select-none` to not interfere with interactions
    4. Use percentage-based positioning so the needle stays aligned with the gauge face across viewport sizes (matching the contain-fit scaling of LaunchScene)

    Verify the PressureGauge className props in page.tsx position the needle correctly over each gauge face:
    - CRIME gauge: `left-[5%] top-[10%] w-[28%] h-[75%]`
    - FRAUD gauge: `right-[5%] top-[10%] w-[28%] h-[75%]`

    These positions may need adjustment based on the new overlay — adjust if needles don't align with gauge centers.

    LP-03 note: The docs button repositioning is DEFERRED per CONTEXT.md. Leave DocsModal at its current `bottom-[3%] left-[3%]` position. No changes needed.
  </action>
  <verify>
    Run `export PATH="/opt/homebrew/bin:$PATH" && cd "/Users/mlbob/Projects/Dr Fraudsworth/app" && npx next build` — build succeeds with no TS errors.
    Visually: needles should appear on both gauges at the correct rotation for current curve fill state.
  </verify>
  <done>
    Both CRIME and FRAUD gauges show CSS-rotated needle images that track fill percentage. Needle transitions smoothly when value changes. New clean overlay renders without baked-in needles. LP-03 (docs button) left unchanged (deferred).
  </done>
</task>

</tasks>

<verification>
- `next build` succeeds with no errors
- PressureGauge.tsx renders needle.png with CSS rotation matching fill percentage
- New curve-overlay.png is loaded and renders cleanly (no baked-in needles visible)
- Needle animation is smooth (CSS transition, no jank)
- Both CRIME and FRAUD gauges have independently rotating needles
</verification>

<success_criteria>
- Needle PNG assets are in public/scene/launch/
- PressureGauge renders Image element with transform: rotate() based on solRaised
- Smooth CSS transition on needle rotation (0.8s ease)
- Visual alignment of needles with gauge face centers confirmed
- Build passes with no TS errors
</success_criteria>

<output>
After completion, create `.planning/phases/85-launch-page-mobile-polish/85-01-SUMMARY.md`
</output>
