---
phase: 90-gap-closure-launch-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/vrf/lib/vrf-flow.ts
  - scripts/deploy/initialize.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "waitForSlotAdvance() has a maxWaitMs wall-clock timeout that throws on expiry"
    - "initialize.ts passes program + programData to all init accountsStrict calls"
    - "initialize.ts verifies account ownership before skipping initialization"
  artifacts:
    - path: "scripts/vrf/lib/vrf-flow.ts"
      provides: "waitForSlotAdvance with timeout"
      contains: "maxWaitMs"
    - path: "scripts/deploy/initialize.ts"
      provides: "Hardened init script with program/programData"
      contains: "programData"
  key_links:
    - from: "scripts/vrf/lib/vrf-flow.ts"
      to: "Date.now()"
      via: "wall-clock timeout check"
      pattern: "maxWaitMs|Date\\.now|timeout"
    - from: "scripts/deploy/initialize.ts"
      to: "programData"
      via: "accountsStrict fields"
      pattern: "programData.*accountsStrict|accountsStrict.*programData"
---

<objective>
Add wall-clock timeout to waitForSlotAdvance() (H030) and fix initialize.ts to pass program/programData accounts and verify ownership before skipping (H016).

Purpose: Close 2 DB audit findings for script/crank hardening.
Output: Timeout-safe slot waiting, deployment script synced with on-chain instruction requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.bulwark/findings/VERIFY-H016.md
@.bulwark/findings/VERIFY-H030.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add maxWaitMs timeout to waitForSlotAdvance (H030)</name>
  <files>scripts/vrf/lib/vrf-flow.ts</files>
  <action>
    Modify `waitForSlotAdvance()` in `scripts/vrf/lib/vrf-flow.ts` (starts at line 159) to accept an optional `maxWaitMs` parameter with a wall-clock timeout.

    Current signature: `export async function waitForSlotAdvance(connection: Connection, targetSlots: number): Promise<void>`

    New signature: `export async function waitForSlotAdvance(connection: Connection, targetSlots: number, maxWaitMs?: number): Promise<void>`

    Default `maxWaitMs`:
    - If not provided, calculate a generous default: `targetSlots * 400 * 3` (3x the expected time at 400ms/slot). This means a 750-slot wait (~5 min expected) gets a 15 min timeout. A 3-slot wait (~1.2s expected) gets a 3.6s timeout — but floor this at 30_000ms (30 seconds minimum) to handle RPC latency.
    - Formula: `const effectiveTimeout = maxWaitMs ?? Math.max(30_000, targetSlots * 400 * 3);`

    Implementation:
    1. Record `const startTime = Date.now();` at the top of the function.
    2. In the `while (true)` polling loop (line 182), add a wall-clock check at the top of each iteration:
       ```ts
       if (Date.now() - startTime > effectiveTimeout) {
         const currentSlot = await connection.getSlot().catch(() => -1);
         throw new Error(
           `waitForSlotAdvance timed out after ${Math.round((Date.now() - startTime) / 1000)}s. ` +
           `Target: ${targetSlot}, current: ${currentSlot}, started at: ${startSlot}`
         );
       }
       ```
    3. Place the timeout check BEFORE the `await sleep(pollMs)` call so we don't sleep unnecessarily.
    4. Also add the timeout check in the initial long-sleep path (line 171-175): if the sleep duration exceeds maxWaitMs, cap it: `const sleepMs = Math.min(estimatedMs - 15_000, effectiveTimeout - 5_000);` (wake up 5s before timeout).

    All existing callers pass only 2 args and will get the generous default timeout. No caller changes needed.
  </action>
  <verify>
    1. `grep "maxWaitMs" scripts/vrf/lib/vrf-flow.ts` confirms the parameter exists
    2. `grep "timed out" scripts/vrf/lib/vrf-flow.ts` confirms the timeout error message
    3. `grep "Date.now()" scripts/vrf/lib/vrf-flow.ts` shows wall-clock checks
    4. TypeScript compiles: `cd scripts && npx tsc --noEmit vrf/lib/vrf-flow.ts` or similar check
  </verify>
  <done>
    waitForSlotAdvance() has a wall-clock timeout that throws on expiry. Default timeout is 3x expected wait time (minimum 30s). Crank and VRF scripts will no longer hang indefinitely if Solana slot production halts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix initialize.ts — program/programData accounts + ownership verification (H016)</name>
  <files>scripts/deploy/initialize.ts</files>
  <action>
    Fix two issues in `scripts/deploy/initialize.ts` per VERIFY-H016:

    **Issue 1: Missing program/programData in accountsStrict calls**

    Read the full initialize.ts file. Find ALL `accountsStrict` calls that correspond to instructions which require `program` and `programData` accounts (added in Phase 78-02). The AMM `initializeAdmin` call (around line 395) already correctly includes both — use that as the reference pattern.

    For each init instruction that requires ProgramData upgrade authority check:
    1. `initializeAuthority` (Transfer Hook) — around line 327. Add `program: hookProgramId` and `programData: hookProgramDataPda` to the accountsStrict call.
    2. `initializeStakePool` (Staking) — find the call. Add `program: stakingProgramId` and `programData: stakingProgramDataPda`.
    3. `initializeEpochState` (Epoch) — find the call. Add `program: epochProgramId` and `programData: epochProgramDataPda`.
    4. `initializeCarnageFund` (Epoch Carnage) — find the call. Add `program: epochProgramId` and `programData: epochProgramDataPda` (same program).
    5. `initialize` (Conversion Vault) — find the call. Add `program: vaultProgramId` and `programData: vaultProgramDataPda`.

    For each program, derive the ProgramData PDA using:
    ```ts
    const [programDataPda] = PublicKey.findProgramAddressSync(
      [programId.toBuffer()],
      new PublicKey("BPFLoaderUpgradeab1e11111111111111111111111")
    );
    ```

    Compute these PDAs once near the top of the script alongside existing PDA derivations.

    **Issue 2: Ownership verification before skipping**

    Currently, the script uses `accountExists()` (just checks `data.length > 0`) to decide whether to skip initialization. For each init step that has a PDA with an `authority` field:
    1. After `accountExists()` returns true, fetch and deserialize the account.
    2. Compare the stored `authority` field against `deployer.publicKey`.
    3. If they don't match, throw an error: `"SECURITY: ${accountName} exists but authority is ${storedAuthority}, expected ${deployer.publicKey}. Possible front-run attack."`.
    4. If they match, log `"${accountName} exists with correct authority, skipping."` and continue.

    Apply this to at minimum:
    - WhitelistAuthority (Transfer Hook): deserialize and check `authority` field
    - BcAdminConfig (Bonding Curve): deserialize and check `authority` field
    - AmmAdminConfig (AMM): already has programData check pattern — verify it also checks authority

    For other accounts (StakePool, EpochState, CarnageFundState, ConversionVault), if they don't have a simple `authority` field to check, at minimum verify the account is owned by the expected program: `accountInfo.owner.equals(expectedProgramId)`.

    Be careful: Read the existing code thoroughly first. Some of these patterns may already be partially implemented from Phase 78. Only add what's missing.
  </action>
  <verify>
    1. `grep -c "programData" scripts/deploy/initialize.ts` shows increased count (should be in every init call)
    2. `grep "SECURITY\|front-run\|authority.*expected" scripts/deploy/initialize.ts` confirms ownership checks
    3. `grep "BPFLoaderUpgradeab1e" scripts/deploy/initialize.ts` confirms ProgramData PDA derivation
    4. TypeScript compiles without errors
  </verify>
  <done>
    All init accountsStrict calls include program + programData accounts matching on-chain instruction requirements. Ownership verification prevents silent acceptance of front-run accounts. Deploy script is synced with Phase 78 on-chain changes.
  </done>
</task>

</tasks>

<verification>
- waitForSlotAdvance() throws on timeout (wall-clock based, not slot-count only)
- initialize.ts passes program + programData to all ProgramData-gated init instructions
- initialize.ts verifies account ownership before skipping any init step
- Both scripts compile without TypeScript errors
</verification>

<success_criteria>
H030 and H016 both closed. Slot waiting has a safety timeout. Deploy scripts are synced with on-chain instruction accounts and have ownership verification.
</success_criteria>

<output>
After completion, create `.planning/phases/90-gap-closure-launch-verification/90-03-SUMMARY.md`
</output>
