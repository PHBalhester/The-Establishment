---
phase: 88-documentation-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Docs/upgrade-cascade.md
  - Docs/DOC_MANIFEST.md
  - Docs/architecture.md
autonomous: true
requirements: [DOC-03]

must_haves:
  truths:
    - "Cross-program CPI dependency graph is documented with safe upgrade order"
    - "Planning artifacts and deployment reports are archived out of active Docs/"
    - "DOC_MANIFEST.md is updated as master index of all active docs with categories and last-verified dates"
  artifacts:
    - path: "Docs/upgrade-cascade.md"
      provides: "CPI dependency graph, breaking change categories, safe upgrade order, upgrade-at-same-address commitment"
      min_lines: 80
    - path: "Docs/DOC_MANIFEST.md"
      provides: "Master index of all active docs with purpose, category, last-verified date"
      min_lines: 40
  key_links:
    - from: "Docs/architecture.md"
      to: "Docs/upgrade-cascade.md"
      via: "cross-reference link"
      pattern: "upgrade-cascade"
---

<objective>
Archive stale planning artifacts, create upgrade-cascade.md documenting cross-program CPI dependencies, and update DOC_MANIFEST.md as the master index.

Purpose: Clean the Docs/ directory so active spec docs are clearly separated from historical artifacts, and document the critical upgrade cascade that operators need for safe program upgrades.
Output: Docs/upgrade-cascade.md, updated DOC_MANIFEST.md, archived files moved to Docs/archive/
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-documentation-overhaul/88-CONTEXT.md
@Docs/DOC_MANIFEST.md
@Docs/architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive stale files and create upgrade-cascade.md</name>
  <files>
    Docs/archive/ (moved files)
    Docs/upgrade-cascade.md
    Docs/architecture.md
  </files>
  <action>
    **Step 1: Archive stale files.** Move these planning artifacts and deployment reports to Docs/archive/:
    - DBS-base-profit-redesign.md (implemented redesign, historical)
    - E2E_Devnet_Test_Report.md (Phase 69 test report, historical)
    - Jupiter_DEX_Integration_Roadmap.md (future planning doc)
    - redeploy-schedule.md (Phase 69 schedule, historical)
    - STATE.json (stale state file)
    - CODERECON_CONTEXT.md (one-time recon context)
    - RECONCILIATION_REPORT.md (one-time reconciliation)
    - installed-ai-skills-guide.md (internal tooling doc, not protocol spec)
    - v1.1-asset-spec.md (asset spec for shipped milestone)

    Use `git mv` for each file to preserve history.

    **Step 2: Create Docs/upgrade-cascade.md.** Read the actual program source to build the CPI dependency graph:
    - Read programs/tax-program/src/lib.rs and its instruction handlers to identify which programs Tax calls via CPI
    - Read programs/epoch-program/src/lib.rs for Epoch's CPI targets
    - Read programs/bonding_curve/src/lib.rs for BC's CPI targets
    - Read programs/transfer-hook/src/lib.rs for Hook's interface
    - Read programs/staking/src/lib.rs for Staking's CPI targets
    - Read programs/conversion-vault/src/lib.rs for Vault's CPI targets
    - Read programs/amm/src/lib.rs for AMM's CPI targets

    Document in upgrade-cascade.md:
    1. CPI dependency graph (which program calls which, which instructions)
    2. Breaking change categories (account layout changes, instruction signature changes, PDA seed changes)
    3. Safe upgrade order (leaf programs first, then programs that CPI into them)
    4. Upgrade-at-same-address commitment statement
    5. Brief "Future" section noting planned 2-of-3 Squads multisig with 48-72hr timelock (v1.4 scope)

    Audience: developer/operational. Concise and actionable.

    **Step 3: Add cross-reference in architecture.md.** Find the appropriate section (likely near deployment or program composition) and add a reference: "For safe upgrade ordering and CPI dependency analysis, see [upgrade-cascade.md](upgrade-cascade.md)."
  </action>
  <verify>
    - `test -f Docs/upgrade-cascade.md` confirms file exists
    - `grep -l "upgrade-cascade" Docs/architecture.md` confirms cross-reference
    - `ls Docs/archive/DBS-base-profit-redesign.md` confirms archive move
    - Upgrade cascade doc contains all 7 programs in the dependency graph
  </verify>
  <done>
    Docs/upgrade-cascade.md exists with CPI dependency graph covering all 7 programs, safe upgrade order, and upgrade-at-same-address commitment. architecture.md references it. 9 stale files archived.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update DOC_MANIFEST.md as master index</name>
  <files>Docs/DOC_MANIFEST.md</files>
  <action>
    Rewrite DOC_MANIFEST.md as the master index of all active docs in Docs/. For each active document:
    - **File name** (exact path)
    - **Purpose** (1-line description)
    - **Category**: spec | audit | operational | reference
    - **Last verified date** (use file modification date as proxy, mark as "needs verification" for files not touched since v0.7 or earlier)

    Categories:
    - **spec**: Protocol behavior documentation (architecture.md, data-model.md, token-economics-model.md, cpi-interface-contract.md, frontend-spec.md, security-model.md, error-handling-playbook.md, account-layout-reference.md, token-interaction-matrix.md, Bonding_Curve_Spec.md, upgrade-cascade.md)
    - **audit**: Security findings (VULNHUNTER-AUDIT-2026-03-05.md, SFK.md, SECURITY_TESTS.md)
    - **operational**: Deployment and operations (deployment-sequence.md, operational-runbook.md, oracle-failure-playbook.md, mainnet-checklist.md, mainnet-readiness-assessment.md, Compute_Budget_Profile.md)
    - **reference**: Project context (project-overview.md, PROJECT_BRIEF.md, liquidity-slippage-analysis.md, edge-case-audit.md)

    Remove the old wave-based structure. The new manifest is a flat categorized index.

    Add a "Docs/archive/" section listing archived files with brief reason for archival.

    Add header noting: "This manifest is the master index. If a doc is not listed here, it is either archived or orphaned."
  </action>
  <verify>
    - `grep -c "Category" Docs/DOC_MANIFEST.md` shows categorization exists
    - Every .md file in Docs/ (non-archive, non-DECISIONS) appears in the manifest
    - Archive section lists moved files
  </verify>
  <done>
    DOC_MANIFEST.md is a flat categorized index of all active docs with purpose, category, and verification status. Archive section documents what was moved and why.
  </done>
</task>

</tasks>

<verification>
- Docs/upgrade-cascade.md exists and covers all 7 programs
- architecture.md has cross-reference to upgrade-cascade.md
- DOC_MANIFEST.md is a categorized index of all active docs
- 9 stale files moved to Docs/archive/
- No active spec docs deleted (only moved to archive)
</verification>

<success_criteria>
DOC-03 is satisfied: cross-program upgrade cascade documented as upgrade-at-same-address commitment with CPI dependency graph and safe upgrade order. Docs/ directory is organized with manifest as master index.
</success_criteria>

<output>
After completion, create `.planning/phases/88-documentation-overhaul/88-01-SUMMARY.md`
</output>
