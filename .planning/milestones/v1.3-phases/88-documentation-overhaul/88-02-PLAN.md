---
phase: 88-documentation-overhaul
plan: 02
type: execute
wave: 2
depends_on: [88-01]
files_modified:
  - Docs/carnage-spec.md
  - Docs/epoch-spec.md
  - Docs/tax-spec.md
  - Docs/transfer-hook-spec.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "Carnage spec accurately describes shared carnage_execution.rs module, 6 paths (BuyOnly+Burn+Sell x CRIME+FRAUD), and carnage_lock_slot behavior"
    - "Epoch spec accurately describes VRF flow, force_carnage, epoch skip behavior, and reserved padding"
    - "Tax spec accurately describes sell floor propagation, is_reversed detection, and all error codes 6014-6017"
    - "Transfer Hook spec accurately describes whitelist PDA, remaining_accounts schema, and dual-hook ordering for PROFIT pools"
  artifacts:
    - path: "Docs/carnage-spec.md"
      provides: "Carnage Fund specification matching current code"
      min_lines: 100
    - path: "Docs/epoch-spec.md"
      provides: "Epoch/VRF specification matching current code"
      min_lines: 100
    - path: "Docs/tax-spec.md"
      provides: "Tax Program specification matching current code"
      min_lines: 100
    - path: "Docs/transfer-hook-spec.md"
      provides: "Transfer Hook specification matching current code"
      min_lines: 80
  key_links:
    - from: "Docs/carnage-spec.md"
      to: "programs/epoch-program/src/carnage_execution.rs"
      via: "code-first documentation"
      pattern: "carnage_execution"
    - from: "Docs/tax-spec.md"
      to: "programs/tax-program/src/"
      via: "code-first documentation"
      pattern: "sell|buy|tax"
---

<objective>
Rewrite the 4 core spec docs (Carnage, Epoch, Tax, Transfer Hook) from scratch based on current program source code.

Purpose: These 4 specs are named in DOC-01 as the most critically stale. Code has changed dramatically across 12 milestones. Fresh code-first rewrites ensure developer documentation matches what the programs actually do today.
Output: 4 fresh spec docs in Docs/ (kebab-case naming)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/88-documentation-overhaul/88-CONTEXT.md
@.planning/phases/88-documentation-overhaul/88-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Carnage and Epoch spec docs from program source</name>
  <files>
    Docs/carnage-spec.md
    Docs/epoch-spec.md
  </files>
  <action>
    **Code-first approach:** Read the actual program source first, then write documentation from what the code does. Do NOT copy from existing stale specs.

    **Carnage spec (Docs/carnage-spec.md):**
    Read these source files:
    - programs/epoch-program/src/carnage_execution.rs (shared module from Phase 82 refactor)
    - programs/epoch-program/src/instructions/execute_carnage.rs
    - programs/epoch-program/src/instructions/execute_carnage_atomic.rs
    - programs/epoch-program/src/state.rs (CarnageFund, EpochState fields)

    Document:
    1. What Carnage is (redistribution mechanism triggered by VRF)
    2. The 6 execution paths: BuyOnly, Burn, Sell x CRIME/FRAUD
    3. How path selection works (VRF randomness -> crime_side enum + carnage type)
    4. Shared module architecture (carnage_execution.rs extracted logic)
    5. Account requirements (23 accounts for sell path, boxed InterfaceAccounts)
    6. ALT requirement for sell path (client-side, >1232 bytes)
    7. carnage_lock_slot behavior and state machine transitions
    8. Dual-hook ordering for PROFIT pools (input hooks first, output hooks second)
    9. Slippage: read_pool_reserves() for pre-CPI check, 50% floor

    **Epoch spec (Docs/epoch-spec.md):**
    Read these source files:
    - programs/epoch-program/src/instructions/ (all instruction handlers)
    - programs/epoch-program/src/state.rs (EpochState with reserved padding)
    - programs/epoch-program/src/lib.rs (instruction routing)

    Document:
    1. Epoch lifecycle: initialize -> trigger_transition -> VRF flow -> carnage -> next epoch
    2. VRF integration: commit_randomness, reveal_randomness, consume_randomness
    3. force_carnage (devnet only, guarded by compile_error! without devnet feature)
    4. Epoch skip behavior (documented as by-design, 4 safety properties)
    5. Bounty system: trigger_epoch_transition bounty with rent-exempt guard
    6. EpochState layout: reserved [u8; 64] padding, #[repr(C)], DATA_LEN assertions
    7. Tax derivation from VRF (low_tax_bps/high_tax_bps populated as min/max of 4 rates)
    8. retry_epoch_vrf for timeout recovery
    9. Anti-reroll protection (ConstraintAddress on randomness account)

    Both docs should include:
    - Program ID reference
    - Account layout summary (field names, types, sizes)
    - Error codes with descriptions
    - CPI dependencies (which other programs are called)
  </action>
  <verify>
    - `test -f Docs/carnage-spec.md && test -f Docs/epoch-spec.md`
    - `grep -c "carnage_execution" Docs/carnage-spec.md` confirms shared module documented
    - `grep -c "reserved" Docs/epoch-spec.md` confirms padding documented
    - `grep -c "force_carnage" Docs/epoch-spec.md` confirms devnet-only instruction documented
  </verify>
  <done>
    Carnage spec documents all 6 paths, shared module, ALT requirement, dual-hook ordering. Epoch spec documents full lifecycle, VRF flow, force_carnage, epoch skip, reserved padding, retry_epoch_vrf.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Tax and Transfer Hook spec docs from program source</name>
  <files>
    Docs/tax-spec.md
    Docs/transfer-hook-spec.md
  </files>
  <action>
    **Code-first approach:** Read program source, write docs from code.

    **Tax spec (Docs/tax-spec.md):**
    Read these source files:
    - programs/tax-program/src/instructions/ (buy.rs, sell.rs, swap_exempt.rs, etc.)
    - programs/tax-program/src/state.rs
    - programs/tax-program/src/pool_reader.rs (is_reversed detection, read_pool_reserves)
    - programs/tax-program/src/lib.rs
    - programs/tax-program/src/errors.rs (codes 6014-6017)

    Document:
    1. Tax calculation: how tax bands are derived from EpochState VRF results
    2. Buy path: SOL -> token with tax extraction
    3. Sell path: token -> SOL with sell floor propagation (computed gross minimum to AMM CPI, NOT minimum_amount_out = 0)
    4. Swap exempt: execute_swap_exempt_cpi with pool/mint params (callers select held vs target pool)
    5. Pool reader: is_reversed canonical mint detection, ownership verification (AMM program owner check)
    6. Error codes: full catalog including 6014-6017 (MinimumOutputFloorViolation etc.)
    7. EpochState mirror struct with #[repr(C)] and DATA_LEN assertion
    8. Transfer Hook integration: remaining_accounts for hook CPIs, HOOK_ACCOUNTS_PER_MINT=4
    9. CPI to AMM: transfer_checked_with_hook helper (not Anchor SPL which drops remaining_accounts)

    **Transfer Hook spec (Docs/transfer-hook-spec.md):**
    Read these source files:
    - programs/transfer-hook/src/lib.rs
    - programs/transfer-hook/src/instructions/ (all handlers)
    - programs/transfer-hook/src/state.rs

    Document:
    1. What the hook does (whitelist validation on Token-2022 transfers)
    2. Whitelist PDA derivation and account layout
    3. ExtraAccountMetaList schema (4 accounts per mint)
    4. Initialize authority with ProgramData upgrade authority check
    5. How hooks fire during transfers (Token-2022 CPI callback)
    6. Which tokens have hooks (CRIME, FRAUD) vs which don't (PROFIT uses SPL Token)
    7. Remaining accounts ordering: input hooks first, output hooks second (dual-hook for PROFIT pools)
    8. Error handling: what happens when whitelist check fails

    Both docs should include program ID, account layouts, error codes, CPI dependencies.
  </action>
  <verify>
    - `test -f Docs/tax-spec.md && test -f Docs/transfer-hook-spec.md`
    - `grep -c "is_reversed" Docs/tax-spec.md` confirms canonical mint detection documented
    - `grep -c "6014\|6015\|6016\|6017" Docs/tax-spec.md` confirms error codes documented
    - `grep -c "ExtraAccountMeta" Docs/transfer-hook-spec.md` confirms hook schema documented
  </verify>
  <done>
    Tax spec documents buy/sell paths, sell floor propagation, pool reader with is_reversed, all error codes 6014-6017. Transfer Hook spec documents whitelist PDA, ExtraAccountMetaList schema, 4 accounts per mint, dual-hook ordering.
  </done>
</task>

</tasks>

<verification>
- All 4 spec docs exist in Docs/ with kebab-case naming
- Each doc was written code-first (not copied from stale specs)
- Carnage: 6 paths, shared module, ALT, dual-hook ordering
- Epoch: full lifecycle, VRF, force_carnage, padding, retry
- Tax: buy/sell paths, sell floor, pool reader, error codes
- Transfer Hook: whitelist, ExtraAccountMetaList, hook ordering
</verification>

<success_criteria>
The 4 named specs in DOC-01 are rewritten from current program source. No stale descriptions of removed or changed behavior. Each doc serves as accurate developer reference for the program it describes.
</success_criteria>

<output>
After completion, create `.planning/phases/88-documentation-overhaul/88-02-SUMMARY.md`
</output>
