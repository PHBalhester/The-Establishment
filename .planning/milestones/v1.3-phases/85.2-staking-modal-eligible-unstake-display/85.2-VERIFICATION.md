---
phase: 85.2-staking-modal-eligible-unstake-display
verified: 2026-03-08T17:30:00Z
status: passed
score: 5/5 must-haves verified
---

# Phase 85.2: Staking Modal Eligible Unstake Display Verification Report

**Phase Goal:** Add individual unstake eligibility badge and protocol-wide locked/unlocked PROFIT stats to the staking modal — single source of truth for cooldown display
**Verified:** 2026-03-08T17:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | User with active stake sees green "Eligible to Unstake" or amber cooldown badge above stats grid on all tabs | VERIFIED | StakingStats.tsx lines 151-166: badge conditional on `hasActiveStake`, green/amber based on `isCooldownActive`. Rendered in StakingForm.tsx above tabs (line 109), visible on all 3 tabs. |
| 2 | User without a stake sees no eligibility badge | VERIFIED | StakingForm.tsx line 120: `hasActiveStake={!!staking.userStakeData && staking.userStakeData.stakedBalance > 0}`. Badge hidden when false (StakingStats.tsx line 152). |
| 3 | StakingStats grid shows "Unlocked PROFIT" and "Locked PROFIT" protocol-wide totals (8 items, 4 rows) | VERIFIED | StakingStats.tsx lines 223-235: two new StatItem entries at end of grid. Grid has 8 StatItems total in `grid-cols-2` layout = 4 rows. |
| 4 | UnstakeTab no longer has its own cooldown banner (single source of truth in StakingStats badge) | VERIFIED | UnstakeTab.tsx has no cooldown banner or countdown display. Comment on line 12 confirms removal. Props `isCooldownActive`/`cooldownRemainingMs` retained for button disable logic only. |
| 5 | Global stats load on modal open and show skeleton/loading state while fetching | VERIFIED | `fetchGlobalStats` called inside `fetchStakingData` (line 344), triggered on `isActive` (line 363). `globalStatsLoading` state drives loading skeletons in StatItems (StakingStats.tsx lines 228, 234). |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/hooks/useStaking.ts` | Global staking stats via getProgramAccounts | VERIFIED | 776 lines. `fetchGlobalStats` (lines 246-287) uses `program.account.userStake.all()`. Exports `globalStats`, `globalStatsLoading`, `isEligibleToUnstake`. No stubs. Imported by StakingForm.tsx. |
| `app/components/staking/StakingStats.tsx` | Eligibility badge + Unlocked/Locked PROFIT stat items | VERIFIED | 240 lines. Badge with "Eligible to Unstake" text (line 157). 8 StatItems including Unlocked/Locked PROFIT. Exported, imported by StakingForm.tsx. |
| `app/components/staking/StakingForm.tsx` | Wires new props from useStaking to StakingStats | VERIFIED | 192 lines. Lines 117-123 wire all new props: `isEligibleToUnstake`, `isCooldownActive`, `cooldownRemainingMs`, `hasActiveStake`, `unlockedProfit`, `lockedProfit`, `globalStatsLoading`. |
| `app/components/staking/UnstakeTab.tsx` | Cooldown banner removed | VERIFIED | 150 lines. No cooldown banner or countdown display. Only forfeiture warning and minimum stake warning remain. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| useStaking.ts | Staking program (all accounts) | `program.account.userStake.all()` | WIRED | Line 252: Anchor typed `.all()` call. Result iterated to sum locked/unlocked PROFIT (lines 260-274). |
| StakingForm.tsx | StakingStats.tsx | New props (eligibility + global stats) | WIRED | Lines 117-123: all 7 new props passed. Props match StakingStatsProps interface exactly. |
| fetchGlobalStats | fetchStakingData lifecycle | Called at end of fetchStakingData | WIRED | Line 344: `await fetchGlobalStats()` inside fetchStakingData, shares 30s poll and burst-refresh lifecycle. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | - | - | - | No TODO, FIXME, placeholder, or stub patterns found in any modified file. |

### Human Verification Required

### 1. Badge Visual Appearance
**Test:** Open staking modal with an active stake. Verify green badge appears above stats grid.
**Expected:** Green pill badge with dot indicator and text "Eligible to Unstake" centered above grid.
**Why human:** Visual appearance (colors, spacing, centering) cannot be verified programmatically.

### 2. Cooldown Badge Countdown
**Test:** Claim rewards to trigger cooldown, then reopen staking modal.
**Expected:** Amber pill badge shows "Cooldown: Xh Ym" with live countdown updating every second.
**Why human:** Real-time countdown behavior requires live testing.

### 3. Badge Visibility Across All Tabs
**Test:** Switch between Stake, Unstake, and Claim tabs while badge is visible.
**Expected:** Badge remains visible and identical on all three tabs (StakingStats is above the tab panel).
**Why human:** Tab switching behavior needs visual confirmation.

### 4. Global Stats Loading State
**Test:** Open staking modal and observe Unlocked/Locked PROFIT rows during initial load.
**Expected:** Show "Loading..." animation, then resolve to real values.
**Why human:** Loading state timing cannot be verified structurally.

---

_Verified: 2026-03-08T17:30:00Z_
_Verifier: Claude (gsd-verifier)_
