---
phase: 85.2-staking-modal-eligible-unstake-display
plan: 01
subsystem: ui
tags: [react, staking, anchor, getProgramAccounts, cooldown, eligibility]

# Dependency graph
requires:
  - phase: 26-29
    provides: Staking program, UserStake account structure, cooldown logic
  - phase: 60-69
    provides: StakingForm/StakingStats component architecture, useStaking hook
provides:
  - Eligibility badge (green/amber) above StakingStats grid on all tabs
  - Protocol-wide locked/unlocked PROFIT stats via getProgramAccounts
  - isEligibleToUnstake derived state in useStaking
affects: [86-test-coverage-sweep]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Global stats via Anchor's .all() in existing hook lifecycle (no separate timer)"
    - "Eligibility badge as single source of truth (removed duplicate cooldown banner)"

key-files:
  created: []
  modified:
    - app/hooks/useStaking.ts
    - app/components/staking/StakingStats.tsx
    - app/components/staking/StakingForm.tsx
    - app/components/staking/UnstakeTab.tsx

key-decisions:
  - "Global stats fetched inside fetchStakingData (shares 30s poll lifecycle, no separate timer)"
  - "formatCountdown removed from UnstakeTab (dead code after banner removal)"

patterns-established:
  - "Single source of truth: eligibility/cooldown display in StakingStats badge only"

# Metrics
duration: 5min
completed: 2026-03-08
---

# Phase 85.2 Plan 01: Staking Modal Eligible Unstake Display Summary

**Eligibility badge + protocol-wide locked/unlocked PROFIT stats via Anchor getProgramAccounts, with UnstakeTab cooldown banner consolidated**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-08T17:09:47Z
- **Completed:** 2026-03-08T17:15:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Added fetchGlobalStats using Anchor's userStake.all() to compute protocol-wide locked/unlocked PROFIT
- Added green "Eligible to Unstake" and amber "Cooldown: Xh Ym" badge above StakingStats grid
- Added 2 new grid items (Unlocked PROFIT, Locked PROFIT) with loading skeletons -- grid now 8 items (4 rows)
- Removed duplicate cooldown banner from UnstakeTab (single source of truth in StakingStats badge)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add global staking stats fetch + eligibility state to useStaking** - `4c2b74f` (feat)
2. **Task 2: Update StakingStats, StakingForm, and UnstakeTab UI** - `3f6236d` (feat)

## Files Created/Modified
- `app/hooks/useStaking.ts` - Added fetchGlobalStats, globalStats state, isEligibleToUnstake derived value
- `app/components/staking/StakingStats.tsx` - Added eligibility badge row + Unlocked/Locked PROFIT grid items
- `app/components/staking/StakingForm.tsx` - Wired new props (eligibility, cooldown, global stats) to StakingStats
- `app/components/staking/UnstakeTab.tsx` - Removed cooldown banner, cleaned up unused formatCountdown helper

## Decisions Made
- Global stats fetched inside fetchStakingData at the end (shares same 30s poll lifecycle) rather than a separate timer -- simpler, no double-fetch risk
- Removed formatCountdown from UnstakeTab (dead code after banner removal; StakingStats has its own copy)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Staking modal now shows eligibility badge on all tabs and protocol-wide stats
- Ready for Phase 86 (Test Coverage Sweep)

---
*Phase: 85.2-staking-modal-eligible-unstake-display*
*Completed: 2026-03-08*
