---
phase: 85.2-staking-modal-eligible-unstake-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/hooks/useStaking.ts
  - app/components/staking/StakingStats.tsx
  - app/components/staking/StakingForm.tsx
  - app/components/staking/UnstakeTab.tsx
autonomous: true

must_haves:
  truths:
    - "User with an active stake sees a green 'Eligible to Unstake' badge or an amber cooldown countdown badge above the stats grid on all tabs"
    - "User without a stake sees no eligibility badge"
    - "StakingStats grid shows 'Unlocked PROFIT' and 'Locked PROFIT' protocol-wide totals (8 items, 4 rows)"
    - "UnstakeTab no longer has its own cooldown banner (single source of truth in StakingStats badge)"
    - "Global stats load on modal open and show skeleton/loading state while fetching"
  artifacts:
    - path: "app/hooks/useStaking.ts"
      provides: "Global staking stats (locked/unlocked PROFIT) via getProgramAccounts"
      contains: "getProgramAccounts"
    - path: "app/components/staking/StakingStats.tsx"
      provides: "Eligibility badge + Unlocked/Locked PROFIT stat items"
      contains: "Eligible to Unstake"
    - path: "app/components/staking/UnstakeTab.tsx"
      provides: "Cooldown banner removed"
    - path: "app/components/staking/StakingForm.tsx"
      provides: "Wires new props (eligibility + global stats) from useStaking to StakingStats"
  key_links:
    - from: "app/hooks/useStaking.ts"
      to: "staking program via getProgramAccounts"
      via: "Anchor program.account.userStake.all() or connection.getProgramAccounts with discriminator filter"
      pattern: "getProgramAccounts|userStake\\.all"
    - from: "app/components/staking/StakingForm.tsx"
      to: "app/components/staking/StakingStats.tsx"
      via: "new props: eligibility state + global locked/unlocked amounts"
      pattern: "unlockedProfit|lockedProfit|isEligible"
---

<objective>
Add individual unstake eligibility badge and protocol-wide locked/unlocked PROFIT stats to the staking modal.

Purpose: Users need to know at a glance whether they can unstake (or how long until they can), and the protocol needs transparency on how much PROFIT is currently locked vs unlocked across all stakers.

Output: Updated StakingStats with eligibility badge row + 2 new grid items, updated useStaking with global stats fetch, cleaned up UnstakeTab (cooldown banner removed).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85.2-staking-modal-eligible-unstake-display/85.2-CONTEXT.md

@app/hooks/useStaking.ts
@app/components/staking/StakingStats.tsx
@app/components/staking/StakingForm.tsx
@app/components/staking/UnstakeTab.tsx
@app/lib/staking/staking-builders.ts (for deriveUserStakePDA, PROGRAM_IDS.STAKING)
@app/lib/anchor.ts (for getStakingProgram)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global staking stats fetch + eligibility state to useStaking</name>
  <files>app/hooks/useStaking.ts</files>
  <action>
Add global staking stats (locked/unlocked PROFIT) to useStaking.ts:

1. **New state**: Add `globalStats: { unlockedProfit: number; lockedProfit: number } | null` state and `globalStatsLoading: boolean`.

2. **Fetch function**: Create `fetchGlobalStats` callback that:
   - Uses `getStakingProgram().account.userStake.all()` to fetch ALL UserStake accounts. This uses Anchor's typed deserialization over getProgramAccounts with the correct discriminator filter automatically. Anchor's `.all()` is the idiomatic way.
   - For each account, reads `lastClaimTs` (i64) and `stakedBalance` (u64).
   - Computes eligibility per account: if `lastClaimTs === 0` OR `(Date.now()/1000 - lastClaimTs) >= COOLDOWN_SECONDS` then that account's balance is "unlocked", otherwise "locked".
   - Sums up `unlockedProfit` and `lockedProfit` (both in PROFIT base units).
   - Sets `globalStats` state.
   - Wraps in try/catch — on error, log and leave globalStats as null (don't crash the modal).

3. **Trigger**: Call `fetchGlobalStats` alongside `fetchStakingData` — on modal open (isActive), on 30s poll interval, and on burst-refresh. The simplest approach: call it inside `fetchStakingData` at the end (after pool + user fetches), so it shares the same lifecycle. This avoids a separate timer. Gate behind a flag to avoid double-fetch: only run if `isActive`.

4. **Expose in return**: Add to UseStakingReturn interface:
   - `globalStats: { unlockedProfit: number; lockedProfit: number } | null`
   - `globalStatsLoading: boolean`
   - `isEligibleToUnstake: boolean` (derived: true when user has stake AND cooldown is NOT active — i.e., `userStakeRaw !== null && userStakeRaw.stakedBalance > 0 && !isCooldownActive`)

5. **Performance note**: `program.account.userStake.all()` does a single RPC call (getProgramAccounts with discriminator filter). On devnet with few stakers this is instant. For mainnet with many stakers, this could be optimized later with a server-side API route + TTL cache. For now, direct client call is fine per CONTEXT.md "Claude's discretion" on performance strategy.

Do NOT create a separate hook — useStaking is the sole hook consumer for staking UI (established pattern). Keep it all in useStaking.
  </action>
  <verify>
Run `export PATH="/opt/homebrew/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && npx tsc --noEmit` — no TypeScript errors.
Verify `globalStats`, `globalStatsLoading`, and `isEligibleToUnstake` are exported in the UseStakingReturn interface.
  </verify>
  <done>
useStaking exports globalStats (locked/unlocked PROFIT sums), globalStatsLoading, and isEligibleToUnstake. Global stats are fetched on modal open and every 30s poll cycle. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update StakingStats, StakingForm, and UnstakeTab UI</name>
  <files>app/components/staking/StakingStats.tsx, app/components/staking/StakingForm.tsx, app/components/staking/UnstakeTab.tsx</files>
  <action>
**StakingStats.tsx** — Add eligibility badge + 2 new grid items:

1. **New props**: Add to StakingStatsProps:
   - `isEligibleToUnstake: boolean`
   - `isCooldownActive: boolean`
   - `cooldownRemainingMs: number`
   - `hasActiveStake: boolean` (controls badge visibility)
   - `unlockedProfit: number | null` (base units, null while loading)
   - `lockedProfit: number | null` (base units, null while loading)
   - `globalStatsLoading: boolean`

2. **Eligibility badge**: Render a full-width row ABOVE the grid (outside the grid div, inside the mb-3 wrapper):
   - Only render when `hasActiveStake` is true (no badge = no stake, implicit per CONTEXT.md).
   - When `!isCooldownActive`: Green pill/badge — text "Eligible to Unstake", green background (`bg-emerald-500/10 text-emerald-400 border border-emerald-500/20`), rounded-full padding.
   - When `isCooldownActive`: Amber pill/badge — text "Cooldown: Xh Ym" using same `formatCountdown` helper (copy from UnstakeTab or extract). Amber colors (`bg-amber-500/10 text-amber-400 border border-amber-500/20`).
   - Badge should be centered, with a small margin-bottom before the grid (`mb-3`).
   - Use a small icon or dot indicator before the text (a simple colored circle via CSS, not an emoji).

3. **New grid items**: Add 2 new StatItem entries at the END of the grid (after "Lifetime Claimed"):
   - "Unlocked PROFIT": value = `formatProfit(unlockedProfit)` + " PROFIT" when not null, loading skeleton when globalStatsLoading.
   - "Locked PROFIT": value = `formatProfit(lockedProfit)` + " PROFIT" when not null, loading skeleton when globalStatsLoading.
   - Grid grows from 6 to 8 items (4 rows of 2 columns). No layout changes needed — grid-cols-2 auto-expands.

**StakingForm.tsx** — Wire new props:

4. Pass the new props from `staking` (useStaking return) to `<StakingStats>`:
   - `isEligibleToUnstake={staking.isEligibleToUnstake}`
   - `isCooldownActive={staking.isCooldownActive}`
   - `cooldownRemainingMs={staking.cooldownRemainingMs}`
   - `hasActiveStake={!!staking.userStakeData && staking.userStakeData.stakedBalance > 0}`
   - `unlockedProfit={staking.globalStats?.unlockedProfit ?? null}`
   - `lockedProfit={staking.globalStats?.lockedProfit ?? null}`
   - `globalStatsLoading={staking.globalStatsLoading}`

**UnstakeTab.tsx** — Remove cooldown banner:

5. Delete the cooldown banner block (lines 145-152 approximately — the `{isCooldownActive && (...)}` div). The eligibility badge in StakingStats is now the single source of truth for cooldown display.
   - Keep the `isCooldownActive` and `cooldownRemainingMs` props in the interface (still needed for button disable logic in StakingForm).
   - Keep the `formatCountdown` helper (or remove if no longer used — check if forfeiture warning uses it. It does NOT, so safe to remove from UnstakeTab if badge has its own).
   - Actually, keep `isCooldownActive` prop since StakingForm still checks it for `actionDisabled`. But the VISUAL banner is removed.

Do NOT change the tab structure, action button logic, or any transaction execution code.
  </action>
  <verify>
Run `export PATH="/opt/homebrew/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && npx tsc --noEmit` — no TypeScript errors.
Run `export PATH="/opt/homebrew/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && npx next build` — build succeeds.
Visually confirm: StakingStats has badge row + 8 grid items. UnstakeTab has no cooldown banner.
  </verify>
  <done>
StakingStats displays eligibility badge (green/amber) above grid when user has active stake. Grid has 8 items including Unlocked/Locked PROFIT. UnstakeTab cooldown banner is removed. Build passes cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npx next build` succeeds
3. StakingStats renders eligibility badge above grid on all 3 tabs (Stake/Unstake/Claim)
4. Badge shows green "Eligible to Unstake" when no cooldown, amber countdown when cooldown active
5. No badge renders when user has no stake
6. Grid shows 8 items: original 6 + Unlocked PROFIT + Locked PROFIT
7. Global stats show loading skeleton while fetching, then real values
8. UnstakeTab no longer has its own cooldown banner
9. Unstake action button still correctly disabled during cooldown (existing logic preserved)
</verification>

<success_criteria>
- Eligibility badge visible on all tabs when user has active stake
- Cooldown countdown in badge matches previous UnstakeTab banner behavior
- Unlocked/Locked PROFIT sums computed correctly from all UserStake accounts
- No duplicate cooldown display (removed from UnstakeTab)
- TypeScript + Next.js build pass
</success_criteria>

<output>
After completion, create `.planning/phases/85.2-staking-modal-eligible-unstake-display/85.2-01-SUMMARY.md`
</output>
