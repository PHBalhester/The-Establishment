---
phase: 82-carnage-refactor
plan: 02
type: execute
wave: 2
depends_on: [82-01]
files_modified: []
autonomous: true
requirements: [CARN-02]

must_haves:
  truths:
    - "All 6 Carnage paths (BuyOnly+Burn+Sell x CRIME+FRAUD) pass integration tests after the refactor"
    - "Compute budget for each Carnage path is within 5% of pre-refactor baseline"
  artifacts: []
  key_links:
    - from: "tests/integration/carnage.test.ts or equivalent"
      to: "programs/epoch-program"
      via: "Devnet integration test execution"
      pattern: "executeCarnage"
---

<objective>
Verify the carnage refactor has zero behavioral regression by running all 6 Carnage integration test paths on devnet and comparing compute unit consumption against pre-refactor baseline.

Purpose: The refactor touched critical financial execution logic (token burns, swaps, SOL wrapping). Any behavioral change -- even a subtle one like different CPI account ordering -- would break Carnage execution on-chain. CU comparison catches hidden inefficiencies from the extraction.

Output: All 6 paths pass. CU values documented. No path regresses >5%.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/82-carnage-refactor/82-CONTEXT.md
@.planning/phases/82-carnage-refactor/82-01-SUMMARY.md
@tests/integration/carnage.test.ts
@scripts/e2e/lib/carnage-flow.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture CU baseline (pre-refactor) and run post-refactor regression</name>
  <files></files>
  <action>
IMPORTANT: The CU baseline must be captured BEFORE the refactor code is deployed. Since Plan 01 only changes Rust source (not deployed), the currently deployed epoch_program on devnet IS the pre-refactor version.

**Step 1: Capture pre-refactor CU baseline**
Run the existing 6 Carnage integration test paths on devnet. For each path, extract the compute units consumed from the transaction logs.

The 6 paths are:
- BuyOnly CRIME
- BuyOnly FRAUD
- Burn CRIME (burn held + buy CRIME)
- Burn FRAUD (burn held + buy FRAUD)
- Sell CRIME (sell held + buy CRIME)
- Sell FRAUD (sell held + buy FRAUD)

Use the existing test infrastructure in `tests/integration/carnage.test.ts` or `scripts/e2e/lib/carnage-flow.ts`. Record CU for each path.

If the existing tests don't individually expose CU per path, parse it from Solana transaction logs: look for "consumed X of Y compute units" in the transaction confirmation.

**Step 2: Deploy refactored epoch_program to devnet**
Build with devnet feature and deploy:
```bash
source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH"
cd /Users/mlbob/Projects/Dr\ Fraudsworth
anchor build -p epoch_program -- --features devnet
anchor deploy -p epoch_program --provider.cluster devnet --provider.wallet keypairs/devnet-wallet.json
```

Note: This is a program upgrade (same program ID), not a fresh deploy. The existing EpochState and CarnageFundState PDAs persist.

**Step 3: Run post-refactor tests**
Run the same 6 paths again on the newly deployed program. Record CU for each.

**Step 4: Compare and gate**
For each path, compute `(post_cu - pre_cu) / pre_cu * 100`. If ANY path regresses >5%, investigate and report (do not silently pass).

Document both baseline and post-refactor CU values in the plan summary.

Note: If devnet state doesn't allow running all 6 paths (e.g., Carnage not pending, wrong epoch state), the crank may need to advance epochs. Use the crank runner or manually trigger epoch transitions as needed. The key thing is that all 6 paths must be exercised.

If devnet testing is not feasible (e.g., wallet balance too low, programs in wrong state), fall back to `cargo test-sbf -p epoch_program` for the Rust-level tests and document why devnet was skipped.
  </action>
  <verify>
All 6 Carnage paths execute successfully (no transaction errors). CU for each path is within 5% of baseline. Results documented.
  </verify>
  <done>
All 6 Carnage paths pass integration tests after refactor. CU regression is within 5% tolerance for every path. Pre/post CU values are documented in the plan summary.
  </done>
</task>

</tasks>

<verification>
1. 6/6 Carnage paths pass (BuyOnly+Burn+Sell x CRIME+FRAUD)
2. No CU regression >5% on any path
3. Pre-refactor and post-refactor CU values documented
</verification>

<success_criteria>
- All 6 Carnage execution paths pass after refactor deployment
- Compute budget within 5% of pre-refactor baseline per path
- Results documented with specific CU numbers
</success_criteria>

<output>
After completion, create `.planning/phases/82-carnage-refactor/82-02-SUMMARY.md`
</output>
