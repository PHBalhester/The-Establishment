---
phase: 78-authority-hardening
plan: 03
type: execute
wave: 2
depends_on: [78-01, 78-02]
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements:
  - AUTH-07

must_haves:
  truths:
    - "Authority map table exists in PROJECT.md Key Decisions section listing every program's authority type, holder, and burn status"
    - "Authority lifecycle strategy (Squads multisig, no immediate burn) is documented"
    - "Full Anchor build succeeds with all authority hardening changes from plans 01 and 02"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Authority map table and lifecycle rationale"
      contains: "Authority Map"
  key_links:
    - from: ".planning/PROJECT.md"
      to: "all 7 programs"
      via: "authority map table"
      pattern: "BcAdminConfig|ProgramData"
---

<objective>
Document the authority map and burn timeline in PROJECT.md, then verify the full build compiles with all authority changes.

Purpose: AUTH-07 requires documenting which authorities exist, who holds them, and the burn plan. This provides a single reference for the security model. The full build verification ensures plans 01 and 02 didn't introduce cross-program issues.

Output: Authority map table in PROJECT.md, full `anchor build` confirmation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/78-authority-hardening/78-CONTEXT.md

Prior plan outputs (needed for authority inventory):
@.planning/phases/78-authority-hardening/78-01-SUMMARY.md
@.planning/phases/78-authority-hardening/78-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document authority map and verify full build</name>
  <files>
    .planning/PROJECT.md
  </files>
  <action>
    1. Read `.planning/PROJECT.md` and find the `## Key Decisions` section.

    2. Add a new row to the Key Decisions table:
       ```
       | Authority hardening (Phase 78) | Prevent front-running of program initialization; single security model under Squads multisig | All 7 programs gated; see Authority Map below |
       ```

    3. Add an `### Authority Map` subsection immediately after the Key Decisions table with this content:

       ```markdown
       ### Authority Map

       Every program authority, its type, current holder, and burn plan.

       | Program | Authority Type | Mechanism | Current Holder | Burn Plan |
       |---------|---------------|-----------|----------------|-----------|
       | AMM | Upgrade Authority | BPF Loader ProgramData | Deployer wallet | Transfer to 2-of-3 Squads multisig (v1.4). Never burn. |
       | AMM | Admin PDA (AdminConfig) | has_one = admin | Deployer wallet | Transfer to Squads multisig. Burn when ready, no timeline. |
       | Transfer Hook | Upgrade Authority | BPF Loader ProgramData | Deployer wallet | Transfer to Squads multisig (v1.4). Never burn. |
       | Transfer Hook | Whitelist Authority (WhitelistAuthority PDA) | authority = Some(signer) | Deployer wallet | Transfer to Squads multisig. Burn when whitelist is final. |
       | Tax Program | Upgrade Authority | BPF Loader ProgramData | Deployer wallet | Transfer to Squads multisig (v1.4). Never burn. |
       | Epoch Program | Upgrade Authority | BPF Loader ProgramData | Deployer wallet | Transfer to Squads multisig (v1.4). Never burn. |
       | Staking | Upgrade Authority | BPF Loader ProgramData | Deployer wallet | Transfer to Squads multisig (v1.4). Never burn. |
       | Conversion Vault | Upgrade Authority | BPF Loader ProgramData | Deployer wallet | Transfer to Squads multisig (v1.4). Never burn. |
       | Bonding Curve | Upgrade Authority | BPF Loader ProgramData | Deployer wallet | Transfer to Squads multisig (v1.4). Never burn. |
       | Bonding Curve | Admin PDA (BcAdminConfig) | has_one = authority | Deployer wallet | Transfer to Squads multisig. Burn when curves graduated. |

       **Lifecycle strategy:** All authorities (upgrade + admin PDAs) transfer to a single 2-of-3 Squads multisig with 48-72hr timelock (v1.4 scope, MN-01). Upgrade authorities are never burned -- preserves ability to fix bugs via timelocked upgrade. Admin PDAs can be burned individually when their function is no longer needed (e.g., BcAdminConfig after both curves graduate).

       **No emergency pause:** Decided against admin-triggered pause mechanism. An admin pause key would be perceived as a rug pull vector by the community. Program upgrade via timelocked multisig is the safety net for critical bugs.
       ```

    4. Verify the full build compiles with all changes from plans 01 and 02:
       ```bash
       source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH"
       cd /Users/mlbob/Projects/Dr\ Fraudsworth
       anchor build 2>&1 | tail -30
       ```
       This builds all programs. If it fails, diagnose and fix (likely a missing import or feature flag issue).

    5. Also verify the devnet feature build for programs that use it:
       ```bash
       anchor build -p bonding_curve -- --features devnet 2>&1 | tail -10
       anchor build -p epoch_program -- --features devnet 2>&1 | tail -10
       anchor build -p tax_program -- --features devnet 2>&1 | tail -10
       anchor build -p conversion_vault -- --features devnet 2>&1 | tail -10
       ```
  </action>
  <verify>
    - `anchor build` completes successfully (all programs compile)
    - `anchor build -p bonding_curve -- --features devnet` succeeds
    - `grep "Authority Map" .planning/PROJECT.md` returns a match
    - `grep "BcAdminConfig" .planning/PROJECT.md` returns a match
    - `cargo test --workspace 2>&1 | grep -E "test result|FAILED"` shows all tests passing
  </verify>
  <done>Authority map documented in PROJECT.md with all 7 programs covered. Full anchor build succeeds with all authority hardening. AUTH-07 complete.</done>
</task>

</tasks>

<verification>
- `anchor build` succeeds for all programs
- PROJECT.md contains Authority Map table with all 10 authority entries
- Authority lifecycle strategy documented (Squads multisig, no emergency pause)
- All workspace tests pass
</verification>

<success_criteria>
- AUTH-07: Authority map and burn timeline documented in PROJECT.md
- Full build verification confirms no cross-program issues from authority hardening
- All 7 AUTH requirements (AUTH-01 through AUTH-07) are complete
</success_criteria>

<output>
After completion, create `.planning/phases/78-authority-hardening/78-03-SUMMARY.md`
</output>
