---
phase: 86-test-coverage-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - programs/bonding_curve/tests/boundary_test.rs
  - programs/bonding_curve/src/math.rs
requirements: [TEST-05, TEST-06, TEST-07]
autonomous: true

must_haves:
  truths:
    - "1-token-remaining boundary produces correct rounding behavior on dust purchase"
    - "Reversed mint ordering does not affect floor calculation correctness"
    - "vault_solvency_mixed_buy_sell proptest passes at 5M iterations or has documented limitation"
  artifacts:
    - path: "programs/bonding_curve/tests/boundary_test.rs"
      provides: "Boundary integration tests for dust purchase and reversed mints"
      min_lines: 150
    - path: "programs/bonding_curve/src/math.rs"
      provides: "Fixed or documented proptest with 5M iteration validation"
  key_links:
    - from: "programs/bonding_curve/tests/boundary_test.rs"
      to: "programs/bonding_curve (compiled .so)"
      via: "LiteSVM loads binary, executes instructions"
      pattern: "LiteSVM::new|set_program"
---

<objective>
Create boundary condition tests for bonding curve edge cases (dust purchase at 1-token-remaining, reversed mint floor calculation) and investigate/fix the proptest regression in vault_solvency_mixed_buy_sell.

Purpose: Boundary conditions and proptest regressions are the highest-risk gaps for mainnet — these tests prove the math holds at extremes.

Output: `programs/bonding_curve/tests/boundary_test.rs` with 2+ tests, proptest passing at 5M iterations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/86-test-coverage-sweep/86-CONTEXT.md

@programs/bonding_curve/tests/refund_clock_test.rs
@programs/bonding_curve/src/math.rs
@programs/bonding_curve/src/constants.rs
@programs/bonding_curve/src/state.rs
@programs/bonding_curve/src/error.rs
@programs/bonding_curve/src/instructions/purchase.rs
@programs/bonding_curve/src/instructions/sell.rs
@programs/amm/src/state.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boundary LiteSVM tests for dust purchase and reversed mints</name>
  <files>programs/bonding_curve/tests/boundary_test.rs</files>
  <action>
Create `programs/bonding_curve/tests/boundary_test.rs` following refund_clock_test.rs patterns.

**Test 1 - TEST-05: 1-token-remaining + dust purchase rounding**
- Set up a single bonding curve via LiteSVM
- Purchase tokens until exactly 1 token (1 * 10^decimals = 1_000_000 raw units) remains before TARGET_TOKENS
  - Strategy: Calculate the SOL needed to buy (TARGET_TOKENS - 1_000_000) tokens, execute that purchase
  - If exact targeting is hard, use LiteSVM account manipulation to set CurveState.total_sold to (TARGET_TOKENS - 1_000_000) directly
- Attempt a minimum dust purchase (MIN_PURCHASE_SOL or smallest viable amount)
- Verify: Does the purchase succeed and cap at the remaining 1 token? Does the math round correctly?
- Verify: The curve transitions to Filled state (total_sold == TARGET_TOKENS) OR correctly handles the "tokens_out > remaining" cap
- Test the edge: What happens with a purchase that would yield 0 tokens? Should return an error (not silently succeed with 0 tokens)

**Test 2 - TEST-06: Reversed mint order floor calculation**
- This tests the AMM pool reader's `is_reversed` detection when token mint bytes are ordered such that token_mint < NATIVE_MINT (SOL)
- NATIVE_MINT starts with 0x06 — virtually all token mints have first byte > 0x06, so this is an unusual case
- Create a mock scenario:
  - Generate a keypair whose pubkey has first byte < 0x06 (brute force a few until found, or use LiteSVM account manipulation to create a "fake" pool with reversed ordering)
  - Set up a pool account with reversed mint_a/mint_b ordering
  - Call the pool reader logic that calculates floor/reserves
  - Verify the is_reversed detection correctly identifies the reversal and returns (sol_reserve, token_reserve) in the right order
- Alternative approach if full LiteSVM pool setup is too heavy: Unit test in math.rs or a standalone test that exercises `read_pool_reserves()` with crafted account data bytes
- The key assertion: floor calculation produces the same result regardless of which mint is canonically first in the pool

**IMPORTANT:** Use `source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH"` before cargo commands.
  </action>
  <verify>
Run: `source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH" && cd programs/bonding_curve && cargo test --test boundary_test -- --nocapture 2>&1 | tail -20`

Both boundary tests pass.
  </verify>
  <done>
2 boundary tests pass:
- test_one_token_remaining_dust_purchase (TEST-05)
- test_reversed_mint_order_floor_calculation (TEST-06)
  </done>
</task>

<task type="auto">
  <name>Task 2: Investigate and fix proptest vault_solvency_mixed_buy_sell regression</name>
  <files>programs/bonding_curve/src/math.rs</files>
  <action>
**Investigation phase:**
1. Run the existing proptest with verbose output to see if it currently fails:
   `cd programs/bonding_curve && PROPTEST_CASES=100000 cargo test vault_solvency_mixed_buy_sell -- --nocapture`
2. If it fails, capture the failing case. Analyze the specific op sequence (num_ops, op_pcts, op_types) that triggers failure.
3. Read the proptest strategy carefully — it uses percentage-based derivation (good, avoids rejection). Check if:
   - The SOL amount calculation can produce values that don't match what on-chain `calculate_tokens_out` expects
   - The sell path mirrors on-chain `calculate_sol_for_tokens` exactly
   - There's a rounding mismatch between test-side vault tracking and on-chain math

**Fix phase (based on what investigation reveals):**
- If root cause is an impossible-in-practice rounding edge case (e.g., selling exactly 1 token at near-zero position where ceil rounding produces 0 SOL gross but test expects positive): narrow the proptest input strategy to exclude the unreachable range. Document WHY the range is unreachable in practice (e.g., MIN_PURCHASE_SOL prevents positions below X).
- If root cause is a real bug in the test (e.g., not matching on-chain math exactly): fix the test to mirror on-chain logic precisely.
- Do NOT change on-chain math code — only fix the test or narrow its strategy.
- Add a comment block explaining the root cause and resolution.

**Validation:**
- Run with 5M iterations: `PROPTEST_CASES=5000000 cargo test vault_solvency_mixed_buy_sell`
- This must pass clean. If it takes too long, 1M is acceptable for the test run but document that 5M was validated.

**IMPORTANT:**
- Per MEMORY.md: proptest strategies must use percentage-based derivation (not prop_assume!) to avoid >50% rejection rates
- Per CONTEXT.md: "On-chain code stays stable (no changes to deployed math)"
  </action>
  <verify>
Run: `source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH" && cd programs/bonding_curve && PROPTEST_CASES=1000000 cargo test vault_solvency_mixed_buy_sell -- --nocapture 2>&1 | tail -10`

Proptest passes at 1M iterations minimum. Any strategy narrowing is documented with justification.
  </verify>
  <done>
vault_solvency_mixed_buy_sell passes at 1M+ iterations. Root cause documented. No on-chain code changes.
  </done>
</task>

</tasks>

<verification>
- `cargo test --test boundary_test` passes both boundary tests
- `PROPTEST_CASES=1000000 cargo test vault_solvency_mixed_buy_sell` passes
- No changes to on-chain program logic (test-only changes, possible proptest strategy narrowing)
- If proptest strategy was narrowed, comment explains why excluded range is unreachable in practice
</verification>

<success_criteria>
- TEST-05 boundary test proves dust purchase at 1-token-remaining handles correctly
- TEST-06 boundary test proves reversed mint ordering doesn't break floor calculation
- TEST-07 proptest regression resolved (passing at 1M+ iterations) or fully documented with known limitation
</success_criteria>

<output>
After completion, create `.planning/phases/86-test-coverage-sweep/86-02-SUMMARY.md`
</output>
