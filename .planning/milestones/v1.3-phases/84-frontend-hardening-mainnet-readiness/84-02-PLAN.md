---
phase: 84-frontend-hardening-mainnet-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/wallet/BalanceDisplay.tsx
  - app/lib/swap/error-map.ts
  - app/hooks/useStaking.ts
  - app/hooks/useSwap.ts
  - app/hooks/useCurrentSlot.ts
  - app/hooks/usePoolPrices.ts
  - app/hooks/useCarnageData.ts
  - app/hooks/useCarnageEvents.ts
  - app/hooks/useEpochState.ts
  - app/hooks/useVisibility.ts
  - app/components/staking/StakingForm.tsx
  - app/lib/staking/staking-builders.ts
  - app/providers/SettingsProvider.tsx
autonomous: true
requirements: [CLN-01, CLN-02, CLN-03, CLN-04, MNR-04, FE-06]

must_haves:
  truths:
    - "BalanceDisplay.tsx no longer exists in the codebase"
    - "Tax error codes 6014-6017 produce human-readable messages in swap error toasts"
    - "No stale DashboardGrid references remain in hook comments"
    - "staking-builders.ts compiles without TS errors"
    - "PriorityFeePreset type is owned by SettingsProvider, not useStaking"
  artifacts:
    - path: "app/lib/swap/error-map.ts"
      provides: "Tax error codes 6014-6017 mapped"
      contains: "MinimumOutputFloorViolation"
    - path: "app/providers/SettingsProvider.tsx"
      provides: "PriorityFeePreset type definition"
      contains: "PriorityFeePreset"
  key_links:
    - from: "app/hooks/useStaking.ts"
      to: "app/providers/SettingsProvider.tsx"
      via: "PriorityFeePreset import"
      pattern: "import.*PriorityFeePreset.*SettingsProvider"
---

<objective>
Remove dead code, fix TypeScript errors, clean up stale comments, extend error maps, and migrate PriorityFeePreset ownership.

Purpose: Code hygiene -- remove accumulated tech debt and fix known issues before mainnet.
Output: Cleaner codebase with no dead code, complete error maps, and correct type ownership.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-frontend-hardening-mainnet-readiness/84-CONTEXT.md

@app/components/wallet/BalanceDisplay.tsx
@app/lib/swap/error-map.ts
@app/hooks/useStaking.ts
@app/lib/staking/staking-builders.ts
@app/providers/SettingsProvider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dead Code Removal + Error Map Extension</name>
  <files>
    app/components/wallet/BalanceDisplay.tsx
    app/lib/swap/error-map.ts
  </files>
  <action>
    1. **CLN-01**: Delete `app/components/wallet/BalanceDisplay.tsx` entirely. Search for any imports of BalanceDisplay across the codebase (`grep -r "BalanceDisplay" app/`). Remove any import statements that reference it. This component has been orphaned since Phase 40.

    2. **CLN-02**: Check if `app/app/swap/page.tsx` exists. Per codebase scan it does NOT exist as a route -- it was already removed. Verify by searching for any references to a `/swap` route (`grep -r "swap/page\|/swap" app/app/`). If no swap route exists, CLN-02 is already satisfied. Document this finding.

    3. **FE-06**: Extend the Tax error map in `app/lib/swap/error-map.ts`:
       - Add Tax codes 6014-6017 to TAX_ERRORS (currently stops at 6013):
         - 6014: InvalidAmmProgram -- "AMM program address verification failed. Please report this issue."
         - 6015: InvalidStakingProgram -- "Staking program address verification failed. Please report this issue."
         - 6016: InsufficientOutput -- "Tax exceeds the swap output. Try a larger amount."
         - 6017: MinimumOutputFloorViolation -- "Your slippage setting is below the protocol's minimum floor (50% of expected output). Increase your minimum output."
       - Also add 6018: InvalidPoolOwner -- "Pool account verification failed. Please report this issue."
       - Update the header comment to reflect the new range: "6000-6018 (19 variants)"
       - Cross-reference against `programs/tax-program/src/errors.rs` to ensure ordering is correct
  </action>
  <verify>
    - `find app/ -name "BalanceDisplay*"` returns no results
    - `grep -r "BalanceDisplay" app/ --include="*.ts" --include="*.tsx"` returns no results
    - `grep "6017" app/lib/swap/error-map.ts` returns the MinimumOutputFloorViolation entry
  </verify>
  <done>
    BalanceDisplay.tsx deleted with no orphan imports. Tax error map covers all 19 error codes (6000-6018). swap/page.tsx removal verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Comment Cleanup + TS Fix + PriorityFee Migration</name>
  <files>
    app/hooks/useStaking.ts
    app/hooks/useSwap.ts
    app/hooks/useCurrentSlot.ts
    app/hooks/usePoolPrices.ts
    app/hooks/useCarnageData.ts
    app/hooks/useCarnageEvents.ts
    app/hooks/useEpochState.ts
    app/hooks/useVisibility.ts
    app/components/staking/StakingForm.tsx
    app/lib/staking/staking-builders.ts
    app/providers/SettingsProvider.tsx
  </files>
  <action>
    1. **CLN-04**: Update stale "DashboardGrid" references in comments across 9 files. The component was renamed/refactored -- references should be updated to reflect current component naming. In each file, replace "DashboardGrid" mentions with the actual current component name (likely "Dashboard" or "main view"). The files are:
       - `app/hooks/useEpochState.ts` (line 80)
       - `app/components/staking/StakingForm.tsx` (line 7)
       - `app/hooks/useVisibility.ts` (lines 22, 102)
       - `app/hooks/useStaking.ts` (line 22)
       - `app/hooks/useCarnageEvents.ts` (line 97)
       - `app/hooks/useCarnageData.ts` (line 59)
       - `app/hooks/useCurrentSlot.ts` (lines 18, 59)
       - `app/hooks/useSwap.ts` (line 18)
       - `app/hooks/usePoolPrices.ts` (line 86)
       Check what the actual parent component is called now by looking at `app/app/page.tsx` or the main layout. Update all comments to reference the correct component name.

    2. **MNR-04**: Fix the TypeScript error in `app/lib/staking/staking-builders.ts`. The file passes `systemProgram: SystemProgram.programId` in instruction accounts. If the on-chain Anchor instruction no longer expects systemProgram (it was removed), delete those lines. Verify by checking the staking IDL at `app/idl/staking.json` -- look at the accounts arrays for stake, claim, and unstake instructions. Remove systemProgram from any instruction builder where the IDL no longer lists it.

    3. **CLN-03**: Migrate PriorityFeePreset type from useStaking.ts to SettingsProvider.tsx:
       - Find where PriorityFeePreset is defined (likely in useStaking.ts or a related file)
       - Move the type definition to `app/providers/SettingsProvider.tsx` where it semantically belongs (settings own priority fee configuration)
       - Export PriorityFeePreset from SettingsProvider.tsx
       - Update all imports across the codebase that reference PriorityFeePreset to import from SettingsProvider or useSettings
       - Files that import PriorityFeePreset: useStaking.ts, SettingsStation.tsx, SlippageConfig.tsx, SwapForm.tsx, useSwap.ts (based on grep results)
  </action>
  <verify>
    - `grep -r "DashboardGrid" app/ --include="*.ts" --include="*.tsx"` returns no results
    - `cd app && npx tsc --noEmit` passes (no TS errors in staking-builders or anywhere else)
    - `grep "PriorityFeePreset" app/providers/SettingsProvider.tsx` shows the type definition
    - `grep "PriorityFeePreset" app/hooks/useStaking.ts` shows an import, not a definition
  </verify>
  <done>
    All 9 files have accurate component references in comments. staking-builders.ts compiles clean. PriorityFeePreset type lives in SettingsProvider with all consumers importing from there.
  </done>
</task>

</tasks>

<verification>
- `cd app && npx tsc --noEmit` passes with zero errors
- `grep -rn "DashboardGrid" app/` returns zero results
- `find app/ -name "BalanceDisplay*"` returns zero results
- Tax error map has entries for codes 6014-6018
</verification>

<success_criteria>
1. BalanceDisplay.tsx deleted, no orphan imports remain
2. swap/page.tsx removal verified (or removed if it still existed)
3. All 9 hook/component files have updated comments (no DashboardGrid)
4. staking-builders.ts has no TS errors
5. PriorityFeePreset type defined in SettingsProvider, imported elsewhere
6. Tax error map covers codes 6000-6018
</success_criteria>

<output>
After completion, create `.planning/phases/84-frontend-hardening-mainnet-readiness/84-02-SUMMARY.md`
</output>
