---
phase: 80-defense-in-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - programs/tax-program/src/helpers/pool_reader.rs
  - programs/tax-program/src/errors.rs
  - programs/epoch-program/src/instructions/execute_carnage.rs
  - programs/epoch-program/src/instructions/execute_carnage_atomic.rs
autonomous: true
requirements: [DEF-01, DEF-02, DEF-06]

must_haves:
  truths:
    - "read_pool_reserves() rejects accounts not owned by AMM program"
    - "read_pool_reserves() returns (sol_reserve, token_reserve) regardless of canonical mint ordering"
    - "Carnage pool accounts reject non-AMM-owned accounts at the Anchor constraint level"
  artifacts:
    - path: "programs/tax-program/src/helpers/pool_reader.rs"
      provides: "Owner-verified, is_reversed-aware pool byte reader"
      contains: "amm_program_id"
    - path: "programs/epoch-program/src/instructions/execute_carnage.rs"
      provides: "Owner-constrained pool accounts for carnage"
      contains: "owner = "
    - path: "programs/epoch-program/src/instructions/execute_carnage_atomic.rs"
      provides: "Owner-constrained pool accounts for atomic carnage"
      contains: "owner = "
  key_links:
    - from: "programs/tax-program/src/helpers/pool_reader.rs"
      to: "AMM program ID"
      via: "require!(*pool_info.owner == amm_program_id())"
      pattern: "pool_info\\.owner.*amm_program"
    - from: "programs/tax-program/src/helpers/pool_reader.rs"
      to: "spl_token::native_mint"
      via: "NATIVE_MINT comparison for is_reversed detection"
      pattern: "native_mint|NATIVE_MINT"
---

<objective>
Harden pool account reads with ownership verification and canonical mint detection.

Purpose: Prevent spoofed pool accounts from feeding arbitrary data to Tax Program swap calculations and Carnage slippage checks. Add is_reversed detection so Tax Program pool reads are correct regardless of AMM canonical mint ordering.

Output: pool_reader.rs with owner check + is_reversed, execute_carnage*.rs with Anchor owner constraints on pool accounts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/80-defense-in-depth/80-CONTEXT.md
@.planning/phases/80-defense-in-depth/80-RESEARCH.md
@programs/tax-program/src/helpers/pool_reader.rs
@programs/tax-program/src/errors.rs
@programs/tax-program/src/helpers/tax_math.rs
@programs/tax-program/src/instructions/swap_sol_buy.rs
@programs/tax-program/src/instructions/swap_sol_sell.rs
@programs/epoch-program/src/instructions/execute_carnage.rs
@programs/epoch-program/src/instructions/execute_carnage_atomic.rs
@programs/epoch-program/src/errors.rs
@programs/epoch-program/src/constants.rs
@programs/tax-program/src/constants.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pool reader ownership verification and is_reversed detection (DEF-01, DEF-02)</name>
  <files>
    programs/tax-program/src/helpers/pool_reader.rs
    programs/tax-program/src/errors.rs
  </files>
  <action>
    In `pool_reader.rs`, add to `read_pool_reserves()`:

    1. **Owner check (DEF-01):** Before reading any bytes, verify `*pool_info.owner == amm_program_id()`. Use `require!()` with a new `TaxError::InvalidPoolOwner` error variant. The `amm_program_id()` function should already exist in tax-program constants — if not, add it referencing the AMM program ID (check `programs/tax-program/src/constants.rs`).

    2. **is_reversed detection (DEF-02):** After reading reserve_a and reserve_b from bytes [137..145] and [145..153]:
       - Read mint_a Pubkey from bytes [9..41] (first 32 bytes after 8-byte discriminator + 1-byte is_initialized)
       - Compare mint_a to `spl_token::native_mint::id()` (NATIVE_MINT)
       - If mint_a == NATIVE_MINT: return (reserve_a, reserve_b) — already (SOL, token)
       - If mint_a != NATIVE_MINT: return (reserve_b, reserve_a) — swap to get (SOL, token)
       - Document return type as `(sol_reserve, token_reserve)`

    3. **CRITICAL: Verify callers.** Read `swap_sol_buy.rs` and `swap_sol_sell.rs` to confirm they expect `(sol_reserve, token_reserve)` ordering. If callers destructure as `(reserve_a, reserve_b)` and use them assuming SOL-first, the is_reversed fix is correct. If callers already compensate, adjust accordingly. Do NOT assume — read the call sites.

    4. Add `InvalidPoolOwner` error variant to `errors.rs`:
       ```rust
       #[msg("Pool account is not owned by AMM program")]
       InvalidPoolOwner,
       ```

    Use `spl_token::native_mint::id()` or `anchor_spl::token::spl_token::native_mint::id()` depending on what's already imported. If neither is available, use the raw NATIVE_MINT bytes (So11111111111111111111111111111111111111112).
  </action>
  <verify>
    `source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && cargo build -p tax-program 2>&1 | tail -20`
    Confirm no compilation errors.
  </verify>
  <done>
    read_pool_reserves() rejects non-AMM-owned accounts with InvalidPoolOwner error, and returns (sol_reserve, token_reserve) regardless of canonical mint ordering. Callers verified to work correctly with the new return semantics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Carnage pool owner constraint (DEF-06)</name>
  <files>
    programs/epoch-program/src/instructions/execute_carnage.rs
    programs/epoch-program/src/instructions/execute_carnage_atomic.rs
  </files>
  <action>
    In BOTH `execute_carnage.rs` and `execute_carnage_atomic.rs`, find the pool account declarations for `crime_pool` and `fraud_pool` (UncheckedAccount or AccountInfo with `/// CHECK:` comments).

    Add Anchor `owner` constraint:
    ```rust
    /// CHECK: Owner verified as AMM program, contents validated by Tax during CPI
    #[account(mut, owner = amm_program_id() @ EpochError::InvalidAmmProgram)]
    pub crime_pool: AccountInfo<'info>,

    /// CHECK: Owner verified as AMM program, contents validated by Tax during CPI
    #[account(mut, owner = amm_program_id() @ EpochError::InvalidAmmProgram)]
    pub fraud_pool: AccountInfo<'info>,
    ```

    Verify that `amm_program_id()` is importable from epoch-program constants and `EpochError::InvalidAmmProgram` exists. If `InvalidAmmProgram` doesn't exist, add it to `programs/epoch-program/src/errors.rs`:
    ```rust
    #[msg("Account is not owned by AMM program")]
    InvalidAmmProgram,
    ```

    Apply to ALL pool account declarations in BOTH files (crime_pool, fraud_pool). Some files may have separate structs for different carnage paths — check all of them.
  </action>
  <verify>
    `source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && cargo build -p epoch-program 2>&1 | tail -20`
    Confirm no compilation errors.
  </verify>
  <done>
    All carnage pool accounts (crime_pool, fraud_pool) in both execute_carnage.rs and execute_carnage_atomic.rs have Anchor-level owner constraints rejecting non-AMM-owned accounts. InvalidAmmProgram error variant exists.
  </done>
</task>

</tasks>

<verification>
- `cargo build -p tax-program` compiles cleanly
- `cargo build -p epoch-program` compiles cleanly
- grep confirms `amm_program_id` referenced in pool_reader.rs
- grep confirms `owner =` constraint on crime_pool and fraud_pool in both execute_carnage files
- grep confirms `native_mint` or `NATIVE_MINT` referenced in pool_reader.rs for is_reversed detection
</verification>

<success_criteria>
1. read_pool_reserves() has owner verification — passing a non-AMM account causes InvalidPoolOwner error
2. read_pool_reserves() returns (sol_reserve, token_reserve) correctly for both canonical orderings
3. Carnage pool accounts have Anchor owner constraints — non-AMM pools rejected before instruction body executes
4. Both programs compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/80-defense-in-depth/80-01-SUMMARY.md`
</output>
