---
phase: 103-off-chain-security-hardening
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - app/lib/__tests__/webhook-validators.test.ts
  - app/lib/__tests__/rate-limit.test.ts
autonomous: true

must_haves:
  truths:
    - "Unit tests cover all 5 account-type validators (EpochState, PoolState, CarnageFundState, StakePool, CurveState)"
    - "Unit tests verify unknown labels pass validation (fail-open for unrecognized accounts)"
    - "Unit tests cover getClientIp rightmost IP extraction from x-forwarded-for"
    - "Unit tests cover getClientIp edge cases (single IP, missing header, x-real-ip fallback)"
    - "All existing vitest tests still pass (no regression)"
    - "Next.js build succeeds with all Phase 103 changes"
    - "Manual RPC proxy guard checks pass (batch → 400, oversize → 413)"
  artifacts:
    - path: "app/lib/__tests__/webhook-validators.test.ts"
      provides: "Unit tests for per-account-type bounds validators"
      contains: "validateDecodedAccount"
      min_lines: 80
    - path: "app/lib/__tests__/rate-limit.test.ts"
      provides: "Unit tests for getClientIp rightmost IP extraction"
      contains: "getClientIp"
      min_lines: 40
  key_links:
    - from: "app/lib/__tests__/webhook-validators.test.ts"
      to: "app/lib/webhook-validators.ts"
      via: "import validateDecodedAccount"
      pattern: "validateDecodedAccount"
    - from: "app/lib/__tests__/rate-limit.test.ts"
      to: "app/lib/rate-limit.ts"
      via: "import getClientIp"
      pattern: "getClientIp"
---

<objective>
Write unit tests for the new security code (webhook validators, IP extraction fix) and run full regression to confirm no breakage from Phase 103 changes.

Purpose: All 3 implementation plans (01, 02, 03) change security-critical production code — the RPC proxy, webhook handler, rate limiter, and build pipeline. This plan ensures: (1) the new code is tested with valid, invalid, and edge-case inputs, (2) existing tests still pass, (3) the app builds cleanly, and (4) the RPC proxy guards actually reject malicious input.

Output: Two new test files, all tests green, build passing, manual verification of security guards.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-off-chain-security-hardening-h008-h010-h119-h003-h007-h096-h015/103-CONTEXT.md
@.planning/phases/103-off-chain-security-hardening-h008-h010-h119-h003-h007-h096-h015/103-RESEARCH.md

@app/lib/webhook-validators.ts
@app/lib/rate-limit.ts
@app/lib/__tests__/bigint-json.test.ts
@app/app/api/rpc/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for webhook-validators.ts</name>
  <files>app/lib/__tests__/webhook-validators.test.ts</files>
  <action>
  Create `app/lib/__tests__/webhook-validators.test.ts` following the same pattern as the existing `bigint-json.test.ts` (imports from vitest, describe/it/expect).

  Import `validateDecodedAccount` from `../webhook-validators`.

  Write the following test groups:

  **describe("validateDecodedAccount")**

  1. **describe("EpochState")**
     - it("accepts valid EpochState with all BPS in [0, 10000]") — pass an object with crimeBuyTaxBps: 500, crimeSellTaxBps: 300, fraudBuyTaxBps: 200, fraudSellTaxBps: 100, lowTaxBps: 50, highTaxBps: 9999. Expect true.
     - it("rejects EpochState with BPS > 10000") — set crimeBuyTaxBps: 10001. Expect false.
     - it("rejects EpochState with negative BPS") — set fraudSellTaxBps: -1. Expect false.
     - it("accepts EpochState at boundary values (0 and 10000)") — set all BPS fields to 0 and 10000. Expect true.
     - it("accepts EpochState with missing optional fields") — pass an object with only crimeBuyTaxBps: 500 (no other BPS fields). Expect true (missing = skip, don't reject).

  2. **describe("PoolState")**
     - it("accepts valid PoolState with positive reserves") — label: "PoolState:CRIME_SOL", reserveA: 1000, reserveB: 2000, feeNumerator: 25, feeDenominator: 10000. Expect true.
     - it("accepts PoolState with zero reserves (during init)") — reserveA: 0, reserveB: 0. Expect true.
     - it("rejects PoolState with negative reserveA") — reserveA: -1. Expect false.
     - it("rejects PoolState with zero feeDenominator") — feeDenominator: 0. Expect false.
     - it("accepts PoolState with __bigint tagged reserves") — reserveA: { __bigint: "1000000000" }. Expect true (numericValue handles tag).

  3. **describe("CarnageFundState")**
     - it("accepts valid CarnageFundState with non-negative balances") — solBalance: 100, tokenBalance: 200. Expect true.
     - it("rejects CarnageFundState with negative balance") — solBalance: -1. Expect false.

  4. **describe("StakePool")**
     - it("accepts valid StakePool") — totalStaked: 5000, rewardsPerTokenStored: { __bigint: "999999999" }. Expect true.
     - it("rejects StakePool with negative totalStaked") — totalStaked: -100. Expect false.
     - it("accepts StakePool with zero values") — totalStaked: 0, rewardsPerTokenStored: 0. Expect true.

  5. **describe("CurveState")**
     - it("accepts valid CurveState") — label: "CurveState:CRIME", currentPrice: 0.001, tokensSold: { __bigint: "500000000" }, solRaised: { __bigint: "1000000" }. Expect true.
     - it("rejects CurveState with negative currentPrice") — currentPrice: -0.5. Expect false.

  6. **describe("unknown labels")**
     - it("returns true for unknown labels (fail-open)") — label: "CarnageSolVault", any object. Expect true.
     - it("returns true for completely unknown labels") — label: "SomeNewAccount", any object. Expect true.

  Keep tests focused and minimal — no mocking needed since validateDecodedAccount is a pure function.
  </action>
  <verify>
  Run `export PATH="/opt/homebrew/bin:$PATH" && cd "/Users/mlbob/Projects/Dr Fraudsworth/app" && npx vitest run lib/__tests__/webhook-validators.test.ts 2>&1 | tail -30` — all tests pass.
  </verify>
  <done>
  webhook-validators.test.ts covers all 5 account types + unknown labels. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for getClientIp rightmost extraction</name>
  <files>app/lib/__tests__/rate-limit.test.ts</files>
  <action>
  Create `app/lib/__tests__/rate-limit.test.ts` following the same vitest pattern.

  Import `getClientIp` from `../rate-limit`.

  The function signature is `getClientIp(request: Request): string`. We need to create mock Request objects with appropriate headers. Use a minimal approach — construct a `new Request("http://localhost", { headers: new Headers({...}) })` for each test case.

  **describe("getClientIp")**

  1. **describe("x-forwarded-for extraction")**
     - it("extracts rightmost IP from multi-hop x-forwarded-for") — headers: `{ "x-forwarded-for": "1.1.1.1, 10.0.0.1, 203.0.113.50" }`. Expect "203.0.113.50" (the rightmost, which Railway's proxy appended).
     - it("handles single IP in x-forwarded-for") — headers: `{ "x-forwarded-for": "203.0.113.50" }`. Expect "203.0.113.50".
     - it("trims whitespace from IPs") — headers: `{ "x-forwarded-for": " 1.1.1.1 , 203.0.113.50 " }`. Expect "203.0.113.50" (trimmed).
     - it("handles IPv6 in x-forwarded-for") — headers: `{ "x-forwarded-for": "::1, 2001:db8::1" }`. Expect "2001:db8::1".

  2. **describe("fallbacks")**
     - it("falls back to x-real-ip when x-forwarded-for absent") — headers: `{ "x-real-ip": "10.0.0.1" }`. Expect "10.0.0.1".
     - it("returns 'unknown' when no IP headers present") — empty headers. Expect "unknown" (or whatever the current fallback is — read the function to confirm).

  3. **describe("regression: leftmost NOT used")**
     - it("does NOT return leftmost IP (attacker-controlled)") — headers: `{ "x-forwarded-for": "ATTACKER_SPOOFED, 10.0.0.1, 203.0.113.50" }`. Expect NOT "ATTACKER_SPOOFED". Expect "203.0.113.50".

  If `getClientIp` is not exported (check first), add it to the exports in rate-limit.ts. The CONTEXT.md says it's already exported (`{ checkRateLimit, getClientIp }`).
  </action>
  <verify>
  Run `export PATH="/opt/homebrew/bin:$PATH" && cd "/Users/mlbob/Projects/Dr Fraudsworth/app" && npx vitest run lib/__tests__/rate-limit.test.ts 2>&1 | tail -30` — all tests pass.
  </verify>
  <done>
  rate-limit.test.ts covers rightmost extraction, fallbacks, and anti-regression for leftmost spoofing. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full regression — existing tests, build, and manual security guard verification</name>
  <files>app/lib/__tests__/webhook-validators.test.ts, app/lib/__tests__/rate-limit.test.ts</files>
  <action>
  Run the full regression suite in order:

  1. **Run ALL vitest tests** (not just the new ones):
     ```
     export PATH="/opt/homebrew/bin:$PATH" && cd app && npx vitest run
     ```
     All tests must pass — new tests from Tasks 1-2 plus the existing bigint-json, split-router, and route-engine tests.

  2. **Run Next.js build**:
     ```
     export PATH="/opt/homebrew/bin:$PATH" && cd app && npx next build
     ```
     Build must succeed with zero errors. This catches any TypeScript issues from the 103-01/02/03 changes plus the new test files.

  3. **Manual RPC proxy guard checks** (curl against local dev server or document the verification commands):
     Start a local dev server (or use the deployed devnet app) and verify:
     - **Batch rejection**: `curl -X POST http://localhost:3000/api/rpc -H "Content-Type: application/json" -d '[{"jsonrpc":"2.0","method":"getHealth","id":1}]'` → expect 400 with "Batch requests not supported"
     - **Body size limit**: Generate a >64KB payload and POST it → expect 413
     - **Normal request still works**: `curl -X POST http://localhost:3000/api/rpc -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"getHealth","id":1}'` → expect 200 with valid response

     If local dev server is not practical (requires CLUSTER_URL etc.), document the curl commands in the SUMMARY as a manual verification checklist for devnet/staging deployment.

  4. **Verify no leftover debug code**: Grep for `console.log` that isn't `console.warn` or `console.error` in the modified files (rpc/route.ts, webhooks/helius/route.ts, rate-limit.ts, webhook-validators.ts). New code should only use warn/error level logging.

  If any test fails or the build breaks, fix the issue in the relevant file before proceeding. Do NOT skip failures.
  </action>
  <verify>
  All vitest tests pass (0 failures). Next.js build succeeds. No leftover debug logging in modified files. Manual security guard commands documented or verified.
  </verify>
  <done>
  Full regression green: all unit tests pass, build succeeds, RPC proxy guards verified, no debug artifacts left behind.
  </done>
</task>

</tasks>

<verification>
1. `export PATH="/opt/homebrew/bin:$PATH" && cd app && npx vitest run` — all tests pass (0 failures)
2. `export PATH="/opt/homebrew/bin:$PATH" && cd app && npx next build` — build succeeds
3. `grep -rn "validateDecodedAccount" app/lib/__tests__/webhook-validators.test.ts` — validator tests exist
4. `grep -rn "getClientIp" app/lib/__tests__/rate-limit.test.ts` — IP extraction tests exist
5. `grep -rn "console.log" app/app/api/rpc/route.ts app/app/api/webhooks/helius/route.ts app/lib/webhook-validators.ts` — no debug console.log (only warn/error)
</verification>

<success_criteria>
- webhook-validators.ts has comprehensive unit tests covering all 5 account types + edge cases
- getClientIp has unit tests proving rightmost extraction + anti-regression for leftmost spoofing
- ALL existing vitest tests still pass (bigint-json, split-router, route-engine)
- Next.js build succeeds with zero errors
- RPC proxy security guards verified (batch → 400, oversize → 413, normal → 200)
- No debug logging left in production code
</success_criteria>

<output>
After completion, create `.planning/phases/103-off-chain-security-hardening-h008-h010-h119-h003-h007-h096-h015/103-04-SUMMARY.md`
</output>
