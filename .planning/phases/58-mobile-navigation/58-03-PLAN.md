---
phase: 58-mobile-navigation
plan: 03
type: execute
wave: 2
depends_on: ["58-01", "58-02"]
files_modified:
  - app/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Below 1024px viewport width, the fallback message is replaced by the MobileNav component showing all 6 stations"
    - "Tapping any station in MobileNav opens its modal as a full-screen slide-up panel"
    - "Above 1024px, the desktop interactive factory scene renders unchanged"
    - "The mobile nav and desktop scene are mutually exclusive via CSS responsive classes (no flash of wrong content)"
    - "Feature parity: all 6 stations accessible on both mobile and desktop"
  artifacts:
    - path: "app/app/page.tsx"
      provides: "Root page with responsive desktop scene / mobile nav split"
      contains: "MobileNav"
  key_links:
    - from: "app/app/page.tsx"
      to: "app/components/mobile/MobileNav.tsx"
      via: "Import and render in lg:hidden block"
      pattern: "import.*MobileNav"
    - from: "app/components/mobile/MobileNav.tsx"
      to: "app/components/modal/ModalProvider.tsx"
      via: "openModal() calls through useModal hook"
      pattern: "openModal"
---

<objective>
Wire MobileNav into page.tsx, replacing the fallback message, and verify the complete mobile navigation flow end-to-end with build check and visual checkpoint.

Purpose: This is the integration plan -- Plans 01 and 02 created the CSS overrides and the MobileNav component separately. This plan connects everything by updating page.tsx and provides a human verification checkpoint to confirm the steampunk mobile navigation looks correct and modals work.

Output: Updated page.tsx with MobileNav in the lg:hidden block. Passing build. Visual confirmation from human.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-mobile-navigation/58-RESEARCH.md
@.planning/phases/58-mobile-navigation/58-CONTEXT.md
@.planning/phases/58-mobile-navigation/58-01-SUMMARY.md
@.planning/phases/58-mobile-navigation/58-02-SUMMARY.md
@app/app/page.tsx
@app/components/mobile/MobileNav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace page.tsx mobile fallback with MobileNav component</name>
  <files>app/app/page.tsx</files>
  <action>
Update page.tsx to import and render MobileNav in place of the existing fallback message.

1. Add import at the top: `import { MobileNav } from '@/components/mobile/MobileNav';`

2. Replace the entire `lg:hidden` `<main>` block (lines 44-58 approximately -- the "simple fallback message" section) with:
```tsx
{/* Mobile navigation: visible below lg (1024px) -- Phase 58 */}
<main className="lg:hidden">
  <MobileNav />
</main>
```

3. Update the file's JSDoc comment at the top to reflect the change:
   - Remove the line about "Phase 59 adds mobile nav" or "factory-themed fallback message"
   - Replace with: "Below 1024px: steampunk-themed mobile navigation (Phase 58) replaces the scene with a vertical station list"

The desktop `<main>` block (hidden lg:block) is completely untouched -- no changes to the factory scene rendering.
  </action>
  <verify>
Run `export PATH="/opt/homebrew/bin:$PATH" && cd "/Users/mlbob/Projects/Dr Fraudsworth/app" && npx next build 2>&1 | tail -20` -- build succeeds. Grep page.tsx for `MobileNav` to confirm import and render. Grep page.tsx to confirm the old fallback text "best experienced on a larger screen" is removed.
  </verify>
  <done>
page.tsx imports and renders MobileNav in the lg:hidden block. The old fallback message is completely removed. The desktop scene block is untouched. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete mobile navigation system for viewports below 1024px:
- Steampunk-themed vertical navigation list with 6 stations (Swap, Carnage, Staking, Wallet, Docs, Settings)
- Factory header illustration with gradient fade and wallet connection badge
- Inline SVG icons for each station in brass accent color
- Full-screen slide-up modals with back arrow close button
- All desktop modal behavior unchanged above 1024px
  </what-built>
  <how-to-verify>
1. Start the dev server: `cd app && npx next dev`
2. Open browser at http://localhost:3000

**Mobile view (resize below 1024px or use DevTools responsive mode at 390x844):**
3. Verify the factory header image is visible at the top (~120px) with a gradient fade to the dark background
4. Verify the wallet status dot is visible in the header corner (gray if not connected)
5. Verify all 6 stations appear as a vertical list with icons, labels, and right chevrons
6. Verify the order is: Swap Machine, Carnage Cauldron, Rewards Vat, Connect Wallet, Documentation Table, Settings
7. Tap/click "Swap Machine" -- verify the modal slides up from the bottom, covering the full viewport
8. Verify the modal has a left-aligned back arrow (not the brass X button)
9. Tap the back arrow -- verify the modal slides down and you return to the nav list
10. Try opening each of the other 5 stations -- verify they all open correctly
11. Verify buttons/items feel responsive to touch (at least 48px tall)

**Desktop view (resize above 1024px):**
12. Verify the interactive factory scene appears as before (no changes)
13. Click a station -- verify the iris-open animation still works
14. Verify the brass X close button is visible (not the back arrow)
15. Verify corner bolts are present on the modal

**Transition test:**
16. With the browser at mobile width, open a station modal
17. Resize the browser above 1024px -- verify the modal adapts (fullscreen -> sized with iris-style chrome)
18. Resize back below 1024px -- verify it becomes fullscreen again

Type "approved" if everything looks good, or describe any issues.
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `grep -n "MobileNav" app/app/page.tsx` shows import and render
2. `grep -c "best experienced on a larger screen" app/app/page.tsx` returns 0 (old fallback removed)
3. Build passes: `npx next build` completes without errors
4. Human verification confirms visual quality and functional correctness
</verification>

<success_criteria>
- page.tsx renders MobileNav in the lg:hidden block
- Old fallback message completely removed
- All 6 stations accessible via mobile nav items
- Modals open as fullscreen slide-up panels on mobile
- Back arrow close button works on mobile modals
- Desktop scene and modal behavior completely unchanged
- Build passes
- Human-verified visual checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/58-mobile-navigation/58-03-SUMMARY.md`
</output>
