---
phase: 21-amm-access-control-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-amm-access-control-verification/21-VERIFICATION.md
autonomous: true

must_haves:
  truths:
    - "AMM swap_authority constraint uses seeds::program = TAX_PROGRAM_ID (verified via code review)"
    - "Direct user calls to AMM swap instructions fail (blocked by Signer + PDA validation)"
    - "Forged swap_authority PDA from wrong program is rejected (blocked by seeds::program)"
    - "Valid Tax Program CPI succeeds (existing tests pass)"
  artifacts:
    - path: ".planning/phases/21-amm-access-control-verification/21-VERIFICATION.md"
      provides: "Access control verification report with Pass/Fail determination"
      contains: "## Verdict"
  key_links:
    - from: "programs/amm/src/instructions/swap_sol_pool.rs"
      to: "TAX_PROGRAM_ID"
      via: "seeds::program constraint (line 370)"
      pattern: "seeds::program = TAX_PROGRAM_ID"
    - from: "programs/amm/src/instructions/swap_profit_pool.rs"
      to: "TAX_PROGRAM_ID"
      via: "seeds::program constraint (line 294)"
      pattern: "seeds::program = TAX_PROGRAM_ID"
---

<objective>
Verify AMM access control enforces Tax Program-only access and document findings.

Purpose: v0.4 milestone gate - prove the CPI-only access control design works before shipping
Output: VERIFICATION.md with Pass/Fail determination, code review findings, and requirement traceability
</objective>

<execution_context>
@./.claude/agents/gsd-executor.md
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-amm-access-control-verification/21-CONTEXT.md
@.planning/phases/21-amm-access-control-verification/21-RESEARCH.md

# AMM source files for code review
@programs/amm/src/instructions/swap_sol_pool.rs
@programs/amm/src/instructions/swap_profit_pool.rs
@programs/amm/src/constants.rs

# Tax Program constants for seed matching verification
@programs/tax-program/src/constants.rs

# Existing test coverage
@programs/amm/tests/test_cpi_access_control.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code Review - Verify Access Control Implementation</name>
  <files>No file modifications - read-only code review</files>
  <action>
Perform structured code review of AMM access control implementation.

**Verification Checklist:**

1. **swap_sol_pool.rs (SwapSolPool struct)**
   - Locate swap_authority account definition (lines 367-372)
   - Verify: `#[account(seeds = [SWAP_AUTHORITY_SEED], bump, seeds::program = TAX_PROGRAM_ID)]`
   - Verify: `pub swap_authority: Signer<'info>`
   - Document: Two-layer protection (PDA derivation + Signer type)

2. **swap_profit_pool.rs (SwapProfitPool struct)**
   - Locate swap_authority account definition (lines 291-296)
   - Verify: Identical constraint pattern to swap_sol_pool
   - Verify: `pub swap_authority: Signer<'info>`

3. **constants.rs (AMM)**
   - Verify TAX_PROGRAM_ID: `FV3kWDtSRDHTdd9fK9L1fkqdWis7Sts5x7nNS4uoSiiu`
   - Verify SWAP_AUTHORITY_SEED: `b"swap_authority"`

4. **constants.rs (Tax Program)**
   - Verify SWAP_AUTHORITY_SEED matches AMM: `b"swap_authority"`

**Record findings:**
- Exact line numbers where constraints are defined
- Confirmation both instructions have identical protection
- Confirmation seeds match between programs
- Any discrepancies or gaps found

**Expected result:** All 4 checks pass - access control is correctly implemented.
  </action>
  <verify>
- All 4 AMM swap instructions (swap_sol_pool AtoB/BtoA, swap_profit_pool AtoB/BtoA) have swap_authority constraint
- TAX_PROGRAM_ID is production value (FV3k...)
- SWAP_AUTHORITY_SEED matches between AMM and Tax Program
  </verify>
  <done>Code review confirms access control constraints are correctly implemented in both swap instructions</done>
</task>

<task type="auto">
  <name>Task 2: Create VERIFICATION.md with Requirement Traceability</name>
  <files>.planning/phases/21-amm-access-control-verification/21-VERIFICATION.md</files>
  <action>
Create VERIFICATION.md documenting the access control verification results.

**Document Structure:**

```markdown
# Phase 21: AMM Access Control Verification

**Phase:** 21-amm-access-control-verification
**Date:** [current date]
**Verified by:** Claude (executor)
**Validated by:** [user - pending]

## Verdict

**PASS** / **FAIL**

[1-2 sentence summary of verification result]

## Requirements Verified

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| AUTH-03 | AMM verifies swap_authority PDA is owned by Tax Program | PASS/FAIL | [location] |
| AUTH-04 | User cannot bypass Tax Program by calling AMM directly | PASS/FAIL | [tests] |

## Code Review Findings

### swap_sol_pool.rs

**swap_authority constraint (lines 367-372):**
[Copy exact constraint code]

**Protection layers:**
1. `Signer<'info>` - Account must have signed the transaction
2. `seeds::program = TAX_PROGRAM_ID` - PDA must be derived from Tax Program

### swap_profit_pool.rs

**swap_authority constraint (lines 291-296):**
[Copy exact constraint code]

**Identical protection to swap_sol_pool:** Yes/No

### constants.rs Verification

| Constant | AMM Value | Tax Program Value | Match |
|----------|-----------|-------------------|-------|
| SWAP_AUTHORITY_SEED | b"swap_authority" | b"swap_authority" | Yes/No |
| TAX_PROGRAM_ID | FV3k... | N/A (AMM only) | - |

## Test Coverage Mapping

### Existing Tests (test_cpi_access_control.rs)

| Test | Requirement | Bypass Vector | Expected Error | Status |
|------|-------------|---------------|----------------|--------|
| test_mock_tax_cpi_swap_sol_pool_a_to_b | AUTH-03 | Valid CPI (positive) | Success | PASS |
| test_mock_tax_cpi_swap_sol_pool_b_to_a | AUTH-03 | Valid CPI (positive) | Success | PASS |
| test_mock_tax_cpi_swap_profit_pool_a_to_b | AUTH-03 | Valid CPI (positive) | Success | PASS |
| test_mock_tax_cpi_swap_profit_pool_b_to_a | AUTH-03 | Valid CPI (positive) | Success | PASS |
| test_direct_call_swap_sol_pool_fails | AUTH-04 | Direct call (no signer) | Error | PASS |
| test_direct_call_swap_profit_pool_fails | AUTH-04 | Direct call (no signer) | Error | PASS |
| test_direct_call_with_wrong_signer_fails | AUTH-04 | Random signer | ConstraintSeeds | PASS |
| test_direct_call_with_user_pda_fails | AUTH-04 | User-derived PDA | ConstraintSeeds | PASS |
| test_fake_tax_cpi_swap_sol_pool_rejected | AUTH-04 | Forged PDA (wrong program) | ConstraintSeeds | PASS |
| test_fake_tax_cpi_swap_profit_pool_rejected | AUTH-04 | Forged PDA (wrong program) | ConstraintSeeds | PASS |
| test_full_cpi_chain_sol_pool_with_hooks | AUTH-03 | Full chain validation | Success | PASS |
| test_full_cpi_chain_profit_pool_dual_hooks | AUTH-03 | Full chain validation | Success | PASS |

### Coverage Analysis

**Instruction coverage:**
- swap_sol_pool: [x] AtoB [x] BtoA
- swap_profit_pool: [x] AtoB [x] BtoA

**Bypass vector coverage:**
- [x] Direct call without swap_authority signed
- [x] Random keypair as swap_authority
- [x] User-derived PDA
- [x] Forged PDA from wrong program (Fake Tax)
- [x] Valid CPI from correct program (Mock Tax)

## Gaps Identified

[List any gaps found - these do NOT block PASS but are documented for future phases]

1. [Gap if any]
2. [Gap if any]

## Conclusion

[Summary statement confirming access control is correctly implemented and tested]

---

*Verification complete. User validation required before marking phase complete.*
```

**Important notes:**
- Run `cargo test -p amm test_cpi_access_control` to confirm all tests pass before creating document
- Use actual line numbers from code review
- Mark any gaps found (e.g., tests that only assert is_err() without checking specific error code)
- The verdict should be PASS if all requirements are verified with evidence
  </action>
  <verify>
Run: `source "$HOME/.cargo/env" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && cargo test -p amm test_cpi_access_control -- --nocapture 2>&1 | head -100`
All 14 tests should pass.
VERIFICATION.md exists with Verdict section.
  </verify>
  <done>VERIFICATION.md created with Pass/Fail verdict, code review findings, and complete requirement traceability matrix</done>
</task>

</tasks>

<verification>
Phase verification:

1. Code review complete:
   - swap_sol_pool.rs has seeds::program = TAX_PROGRAM_ID on swap_authority
   - swap_profit_pool.rs has identical constraint
   - TAX_PROGRAM_ID = FV3kWDtSRDHTdd9fK9L1fkqdWis7Sts5x7nNS4uoSiiu (production)
   - SWAP_AUTHORITY_SEED matches between AMM and Tax Program

2. Tests pass:
   - `cargo test -p amm test_cpi_access_control` returns success

3. VERIFICATION.md exists with:
   - Verdict (PASS or FAIL)
   - Requirement traceability (AUTH-03, AUTH-04)
   - Code locations documented
   - Test coverage mapped
</verification>

<success_criteria>
1. AMM swap_authority constraint validates seeds::program = TAX_PROGRAM_ID (code review verified)
2. User calling AMM swap instructions directly (without Tax Program) receives error
3. Integration test confirms complete flow: user must go through Tax Program to swap
4. VERIFICATION.md documents Pass/Fail determination with evidence
</success_criteria>

<output>
After completion, create `.planning/phases/21-amm-access-control-verification/21-01-SUMMARY.md`
</output>
