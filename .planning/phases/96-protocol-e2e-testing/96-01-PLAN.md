---
phase: 96-protocol-e2e-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/e2e/lib/load-deployment.ts
  - scripts/e2e/devnet-e2e-validation.ts
  - scripts/e2e/lib/swap-flow.ts
  - scripts/e2e/lib/user-setup.ts
  - scripts/e2e/smoke-test.ts
  - scripts/e2e/security-verification.ts
  - scripts/e2e/carnage-hunter.ts
autonomous: true
requirements: [E2E-01, E2E-02, E2E-06, E2E-10]

must_haves:
  truths:
    - "All 8 swap routes execute successfully on devnet (4 SOL pool + 4 vault conversion)"
    - "Tax distribution on SOL pool swaps is verified as 75/24/1 (escrow/carnage/treasury) within rounding tolerance"
    - "Conversion vault swaps CRIME<->PROFIT and FRAUD<->PROFIT at 100:1 rate work both directions"
    - "Edge cases reject gracefully: zero-amount swap, insufficient balance, slippage protection"
  artifacts:
    - path: "scripts/e2e/lib/load-deployment.ts"
      provides: "Adapter from deployments/devnet.json to PDAManifest interface"
      exports: ["loadDeployment"]
    - path: "scripts/e2e/devnet-e2e-validation.ts"
      provides: "Full E2E orchestrator using deployment.json addresses"
    - path: "scripts/e2e/lib/swap-flow.ts"
      provides: "All 8 swap pairs with per-pair pass/fail and TX signature capture"
  key_links:
    - from: "scripts/e2e/lib/load-deployment.ts"
      to: "deployments/devnet.json"
      via: "fs.readFileSync + JSON.parse"
      pattern: "deployments/devnet\\.json"
    - from: "scripts/e2e/devnet-e2e-validation.ts"
      to: "scripts/e2e/lib/load-deployment.ts"
      via: "import loadDeployment"
      pattern: "loadDeployment"
---

<objective>
Migrate all E2E scripts from pda-manifest.json to deployments/devnet.json, then execute the full swap/vault/edge-case test suite against the Phase 95 deployment.

Purpose: The existing E2E infrastructure references stale pda-manifest.json addresses from pre-Phase-95 deployments. Without migration, every script will fail with "Account not found." Once migrated, we validate all 8 swap routes, tax distribution correctness, vault conversions, and edge case handling -- covering E2E-01, E2E-02, E2E-06, and E2E-10.

Output: Working `loadDeployment()` adapter, migrated scripts, and all 8 swap pairs passing with TX signatures logged.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/96-protocol-e2e-testing/96-CONTEXT.md
@.planning/phases/96-protocol-e2e-testing/96-RESEARCH.md
@deployments/devnet.json
@scripts/e2e/devnet-e2e-validation.ts
@scripts/e2e/lib/swap-flow.ts
@scripts/e2e/lib/user-setup.ts
@scripts/e2e/lib/load-deployment.ts
@scripts/e2e/smoke-test.ts
@scripts/e2e/security-verification.ts
@scripts/e2e/carnage-hunter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loadDeployment() adapter and migrate all E2E scripts</name>
  <files>
    scripts/e2e/lib/load-deployment.ts
    scripts/e2e/devnet-e2e-validation.ts
    scripts/e2e/lib/swap-flow.ts
    scripts/e2e/lib/user-setup.ts
    scripts/e2e/smoke-test.ts
    scripts/e2e/security-verification.ts
    scripts/e2e/carnage-hunter.ts
  </files>
  <action>
    Create `scripts/e2e/lib/load-deployment.ts` with a `loadDeployment()` function that:
    1. Reads `deployments/devnet.json` (resolve path from `__dirname/../../../deployments/devnet.json`)
    2. Returns an object matching the existing `PDAManifest` interface shape from devnet-e2e-validation.ts
    3. Maps camelCase keys from deployment.json to PascalCase keys expected by PDAManifest (e.g., `d.programs.amm` -> `programs.AMM`, `d.programs.transferHook` -> `programs.TransferHook`, etc.)
    4. Includes the pools object with `pool`, `vaultA`, `vaultB` subkeys (these already match)

    Then update all 6 files that reference `pda-manifest.json`:
    - `devnet-e2e-validation.ts` line ~93: Replace `fs.readFileSync(manifestPath)` with `import { loadDeployment } from "./lib/load-deployment"` and `const manifest = loadDeployment()`
    - `security-verification.ts` line ~792: Same pattern
    - `carnage-hunter.ts` line ~158: Same pattern
    - `smoke-test.ts` line ~31: Same pattern
    - `overnight-runner.ts` line ~194: Same pattern (even though deprecated, fix for completeness)
    - `user-setup.ts`: Update any ManifestMints interface references if needed

    Also update `swap-flow.ts` to ensure tax verification uses the CURRENT 75/24/1 ratio. Search for any hardcoded 71/24/5 references and update comments/assertions. Best practice: read tax ratios from on-chain EpochState rather than hardcoding, but at minimum ensure assertions match 75/24/1.

    Reduce MIN_BALANCE_SOL in devnet-e2e-validation.ts from 5 to 2 (Phase 95 used conservative devnet SOL).
  </action>
  <verify>
    Run `export PATH="/opt/homebrew/bin:$PATH" && npx tsx scripts/e2e/smoke-test.ts` -- should load addresses from deployments/devnet.json and attempt a swap without "Account not found" errors. Grep all scripts for "pda-manifest" to confirm zero references remain: `grep -r "pda-manifest" scripts/e2e/`
  </verify>
  <done>
    All 6 scripts import from loadDeployment(). Zero references to pda-manifest.json in scripts/e2e/. Smoke test connects to correct Phase 95 addresses.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute full E2E validation -- all 8 swaps, tax verification, vault, and edge cases</name>
  <files>
    scripts/e2e/devnet-e2e-validation.ts
    scripts/e2e/lib/swap-flow.ts
    scripts/e2e/security-verification.ts
  </files>
  <action>
    Extend the E2E validation orchestrator and swap-flow to cover all requirements:

    **E2E-01 (8 swap pairs):** Ensure `runSwapFlow()` executes all 4 SOL pool directions (CRIME buy, CRIME sell, FRAUD buy, FRAUD sell) and `runVaultTests()` executes all 4 vault conversions (CRIME->PROFIT, PROFIT->CRIME, FRAUD->PROFIT, PROFIT->FRAUD). Each pair must log: pair name, direction, input amount, output amount, TX signature, pass/fail. Use small amounts (0.003 SOL for SOL pool swaps, minimum viable for vault). Remember sell path needs v0 VersionedTransaction with ALT (address from deployments/devnet.json `.alt`).

    **E2E-02 (tax distribution):** After each SOL pool swap, snapshot escrow/carnage/treasury balances before and after. Compute deltas. Assert 75/24/1 split within 2 lamport rounding tolerance. Log the actual percentages.

    **E2E-06 (conversion vault):** Already covered by vault tests. Ensure both directions tested for both token pairs. Verify output amounts match 100:1 ratio.

    **E2E-10 (edge cases):** Add to `security-verification.ts` or the main orchestrator:
    - Zero-amount swap attempt -> expect rejection (program error, not crash)
    - Insufficient balance swap (try selling more tokens than owned) -> expect rejection
    - Excessive slippage (set min_out very high) -> expect slippage error
    Each edge case: log expected error, actual error, pass/fail.

    Run the full suite: `set -a && source .env.devnet && set +a && FULL=1 npx tsx scripts/e2e/devnet-e2e-validation.ts`

    Capture all TX signatures and results in JSONL log. The E2EReporter should generate a preliminary results section.
  </action>
  <verify>
    `FULL=1 npx tsx scripts/e2e/devnet-e2e-validation.ts` completes with all 8 swap pairs passing, tax distribution verified on SOL pool swaps, vault conversions verified, and edge cases producing expected rejections. Check `scripts/e2e/e2e-run.jsonl` for logged entries with status: "pass".
  </verify>
  <done>
    8/8 swap pairs pass with TX signatures logged. Tax distribution verified as 75/24/1. 4/4 vault conversions at 100:1 pass. 3/3 edge cases (zero-amount, insufficient balance, slippage) correctly rejected. Results in JSONL log.
  </done>
</task>

</tasks>

<verification>
- `grep -r "pda-manifest" scripts/e2e/` returns zero matches
- `scripts/e2e/e2e-run.jsonl` contains pass entries for all 8 swap pairs
- Tax distribution deltas show 75/24/1 within rounding tolerance
- Edge case entries show expected rejections
</verification>

<success_criteria>
All 8 swap routes execute on Phase 95 deployment. Tax split verified. Vault conversions verified. Edge cases handled gracefully. All scripts use deployments/devnet.json.
</success_criteria>

<output>
After completion, create `.planning/phases/96-protocol-e2e-testing/96-01-SUMMARY.md`
</output>
