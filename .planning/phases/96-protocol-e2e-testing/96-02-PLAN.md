---
phase: 96-protocol-e2e-testing
plan: 02
type: execute
wave: 2
depends_on: [96-01]
files_modified:
  - app/app/api/webhooks/helius/route.ts
  - scripts/e2e/devnet-e2e-validation.ts
  - scripts/e2e/lib/staking-flow.ts
autonomous: false
requirements: [E2E-03, E2E-04, E2E-05, E2E-07]

must_haves:
  truths:
    - "Charts display OHLCV candle data for swaps executed in Plan 01"
    - "Epoch advancement observed -- crank transitions epoch, tax rates change between epochs"
    - "Carnage event observed -- either atomic or fallback path fires during natural crank operation"
    - "Staking lifecycle works: stake PROFIT, earn SOL yield across epoch transitions, claim rewards, unstake"
    - "Frontend displays epoch info, tax rates, pool reserves, and staking stats matching on-chain state"
  artifacts:
    - path: "scripts/e2e/lib/staking-flow.ts"
      provides: "Full staking lifecycle test (stake/earn/claim/unstake)"
    - path: "app/app/api/webhooks/helius/route.ts"
      provides: "Working Helius webhook processing for chart data"
  key_links:
    - from: "app/app/api/webhooks/helius/route.ts"
      to: "Helius webhook dashboard"
      via: "Program ID filter matching deployments/devnet.json"
      pattern: "programs\\.amm|SwapExecuted"
    - from: "scripts/e2e/lib/staking-flow.ts"
      to: "deployments/devnet.json"
      via: "loadDeployment() from Plan 01"
      pattern: "loadDeployment|stakePool|stakingAuthority"
---

<objective>
Debug and fix the chart pipeline, observe epoch/carnage behavior from the Railway crank, and execute the full staking lifecycle on devnet.

Purpose: Charts are broken (likely Helius webhook pointing at old program IDs). Epoch advancement and Carnage must be observed from the naturally running Railway crank (not script-triggered). Staking lifecycle must be proven end-to-end. Frontend data accuracy is cross-checked against on-chain state. This covers E2E-03, E2E-04, E2E-05, and E2E-07.

Output: Working chart pipeline, epoch/carnage observations logged, staking lifecycle proven, frontend accuracy verified.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/96-protocol-e2e-testing/96-CONTEXT.md
@.planning/phases/96-protocol-e2e-testing/96-RESEARCH.md
@.planning/phases/96-protocol-e2e-testing/96-01-SUMMARY.md
@deployments/devnet.json
@app/app/api/webhooks/helius/route.ts
@app/app/api/candles/route.ts
@scripts/e2e/lib/staking-flow.ts
@scripts/crank/crank-runner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debug and fix chart pipeline (Helius webhook + candle aggregation)</name>
  <files>
    app/app/api/webhooks/helius/route.ts
  </files>
  <action>
    Diagnose the broken chart pipeline by checking each stage:

    **Stage 1 -- Webhook registration:** Check if the Helius webhook is configured with the current Phase 95 program IDs from `deployments/devnet.json`. The AMM program ID is `5JsSAL3kJDUWD4ZveYXYZmgm1eVqueesTZVdAvtZg8cR`. If the webhook is pointing at old IDs, it needs re-registration. Check Helius dashboard config or use the Helius API to list/update webhooks. The webhook URL should be the Railway deployment's `/api/webhooks/helius` endpoint.

    **Stage 2 -- Event parsing:** Check if the webhook handler (`app/app/api/webhooks/helius/route.ts`) is parsing SwapExecuted events correctly. Verify the event discriminator matches the current AMM program. Check Postgres `swap_events` table for any rows from Phase 95 swaps.

    **Stage 3 -- Candle aggregation:** Check if pool addresses in the candle aggregator match `deployments/devnet.json` pool addresses (`crimeSol.pool` and `fraudSol.pool`). Verify Postgres `candles` table has data.

    **Stage 4 -- Frontend API:** Test `/api/candles?pool={crimeSolPool}&resolution=1m` endpoint directly to see if it returns data.

    Fix whatever is broken. Most likely: re-register the Helius webhook with the new AMM program ID. May also need to update any hardcoded pool addresses in the candle aggregator.

    If the webhook secret needs updating, that is an env var on Railway (`HELIUS_WEBHOOK_SECRET`). The user may need to update this -- flag it clearly.
  </action>
  <verify>
    Execute a swap on devnet (reuse smoke-test.ts from Plan 01). Within 30 seconds, check: (1) Postgres `swap_events` has a new row, (2) `/api/candles` returns data for the pool, (3) the frontend chart page shows at least one candle. If Postgres is not directly accessible, verify via the `/api/candles` endpoint returning non-empty data.
  </verify>
  <done>
    Chart pipeline is end-to-end functional: swaps appear as candles in the frontend chart within seconds of execution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Observe epoch/carnage + execute staking lifecycle</name>
  <files>
    scripts/e2e/devnet-e2e-validation.ts
    scripts/e2e/lib/staking-flow.ts
  </files>
  <action>
    **E2E-03 (Epoch advancement):** Write observation logic in the E2E orchestrator that:
    1. Reads current EpochState (epoch_number, current tax rates)
    2. Waits for the Railway crank to advance the epoch (poll every 30s, timeout after 15 min)
    3. Reads EpochState again -- epoch_number should have incremented
    4. Compares tax rates before/after -- they should differ (VRF randomness changes rates)
    5. Logs: epoch_before, epoch_after, rates_before, rates_after, pass/fail

    **E2E-04 (Carnage):** During the epoch observation wait:
    1. Monitor CarnageFund account for balance changes
    2. After epoch transition, check if Carnage fired (carnage vault balance changed, or pool reserves show rebalancing)
    3. If no Carnage fires in the observation window, note it as "not triggered this epoch" (not a failure -- Carnage is probabilistic). If the crank has been running for a while, check historical epoch data for any past Carnage events.
    4. Log: carnage_observed (true/false), evidence (balance delta or "probabilistic -- not triggered")

    **E2E-05 (Staking lifecycle):** Update `staking-flow.ts` to use `loadDeployment()` and execute:
    1. User from Plan 01 should have some PROFIT tokens from vault conversions. If not, acquire some via CRIME->PROFIT vault conversion first.
    2. Stake PROFIT tokens (small amount, e.g., 100 PROFIT)
    3. Wait for at least 1 epoch transition (observe epoch_number increment)
    4. Read pending rewards -- should be > 0 SOL
    5. Claim rewards -- verify SOL balance increases
    6. Unstake PROFIT -- verify PROFIT balance returns
    7. Log each step with TX signature and pass/fail

    **E2E-07 (Frontend accuracy):** After the above tests, read on-chain state (epoch info, tax rates, pool reserves, staking stats) via script. Output these values clearly so the user can cross-check against the frontend display in the checkpoint task below.

    Run as part of FULL=1 mode in the E2E orchestrator.
  </action>
  <verify>
    E2E orchestrator (FULL=1) shows: epoch advanced (epoch_number incremented), tax rates changed, staking lifecycle completed (stake TX, claim TX, unstake TX all have signatures), and on-chain state values are printed for frontend cross-check.
  </verify>
  <done>
    Epoch advancement observed with tax rate changes. Staking lifecycle proven (stake -> earn -> claim -> unstake). Carnage observation logged (triggered or noted as probabilistic). On-chain state printed for frontend verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Chart pipeline fixed and rendering candle data. Epoch/carnage/staking all exercised. On-chain state values printed by script for cross-checking.</what-built>
  <how-to-verify>
    1. Visit the devnet frontend (Railway URL) and navigate to the trading page
    2. Check charts: at least one candle should be visible for CRIME/SOL or FRAUD/SOL pool
    3. Check epoch info display: epoch number, current tax rates -- compare to values printed by the E2E script
    4. Check pool reserves display: SOL and token amounts -- compare to script output
    5. Check staking stats: if visible, compare staked amount and rewards to script output
    6. Execute a swap via the frontend UI -- verify it succeeds and a new candle appears on the chart
    7. Try all 8 swap routes via frontend (4 SOL pool + 4 vault) -- confirm each works
  </how-to-verify>
  <resume-signal>Type "approved" if charts work and frontend data matches on-chain state, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Charts show candle data from recent swaps
- Epoch number has advanced since Plan 01 swaps
- Tax rates differ between epochs (VRF randomness working)
- Staking lifecycle TX signatures logged (stake, claim, unstake)
- Frontend data matches on-chain state (user-verified)
</verification>

<success_criteria>
Chart pipeline functional. Epoch advancement and tax rate changes observed. Full staking lifecycle proven. Carnage observation logged. Frontend data accuracy confirmed by user.
</success_criteria>

<output>
After completion, create `.planning/phases/96-protocol-e2e-testing/96-02-SUMMARY.md`
</output>
