---
phase: 109-public-push-mainnet-vault-upgrade-announcement
plan: 03
type: execute
wave: 3
depends_on: ["109-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Conversion vault program on mainnet has updated bytecode (last_deploy_slot changed)"
    - "Squads vault PDA still holds upgrade authority after execution"
    - "All 8 multi-hop swap routes work at small size (0.05 SOL) with zero intermediate token leakage"
    - "40+ SOL multi-hop simulation in Phantom shows no malicious Blowfish warning and no intermediate token leakage in wallet preview"
  artifacts: []
  key_links:
    - from: "Squads proposal"
      to: "conversion vault program (5uaw...)"
      via: "BPFLoaderUpgradeable::Upgrade execution"
      pattern: "last_deploy_slot"
    - from: "convert_v2 instruction"
      to: "user wallet"
      via: "multi-hop swap transaction"
      pattern: "convertV2"
---

<objective>
Execute the Squads upgrade after the 1-hour (3600s) timelock expiry, verify the on-chain upgrade succeeded, then conduct user-led mainnet smoke tests across all 8 multi-hop swap routes and the Blowfish large-swap simulation gate.

Purpose: Confirm the conversion vault upgrade is live and working correctly on mainnet before deploying the updated frontend. The smoke tests prove convert_v2 works in production and that multi-hop swaps have zero intermediate token leakage.
Output: Verified mainnet upgrade with 8/8 smoke test routes passing and Blowfish gate cleared.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-public-push-mainnet-vault-upgrade-announcement/109-02-SUMMARY.md
@deployments/mainnet.json
@Docs/mainnet-governance.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute Squads upgrade + post-upgrade verification</name>
  <files>None (all operations are on-chain)</files>
  <action>
    **Step 1: Verify 1-hour (3600s) timelock has expired**

    Check the Squads proposal status. The 1-hour (3600s) timelock from Plan 02 must have expired before execution is possible. Calculate from the recorded timelock start time in Plan 02 SUMMARY. If not expired, inform user of remaining wait time.

    IMPORTANT: The timelock is 3600 seconds (1 hour), NOT 15 minutes. This is set in .env.mainnet as SQUADS_TIMELOCK_SECONDS=3600.

    **Step 2: Execute the upgrade**

    Using the mainnet-upgrade-vault.ts script (or equivalent), call `multisig.rpc.proposalExecute()` (or equivalent) to execute the approved + timelocked upgrade proposal. This triggers the BPFLoaderUpgradeable::Upgrade instruction through the Squads vault.

    **Step 3: Post-upgrade verification**

    1. Verify bytecode changed:
       ```bash
       solana program show 5uawA6ehYTu69Ggvm3LSK84qFawPKxbWgfngwj15NRJ --url mainnet-beta
       ```
       Compare last_deploy_slot to the value recorded in Plan 02 -- it MUST have changed.

    2. Verify authority still held by vault:
       ```bash
       solana program show 5uawA6ehYTu69Ggvm3LSK84qFawPKxbWgfngwj15NRJ --url mainnet-beta
       ```
       Authority field must show 4SMcPtixKvjgj3U5N7C4kcnHYcySudLZfFWc523NAvXJ (Squads vault PDA).

    3. Optionally run verify-authority.ts against all programs to confirm nothing else changed:
       ```bash
       npx tsx scripts/deploy/verify-authority.ts
       ```

    4. Verify existing convert instruction still works (backwards compatibility) by checking recent mainnet transactions for the conversion vault program -- no errors should be occurring.

    Record: new last_deploy_slot, authority confirmation, buffer account status (should be closed after upgrade, rent reclaimed).
  </action>
  <verify>
    - `solana program show 5uawA6ehYTu69Ggvm3LSK84qFawPKxbWgfngwj15NRJ --url mainnet-beta` shows new last_deploy_slot (different from Plan 02 recorded value)
    - Authority field shows 4SMcPtixKvjgj3U5N7C4kcnHYcySudLZfFWc523NAvXJ
    - Buffer account is closed (rent reclaimed to vault PDA)
  </verify>
  <done>
    Conversion vault upgraded on mainnet. New bytecode is live with convert_v2 instruction available. Squads vault retains upgrade authority. Buffer rent reclaimed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Conversion vault upgraded on mainnet with convert_v2 instruction now available. The existing convert instruction is unchanged (backwards compatible). You now need to test all 8 multi-hop swap routes on mainnet at small size.

    The CURRENT mainnet Railway frontend still uses the OLD convert instruction. To test convert_v2 routes (routes 7+8), use a local dev server pointed at mainnet:

    ```bash
    cd "/Users/mlbob/Projects/Dr Fraudsworth"
    NEXT_PUBLIC_CLUSTER=mainnet npm run dev
    ```

    This starts the frontend at http://localhost:3000 with mainnet addresses and convert_v2 client code. Routes 1-6 (non-vault routes) can be tested on either the local dev server or the Railway mainnet service. Routes 7+8 (convert_v2 vault routes) MUST be tested on the local dev server since Railway doesn't have the updated client code yet.
  </what-built>
  <how-to-verify>
    **Part A: 8 Multi-Hop Route Tests (0.05 SOL each)**

    Start the local dev server with `NEXT_PUBLIC_CLUSTER=mainnet npm run dev` and use http://localhost:3000 for testing.

    Test each of these 8 swap directions at 0.05 SOL. For each, report the TX signature:

    1. SOL -> CRIME (buy CRIME with SOL)
    2. SOL -> FRAUD (buy FRAUD with SOL)
    3. SOL -> PROFIT (buy PROFIT via multi-hop: SOL->CRIME->PROFIT or SOL->FRAUD->PROFIT)
    4. CRIME -> SOL (sell CRIME for SOL)
    5. FRAUD -> SOL (sell FRAUD for SOL)
    6. PROFIT -> SOL (sell PROFIT via multi-hop: PROFIT->CRIME->SOL or PROFIT->FRAUD->SOL)
    7. CRIME -> PROFIT (direct convert via vault -- uses convert_v2)
    8. FRAUD -> PROFIT (direct convert via vault -- uses convert_v2)

    For each TX, verify:
    - Transaction succeeded (no errors)
    - No intermediate tokens left in your wallet after the swap
    - Correct amounts received (within expected slippage)

    **Part B: Blowfish Large Swap Gate**

    Have someone simulate (preview only, do NOT execute) a 40+ SOL multi-hop swap in Phantom:
    - Check the Phantom wallet preview (Blowfish simulation)
    - Must show NO "malicious transaction" or similar warning
    - Must show NO intermediate token transfers in the preview (only input SOL and output token)
    - Screenshot the preview if possible

    Report all 8 TX signatures for Part A and the Blowfish result for Part B.
  </how-to-verify>
  <resume-signal>
    Report:
    - 8 TX signatures (one per route) and whether each passed
    - Blowfish gate result (pass/fail/deferred)
    Type "all pass" if everything works, or describe any failures
  </resume-signal>
</task>

</tasks>

<verification>
- Upgrade executed successfully (last_deploy_slot changed)
- Authority retained by Squads vault PDA
- 8/8 multi-hop routes pass at 0.05 SOL
- Blowfish gate cleared (no malicious warnings, no intermediate tokens in preview)
</verification>

<success_criteria>
- Conversion vault running updated bytecode on mainnet
- All 8 swap directions verified working with zero intermediate token leakage
- Blowfish simulation shows clean preview for large swaps
- No regressions in existing convert instruction behavior
</success_criteria>

<output>
After completion, create `.planning/phases/109-public-push-mainnet-vault-upgrade-announcement/109-03-SUMMARY.md`
</output>
