---
phase: 109-public-push-mainnet-vault-upgrade-announcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/104-open-source-release-and-ottersec-verification/apply-sanitization.sh
  - /tmp/drfraudsworth-public (staging directory — all synced files)
autonomous: false

must_haves:
  truths:
    - "Public repo at MetalLegBob/drfraudsworth contains v1.5 changes (Phases 105, 106, 106.1, 108)"
    - "Zero secrets exist in the public repo HEAD (all 6 checklist phases pass clean)"
    - "Phase 107 (Jupiter SDK) is NOT in the public repo"
    - "SECURITY_AUDIT_SUMMARY.md is updated to reflect Phase 108 and Phase 106 SOS audit changes"
    - "Single squashed commit covers all v1.5 delta since commit 946703f"
  artifacts:
    - path: "/tmp/drfraudsworth-public"
      provides: "Sanitized staging directory with v1.5 codebase"
    - path: ".planning/phases/104-open-source-release-and-ottersec-verification/apply-sanitization.sh"
      provides: "Updated sanitization script covering v1.5 secrets"
  key_links:
    - from: "apply-sanitization.sh"
      to: "/tmp/drfraudsworth-public"
      via: "sed/rm commands against staging directory"
      pattern: "STAGING=.*drfraudsworth-public"
---

<objective>
Sync the public GitHub repo with all v1.5 changes (Phases 105, 106, 106.1, 108) while excluding Phase 107 (Jupiter SDK). Follow the exact Phase 104 pattern: rsync from private to staging, update and run apply-sanitization.sh, execute the full 6-phase Docs/public-push-checklist.md, get user approval, then squash-commit and push.

Purpose: Code must be public BEFORE the on-chain upgrade for maximum transparency. Community can review convert_v2 before it goes live. OtterSec re-verification also requires the public repo to contain the new source code.
Output: Updated public repo at MetalLegBob/drfraudsworth with single squashed commit covering v1.5 delta.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@Docs/public-push-checklist.md
@.planning/phases/104-open-source-release-and-ottersec-verification/104-01-SUMMARY.md
@.planning/phases/104-open-source-release-and-ottersec-verification/104-05-SUMMARY.md
@.planning/phases/104-open-source-release-and-ottersec-verification/apply-sanitization.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rsync private repo to staging + update apply-sanitization.sh for v1.5</name>
  <files>
    .planning/phases/104-open-source-release-and-ottersec-verification/apply-sanitization.sh
    /tmp/drfraudsworth-public (staging directory)
  </files>
  <action>
    1. Clean the staging directory (delete everything except .git/):
       ```bash
       cd /tmp/drfraudsworth-public
       find . -maxdepth 1 ! -name '.git' ! -name '.' -exec rm -rf {} +
       ```

    2. Rsync the full private repo to staging with exclusions. Build the exclude list to match Phase 104-01 patterns PLUS exclude Phase 107 (Jupiter SDK):
       - Exclude: keypairs/, .env, .env.*, app/.env.local, .dbs/, .dbs-archive/, .dbs-archive-*, app/certificates/, node_modules/, .next/, target/, .DS_Store, *.pem, *.key, *-keypair.json, .mcp.json
       - Exclude: sdk/jupiter-adapter/ (Phase 107 -- explicitly excluded per CONTEXT.md)
       - Exclude: scripts/e2e/stress-keypairs.json, scripts/graduation/graduation-state.json*
       - Exclude: keypairs/mainnet-*-mint.json, keypairs/squads-signer-*.json
       - Exclude: .claude/projects/ (private Claude project config)
       - Exclude: ci-artifacts/
       - Exclude: "Dr Fraudsworth" (directory with space -- not public)
       Use the same rsync command pattern from Phase 104-01.

    3. Update apply-sanitization.sh for any NEW secrets introduced in v1.5 phases. Before updating, scan the private repo's .audit/ and .bulwark/ for any new secret references introduced since Phase 104:
       - Check .audit/findings/ for any new files with secret values
       - Check .bulwark/findings/ for any new files with secret values
       - Check .planning/phases/105-*, 106-*, 108-* for any secret values in planning docs
       - If new secrets are found, add sed patterns to apply-sanitization.sh

    4. Run apply-sanitization.sh against the staging directory.

    5. Update SECURITY_AUDIT_SUMMARY.md to reflect Phase 108 (zAuth remediation) and Phase 106 SOS audit changes. This is unconditional -- both phases produced audit-relevant changes that must be reflected in the public summary.

    6. Verify Phase 107 exclusion: confirm NO files exist under /tmp/drfraudsworth-public/sdk/jupiter-adapter/.

    7. Verify Phase 106.1 inclusion: confirm /tmp/drfraudsworth-public/app/hooks/useProtocolWallet.ts exists (a key Phase 106.1 file for cluster-aware transaction submission).

    IMPORTANT: The rsync must use --delete behavior (achieved by the clean-then-rsync approach above) to remove files that no longer exist in the private repo. Phase 104 was a fresh repo; this is an incremental update to an existing one.
  </action>
  <verify>
    - `ls /tmp/drfraudsworth-public/sdk/jupiter-adapter/ 2>/dev/null` returns empty (Phase 107 excluded)
    - `ls /tmp/drfraudsworth-public/keypairs/ 2>/dev/null` returns empty (no keypairs)
    - `ls /tmp/drfraudsworth-public/.env 2>/dev/null` returns empty (no env files)
    - `ls /tmp/drfraudsworth-public/programs/conversion-vault/src/` shows convert_v2 files (Phase 106 included)
    - `ls /tmp/drfraudsworth-public/app/hooks/useProtocolWallet.ts` exists (Phase 106.1 included)
    - `grep -r '[REDACTED-CRANK-KEY-PREFIX]' /tmp/drfraudsworth-public/ 2>/dev/null` returns empty
    - `grep -r '[REDACTED-MAINNET-KEY]' /tmp/drfraudsworth-public/ 2>/dev/null` returns empty
    - `grep -r '62b9474f' /tmp/drfraudsworth-public/ 2>/dev/null` returns empty
    - `grep -r 'sm_AoqHCi' /tmp/drfraudsworth-public/ 2>/dev/null` returns empty
  </verify>
  <done>
    Staging directory contains v1.5 codebase with Phase 107 excluded and Phase 106.1 included, SECURITY_AUDIT_SUMMARY.md updated unconditionally, all known secrets redacted by apply-sanitization.sh, and no keypairs/env files present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute full 6-phase public-push-checklist.md against staging</name>
  <files>/tmp/drfraudsworth-public (staging directory)</files>
  <action>
    Execute ALL 6 phases of Docs/public-push-checklist.md against the staging directory at /tmp/drfraudsworth-public/. Run every single grep/find/ls command from the checklist verbatim and record results.

    Phase 1: Automated Secret Scanning (1A through 1G -- all 7 sub-checks)
    Phase 2: Dangerous File Detection (2A and 2B)
    Phase 3: Audit & Bulwark Findings Deep Scan (3A, 3B, 3C manual spot checks)
    Phase 4: Personal Information (4A and 4B)
    Phase 5: Cross-Check Against Known Secrets (all 9 secrets from the table)
    Phase 6: Final Gate (git diff --stat review, no binary files, etc.)

    For Phase 3C manual spot checks: read and verify each listed file (.bulwark/findings/H001.md, S001.md, S004.md, S010.md, H120.md if it exists, .audit/findings/S004.md).

    If ANY check fails (returns matches that are NOT confirmed safe), STOP and fix. Either:
    - Add a new sed pattern to apply-sanitization.sh and re-run it
    - Remove the offending file if it should not be in the public repo
    Then re-run the failing checks to confirm clean.

    Record the results of all 6 phases (pass/fail per check) for the summary.
  </action>
  <verify>
    All 6 phases of the checklist pass with zero unsafe matches. Document the exact count of checks run and their results.
  </verify>
  <done>
    6-phase sanitization checklist passes completely clean -- zero secrets, zero dangerous files, zero personal info leaks, zero known secret cross-matches.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Staging directory at /tmp/drfraudsworth-public/ synced with all v1.5 changes (Phases 105, 106, 106.1, 108), sanitized via apply-sanitization.sh, and verified clean through all 6 phases of the public-push-checklist. Phase 107 (Jupiter SDK) excluded. SECURITY_AUDIT_SUMMARY.md updated. Ready for your manual verification pass before push.
  </what-built>
  <how-to-verify>
    1. Review the automated checklist results provided in the task output
    2. Run your own spot-checks against /tmp/drfraudsworth-public/:
       - Check a few .bulwark/findings/ files for any base58 strings that look like private keys
       - Verify shared/programs.ts has placeholder Helius URL (not real key)
       - Verify CLAUDE.md is the sanitized public version
       - Verify no sdk/jupiter-adapter/ directory exists
    3. Run `cd /tmp/drfraudsworth-public && git diff --stat` to see all files that changed since last push (commit 946703f)
    4. Verify the diff contains only expected v1.5 changes
    5. If you want to do the full 3-pass manual verification like Phase 104, go ahead -- security first
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with squash commit + push, or describe any issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Squash commit and push to public repo</name>
  <files>/tmp/drfraudsworth-public (git operations)</files>
  <action>
    1. Verify git config in the staging directory is correct:
       ```bash
       cd /tmp/drfraudsworth-public
       git config user.name   # Must be MetalLegBob
       git config user.email  # Must be metallegbob@gmail.com
       ```
       If wrong, set them: `git config user.name "MetalLegBob"` and `git config user.email "metallegbob@gmail.com"`

    2. Stage all changes:
       ```bash
       git add -A
       ```

    3. Create a single squashed commit covering all v1.5 changes:
       ```bash
       git commit -m "v1.5: crank hardening, vault convert-all, security remediation

       Phase 105: Crank randomness account lifecycle, VRF retry tuning, Telegram alerting
       Phase 106: Vault convert_v2 instruction (convert-all), multi-hop zero-leakage
       Phase 106.1: Cluster-aware skipPreflight for devnet compatibility
       Phase 108: zAuth vulnerability report triage and remediation
       Updated audit reports (.audit/, .bulwark/, SECURITY_AUDIT_SUMMARY.md)"
       ```

    4. Push to origin main:
       ```bash
       git push origin main
       ```

    5. Verify push succeeded:
       ```bash
       gh repo view MetalLegBob/drfraudsworth --json url,defaultBranchRef
       ```

    IMPORTANT: Do NOT force push. This is an incremental commit on top of the existing history (last commit 946703f).
  </action>
  <verify>
    - `gh repo view MetalLegBob/drfraudsworth --json defaultBranchRef` shows the new commit hash
    - The public repo is accessible at https://github.com/MetalLegBob/drfraudsworth
    - `git log --oneline -2` in staging directory shows the new v1.5 commit on top of 946703f
  </verify>
  <done>
    Public repo MetalLegBob/drfraudsworth updated with single squashed v1.5 commit. Community can review convert_v2 source code. Commit hash recorded for OtterSec re-verification in Plan 04.
  </done>
</task>

</tasks>

<verification>
- Public repo contains programs/conversion-vault/src/ with convert_v2 code
- Public repo does NOT contain sdk/jupiter-adapter/
- Public repo contains app/hooks/useProtocolWallet.ts (Phase 106.1)
- Public repo has zero secrets (verified by 6-phase checklist)
- Git history shows clean incremental commit on top of 946703f
- SECURITY_AUDIT_SUMMARY.md reflects Phase 108 and Phase 106 audit changes
</verification>

<success_criteria>
- Public GitHub repo updated with v1.5 codebase (excluding Phase 107)
- Zero secrets in any file at HEAD
- Single squashed commit with descriptive message
- Commit hash available for OtterSec re-verification
- User manually verified before push
</success_criteria>

<output>
After completion, create `.planning/phases/109-public-push-mainnet-vault-upgrade-announcement/109-01-SUMMARY.md`
</output>
