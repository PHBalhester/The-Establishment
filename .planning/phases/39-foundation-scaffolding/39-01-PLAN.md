---
phase: 39-foundation-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - app/package.json
  - app/next.config.ts
  - app/postcss.config.mjs
  - app/tsconfig.json
  - app/instrumentation-client.ts
  - app/lib/empty.ts
  - app/app/layout.tsx
  - app/app/page.tsx
  - app/app/globals.css
  - shared/package.json
  - shared/tsconfig.json
  - shared/index.ts
  - shared/constants.ts
  - shared/programs.ts
autonomous: true

must_haves:
  truths:
    - "`npm run dev` in app/ starts a working Next.js dev server that renders a page in the browser"
    - "shared/ package exports PROGRAM_IDS, MINTS, SEEDS, TOKEN_DECIMALS and can be imported by app/"
    - "Tailwind CSS utility classes work in app/ (visible styling on page)"
    - "Buffer polyfill is active before React hydration (instrumentation-client.ts)"
    - "Turbopack resolveAlias stubs prevent 'Module not found' errors for fs/net/tls"
  artifacts:
    - path: "app/package.json"
      provides: "Next.js project config with dependencies and scripts"
      contains: "@dr-fraudsworth/shared"
    - path: "app/next.config.ts"
      provides: "Turbopack config with resolveAlias and transpilePackages"
      contains: "resolveAlias"
    - path: "app/instrumentation-client.ts"
      provides: "Buffer polyfill before React hydration"
      contains: "globalThis.Buffer"
    - path: "shared/constants.ts"
      provides: "PDA seeds, program IDs, token decimals, fee constants"
      contains: "PROGRAM_IDS"
    - path: "shared/index.ts"
      provides: "Barrel export for shared package"
      exports: ["PROGRAM_IDS", "MINTS", "SEEDS", "TOKEN_DECIMALS"]
    - path: "package.json"
      provides: "Root workspace config"
      contains: "workspaces"
  key_links:
    - from: "app/next.config.ts"
      to: "@dr-fraudsworth/shared"
      via: "transpilePackages"
      pattern: "transpilePackages.*@dr-fraudsworth/shared"
    - from: "app/app/page.tsx"
      to: "@dr-fraudsworth/shared"
      via: "import"
      pattern: "from.*@dr-fraudsworth/shared"
    - from: "app/instrumentation-client.ts"
      to: "buffer"
      via: "globalThis.Buffer assignment"
      pattern: "globalThis\\.Buffer"
---

<objective>
Create the Next.js 16 project scaffold in `app/`, the shared constants package in `shared/`, configure npm workspaces for monorepo linking, set up Tailwind CSS v4, Buffer polyfill, and Turbopack node module stubs.

Purpose: This is the foundation every subsequent frontend phase builds on. Without a working dev server, shared imports, and browser polyfills, nothing else can proceed.
Output: Working `npm run dev` with a proof-of-life page that imports from `@dr-fraudsworth/shared`, styled with Tailwind, with Buffer available globally.
</objective>

<execution_context>
@./.claude/agents/gsd-executor.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-foundation-scaffolding/39-CONTEXT.md
@.planning/phases/39-foundation-scaffolding/39-RESEARCH.md
@package.json
@tsconfig.json
@.gitignore
@scripts/deploy/pda-manifest.json
@tests/integration/helpers/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared/ constants package with npm workspace configuration</name>
  <files>
    shared/package.json
    shared/tsconfig.json
    shared/index.ts
    shared/constants.ts
    shared/programs.ts
    package.json
  </files>
  <action>
    1. Create `shared/package.json` with name `@dr-fraudsworth/shared`, version `0.0.1`, main and types pointing to `index.ts`. Do NOT add `"type": "module"` -- the shared package exports raw `.ts` files transpiled by Next.js via transpilePackages. Add `@solana/web3.js` as a peer dependency (it's hoisted from root).

    2. Create `shared/tsconfig.json` extending nothing (standalone), targeting ES2020, with strict mode enabled, resolveJsonModule true, esModuleInterop true. Include `*.ts` files.

    3. Create `shared/constants.ts` containing:
       - `PROGRAM_IDS` object with all 5 program PublicKeys (AMM, TRANSFER_HOOK, TAX_PROGRAM, EPOCH_PROGRAM, STAKING) from `scripts/deploy/pda-manifest.json`
       - `MINTS` object with CRIME, FRAUD, PROFIT PublicKeys from pda-manifest.json
       - `TOKEN_DECIMALS = 6`
       - `MINIMUM_STAKE = 1_000_000`
       - `SOL_POOL_FEE_BPS = 100`
       - `PROFIT_POOL_FEE_BPS = 50`
       - `SEEDS` object with all PDA seed buffers (use `Buffer.from()` for each seed string) -- mirror the exact seeds from `tests/integration/helpers/constants.ts`:
         - Staking: STAKE_POOL, ESCROW_VAULT, STAKE_VAULT, USER_STAKE
         - Transfer Hook: WHITELIST_AUTHORITY ("authority"), WHITELIST_ENTRY ("whitelist"), EXTRA_ACCOUNT_META ("extra-account-metas")
         - AMM: ADMIN ("admin"), POOL ("pool"), VAULT ("vault"), VAULT_A ("a"), VAULT_B ("b"), SWAP_AUTHORITY ("swap_authority")
         - Tax: TAX_AUTHORITY ("tax_authority")
         - Epoch: EPOCH_STATE, CARNAGE_FUND, CARNAGE_SOL_VAULT, CARNAGE_CRIME_VAULT, CARNAGE_FRAUD_VAULT, CARNAGE_SIGNER, STAKING_AUTHORITY
       Mark the object `as const`.

    4. Create `shared/programs.ts` with a `DEVNET_ALT` PublicKey constant (`EyUncwUhSwCVyTnbeoKe7Ug33sUDGAQDLDBQ5fVP9Vuf`). Also export the devnet RPC URL as `DEVNET_RPC_URL = "https://devnet.helius-rpc.com/?api-key=[REDACTED-DEVNET-HELIUS-KEY]"` (this is the same key already in pda-manifest.json, not a secret -- it's Helius free tier).

    5. Create `shared/index.ts` as a barrel export re-exporting everything from `constants.ts` and `programs.ts`.

    6. Update root `package.json`: Add `"workspaces": ["shared", "app"]` field. Do NOT add `"type": "module"`. Do NOT change existing scripts or dependencies. The workspaces field goes at the top level alongside "name", "version", etc.

    IMPORTANT: Do NOT modify any files in tests/ or scripts/. The shared package is for app/ consumption only per CONTEXT.md decision.
  </action>
  <verify>
    Run `cd /Users/mlbob/Projects/Dr\ Fraudsworth && cat shared/package.json` to confirm package exists.
    Run `cat shared/constants.ts | head -20` to confirm PROGRAM_IDS object exists.
    Run `cat package.json | grep workspaces` to confirm workspaces field added.
    Run `node -e "const s = require('./shared/constants.ts')" 2>&1 || echo "Expected - .ts files need transpilation"` -- this is expected to fail since it's raw TS; the real test is Next.js importing it.
  </verify>
  <done>
    shared/ directory exists with package.json, tsconfig.json, index.ts, constants.ts, programs.ts.
    Root package.json has workspaces field including "shared" and "app".
    Constants match pda-manifest.json program IDs and mints exactly.
    All PDA seeds match tests/integration/helpers/constants.ts seed strings exactly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Next.js 16 project with Tailwind, polyfills, and shared import</name>
  <files>
    app/package.json
    app/next.config.ts
    app/postcss.config.mjs
    app/tsconfig.json
    app/instrumentation-client.ts
    app/lib/empty.ts
    app/app/layout.tsx
    app/app/page.tsx
    app/app/globals.css
    .gitignore
  </files>
  <action>
    1. Run `npx create-next-app@latest app --yes` from the project root. This creates app/ with Next.js 16, TypeScript, Tailwind CSS v4, App Router, and Turbopack defaults. Let the scaffold complete fully.

    2. After scaffold completes, `cd app` and install Solana/Anchor dependencies:
       ```
       npm install @coral-xyz/anchor @solana/web3.js buffer
       ```
       Also add the shared workspace dependency:
       ```
       npm install @dr-fraudsworth/shared@*
       ```
       (npm workspaces will resolve this to the local `shared/` directory)

    3. Replace `app/next.config.ts` with:
       - `transpilePackages: ["@dr-fraudsworth/shared"]`
       - `turbopack.resolveAlias` mapping `fs`, `net`, `tls` to `{ browser: "./lib/empty.ts" }`
       - Use the exact pattern from 39-RESEARCH.md "Complete next.config.ts for Solana dApp" example
       - Do NOT add webpack() function -- Turbopack is the default bundler

    4. Create `app/instrumentation-client.ts`:
       ```typescript
       import { Buffer } from "buffer";
       globalThis.Buffer = Buffer;
       ```
       This runs before React hydration per Next.js 16 convention.

    5. Create `app/lib/empty.ts`:
       ```typescript
       export default {};
       ```
       Used by resolveAlias to stub Node.js modules.

    6. Replace `app/app/globals.css` to contain only:
       ```css
       @import "tailwindcss";
       ```
       This is Tailwind v4 CSS-first config -- no tailwind.config.js needed.

    7. Replace `app/app/layout.tsx` with a minimal root layout:
       - html + body tags, import globals.css
       - Metadata title "Dr. Fraudsworth" and description
       - No providers yet (wallet/Privy come in Phase 40)
       - Clean, minimal -- this is tech validation, not design

    8. Replace `app/app/page.tsx` with a proof-of-life page:
       - `"use client"` directive (needed for useEffect + Anchor in browser)
       - Import `PROGRAM_IDS` and `TOKEN_DECIMALS` from `@dr-fraudsworth/shared`
       - Display the imported constants on the page to prove shared/ imports work
       - Show a status indicator (text) confirming "Shared imports: OK" or similar
       - Use basic Tailwind classes (flex, text-center, etc.) to prove Tailwind works
       - No Anchor/RPC calls yet -- that's Plan 02

    9. Verify `app/postcss.config.mjs` exists (created by create-next-app) and contains `@tailwindcss/postcss` plugin. If not, create it:
       ```javascript
       const config = {
         plugins: {
           "@tailwindcss/postcss": {},
         },
       };
       export default config;
       ```

    10. Update root `.gitignore` to add:
        ```
        # Next.js app build artifacts
        app/.next/
        app/idl/
        ```
        `app/idl/` is gitignored because IDL files are derived artifacts synced at build time (Plan 02). `app/.next/` is the Next.js build cache.

    11. Run a clean install from the project root to set up workspace hoisting:
        ```
        rm -rf node_modules package-lock.json app/node_modules
        npm install
        ```
        This lets npm recalculate hoisting with the new workspaces config.

    IMPORTANT: Do NOT add `"type": "module"` to root package.json (would break ts-mocha tests). Do NOT create tailwind.config.js (v4 uses CSS-first). Do NOT add webpack() to next.config.ts (Turbopack is default).
  </action>
  <verify>
    1. `cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && export PATH="/opt/homebrew/bin:$PATH" && npx next dev --port 3099` -- should start without build errors (run for ~10 seconds, check for "Ready" message, then kill).
    2. Check that `app/next.config.ts` contains `resolveAlias` and `transpilePackages`.
    3. Check that `app/instrumentation-client.ts` exists with Buffer polyfill.
    4. Check that `app/app/page.tsx` imports from `@dr-fraudsworth/shared`.
    5. Check `.gitignore` includes `app/.next/` and `app/idl/`.
  </verify>
  <done>
    Next.js dev server starts without errors.
    page.tsx successfully imports and displays shared constants (PROGRAM_IDS, TOKEN_DECIMALS).
    Tailwind CSS utility classes render correctly.
    Buffer polyfill configured via instrumentation-client.ts.
    Turbopack resolveAlias stubs fs/net/tls.
    No `"type": "module"` in root package.json.
    app/.next/ and app/idl/ in .gitignore.
  </done>
</task>

</tasks>

<verification>
1. `cd app && npm run dev` starts successfully (no build errors, "Ready" message appears)
2. Visit http://localhost:3000 -- page renders with "Dr. Fraudsworth" heading and shared constants displayed
3. Browser console has no "Buffer is not defined" errors
4. Browser console has no "Module not found" errors
5. `grep -r "workspaces" package.json` shows workspace config
6. `cat shared/constants.ts | grep PROGRAM_IDS` shows program IDs
</verification>

<success_criteria>
- Next.js 16 dev server starts and renders a page
- @dr-fraudsworth/shared imports resolve in the browser
- Tailwind CSS classes apply visual styles
- No polyfill or module resolution errors
- Existing tests/scripts infrastructure NOT broken (root tsconfig, ts-mocha still work)
</success_criteria>

<output>
After completion, create `.planning/phases/39-foundation-scaffolding/39-01-SUMMARY.md`
</output>
