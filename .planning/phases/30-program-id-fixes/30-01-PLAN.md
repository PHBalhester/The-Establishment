---
phase: 30-program-id-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - keypairs/amm-keypair.json
  - keypairs/tax-program-keypair.json
  - keypairs/staking-keypair.json
  - keypairs/taXaejVkdjrcJnnCYnvtMeBBnx5bufGcPAphxpZRTvz.json
  - target/deploy/epoch_program-keypair.json
  - target/deploy/staking-keypair.json
  - target/deploy/stub_staking-keypair.json
  - target/deploy/mock_tax_program-keypair.json
  - target/deploy/fake_tax_program-keypair.json
  - programs/tax-program/src/constants.rs
  - programs/amm/tests/test_cpi_access_control.rs
  - programs/amm/tests/test_swap_sol_pool.rs
  - programs/amm/tests/test_swap_profit_pool.rs
  - programs/amm/tests/test_pool_initialization.rs
  - programs/amm/tests/test_transfer_routing.rs
  - programs/tax-program/tests/test_swap_sol_buy.rs
  - programs/tax-program/tests/test_swap_sol_sell.rs
  - programs/tax-program/tests/test_swap_exempt.rs
  - Anchor.toml
autonomous: false

must_haves:
  truths:
    - "Every production program's keypairs/ file derives to the same pubkey as its declare_id! macro"
    - "Tax Program's epoch_program_id() returns AH7yaWFUrWmXGDKEwr8w2EMEiH6PxaxE2vT9orjRzvod (the actual Epoch Program ID)"
    - "All test helper functions reference the current declare_id! values, not stale old IDs"
    - "Anchor.toml [programs.devnet] section lists all 5 production program IDs"
  artifacts:
    - path: "keypairs/amm-keypair.json"
      provides: "AMM program keypair (source of truth)"
    - path: "keypairs/staking-keypair.json"
      provides: "Staking program keypair (source of truth)"
    - path: "programs/tax-program/src/constants.rs"
      provides: "Cross-program ID references including fixed epoch_program_id()"
      contains: "AH7yaWFUrWmXGDKEwr8w2EMEiH6PxaxE2vT9orjRzvod"
    - path: "Anchor.toml"
      provides: "[programs.devnet] section with all 5 production IDs"
      contains: "programs.devnet"
  key_links:
    - from: "keypairs/*.json"
      to: "target/deploy/*-keypair.json"
      via: "file copy"
      pattern: "keypairs derived pubkeys match target/deploy derived pubkeys"
    - from: "target/deploy/*-keypair.json"
      to: "programs/*/src/lib.rs declare_id!"
      via: "anchor keys sync"
      pattern: "declare_id! matches keypair pubkey"
    - from: "programs/tax-program/src/constants.rs epoch_program_id()"
      to: "programs/epoch-program/src/lib.rs declare_id!"
      via: "manual ID string update"
      pattern: "AH7yaWFUrWmXGDKEwr8w2EMEiH6PxaxE2vT9orjRzvod"
---

<objective>
Reconcile all program keypairs, fix the Tax Program's epoch_program_id() placeholder, update stale test IDs, and add devnet configuration to Anchor.toml.

Purpose: This is the foundation of v0.7 integration -- nothing works if programs reference wrong IDs. The Tax Program's broken epoch_program_id() placeholder blocks all Carnage CPI paths, and stale test IDs cause false confidence in test results.

Output: Consistent keypairs/ directory (source of truth for all 8 programs), correct declare_id! macros, fixed cross-program references, updated test files, and Anchor.toml with devnet section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-program-id-fixes/30-RESEARCH.md
@.planning/phases/30-program-id-fixes/30-CONTEXT.md
@Anchor.toml
@programs/tax-program/src/constants.rs
@programs/amm/tests/test_cpi_access_control.rs
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Missing Staking Program keypair -- is the vanity keypair recoverable?</decision>
  <context>
The Staking Program uses declare_id!("StakFwVR1u8TuDtfv9tjLTpQbBH3rPLqe5UHJJPkEXF") which is a vanity address (starts with "StakF"). However, no keypair file exists anywhere in the repo that derives to this public key:

- keypairs/ directory: No staking keypair file
- target/deploy/staking-keypair.json: Derives to G1rqChKE... (WRONG)

If you have this vanity keypair saved elsewhere (another machine, backup, keychain), we can copy it into keypairs/staking-keypair.json and proceed.

If the keypair is lost, we must generate a new keypair (random or vanity) and update ALL references:
- declare_id! in programs/staking/src/lib.rs
- staking_program_id() in programs/tax-program/src/constants.rs
- Anchor.toml [programs.localnet] staking = "..."
- Anchor.toml [programs.devnet] staking = "..."
- Any test files referencing the staking program ID

This is the single biggest variable in Phase 30 scope.
  </context>
  <options>
    <option id="option-a">
      <name>I have the keypair -- provide the file path</name>
      <pros>Zero code changes needed for staking ID, just copy the file</pros>
      <cons>None</cons>
    </option>
    <option id="option-b">
      <name>Keypair is lost -- generate a new random keypair</name>
      <pros>Fast, immediate, move forward</pros>
      <cons>Lose the vanity "StakF..." prefix, need to update all staking ID references</cons>
    </option>
    <option id="option-c">
      <name>Keypair is lost -- generate a new vanity keypair</name>
      <pros>Keeps memorable "Stak..." prefix</pros>
      <cons>Vanity generation takes time (minutes to hours depending on prefix length), still need to update all references</cons>
    </option>
  </options>
  <resume-signal>Select option-a (and provide file path), option-b, or option-c</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Reconcile all keypairs and sync declare_id! macros</name>
  <files>
    keypairs/amm-keypair.json
    keypairs/tax-program-keypair.json
    keypairs/staking-keypair.json
    keypairs/taXaejVkdjrcJnnCYnvtMeBBnx5bufGcPAphxpZRTvz.json
    target/deploy/epoch_program-keypair.json
    target/deploy/staking-keypair.json
    target/deploy/stub_staking-keypair.json
    target/deploy/mock_tax_program-keypair.json
    target/deploy/fake_tax_program-keypair.json
  </files>
  <action>
**Keypairs/ directory (source of truth) -- fix missing and stale files:**

1. **AMM (MISSING in keypairs/):** Copy `target/deploy/amm-keypair.json` to `keypairs/amm-keypair.json`. Verify it derives to `zFW9moTqWoBhCJ2eVREhrkasaNwvhprCoKCmJZfrUxa` using `solana-keygen pubkey`.

2. **Tax Program (STALE in keypairs/):** Copy `target/deploy/tax_program-keypair.json` to `keypairs/tax-program-keypair.json` (overwriting the stale TAXr5uS1... file). Verify it derives to `FV3kWDtSRDHTdd9fK9L1fkqdWis7Sts5x7nNS4uoSiiu`. Delete the stale vanity file `keypairs/taXaejVkdjrcJnnCYnvtMeBBnx5bufGcPAphxpZRTvz.json` (it was from an earlier attempt and doesn't match any program).

3. **Staking (MISSING in keypairs/):** Based on checkpoint decision:
   - If option-a: Copy provided keypair to `keypairs/staking-keypair.json`
   - If option-b: Generate new random keypair with `solana-keygen new --outfile keypairs/staking-keypair.json --no-bip39-passphrase`, note the new pubkey
   - If option-c: Generate vanity keypair with `solana-keygen grind --starts-with Stak:1`, copy result to `keypairs/staking-keypair.json`, note the new pubkey

4. **Transfer Hook, Epoch Program:** Already correct in keypairs/. Verify with `solana-keygen pubkey`.

**Target/deploy/ directory -- copy from keypairs/ to ensure Anchor builds correctly:**

5. Copy `keypairs/epoch-program.json` to `target/deploy/epoch_program-keypair.json` (currently mismatched -- HdXeALpi...)
6. Copy `keypairs/staking-keypair.json` to `target/deploy/staking-keypair.json` (currently mismatched -- G1rqChKE...)
7. Copy `keypairs/StUbofRk12S7JrEUoQJFjMe6FmACNoRpbNMyjn311ZU.json` to `target/deploy/stub_staking-keypair.json` (currently mismatched -- FenuJSSK...)
8. For Mock Tax and Fake Tax: Copy `target/deploy/mock_tax_program-keypair.json` to `keypairs/mock-tax-keypair.json` and `target/deploy/fake_tax_program-keypair.json` to `keypairs/fake-tax-keypair.json` (overwrite stale keypairs/ files with correct ones from target/deploy/).

**Sync declare_id! macros:**

9. Run `anchor keys sync` to update all declare_id! macros from target/deploy/ keypairs. Only Epoch, Staking, and Stub Staking should change (others already match). If Staking keypair changed (option-b/c), its declare_id! will get the new pubkey.

10. Verify with `anchor keys list` that ALL 8 programs show the expected pubkeys.

**IMPORTANT:** Do steps 5-8 BEFORE step 9. `anchor keys sync` reads from target/deploy/. If target/deploy/ has wrong keypairs, sync will write wrong IDs.
  </action>
  <verify>
Run `solana-keygen pubkey keypairs/<file>.json` for each of the 8 programs and compare against `anchor keys list` output. Every pair must match.

Run `grep -n "declare_id" programs/*/src/lib.rs programs/*/src/lib.rs` and confirm each declare_id! matches its keypair.
  </verify>
  <done>
All 8 programs have keypair files in keypairs/ that match their declare_id! macros. target/deploy/ keypairs match keypairs/. `anchor keys list` shows correct IDs for all 8 programs. No stale or mismatched keypairs remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix cross-program ID references, test IDs, and Anchor.toml</name>
  <files>
    programs/tax-program/src/constants.rs
    programs/amm/tests/test_cpi_access_control.rs
    programs/amm/tests/test_swap_sol_pool.rs
    programs/amm/tests/test_swap_profit_pool.rs
    programs/amm/tests/test_pool_initialization.rs
    programs/amm/tests/test_transfer_routing.rs
    programs/tax-program/tests/test_swap_sol_buy.rs
    programs/tax-program/tests/test_swap_sol_sell.rs
    programs/tax-program/tests/test_swap_exempt.rs
    Anchor.toml
  </files>
  <action>
**Fix the critical blocker -- Tax Program epoch_program_id() placeholder:**

1. In `programs/tax-program/src/constants.rs` line 48, replace:
   ```rust
   Pubkey::from_str("EpochProgram1111111111111111111111111111111").unwrap()
   ```
   with:
   ```rust
   Pubkey::from_str("AH7yaWFUrWmXGDKEwr8w2EMEiH6PxaxE2vT9orjRzvod").unwrap()
   ```

2. If Staking keypair changed (option-b/c from checkpoint), also update:
   - `programs/tax-program/src/constants.rs` staking_program_id() to the new Staking ID
   - `programs/staking/src/constants.rs` is OK (it references Tax and Epoch, not itself)
   - But check if Epoch or Stub-Staking reference the Staking ID and update those too

**Fix stale AMM test program IDs:**

3. In ALL 5 AMM test files, replace `BDwTJT4966CGcMP4HQS1QAp72MSF6nszhAD7M1V9xTNx` with the actual AMM declare_id! (`zFW9moTqWoBhCJ2eVREhrkasaNwvhprCoKCmJZfrUxa`):
   - `programs/amm/tests/test_cpi_access_control.rs` (line 48)
   - `programs/amm/tests/test_swap_sol_pool.rs` (line 44)
   - `programs/amm/tests/test_swap_profit_pool.rs` (line 42)
   - `programs/amm/tests/test_pool_initialization.rs` (line 42)
   - `programs/amm/tests/test_transfer_routing.rs` (line 52)

4. In `programs/amm/tests/test_cpi_access_control.rs`, replace stale Mock/Fake Tax IDs:
   - Replace `J5CK3BiYwiQtt7Yfx3PLNrFr7YWCVGrskXiGvtYBqd5W` (line 56) with `9irnHg1ddyLeeDTcuXYMa8Zby7uafL5PpkZ7LPfzzNw9` (Mock Tax declare_id!)
   - Replace `EbN9johTcjch29b4kBU5N5Ked2skJnahKGViMWg5Y7GP` (line 64) with `7i38TDxugSPSV9ciUNTbnEeBps5C5xiQSSY7kNG65YnJ` (Fake Tax declare_id!)

**Fix Tax Program test epoch IDs:**

5. In 3 Tax Program test files, replace the epoch placeholder with real ID:
   - `programs/tax-program/tests/test_swap_sol_buy.rs` (line 76): Replace `EpochProgram1111111111111111111111111111111` with `AH7yaWFUrWmXGDKEwr8w2EMEiH6PxaxE2vT9orjRzvod`
   - `programs/tax-program/tests/test_swap_sol_sell.rs` (line 74): Same replacement
   - `programs/tax-program/tests/test_swap_exempt.rs` (line 55): Same replacement

**Update Anchor.toml with devnet section:**

6. Add a `[programs.devnet]` section to Anchor.toml with all 5 production program IDs. The IDs should be identical to `[programs.localnet]` (same keypairs, different cluster). Only include the 5 production programs (not test helpers):
   ```toml
   [programs.devnet]
   amm = "zFW9moTqWoBhCJ2eVREhrkasaNwvhprCoKCmJZfrUxa"
   epoch_program = "AH7yaWFUrWmXGDKEwr8w2EMEiH6PxaxE2vT9orjRzvod"
   transfer_hook = "9UyWsQ6vMDXRfwmCm66hWpje8SPWRFDXneYb3EoPapAQ"
   tax_program = "FV3kWDtSRDHTdd9fK9L1fkqdWis7Sts5x7nNS4uoSiiu"
   staking = "StakFwVR1u8TuDtfv9tjLTpQbBH3rPLqe5UHJJPkEXF"
   ```
   (Adjust staking ID if it changed from checkpoint decision.)

7. Also update `[programs.localnet]` staking ID if it changed from checkpoint decision.

**Do NOT update:**
- Cross-program refs that are already correct (see research Cross-Program ID Reference Map -- only epoch_program_id() in tax constants.rs is broken)
- The AMM's TAX_PROGRAM_ID (already correct: FV3kWDtSRDHTdd9fK9L1fkqdWis7Sts5x7nNS4uoSiiu)
- Staking's tax_program_id() or epoch_program_id() (already correct)

**Run a final grep to confirm no stale IDs remain:**
```bash
grep -rn "EpochProgram1111111111111111111111111111111" programs/
grep -rn "BDwTJT4966CGcMP4HQS1QAp72MSF6nszhAD7M1V9xTNx" programs/
grep -rn "J5CK3BiYwiQtt7Yfx3PLNrFr7YWCVGrskXiGvtYBqd5W" programs/
grep -rn "EbN9johTcjch29b4kBU5N5Ked2skJnahKGViMWg5Y7GP" programs/
```
All four greps should return zero results.
  </action>
  <verify>
1. `grep -rn "EpochProgram1111111111111111111111111111111" programs/` returns nothing
2. `grep -rn "BDwTJT4966CGcMP4HQS1QAp72MSF6nszhAD7M1V9xTNx" programs/` returns nothing
3. `grep -rn "J5CK3BiYwiQtt7Yfx3PLNrFr7YWCVGrskXiGvtYBqd5W" programs/` returns nothing
4. `grep -rn "EbN9johTcjch29b4kBU5N5Ked2skJnahKGViMWg5Y7GP" programs/` returns nothing
5. `grep "programs.devnet" Anchor.toml` returns the section header
6. Tax Program constants.rs contains `AH7yaWFUrWmXGDKEwr8w2EMEiH6PxaxE2vT9orjRzvod` at the epoch_program_id() function
  </verify>
  <done>
Tax Program epoch_program_id() returns the real Epoch Program ID. All stale test IDs are replaced with current declare_id! values. Anchor.toml has a [programs.devnet] section with all 5 production IDs. Zero stale/placeholder program IDs remain in any program source or test file.
  </done>
</task>

</tasks>

<verification>
1. Run `solana-keygen pubkey keypairs/<file>` for all 8 keypair files and confirm each matches its program's declare_id! macro
2. Run `anchor keys list` and confirm output matches keypairs/ pubkeys
3. Grep for all known stale IDs in programs/ directory -- zero matches
4. Confirm Anchor.toml [programs.devnet] section exists with 5 production IDs
5. Confirm Tax Program constants.rs epoch_program_id() contains AH7yaWFUrWmXGDKEwr8w2EMEiH6PxaxE2vT9orjRzvod
</verification>

<success_criteria>
- Every keypairs/ file matches its declare_id! macro (8/8 programs)
- target/deploy/ keypairs match keypairs/ (8/8 programs)
- Tax Program epoch_program_id() returns AH7yaWFUrWmXGDKEwr8w2EMEiH6PxaxE2vT9orjRzvod
- Zero stale program IDs in programs/ source and test files
- Anchor.toml has [programs.devnet] with 5 production IDs
</success_criteria>

<output>
After completion, create `.planning/phases/30-program-id-fixes/30-01-SUMMARY.md`
</output>
