---
phase: 05-convergence
plan: 10
type: execute
wave: 5
depends_on: ["05-09"]
files_modified:
  - Docs/Tax_Pool_Logic_Spec.md
  - Docs/Carnage_Fund_Spec.md
  - Docs/Epoch_State_Machine_Spec.md
  - .planning/audit/GAPS.md
autonomous: true

must_haves:
  truths:
    - "Tax spec has event emissions section"
    - "Carnage spec has operational monitoring section"
    - "Tax/Epoch spec has tax band boundary documentation"
    - "GAPS.md shows all 3 LOW gaps as Filled"
    - "All 24 gaps now resolved"
  artifacts:
    - path: "Docs/Tax_Pool_Logic_Spec.md"
      provides: "Event emissions documentation"
      contains: "#[event]"
    - path: "Docs/Carnage_Fund_Spec.md"
      provides: "Operational runbooks"
      contains: "Monitoring"
    - path: ".planning/audit/GAPS.md"
      provides: "All gaps filled"
      contains: "LOW | 0 | 3"
  key_links:
    - from: "Docs/Tax_Pool_Logic_Spec.md"
      to: "Docs/Epoch_State_Machine_Spec.md"
      via: "Tax band boundary values"
      pattern: "bps"
---

<objective>
Fill all LOW-priority gaps: GAP-008 (Tax event emissions), GAP-052 (Carnage operational runbooks), and GAP-062 (Tax band boundary conditions).

Purpose: These are the final 3 gaps to fill. They are LOW priority but still important for completeness. After this plan, all 24 gaps will be resolved and ready for final verification.

Output: Complete specification documentation with all gaps filled.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/audit/GAPS.md
@.planning/phases/05-convergence/05-CONTEXT.md
@Docs/Tax_Pool_Logic_Spec.md
@Docs/Carnage_Fund_Spec.md
@Docs/Epoch_State_Machine_Spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Event Emissions to Tax Spec (GAP-008)</name>
  <files>Docs/Tax_Pool_Logic_Spec.md</files>
  <action>
Add a "## Events" section to Tax_Pool_Logic_Spec.md documenting the events emitted by taxed swaps.

Use Epoch_State_Machine_Spec.md Section 12 as the quality template.

```markdown
## Events

The Tax Program emits events for all swap operations to enable off-chain tracking and analytics.

### TaxedSwap Event

Emitted after every successful taxed swap operation.

```rust
#[event]
pub struct TaxedSwap {
    /// The user performing the swap
    pub user: Pubkey,

    /// Pool type: SolCrime, SolFraud, CrimeProfit, FraudProfit
    pub pool_type: PoolType,

    /// Direction: Buy or Sell (relative to IP token)
    pub direction: SwapDirection,

    /// Input amount before tax
    pub input_amount: u64,

    /// Output amount after swap
    pub output_amount: u64,

    /// Total tax amount deducted
    pub tax_amount: u64,

    /// Tax rate in basis points (e.g., 400 = 4%)
    pub tax_rate_bps: u16,

    /// Portion sent to yield escrow (75%)
    pub yield_portion: u64,

    /// Portion sent to Carnage vault (24%)
    pub carnage_portion: u64,

    /// Portion sent to treasury (1%)
    pub treasury_portion: u64,

    /// Current epoch number
    pub epoch: u32,

    /// Slot of the transaction
    pub slot: u64,
}
```

### SwapDirection Enum

```rust
#[derive(AnchorSerialize, AnchorDeserialize, Clone, Copy)]
pub enum SwapDirection {
    Buy,   // Buying IP token (CRIME/FRAUD/PROFIT)
    Sell,  // Selling IP token
}
```

### Event Usage

**Off-chain indexers can use these events to:**
- Track total tax collected per epoch
- Calculate individual user swap history
- Monitor yield distribution accuracy
- Verify Carnage funding rates
- Build analytics dashboards

**Example Event Log:**
```json
{
  "name": "TaxedSwap",
  "data": {
    "user": "7xKX...abc",
    "pool_type": "SolCrime",
    "direction": "Buy",
    "input_amount": 1000000000,
    "output_amount": 980000000,
    "tax_amount": 40000000,
    "tax_rate_bps": 400,
    "yield_portion": 30000000,
    "carnage_portion": 9600000,
    "treasury_portion": 400000,
    "epoch": 42,
    "slot": 123456789
  }
}
```
```
  </action>
  <verify>
Tax_Pool_Logic_Spec.md contains:
- "## Events" section
- TaxedSwap event struct with all fields documented
- SwapDirection enum definition
- Event usage examples
- Example event log JSON
  </verify>
  <done>Tax spec documents event emissions for off-chain tracking</done>
</task>

<task type="auto">
  <name>Task 2: Add Operational Runbooks to Carnage Spec (GAP-052)</name>
  <files>Docs/Carnage_Fund_Spec.md</files>
  <action>
Add Section 12.3 "Operational Monitoring" to Carnage_Fund_Spec.md documenting operational runbooks for monitoring Carnage health.

```markdown
### 12.3 Operational Monitoring

Carnage is permissionless and cannot be modified by admins. Monitoring is observational only.

#### Key Metrics to Track

| Metric | Description | Source | Alert Threshold |
|--------|-------------|--------|-----------------|
| `carnage_sol_balance` | SOL accumulated in Carnage vault | On-chain account | > 1000 SOL (cap reached) |
| `trigger_rate` | % of epochs that trigger Carnage | Event history | < 50% (unexpected) |
| `success_rate` | % of triggers with atomic success | Event history | < 80% (investigate) |
| `retry_success_rate` | % of pending that succeed on retry | Event history | < 90% (CU issues) |
| `crime_burned_total` | Cumulative CRIME burned | CarnageFundState | Informational |
| `fraud_burned_total` | Cumulative FRAUD burned | CarnageFundState | Informational |

#### Alert Thresholds

**Level 1 - Informational:**
- Carnage triggered but executed via retry (not atomic)
- SOL balance approaching cap (> 800 SOL)

**Level 2 - Warning:**
- 3+ consecutive retries in a row
- SOL balance at cap for 2+ epochs
- Trigger rate below 50% for 10 epochs

**Level 3 - Investigation Required:**
- 5+ consecutive Carnage execution failures
- SOL retained but never executed (potential bug)
- Unusual burn ratios (CRIME/FRAUD not proportional to activity)

#### Investigation Checklist

If persistent Carnage failures occur:

1. **Check Compute Budget:**
   - Is swap CU exceeding estimates?
   - Are hook costs higher than expected?
   - Action: None (protocol is immutable), but document for future reference

2. **Check Pool State:**
   - Are pool reserves in unexpected state?
   - Is k value correct?
   - Action: Verify AMM invariants

3. **Check Token Program:**
   - Are transfer hooks executing correctly?
   - Any unexpected Token-2022 behavior?
   - Action: Verify hook program state

4. **Check VRF:**
   - Is Switchboard responding?
   - Are callbacks being delivered?
   - Action: Check Switchboard dashboard

**Important:** No admin intervention is possible. If a fundamental issue exists:
- SOL is retained (never lost)
- Users may need to manually execute via permissionless retry
- Protocol continues operating (just without Carnage burns)

#### Monitoring Implementation

No on-chain monitoring is required. Off-chain monitoring should:
1. Subscribe to Carnage events via WebSocket
2. Index CarnageFundState account changes
3. Track epoch transitions and Carnage triggers
4. Alert on threshold violations

**Dashboard Recommendations:**
- Real-time: Current SOL balance, pending status
- Daily: Trigger rate, success rate, total burned
- Weekly: Burn trends, SOL throughput
```
  </action>
  <verify>
Carnage_Fund_Spec.md contains:
- Section 12.3 "Operational Monitoring"
- Key metrics table with alert thresholds
- Alert levels (Informational, Warning, Investigation)
- Investigation checklist with 4+ items
- Note that no admin intervention is possible
- Monitoring implementation guidance
  </verify>
  <done>Carnage spec has operational runbooks for monitoring health</done>
</task>

<task type="auto">
  <name>Task 3: Add Tax Band Boundary Conditions (GAP-062)</name>
  <files>Docs/Epoch_State_Machine_Spec.md</files>
  <action>
Add explicit tax band boundary documentation to Epoch_State_Machine_Spec.md Section 6 (State Machine) or create new subsection.

```markdown
### 6.X Tax Band Boundary Conditions

Tax rates are determined by VRF randomness and follow exact discrete values.

#### Achievable Tax Rates

| Band | Possible Values (bps) | VRF Byte Mapping |
|------|----------------------|------------------|
| Low | 100, 200, 300, 400 | `vrf_byte % 4` selects index |
| High | 1100, 1200, 1300, 1400 | `vrf_byte % 4` selects index |

**Exact Calculation:**
```rust
const LOW_RATES: [u16; 4] = [100, 200, 300, 400];      // 1%, 2%, 3%, 4%
const HIGH_RATES: [u16; 4] = [1100, 1200, 1300, 1400]; // 11%, 12%, 13%, 14%

fn select_rate(vrf_byte: u8, band: TaxBand) -> u16 {
    let index = (vrf_byte % 4) as usize;
    match band {
        TaxBand::Low => LOW_RATES[index],
        TaxBand::High => HIGH_RATES[index],
    }
}
```

#### Boundary Behavior

| Question | Answer |
|----------|--------|
| Is 1% exactly achievable? | Yes, when `vrf_byte % 4 == 0` and band is Low |
| Is 14% exactly achievable? | Yes, when `vrf_byte % 4 == 3` and band is High |
| Can values between (e.g., 1.5%) occur? | No, only the 8 discrete values |
| Is there any rounding? | No, rates are selected from array |

#### VRF Byte Distribution

The VRF callback provides a random byte (0-255). After `% 4`:
- 0: Selects first rate (100 or 1100 bps)
- 1: Selects second rate (200 or 1200 bps)
- 2: Selects third rate (300 or 1300 bps)
- 3: Selects fourth rate (400 or 1400 bps)

Each rate has exactly 25% probability (64 of 256 byte values map to each).

#### Testing Implications

For comprehensive testing:
- Test all 8 tax rates explicitly
- Test VRF byte values 0, 1, 2, 3, 252, 253, 254, 255 (boundaries)
- Verify no intermediate values possible
- Verify rate changes at epoch boundary (not mid-epoch)
```
  </action>
  <verify>
Epoch_State_Machine_Spec.md contains:
- Tax band boundary section
- Table with all 8 achievable values
- Boundary behavior Q&A table
- VRF byte distribution explanation
- Testing implications
  </verify>
  <done>Epoch spec documents exact tax band boundaries for implementers and testers</done>
</task>

<task type="auto">
  <name>Task 4: Update GAPS.md Status for All LOW Gaps</name>
  <files>.planning/audit/GAPS.md</files>
  <action>
Update GAPS.md to mark GAP-008, GAP-052, and GAP-062 as Filled.

For each gap:
- Status: **Filled**
- Resolution: Brief description
- Filled In: Specific document and section
- Iteration: 1

Update Dashboard:
- LOW: 3 -> 0 Open, 3 Filled
- Total: 24 -> 0 Open, 24 Filled

Move all three gap entries to "## Filled Gaps" section.

Update summary:
```markdown
**All 24 gaps resolved!**
- HIGH: 5 Filled
- MEDIUM: 16 Filled
- LOW: 3 Filled
- Total: 24/24 Complete
```
  </action>
  <verify>
- GAP-008 shows Status: **Filled** (Tax events)
- GAP-052 shows Status: **Filled** (Carnage runbooks)
- GAP-062 shows Status: **Filled** (Tax band boundaries)
- Dashboard shows LOW: 0 Open, 3 Filled
- Dashboard shows Total: 0 Open, 24 Filled
- All gaps now in Filled Gaps section
  </verify>
  <done>GAPS.md reflects all 24 gaps as resolved</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Tax_Pool_Logic_Spec.md has event emissions section
2. Carnage_Fund_Spec.md has operational monitoring section
3. Epoch_State_Machine_Spec.md has tax band boundary documentation
4. GAPS.md Dashboard shows 0 Open, 24 Filled
5. All gaps now in Filled Gaps section
6. Ready for final verification (Plan 11)
</verification>

<success_criteria>
- [ ] GAP-008 filled with TaxedSwap event struct
- [ ] GAP-052 filled with operational monitoring runbooks
- [ ] GAP-062 filled with tax band boundary conditions
- [ ] GAPS.md shows all 24 gaps filled
- [ ] Documentation complete - ready for final verification
</success_criteria>

<output>
After completion, create `.planning/phases/05-convergence/05-10-SUMMARY.md`
</output>
