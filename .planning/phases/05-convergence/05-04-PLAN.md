---
phase: 05-convergence
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - Docs/Tax_Pool_Logic_Spec.md
  - .planning/audit/GAPS.md
autonomous: true

must_haves:
  truths:
    - "Tax_Pool_Logic_Spec.md has CPI depth analysis section"
    - "Tax_Pool_Logic_Spec.md has error handling section with TaxError enum"
    - "Tax_Pool_Logic_Spec.md has compute budget estimates"
    - "Tax_Pool_Logic_Spec.md has CPI authority signing documentation"
    - "GAPS.md shows GAP-006, GAP-007, GAP-065, GAP-066 as Filled"
  artifacts:
    - path: "Docs/Tax_Pool_Logic_Spec.md"
      provides: "Complete Tax Program specification"
      contains: "## CPI Depth Analysis"
    - path: ".planning/audit/GAPS.md"
      provides: "Gap tracking with MEDIUM progress"
      contains: "GAP-006"
  key_links:
    - from: "Docs/Tax_Pool_Logic_Spec.md"
      to: "Docs/Carnage_Fund_Spec.md"
      via: "Consistent CPI depth documentation"
      pattern: "depth"
---

<objective>
Complete Tax_Pool_Logic_Spec.md by filling remaining MEDIUM gaps: GAP-006 (CPI depth analysis), GAP-007 (error handling), GAP-065 (compute budget estimates), and GAP-066 (authority signing documentation).

Purpose: Tax spec had the most gaps in Phase 4 audit (5 total). Plans 01 filled the HIGH gaps. This plan completes the spec to match Epoch_State_Machine_Spec.md quality.

Output: Comprehensive Tax_Pool_Logic_Spec.md with all gap categories covered.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/audit/GAPS.md
@.planning/phases/05-convergence/05-CONTEXT.md
@Docs/Tax_Pool_Logic_Spec.md
@Docs/Epoch_State_Machine_Spec.md (quality template)
@Docs/Carnage_Fund_Spec.md (CPI depth pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CPI Depth Analysis to Tax Spec (GAP-006)</name>
  <files>Docs/Tax_Pool_Logic_Spec.md</files>
  <action>
Add a "## CPI Depth Analysis" section to Tax_Pool_Logic_Spec.md documenting the CPI chains for swap operations.

Include:

```markdown
## CPI Depth Analysis

### swap_sol_buy CPI Chain

```
Tax Program::swap_sol_buy (entry point)
  └─> AMM Program::swap_sol_pool (depth 1)
      ├─> Token-2022::transfer_checked (depth 2) [CRIME/FRAUD output]
      │   └─> Transfer Hook Program::execute (depth 3)
      └─> SPL Token::transfer (depth 2) [WSOL input, no hook]
  └─> Staking Program::deposit_rewards (depth 1, parallel path)
      └─> System Program::transfer (depth 2)
```

**Max Depth:** 3 (CRIME/FRAUD path with hook)

### swap_profit_pool CPI Chain

```
Tax Program::swap_profit_buy (entry point)
  └─> AMM Program::swap (depth 1)
      ├─> Token-2022::transfer_checked (depth 2) [CRIME/FRAUD side]
      │   └─> Transfer Hook Program::execute (depth 3)
      └─> Token-2022::transfer_checked (depth 2) [PROFIT side]
          └─> Transfer Hook Program::execute (depth 3)
```

**Max Depth:** 3 (both sides have hooks)

Note: User-initiated swaps max at depth 3. Carnage execution (via Epoch callback) adds one more level, reaching depth 4.
```
  </action>
  <verify>
Tax_Pool_Logic_Spec.md contains:
- "## CPI Depth Analysis" section
- ASCII diagram for swap_sol_buy
- ASCII diagram for swap_profit_pool
- Max depth stated for each chain
- Note about Carnage adding depth
  </verify>
  <done>Tax spec documents CPI depth for all swap variants</done>
</task>

<task type="auto">
  <name>Task 2: Add Error Handling to Tax Spec (GAP-007)</name>
  <files>Docs/Tax_Pool_Logic_Spec.md</files>
  <action>
Add a "## Error Handling" section with complete TaxError enum to Tax_Pool_Logic_Spec.md.

Use Epoch_State_Machine_Spec.md Section 11 as the quality template.

```rust
#[error_code]
pub enum TaxError {
    #[msg("Invalid pool type for this operation")]
    InvalidPoolType,

    #[msg("Tax calculation overflow")]
    TaxOverflow,

    #[msg("Slippage tolerance exceeded")]
    SlippageExceeded,

    #[msg("Invalid epoch state - cannot determine tax rates")]
    InvalidEpochState,

    #[msg("Insufficient input amount for swap")]
    InsufficientInput,

    #[msg("Output amount below minimum")]
    OutputBelowMinimum,

    #[msg("Invalid swap authority PDA")]
    InvalidSwapAuthority,

    #[msg("Token program mismatch - expected SPL Token for WSOL")]
    WsolProgramMismatch,

    #[msg("Token program mismatch - expected Token-2022 for CRIME/FRAUD/PROFIT")]
    Token2022ProgramMismatch,

    #[msg("Invalid token account owner")]
    InvalidTokenOwner,

    #[msg("Carnage-only instruction called by non-Carnage authority")]
    UnauthorizedCarnageCall,
}
```

Include brief descriptions of when each error is raised.
  </action>
  <verify>
Tax_Pool_Logic_Spec.md contains:
- "## Error Handling" section
- Complete #[error_code] enum definition
- At least 10 error variants covering swap failures
- Brief descriptions of error conditions
  </verify>
  <done>Tax spec has comprehensive error handling documentation</done>
</task>

<task type="auto">
  <name>Task 3: Add Compute Budget Estimates (GAP-065) and Authority Signing (GAP-066)</name>
  <files>Docs/Tax_Pool_Logic_Spec.md</files>
  <action>
Add two sections to Tax_Pool_Logic_Spec.md:

**Section 1: Compute Budget Analysis (GAP-065)**

```markdown
## Compute Budget Analysis

| Operation | CPI Chain | Estimated CU | Notes |
|-----------|-----------|--------------|-------|
| swap_sol_buy (CRIME) | Tax->AMM->T22->Hook | ~120k | Hook adds ~30k |
| swap_sol_sell (CRIME) | Tax->AMM->SPL+T22->Hook | ~130k | Mixed token programs |
| swap_sol_buy (FRAUD) | Tax->AMM->T22->Hook | ~120k | Same as CRIME |
| swap_sol_sell (FRAUD) | Tax->AMM->SPL+T22->Hook | ~130k | Same as CRIME |
| swap_profit_buy | Tax->AMM->T22->Hook×2 | ~150k | Both sides have hooks |
| swap_profit_sell | Tax->AMM->T22->Hook×2 | ~150k | Both sides have hooks |

**Recommendations:**
- Frontend should request 200k CU for all standard swaps
- Request 300k CU for complex multi-hop trades
- Carnage execution has dedicated 260k CU budget (see Epoch spec)
```

**Section 2: CPI Authority Chain (GAP-066)**

```markdown
## CPI Authority Chain

### User Swap Flow

```
Tax Program::swap_sol_buy
  User signs ───────────────────────────────────────────────┐
  │                                                          │
  └─> AMM::swap                                              │
      Tax PDA signs as swap_authority ───────────────────┐  │
      │                                                   │  │
      └─> Token-2022::transfer_checked [user -> pool]    │  │
          User signs (via instruction signer) ───────────┘  │
      └─> Token-2022::transfer_checked [pool -> user]       │
          Pool PDA signs via AMM program                    │
```

### Carnage Execution Flow (Permissionless)

```
Epoch Program::execute_carnage_inner (no user signer)
  │
  └─> Tax Program::swap_exempt
      Carnage PDA signs ──────────────────────────────┐
      │                                                │
      └─> AMM::swap                                    │
          Tax PDA signs ──────────────────────────┐   │
          │                                        │   │
          └─> Token transfers                      │   │
              Carnage vault PDA or Pool PDA signs ─┘   │
                                                       │
```

**Key Signers:**
| PDA | Seeds | Signs For |
|-----|-------|-----------|
| swap_authority | ["swap_authority"] | AMM swap invocation |
| carnage_signer | ["carnage_signer"] | Tax::swap_exempt |
| pool_authority | ["pool", pool_id] | Token transfers from pool vaults |
```
  </action>
  <verify>
Tax_Pool_Logic_Spec.md contains:
- "## Compute Budget Analysis" section with CU estimates table
- "## CPI Authority Chain" section with signing diagrams
- Recommendations for frontend CU requests
- Key Signers table with PDA seeds
  </verify>
  <done>Tax spec has compute budget estimates and authority signing documentation</done>
</task>

<task type="auto">
  <name>Task 4: Update GAPS.md Status for Tax Spec MEDIUM Gaps</name>
  <files>.planning/audit/GAPS.md</files>
  <action>
Update GAPS.md to mark GAP-006, GAP-007, GAP-065, and GAP-066 as Filled.

For each gap entry:
- Status: **Filled**
- Resolution: Brief description of what was added
- Filled In: Tax_Pool_Logic_Spec.md Section X
- Iteration: 1

Update Dashboard counts:
- MEDIUM: 16 -> 12 Open, 4 Filled

Move all four gap entries to "## Filled Gaps" section.
  </action>
  <verify>
- GAP-006 shows Status: **Filled** (CPI depth)
- GAP-007 shows Status: **Filled** (error handling)
- GAP-065 shows Status: **Filled** (compute budget)
- GAP-066 shows Status: **Filled** (authority signing)
- Dashboard shows MEDIUM: 12 Open, 4 Filled
  </verify>
  <done>GAPS.md reflects Tax spec MEDIUM gaps as resolved</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Tax_Pool_Logic_Spec.md is now comprehensive (matching Epoch spec quality)
2. All Tax spec gaps filled (5 HIGH from Plans 01/02, 4 MEDIUM from this plan)
3. CPI depth analysis consistent with Carnage spec documentation
4. GAPS.md Dashboard shows progress on MEDIUM tier
5. Tax spec now has: Account architecture, Instructions, CPI depth, Errors, Compute budget, Authority chain
</verification>

<success_criteria>
- [ ] GAP-006 filled with CPI depth analysis
- [ ] GAP-007 filled with TaxError enum
- [ ] GAP-065 filled with compute budget estimates
- [ ] GAP-066 filled with authority signing documentation
- [ ] Tax spec now matches Epoch spec comprehensiveness
- [ ] GAPS.md tracking updated
</success_criteria>

<output>
After completion, create `.planning/phases/05-convergence/05-04-SUMMARY.md`
</output>
