---
phase: 98-mainnet-checklist
plan: 02
type: execute
wave: 2
depends_on: [98-01]
requirements: [CHECK-01, CHECK-02, CHECK-04]
files_modified:
  - Docs/mainnet-deploy-checklist.md
  - Docs/mainnet-checklist.md
autonomous: true

must_haves:
  truths:
    - "An operator can execute the entire mainnet deployment by following the checklist document step-by-step without any external knowledge"
    - "Every checklist item has an Action command, a Verify command, and an Expected output pattern"
    - "All 15 deployment pitfalls appear as WARNING boxes at the exact step where each could occur"
    - "A SOL budget table with line items and 20% contingency tells the operator exactly how much to fund the deployer wallet"
    - "GO/NO-GO checkbox gates between every stage prevent proceeding past failures"
  artifacts:
    - path: "Docs/mainnet-deploy-checklist.md"
      provides: "Comprehensive 8-stage mainnet deployment procedure with verification at every step"
      min_lines: 400
  key_links:
    - from: "Docs/mainnet-deploy-checklist.md"
      to: "scripts/deploy/stage-*"
      via: "bash command references"
      pattern: "stage-[0-7]"
    - from: "Docs/mainnet-deploy-checklist.md"
      to: "scripts/deploy/verify.ts"
      via: "verification command references"
      pattern: "verify\\.ts"
---

<objective>
Write the comprehensive mainnet deployment checklist document that covers all 8 stages (0-7), with every item having a verification command, all 15 pitfall WARNINGs inline, SOL budget with line items, and GO/NO-GO gates. Replace the stale v0.8-era checklist.

Purpose: This document IS the mainnet deployment procedure. An operator follows it step-by-step. It references the stage scripts from Plan 01 as the automation layer.

Output: Docs/mainnet-deploy-checklist.md (new), Docs/mainnet-checklist.md (deleted).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-mainnet-checklist/98-CONTEXT.md
@.planning/phases/98-mainnet-checklist/98-RESEARCH.md
@.planning/phases/98-mainnet-checklist/98-01-SUMMARY.md
@scripts/deploy/stage-0-preflight.sh
@scripts/deploy/stage-1-build.sh
@scripts/deploy/stage-2-deploy.sh
@scripts/deploy/stage-3-initialize.sh
@scripts/deploy/stage-4-infra.sh
@scripts/deploy/stage-5-launch.sh
@scripts/deploy/stage-6-graduation.sh
@scripts/deploy/stage-7-governance.sh
@scripts/deploy/deploy-all.sh
@Docs/mainnet-checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the comprehensive mainnet deployment checklist</name>
  <files>Docs/mainnet-deploy-checklist.md</files>
  <action>
    Create a comprehensive mainnet deployment checklist at Docs/mainnet-deploy-checklist.md organized into 8 stages (0-7). This document replaces the stale Docs/mainnet-checklist.md from v0.8.

    **Document structure:**

    1. **Header section:**
       - Title: "Dr. Fraudsworth's Finance Factory -- Mainnet Deployment Checklist"
       - Version, last updated date
       - Overview: What this document is and how to use it
       - Deployment timing strategy (from CONTEXT.md): Stages 0-4 days before launch, Stage 5 at launch moment, Stage 6 after graduation, Stage 7 after stability
       - Prerequisites summary (what must be ready before Stage 0)

    2. **Stage 0: Preflight** (can be done days before launch)
       - 0.1 Toolchain version gate (solana 3.x, anchor 0.32.x, rustc 1.79+, node 18+)
       - 0.2 Environment files (.env.mainnet exists, all vars populated)
       - 0.3 Wallet verification (mainnet deployer balance >= 32 SOL)
       - 0.4 Keypair safety (no keypairs in git staging, mainnet vanity mints exist)
       - 0.5 RPC connectivity (Helius mainnet endpoint responds)
       - Each item: Action command, Verify command, Expected output
       - Automated: `./scripts/deploy/stage-0-preflight.sh mainnet`
       - GO/NO-GO gate with checkbox list

    3. **Stage 1: Build & Verify Binaries** (can be done days before launch)
       - 1.1 Compile all 7 programs via build.sh
       - 1.2 Generate binary hash manifest
       - 1.3 Binary address cross-validation (no devnet addresses in .so files)
       - 1.4 Record binary sizes for SOL budget verification
       - WARNING (Pitfall #3): Must have mint keypairs before build
       - WARNING (Pitfall #4): Feature-flagged build split
       - WARNING (Pitfall #8): Devnet addresses in mainnet binaries
       - Automated: `./scripts/deploy/stage-1-build.sh mainnet`
       - GO/NO-GO gate

    4. **Stage 2: Deploy Programs** (can be done days before launch)
       - 2.1-2.7 Deploy each of the 7 programs with `solana program deploy`
       - Each deploy: verify Executable=true, Authority=deployer pubkey
       - WARNING (Pitfall #2): Solana CLI v3 --keypair required
       - WARNING (Pitfall #10): Stop crank before deploying
       - WARNING (Pitfall #11): Path with spaces workaround
       - Automated: `./scripts/deploy/stage-2-deploy.sh mainnet`
       - GO/NO-GO gate

    5. **Stage 3: Initialize Core** (can be done days before launch)
       - 3.1 Run initialize.ts (23 idempotent steps)
       - 3.2 Verify mints created with metadata + MetadataPointer
       - 3.3 Verify PDAs initialized (EpochState, StakePool, CarnageFund, etc.)
       - 3.4 Verify BcAdminConfig initialized
       - WARNING (Pitfall #1): Source .env BEFORE initialize.ts -- cost ~50 SOL on Phase 69
       - WARNING (Pitfall #6): DO NOT create pools here -- pools created during graduation
       - WARNING (Pitfall #7): Carnage WSOL account owner validation
       - WARNING (Pitfall #13): skipPreflight silent TX failures -- use confirmOrThrow
       - Automated: `./scripts/deploy/stage-3-initialize.sh mainnet`
       - GO/NO-GO gate

    6. **Stage 4: Infrastructure** (can be done days before launch)
       - 4.1 Create Address Lookup Table via create-alt.ts
       - 4.2 Generate shared/constants.ts via generate-constants.ts
       - 4.3 Sync IDLs to app/idl/
       - 4.4 Verify frontend builds with new constants
       - 4.5 Deploy frontend to Railway (manual step -- see Phase 98.1)
       - WARNING (Pitfall #5): IDL sync after deploy
       - Automated: `./scripts/deploy/stage-4-infra.sh mainnet`
       - GO/NO-GO gate

    7. **Stage 5: Launch** (THE PUBLIC LAUNCH MOMENT)
       - 5.1 Final confirmation: all stages 0-4 verified, team ready
       - 5.2 Initialize bonding curves (CRIME + FRAUD)
       - 5.3 Verify curves are active with correct parameters
       - 5.4 Set NEXT_PUBLIC_SITE_MODE=launch on Railway
       - 5.5 Verify launch page accessible and functional
       - Automated: `./scripts/deploy/stage-5-launch.sh mainnet`
       - GO/NO-GO gate
       - **Monitoring sub-checklist** (between Stage 5 and Stage 6):
         - Watch curve progress (both CRIME and FRAUD)
         - Monitor for buy/sell issues
         - Have graduate.ts ready to run
         - Have rollback plan ready (refund path documented)
         - Expected fill time: depends on community

    8. **Stage 6: Post-Graduation** (after both curves fill)
       - 6.1 Verify both curves at 100% capacity
       - 6.2 Run graduate.ts (13-step graduation)
       - 6.3 Verify AMM pools created with correct SOL + token amounts
       - 6.4 Verify pool vaults whitelisted
       - 6.5 Verify whitelist authority burned
       - 6.6 Start crank runner on Railway
       - 6.7 Set NEXT_PUBLIC_SITE_MODE=live on Railway
       - 6.8 Run full verify.ts
       - WARNING (Pitfall #9): Set CARNAGE_WSOL_PUBKEY env var on Railway
       - WARNING (Pitfall #14): RPC propagation delay after state changes
       - Automated: `./scripts/deploy/stage-6-graduation.sh mainnet`
       - GO/NO-GO gate

    9. **Stage 7: Squads & Monitoring** (after trading is stable)
       - 7.1 Run setup-squads.ts to create 2-of-3 multisig
       - 7.2 Run transfer-authority.ts for all 10 authorities
       - 7.3 Run verify-authority.ts
       - 7.4 Set initial timelock (2 hours)
       - 7.5 Document timelock progression schedule (2hr -> 24hr after 48-72hr stability)
       - WARNING (Pitfall #12): Squads TX creator must be member
       - WARNING (Pitfall #15): BorshCoder snake_case vs camelCase
       - Automated: `./scripts/deploy/stage-7-governance.sh mainnet`
       - GO/NO-GO gate

    **Format for every checklist item:**
    ```markdown
    ### N.M [Action Name]

    **Action:**
    ```bash
    [exact command]
    ```

    **Verify:**
    ```bash
    [verification command]
    ```

    **Expected:**
    ```
    [what the operator should see]
    ```

    - [ ] Verified -- output matches expected
    ```

    **Pitfall WARNING format:**
    ```markdown
    > **WARNING (Pitfall #N - [Short name]):** [Description of what goes wrong, why, and how this step avoids it. Include cost of previous failure if known.]
    ```

    Use the exact pitfall content from 98-RESEARCH.md (15 pitfalls). Place each at the exact step where it could occur, not in an appendix.
  </action>
  <verify>
    Verify the file exists and has comprehensive coverage: `wc -l Docs/mainnet-deploy-checklist.md` should be 400+ lines. Grep for all 15 pitfalls: `grep -c "WARNING (Pitfall" Docs/mainnet-deploy-checklist.md` should return >= 15. Grep for GO/NO-GO gates: `grep -c "GO/NO-GO" Docs/mainnet-deploy-checklist.md` should be >= 8.
  </verify>
  <done>
    Docs/mainnet-deploy-checklist.md exists with 400+ lines covering all 8 stages (0-7). Every checklist item has Action/Verify/Expected sections. All 15 pitfall WARNINGs appear inline at the correct steps. GO/NO-GO gates exist between every stage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add SOL budget section + delete old checklist</name>
  <files>
    Docs/mainnet-deploy-checklist.md
    Docs/mainnet-checklist.md
  </files>
  <action>
    1. **Add SOL Budget section to Docs/mainnet-deploy-checklist.md** (append after Stage 7):

    Create an "Appendix A: SOL Budget" section with:
    - Program deployment costs table (7 programs with binary sizes and rent estimates from RESEARCH.md)
    - Mint creation costs (3 mints with Token-2022 extensions)
    - PDA account costs (~45 accounts broken down by category)
    - Pool creation costs (2 pools, noting SOL comes from curves not deployer)
    - Infrastructure costs (ALT, TX fees, priority fees)
    - Operational funding (crank, carnage vault seed, Squads)
    - Budget summary table with subtotals and +20% contingency
    - Separate "Community-funded" section for bonding curve fills (1000 SOL)
    - NOTE: "Run `solana rent <bytes>` for exact costs after mainnet build. These estimates use devnet binary sizes."
    - Total deployer wallet requirement: ~32 SOL

    Use the exact figures from 98-RESEARCH.md "SOL Budget Estimation" section.

    2. **Add "Appendix B: Emergency Procedures"** section:
    - Rollback during fill period (refund path)
    - Hot-fix during launch window (deployer retains authority)
    - What to do if crank crashes
    - What to do if VRF oracle is down

    3. **Delete the old checklist:**
    - Delete Docs/mainnet-checklist.md (the stale v0.8-era document)
    - The new Docs/mainnet-deploy-checklist.md completely replaces it
  </action>
  <verify>
    Verify SOL budget section exists: `grep -c "SOL Budget" Docs/mainnet-deploy-checklist.md` returns >= 1. Verify budget total: `grep "32" Docs/mainnet-deploy-checklist.md` finds the total. Verify old checklist deleted: `test ! -f Docs/mainnet-checklist.md && echo "DELETED" || echo "STILL EXISTS"`.
  </verify>
  <done>
    Docs/mainnet-deploy-checklist.md has SOL budget appendix with line items totaling ~32 SOL + 20% contingency. Emergency procedures appendix exists. Old Docs/mainnet-checklist.md is deleted. The complete checklist document satisfies CHECK-01 (comprehensive coverage), CHECK-02 (every item verifiable), and CHECK-04 (SOL budget estimated).
  </done>
</task>

</tasks>

<verification>
- Docs/mainnet-deploy-checklist.md is a comprehensive 400+ line document covering Stages 0-7
- Every checklist item follows Action/Verify/Expected format
- All 15 pitfall WARNINGs placed inline at correct steps
- GO/NO-GO gates between every stage (8 total)
- SOL budget appendix with line items and 20% contingency (~32 SOL total)
- Emergency procedures appendix exists
- Old Docs/mainnet-checklist.md deleted
- Stage scripts from Plan 01 are referenced correctly
</verification>

<success_criteria>
- An operator with no prior knowledge can execute a mainnet deploy by following the document alone
- No "trust it worked" items -- every single action has verification
- The 15 hard-won pitfalls are prominently placed, not in a footnote appendix
- SOL budget answers "how much do I need in the deployer wallet?" clearly
- Old stale checklist is gone
</success_criteria>

<output>
After completion, create `.planning/phases/98-mainnet-checklist/98-02-SUMMARY.md`
</output>
