---
phase: 56-station-content
plan: 05
type: execute
wave: 3
depends_on: ["56-02", "56-03", "56-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 6 station modals open from the factory scene and display real content"
    - "Big Red Button shows 3D appearance and correct visual states"
    - "Station crossfade works when clicking different stations while one is open"
    - "Toast notifications appear above the modal on swap success/error"
    - "Chart renders correctly inside the swap modal without artifacts"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint for all 6 station modals and the Big Red Button.

Purpose: Phase 56 is the highest-complexity frontend phase -- every station must open correctly, display live data, and the Big Red Button must provide visceral feedback. Human eyes verify what automated tests cannot: visual layout, animation smoothness, and aesthetic coherence.
Output: Confirmation that all stations work or a list of issues to fix.
</objective>

<execution_context>
@./.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/56-station-content/56-CONTEXT.md
@.planning/phases/56-station-content/56-01-SUMMARY.md
@.planning/phases/56-station-content/56-02-SUMMARY.md
@.planning/phases/56-station-content/56-03-SUMMARY.md
@.planning/phases/56-station-content/56-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run pre-flight checks</name>
  <files></files>
  <action>
1. Start the Next.js dev server: `export PATH="/opt/homebrew/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && npx next dev`
2. Verify it starts without build errors.
3. Run TypeScript check: `npx tsc --noEmit` -- zero errors expected.
4. If there are build or type errors, fix them before proceeding to the visual checkpoint.
5. Report the dev server URL to the user (typically http://localhost:3000).
  </action>
  <verify>Dev server runs, TypeScript compiles cleanly, no console errors on page load.</verify>
  <done>Dev server running and accessible at localhost:3000 with no build errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All 6 factory station modals with real content:
1. **Swap Machine**: Stats bar (token prices + tax rates), TradingView chart, SwapForm with Big Red Button
2. **Carnage Cauldron**: SOL vault balance, lifetime burn stats, last 5 events
3. **Rewards Vat**: Staking stats + stake/unstake/claim tabs
4. **Connect Wallet**: Two-path connection (browser wallet + social login)
5. **Documentation Table**: Nextra iframe (may show loading if docs-site not running)
6. **Settings**: Slippage tolerance + priority fee presets + RPC display

Plus: Big Red Button 3D CSS styling and toast notification system.
  </what-built>
  <how-to-verify>
Visit http://localhost:3000 on a desktop browser (1280px+ viewport).

**Test each station (click each machine on the factory scene):**

1. **Swap Machine** (the large machine):
   - [ ] Stats bar shows token prices (may show loading skeletons -- that's OK without RPC)
   - [ ] TradingView chart renders without artifacts (may show "Loading chart data..." without RPC)
   - [ ] Swap form renders with token selectors, input fields, smart routing toggle
   - [ ] Big Red Button is visible, shows disabled/dimmed state (no wallet connected)
   - [ ] Big Red Button has 3D raised appearance with red gradient and shadow depth

2. **Carnage Cauldron** (the bubbling pot):
   - [ ] Shows vault balance section and lifetime stats
   - [ ] Shows recent events section (may be empty without historical data)
   - [ ] Dark content renders cleanly inside brass chrome frame

3. **Rewards Vat** (the vat/barrel):
   - [ ] Staking stats section shows above tabs
   - [ ] Three tabs visible: Stake, Unstake, Claim
   - [ ] Switching tabs shows different content

4. **Connect Wallet** (the sign):
   - [ ] Two paths visible: "Browser Wallet" and "Sign In"
   - [ ] No double-overlay or double-backdrop (content only, no ConnectModal overlay)
   - [ ] Clicking "Connect Wallet" triggers Privy wallet selector

5. **Documentation Table** (the desk/table):
   - [ ] Shows loading state or iframe content
   - [ ] If docs-site not running: shows loading then direct link fallback
   - [ ] Modal is wider than standard stations (800px max-width)

6. **Settings** (the gears):
   - [ ] Slippage preset buttons visible (0.5%, 1%, 2%, Custom)
   - [ ] Priority fee presets visible (None, Low, Med, High, Turbo)
   - [ ] RPC endpoint URL displayed

**Test interactions:**
- [ ] Station crossfade: Click one station, then click a different one on the scene. Content should swap with fade animation (no flash, no close/reopen)
- [ ] Modal close: Press Escape, click backdrop, or click X button -- all should close with fade-out
- [ ] Iris animation: Click origin for each modal matches the clicked station's position
- [ ] Keyboard navigation: Tab through stations, Enter to open

**Test Big Red Button states (if wallet connected):**
- [ ] Connect wallet first (click Connect Wallet station)
- [ ] Open Swap Machine, enter an amount
- [ ] Big Red Button should become enabled (bright red glow)
- [ ] If you can execute a swap: button should show loading (spinning), then success (green flash) or error (shake)
- [ ] Toast should appear at bottom-right on success/error

**Report any issues -- describe what you see vs. what you expected.**
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 stations work correctly, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
All 6 station modals display real content, Big Red Button has correct 3D appearance, toast system works, station crossfade is smooth, no visual artifacts.
</verification>

<success_criteria>
- Human confirms all 6 stations render correct content in their modals
- Big Red Button has 3D physical appearance fitting steampunk aesthetic
- Station crossfade and modal animations work smoothly
- No layout breakage, no console errors, no visual artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/56-station-content/56-05-SUMMARY.md`
</output>
