---
phase: 56-station-content
plan: 03
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - app/components/station/CarnageStation.tsx
  - app/components/station/StakingStation.tsx
autonomous: true

must_haves:
  truths:
    - "Carnage Cauldron modal shows SOL vault balance, lifetime stats, and last 5 events with live data"
    - "Rewards Vat modal provides stake/unstake/claim tabs and stats with live on-chain data"
    - "Both modals auto-refresh data while open (hooks refetch on mount)"
    - "Existing CarnageCard and StakingForm component logic is unchanged"
  artifacts:
    - path: "app/components/station/CarnageStation.tsx"
      provides: "Carnage station modal panel wrapping CarnageCard"
      exports: ["default"]
    - path: "app/components/station/StakingStation.tsx"
      provides: "Staking station modal panel wrapping StakingForm"
      exports: ["default"]
  key_links:
    - from: "app/components/station/CarnageStation.tsx"
      to: "app/components/dashboard/CarnageCard.tsx"
      via: "renders CarnageCard with hook data passed as props"
      pattern: "<CarnageCard"
    - from: "app/components/station/StakingStation.tsx"
      to: "app/components/staking/StakingForm.tsx"
      via: "renders StakingForm directly (it calls its own hooks)"
      pattern: "<StakingForm"
---

<objective>
Build the Carnage Cauldron and Rewards Vat station panels by re-parenting existing CarnageCard and StakingForm components into themed modal wrappers.

Purpose: STATION-02 (Carnage) and STATION-03 (Staking) provide live on-chain data dashboards for the two yield-generating features. These are thin wrappers -- the existing components already have all UI and data logic.
Output: CarnageStation.tsx and StakingStation.tsx, both default exports for React.lazy.
</objective>

<execution_context>
@./.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-station-content/56-RESEARCH.md
@.planning/phases/56-station-content/56-CONTEXT.md
@.planning/phases/56-station-content/56-01-SUMMARY.md
@app/components/dashboard/CarnageCard.tsx
@app/components/dashboard/DashboardGrid.tsx
@app/components/staking/StakingForm.tsx
@app/hooks/useCarnageData.ts
@app/hooks/useCarnageEvents.ts
@app/hooks/useEpochState.ts
@app/hooks/useStaking.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CarnageStation panel</name>
  <files>
    app/components/station/CarnageStation.tsx
  </files>
  <action>
Create `app/components/station/CarnageStation.tsx`:

1. `'use client'` directive, JSDoc header explaining this is a thin wrapper that re-parents CarnageCard into the modal context.
2. DEFAULT EXPORT (required by React.lazy).
3. This component follows the DashboardGrid orchestrator pattern: call hooks here, pass data as props to CarnageCard.
4. Import and call:
   - `useCarnageData()` -> carnageData, loading, error
   - `useCarnageEvents()` -> events, loading (eventsLoading)
   - `useEpochState()` -> epochState (for currentEpoch to compute "X epochs ago")
5. Render CarnageCard with all its props mapped from hook data (same prop mapping as DashboardGrid lines 201-213):
   ```tsx
   <CarnageCard
     totalCrimeBurned={carnageData?.totalCrimeBurned ?? null}
     totalFraudBurned={carnageData?.totalFraudBurned ?? null}
     totalSolSpent={carnageData?.totalSolSpent ?? null}
     totalTriggers={carnageData?.totalTriggers ?? null}
     lastTriggerEpoch={carnageData?.lastTriggerEpoch ?? null}
     vaultBalanceLamports={carnageData?.vaultBalanceLamports ?? null}
     currentEpoch={epochState?.currentEpoch ?? null}
     loading={carnageLoading}
     error={carnageError}
     events={carnageEvents}
     eventsLoading={eventsLoading}
   />
   ```
6. CarnageCard already uses zinc-900 backgrounds -- these will display correctly against the dark `.station-content` wrapper from Plan 01. However, CarnageCard's outer div has `bg-zinc-900 border border-zinc-800 rounded-lg p-6`. Inside the station-content wrapper (which is already dark bg-factory-bg), the zinc-900 card will look slightly lighter, providing subtle elevation. This is fine.
7. No additional layout wrapping needed -- CarnageCard is self-contained with vault balance, lifetime stats, and recent events sections.
8. The auto-refresh behavior comes from the hooks themselves: `useCarnageData` and `useCarnageEvents` fetch on mount and subscribe to updates. Since the station component only mounts when the modal opens (React.lazy + conditional render), hooks fire on modal open and clean up on modal close. Fresh data on every open per RESEARCH.md Pitfall 3.
  </action>
  <verify>
Run `export PATH="/opt/homebrew/bin:$PATH" && npx tsc --noEmit` in the app directory. CarnageStation.tsx should compile cleanly with zero errors. Verify it has a default export.
  </verify>
  <done>
CarnageStation.tsx renders CarnageCard with live on-chain data from useCarnageData, useCarnageEvents, and useEpochState hooks. Component mounts/unmounts with modal open/close, ensuring fresh data on each open. Default export for React.lazy.
  </done>
</task>

<task type="auto">
  <name>Task 2: StakingStation panel</name>
  <files>
    app/components/station/StakingStation.tsx
  </files>
  <action>
Create `app/components/station/StakingStation.tsx`:

1. `'use client'` directive, JSDoc header explaining this wraps StakingForm (which is already a self-contained hook consumer like SwapForm).
2. DEFAULT EXPORT (required by React.lazy).
3. StakingForm already calls `useStaking()` internally and manages its own tabs, form state, and execution. Unlike CarnageCard (which is props-only), StakingForm is a hook consumer -- so the station wrapper is even thinner.
4. The CONTEXT.md specifies a Stats tab in addition to Stake/Unstake/Claim. StakingForm currently has 3 tabs: Stake, Unstake, Claim. The Stats tab should show:
   - Pending rewards
   - User's staked balance
   - Total protocol staked
   - Last 5 epoch rewards
   - Total protocol rewards distributed

   However, adding a Stats tab requires modifying StakingForm or the useStaking hook. To avoid modifying existing component logic (re-parent without rewrite principle), create a separate StakingStats section ABOVE the form that always shows these stats, using the data StakingForm's StakingStats component already displays plus additional protocol-level metrics.

   ACTUALLY: StakingForm already renders `<StakingStats>` above the tab card (lines 77-85). The StakingStats component already shows: rewardRate, pendingRewards, userStakedBalance, userTotalClaimed, protocolTotalDistributed, protocolTotalClaimed. This covers most of the CONTEXT.md Stats tab requirements. The "last 5 epoch rewards" and "total protocol staked" are the only missing items, and those would require new data fetching (epoch history queries). These can be added later as an enhancement.

   For now, StakingStation simply renders StakingForm, which includes StakingStats. This satisfies STATION-03 requirements for stake/unstake/claim tabs with pending rewards, staked balance, and total protocol staked.

5. Render:
   ```tsx
   export default function StakingStation() {
     return <StakingForm />;
   }
   ```

6. StakingForm uses `max-w-md mx-auto` (line 75). Inside the modal, this centers the form within the 700px modal width. This is actually appropriate for a form-centric modal -- the form doesn't need to span the full 700px. Keep it as-is.

7. Like CarnageStation, the auto-refresh behavior comes from useStaking hook mounting/unmounting with the modal open/close cycle.
  </action>
  <verify>
Run `export PATH="/opt/homebrew/bin:$PATH" && npx tsc --noEmit` in the app directory. StakingStation.tsx should compile cleanly. Verify it has a default export. Verify StakingForm is imported and rendered.
  </verify>
  <done>
StakingStation.tsx renders StakingForm directly (self-contained hook consumer with Stake/Unstake/Claim tabs plus StakingStats section). Default export for React.lazy. Hooks mount/unmount with modal lifecycle for fresh data on each open.
  </done>
</task>

</tasks>

<verification>
1. CarnageStation.tsx exists with default export, calls 3 hooks, passes data to CarnageCard
2. StakingStation.tsx exists with default export, renders StakingForm
3. Both compile without TypeScript errors
4. Opening Carnage Cauldron station shows vault balance, lifetime stats, recent events
5. Opening Rewards Vat station shows staking stats + stake/unstake/claim tabs
6. Closing and reopening either modal fetches fresh data (hooks remount)
</verification>

<success_criteria>
- Carnage Cauldron modal shows live SOL vault balance, CRIME/FRAUD burn stats, trigger count, and last 5 events
- Rewards Vat modal provides functional stake/unstake/claim interface with pending rewards and staked balances
- Both modals display correctly inside the dark station-content wrapper within brass chrome
- No existing component logic modified (CarnageCard and StakingForm unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/56-station-content/56-03-SUMMARY.md`
</output>
