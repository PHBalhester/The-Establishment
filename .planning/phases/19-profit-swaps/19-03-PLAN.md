---
phase: 19-profit-swaps
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - programs/tax-program/tests/test_swap_profit_buy.rs
  - programs/tax-program/tests/test_swap_profit_sell.rs
autonomous: true

must_haves:
  truths:
    - "swap_profit_buy routes through AMM CPI successfully"
    - "swap_profit_sell routes through AMM CPI successfully"
    - "No tax is deducted on PROFIT pool swaps"
    - "Slippage protection works for PROFIT swaps"
    - "Dual Token-2022 transfers work (both sides have hooks)"
  artifacts:
    - path: "programs/tax-program/tests/test_swap_profit_buy.rs"
      provides: "Integration tests for swap_profit_buy"
      contains: ["test_profit_buy_no_tax", "test_profit_buy_slippage"]
    - path: "programs/tax-program/tests/test_swap_profit_sell.rs"
      provides: "Integration tests for swap_profit_sell"
      contains: ["test_profit_sell_no_tax", "test_profit_sell_slippage"]
  key_links:
    - from: "test_swap_profit_buy.rs"
      to: "swap_profit_buy instruction"
      via: "LiteSVM transaction execution"
      pattern: "swap_profit_buy_data"
    - from: "test_swap_profit_sell.rs"
      to: "swap_profit_sell instruction"
      via: "LiteSVM transaction execution"
      pattern: "swap_profit_sell_data"
---

<objective>
Create integration tests for PROFIT pool swap instructions using LiteSVM.

Purpose: Verify the complete CPI chain works: Tax Program -> AMM -> Token-2022 transfers. Prove no tax is collected on PROFIT swaps (user receives full output minus only LP fee).

Output: Two test files covering swap_profit_buy and swap_profit_sell.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-profit-swaps/19-02-SUMMARY.md
@programs/tax-program/tests/test_swap_sol_buy.rs (Phase 18 test pattern)
@programs/amm/tests/test_swap_profit.rs (AMM PROFIT swap tests)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create swap_profit_buy integration tests</name>
  <files>programs/tax-program/tests/test_swap_profit_buy.rs</files>
  <action>
Create integration tests for swap_profit_buy using LiteSVM.

**Key differences from test_swap_sol_buy.rs:**
- PROFIT pools have BOTH sides as Token-2022 (dual T22)
- NO tax distribution accounts needed
- LP fee is 50 bps (0.5%), not 100 bps
- Verify user gets full output minus LP fee only (no tax deduction)

**Test context setup (ProfitBuyTestContext):**
1. Deploy AMM and Tax Program
2. Initialize AMM AdminConfig
3. Create TWO Token-2022 mints (CRIME-like and PROFIT-like)
4. Initialize PROFIT pool with seed amounts
5. Create user with funded token accounts
6. Derive swap_authority from Tax Program

**Build instruction helper:**
```rust
fn swap_profit_buy_data(amount_in: u64, minimum_output: u64, is_crime: bool) -> Vec<u8>
```

**Account list (simpler than SOL swaps):**
user, swap_authority, pool, vault_a, vault_b, mint_a, mint_b, user_token_a, user_token_b, amm_program, token_2022_program

**Tests to include:**
1. `test_profit_buy_no_tax` - Verify user receives full output minus LP fee only
2. `test_profit_buy_slippage` - Slippage protection works
3. `test_profit_buy_zero_fails` - Zero amount rejected
4. `test_profit_buy_consecutive` - Multiple swaps work

**LP fee verification:**
- PROFIT pool LP fee = 50 bps (0.5%)
- effective_input = amount_in * 9950 / 10000
- output = (reserve_out * effective_input) / (reserve_in + effective_input)
- User should receive exactly this output (no additional deductions)
  </action>
  <verify>
Run: `source "$HOME/.cargo/env" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && cargo test -p tax-program test_profit_buy -- --nocapture`
All tests should pass.
  </verify>
  <done>
test_swap_profit_buy.rs exists with:
- ProfitBuyTestContext setting up dual T22 pool
- 4+ tests covering no-tax output, slippage, zero amount, consecutive swaps
- All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create swap_profit_sell integration tests</name>
  <files>programs/tax-program/tests/test_swap_profit_sell.rs</files>
  <action>
Create integration tests for swap_profit_sell using LiteSVM.

**Similar structure to test_swap_profit_buy.rs, but:**
- Direction = 1 (BtoA) - selling PROFIT for CRIME/FRAUD
- User sends PROFIT (token B), receives CRIME/FRAUD (token A)

**Tests to include:**
1. `test_profit_sell_no_tax` - Verify user receives full output minus LP fee only
2. `test_profit_sell_slippage` - Slippage protection works
3. `test_profit_sell_zero_fails` - Zero amount rejected
4. `test_profit_sell_consecutive` - Multiple swaps work

**Key verification:**
- User's PROFIT balance decreases by amount_in
- User's CRIME/FRAUD balance increases by expected output
- No SOL leaves user's account (no tax distribution)
  </action>
  <verify>
Run: `source "$HOME/.cargo/env" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && cargo test -p tax-program test_profit_sell -- --nocapture`
All tests should pass.
  </verify>
  <done>
test_swap_profit_sell.rs exists with:
- Shared context or similar pattern to buy tests
- 4+ tests covering no-tax output, slippage, zero amount, consecutive swaps
- All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite</name>
  <files>None (verification only)</files>
  <action>
Run the complete Tax Program test suite to verify:
1. All new PROFIT swap tests pass
2. All existing SOL swap tests still pass
3. All unit tests still pass

Count total tests and verify no regressions.
  </action>
  <verify>
Run: `source "$HOME/.cargo/env" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && cargo test -p tax-program`
Should report all tests passing.

Expected: ~45+ tests (38 from Phase 18 + 8+ new PROFIT swap tests)
  </verify>
  <done>
Full test suite passes with increased test count.
No regressions in existing tests.
  </done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p tax-program test_profit_buy` passes (4+ tests)
- [ ] `cargo test -p tax-program test_profit_sell` passes (4+ tests)
- [ ] `cargo test -p tax-program` passes (full suite, ~45+ tests)
- [ ] No tax distribution in PROFIT swap tests (user SOL balance unchanged)
- [ ] Output matches LP fee calculation (not tax calculation)
</verification>

<success_criteria>
- PROFIT swap integration tests prove CPI chain works
- Tests verify 0% protocol tax (only 0.5% LP fee)
- All existing tests continue to pass
- Phase 19 complete and verified
</success_criteria>

<output>
After completion, create `.planning/phases/19-profit-swaps/19-03-SUMMARY.md`
</output>
