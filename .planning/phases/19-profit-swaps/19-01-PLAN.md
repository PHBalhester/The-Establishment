---
phase: 19-profit-swaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - programs/tax-program/src/events.rs
autonomous: true

must_haves:
  truths:
    - "UntaxedSwap event can be emitted by Tax Program"
    - "PoolType enum includes CrimeProfit and FraudProfit variants"
  artifacts:
    - path: "programs/tax-program/src/events.rs"
      provides: "UntaxedSwap event and PoolType variants for PROFIT swaps"
      contains: ["UntaxedSwap", "CrimeProfit", "FraudProfit"]
  key_links:
    - from: "events.rs"
      to: "Tax_Pool_Logic_Spec.md Section 20.3"
      via: "UntaxedSwap field definitions"
      pattern: "pub struct UntaxedSwap"
---

<objective>
Add UntaxedSwap event and PROFIT pool PoolType variants to Tax Program events.

Purpose: Enable Phase 19 swap instructions to emit proper events for untaxed PROFIT pool swaps. The UntaxedSwap event (per spec Section 20.3) differs from TaxedSwap by omitting tax-related fields.

Output: Updated events.rs with UntaxedSwap struct and CrimeProfit/FraudProfit enum variants.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-profit-swaps/19-CONTEXT.md
@.planning/phases/19-profit-swaps/19-RESEARCH.md
@Docs/Tax_Pool_Logic_Spec.md (Section 20.3)
@programs/tax-program/src/events.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PoolType variants and UntaxedSwap event</name>
  <files>programs/tax-program/src/events.rs</files>
  <action>
Update events.rs to add PROFIT pool support:

1. Add two new variants to PoolType enum (after SolFraud):
   - `CrimeProfit` - CRIME/PROFIT pool
   - `FraudProfit` - FRAUD/PROFIT pool

2. Add UntaxedSwap event struct per Tax_Pool_Logic_Spec.md Section 20.3:
   ```rust
   #[event]
   pub struct UntaxedSwap {
       /// The user performing the swap
       pub user: Pubkey,
       /// Pool type: CrimeProfit or FraudProfit
       pub pool_type: PoolType,
       /// Direction: Buy or Sell (relative to PROFIT)
       pub direction: SwapDirection,
       /// Input amount
       pub input_amount: u64,
       /// Output amount after LP fee
       pub output_amount: u64,
       /// LP fee amount deducted
       pub lp_fee: u64,
       /// Slot of the transaction
       pub slot: u64,
   }
   ```

3. Add doc comments matching spec explanations.

Note: output_amount and lp_fee will be set to 0 initially since we cannot easily get them from CPI return data (same pattern as TaxedSwap.output_amount in swap_sol_buy). The AMM's SwapEvent captures full details.
  </action>
  <verify>
Run: `source "$HOME/.cargo/env" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && cargo check -p tax-program`
Should compile without errors.
  </verify>
  <done>
events.rs has:
- PoolType enum with SolCrime, SolFraud, CrimeProfit, FraudProfit variants
- UntaxedSwap event struct with all fields from spec Section 20.3
- cargo check passes
  </done>
</task>

</tasks>

<verification>
- [ ] `cargo check -p tax-program` passes
- [ ] PoolType has 4 variants: SolCrime, SolFraud, CrimeProfit, FraudProfit
- [ ] UntaxedSwap event has fields: user, pool_type, direction, input_amount, output_amount, lp_fee, slot
</verification>

<success_criteria>
- events.rs compiles with new event and variants
- Ready for Plan 02 to use UntaxedSwap in swap instruction handlers
</success_criteria>

<output>
After completion, create `.planning/phases/19-profit-swaps/19-01-SUMMARY.md`
</output>
