---
phase: 03.1-name-changes-yield-edits
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - Docs/Token_Program_Reference.md
  - Docs/Epoch_State_Machine_Spec.md
  - Docs/Bonding_Curve_Spec.md
  - Docs/Carnage_Fund_Spec.md
  - Docs/Tax_Pool_Logic_Spec.md
  - Docs/AMM_Implementation.md
  - Docs/New_Yield_System_Spec.md
  - Docs/Protocol_Initialzation_and_Launch_Flow.md
autonomous: true

must_haves:
  truths:
    - "Grep for ipa_ in Docs/ returns zero hits"
    - "Grep for ipb_ in Docs/ returns zero hits"
    - "Grep for op4_ in Docs/ returns zero hits"
    - "All account names use crime_, fraud_, or profit_ prefixes"
    - "Pool descriptors use Crime Pool, Fraud Pool, Profit Pool"
  artifacts:
    - path: "Docs/Epoch_State_Machine_Spec.md"
      provides: "EpochState struct with updated field names"
      contains: "crime_buy_tax_bps"
    - path: "Docs/Transfer_Hook_Spec.md"
      provides: "Whitelist table with updated vault names"
      contains: "CRIME/SOL Pool Vault"
  key_links:
    - from: "Docs/Epoch_State_Machine_Spec.md"
      to: "Account field naming convention"
      via: "ipa_ -> crime_, ipb_ -> fraud_ replacement"
      pattern: "crime_.*_tax_bps|fraud_.*_tax_bps"
---

<objective>
Update cascading name changes: account names (ipa_vault -> crime_vault), pool names (Pool A -> Crime Pool), and code example variable names.

Purpose: Ensure complete consistency after token name replacement - all derived identifiers must also update.

Output: All account names, vault names, pool descriptors, and code examples use the new thematic naming.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-name-changes-yield-edits/03.1-RESEARCH.md
@.planning/phases/03.1-name-changes-yield-edits/03.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ipa_ prefixed names with crime_ prefixes</name>
  <files>
    Docs/Token_Program_Reference.md
    Docs/Epoch_State_Machine_Spec.md
    Docs/Bonding_Curve_Spec.md
    Docs/Carnage_Fund_Spec.md
    Docs/Tax_Pool_Logic_Spec.md
    Docs/AMM_Implementation.md
    Docs/Protocol_Initialzation_and_Launch_Flow.md
  </files>
  <action>
    For each file containing `ipa_` patterns:

    1. Use Edit tool with replace_all=true to replace:
       - `ipa_vault` -> `crime_vault`
       - `ipa_buy_tax_bps` -> `crime_buy_tax_bps`
       - `ipa_sell_tax_bps` -> `crime_sell_tax_bps`
       - `ipa_mint` -> `crime_mint`
       - `ipa_pool` -> `crime_pool`
       - Any other `ipa_*` patterns found

    2. Use targeted search first to identify all patterns:
       ```bash
       grep -rn "ipa_" Docs/
       ```

    Expected: ~58 replacements across 9 files (see research)

    WHY: Account and variable names should match the token they reference. ipa_ was derived from IPA; crime_ derives from CRIME.
  </action>
  <verify>
    Run: grep -rn "ipa_" Docs/
    Expected: No matches (0 hits)

    Run: grep -rn "crime_" Docs/ | wc -l
    Expected: Matches in expected files
  </verify>
  <done>
    ipa_ prefix appears nowhere in Docs/; crime_ prefix used consistently.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace ipb_ prefixed names with fraud_ prefixes</name>
  <files>
    Docs/Token_Program_Reference.md
    Docs/Epoch_State_Machine_Spec.md
    Docs/Bonding_Curve_Spec.md
    Docs/Carnage_Fund_Spec.md
    Docs/Tax_Pool_Logic_Spec.md
    Docs/AMM_Implementation.md
    Docs/Protocol_Initialzation_and_Launch_Flow.md
  </files>
  <action>
    For each file containing `ipb_` patterns:

    1. Use Edit tool with replace_all=true to replace:
       - `ipb_vault` -> `fraud_vault`
       - `ipb_buy_tax_bps` -> `fraud_buy_tax_bps`
       - `ipb_sell_tax_bps` -> `fraud_sell_tax_bps`
       - `ipb_mint` -> `fraud_mint`
       - `ipb_pool` -> `fraud_pool`
       - Any other `ipb_*` patterns found

    2. Use targeted search first to identify all patterns:
       ```bash
       grep -rn "ipb_" Docs/
       ```

    Expected: ~41 replacements across 8 files (see research)

    WHY: Account and variable names should match the token they reference. ipb_ was derived from IPB; fraud_ derives from FRAUD.
  </action>
  <verify>
    Run: grep -rn "ipb_" Docs/
    Expected: No matches (0 hits)

    Run: grep -rn "fraud_" Docs/ | wc -l
    Expected: Matches in expected files
  </verify>
  <done>
    ipb_ prefix appears nowhere in Docs/; fraud_ prefix used consistently.
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace op4_ prefixed names with profit_ prefixes</name>
  <files>
    Docs/Token_Program_Reference.md
    Docs/Bonding_Curve_Spec.md
    Docs/New_Yield_System_Spec.md
    Docs/Protocol_Initialzation_and_Launch_Flow.md
  </files>
  <action>
    For each file containing `op4_` patterns:

    1. Use Edit tool with replace_all=true to replace:
       - `op4_vault` -> `profit_vault`
       - `op4_mint` -> `profit_mint`
       - `op4_pool` -> `profit_pool`
       - `user_op4_account` -> `user_profit_account`
       - Any other `op4_*` patterns found

    2. Use targeted search first to identify all patterns:
       ```bash
       grep -rn "op4_" Docs/
       ```

    Expected: ~9 replacements across 3 files (see research)

    IMPORTANT: Include New_Yield_System_Spec.md which has op4_mint and op4_vault references.

    WHY: Account and variable names should match the token they reference. op4_ was derived from OP4; profit_ derives from PROFIT.
  </action>
  <verify>
    Run: grep -rn "op4_" Docs/
    Expected: No matches (0 hits)

    Run: grep -rn "profit_" Docs/ | wc -l
    Expected: Matches in expected files (primarily New_Yield_System_Spec.md)
  </verify>
  <done>
    op4_ prefix appears nowhere in Docs/; profit_ prefix used consistently.
  </done>
</task>

</tasks>

<verification>
Final verification that ALL cascading names are replaced:

```bash
# All three must return empty:
grep -rn "ipa_" Docs/
grep -rn "ipb_" Docs/
grep -rn "op4_" Docs/

# These should show the new naming:
grep -rn "crime_vault\|crime_mint\|crime_buy_tax" Docs/ | head -3
grep -rn "fraud_vault\|fraud_mint\|fraud_buy_tax" Docs/ | head -3
grep -rn "profit_vault\|profit_mint" Docs/ | head -3
```

Additionally verify Pool names (Pool A, Pool B, Pool 4) if any exist - update to Crime Pool, Fraud Pool, Profit Pool.
</verification>

<success_criteria>
1. Zero occurrences of ipa_, ipb_, or op4_ prefixes in Docs/
2. crime_, fraud_, profit_ prefixes used consistently in account/variable names
3. Code examples in specs have updated field names (e.g., crime_buy_tax_bps)
4. Rust struct fields use new naming convention
5. No broken code syntax in markdown code blocks
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-name-changes-yield-edits/03.1-02-SUMMARY.md`
</output>
