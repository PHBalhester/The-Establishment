---
phase: 107-jupiter-amm-adapter-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - sdk/jupiter-adapter/Cargo.toml
  - sdk/jupiter-adapter/src/lib.rs
  - sdk/jupiter-adapter/src/constants.rs
  - sdk/jupiter-adapter/src/math/mod.rs
  - sdk/jupiter-adapter/src/math/amm_math.rs
  - sdk/jupiter-adapter/src/math/tax_math.rs
  - sdk/jupiter-adapter/src/math/vault_math.rs
  - sdk/jupiter-adapter/src/state/mod.rs
  - sdk/jupiter-adapter/src/state/pool_state.rs
  - sdk/jupiter-adapter/src/state/epoch_state.rs
  - sdk/jupiter-adapter/src/accounts/mod.rs
  - sdk/jupiter-adapter/src/accounts/addresses.rs
autonomous: true
requirements:
  - JUP-01
  - JUP-04
  - JUP-06

must_haves:
  truths:
    - "sdk/jupiter-adapter crate compiles with cargo build (no BPF, native target only)"
    - "Pure math functions produce identical output to on-chain originals for known test vectors"
    - "EpochState and PoolState parsers extract correct fields from raw bytes at known offsets"
    - "All mainnet addresses are hardcoded as Pubkey constants with zero network calls"
    - "JUP-06 in REQUIREMENTS.md describes vault-based PROFIT acquisition (not PROFIT AMM pools)"
  artifacts:
    - path: "sdk/jupiter-adapter/Cargo.toml"
      provides: "Crate manifest with jupiter-amm-interface 0.5.0, solana-sdk 2.2, sha2, anyhow, rust_decimal"
      contains: "jupiter-amm-interface"
    - path: "sdk/jupiter-adapter/src/math/amm_math.rs"
      provides: "Exact copy of calculate_effective_input, calculate_swap_output from programs/amm/src/helpers/math.rs"
      contains: "calculate_effective_input"
    - path: "sdk/jupiter-adapter/src/math/tax_math.rs"
      provides: "Exact copy of calculate_tax from programs/tax-program/src/helpers/tax_math.rs"
      contains: "calculate_tax"
    - path: "sdk/jupiter-adapter/src/state/epoch_state.rs"
      provides: "Raw byte parser for EpochState (crime_buy/sell_tax_bps, fraud_buy/sell_tax_bps)"
      contains: "get_tax_bps"
    - path: "sdk/jupiter-adapter/src/state/pool_state.rs"
      provides: "Raw byte parser for PoolState (mint_a, reserve_a, reserve_b, lp_fee_bps)"
      contains: "reserve_a"
    - path: "sdk/jupiter-adapter/src/accounts/addresses.rs"
      provides: "All mainnet Pubkey constants (programs, mints, PDAs, pools, hook accounts)"
      contains: "TAX_PROGRAM_ID"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated JUP-06 text reflecting vault-based PROFIT acquisition"
      contains: "vault-based PROFIT acquisition"
  key_links:
    - from: "sdk/jupiter-adapter/src/math/amm_math.rs"
      to: "programs/amm/src/helpers/math.rs"
      via: "byte-for-byte copy of pure functions"
      pattern: "calculate_effective_input"
    - from: "sdk/jupiter-adapter/src/math/tax_math.rs"
      to: "programs/tax-program/src/helpers/tax_math.rs"
      via: "byte-for-byte copy of calculate_tax + split_distribution"
      pattern: "calculate_tax"
    - from: "Cargo.toml"
      to: "sdk/jupiter-adapter/Cargo.toml"
      via: "workspace members list"
      pattern: "sdk/jupiter-adapter"
---

<objective>
Create the sdk/jupiter-adapter crate skeleton with workspace integration, pure math functions copied from on-chain programs, raw byte state parsers, and all hardcoded mainnet addresses. Also update JUP-06 requirement text to reflect vault-based PROFIT acquisition (PROFIT AMM pools do not exist).

Purpose: This is the foundation layer that all Amm trait implementations build on. Getting the math and parsing exactly right here ensures quote accuracy later.
Output: A compiling Rust crate at sdk/jupiter-adapter/ with math, state parsing, and address modules. Updated REQUIREMENTS.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/107-jupiter-amm-adapter-sdk/107-CONTEXT.md
@.planning/phases/107-jupiter-amm-adapter-sdk/107-RESEARCH.md
@deployments/mainnet.json
@programs/amm/src/helpers/math.rs
@programs/tax-program/src/helpers/tax_math.rs
@programs/tax-program/src/state/epoch_state_reader.rs
@programs/tax-program/src/helpers/pool_reader.rs
@programs/conversion-vault/src/instructions/convert.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create crate skeleton with workspace integration and dependencies</name>
  <files>
    Cargo.toml
    sdk/jupiter-adapter/Cargo.toml
    sdk/jupiter-adapter/src/lib.rs
  </files>
  <action>
  **FIRST -- Verify Phase 106 outputs:** Before ANY crate work, run `grep -r "convert_v2" programs/conversion-vault/src/lib.rs` and confirm the convert_v2 instruction is registered. Also confirm `programs/conversion-vault/src/instructions/convert_v2.rs` exists and contains the `minimum_output` parameter. If either check fails, STOP and report -- Phase 106 is not complete.

  **SECOND -- Verify REQUIREMENTS.md:** Confirm JUP-06 in `.planning/REQUIREMENTS.md` says "SDK handles vault-based PROFIT acquisition for all 4 directions (CRIME->PROFIT, PROFIT->CRIME, FRAUD->PROFIT, PROFIT->FRAUD) via Conversion Vault at fixed 100:1 rate". This was updated during plan revision. If it still says "4 PROFIT pool swap directions", update it to the correct text. PROFIT AMM pools do NOT exist -- PROFIT is vault-only.

  **Root Cargo.toml:** Add `"sdk/jupiter-adapter"` to the workspace members list. The current members are `["programs/*", "tests/cross-crate"]`. Add the SDK path so it becomes `["programs/*", "tests/cross-crate", "sdk/jupiter-adapter"]`. Since it is NOT under `programs/*`, Anchor's `anchor build` will never attempt BPF compilation.

  **sdk/jupiter-adapter/Cargo.toml:** Create with:
  ```toml
  [package]
  name = "drfraudsworth-jupiter-adapter"
  version = "0.1.0"
  edition = "2021"
  description = "Jupiter AMM adapter SDK for Dr. Fraudsworth DEX protocol"
  license = "MIT"
  repository = "https://github.com/MetalLegBob/drfraudsworth"
  keywords = ["jupiter", "solana", "dex", "amm"]

  [dependencies]
  jupiter-amm-interface = "0.5.0"
  solana-sdk = "2.2"
  rust_decimal = "1.36.0"
  ahash = "0.8"
  anyhow = "1"
  sha2 = "0.10"

  [dev-dependencies]
  # LiteSVM tests added in Plan 03
  ```

  CRITICAL: Use jupiter-amm-interface version 0.5.0, NOT 0.6.1. v0.6.1 requires solana-pubkey 4.0 (Solana v3/v4 crate split) which is incompatible with this project's solana-sdk 2.2.

  **sdk/jupiter-adapter/src/lib.rs:** Create crate root that re-exports public modules:
  ```rust
  pub mod constants;
  pub mod math;
  pub mod state;
  pub mod accounts;
  ```
  Add a doc comment explaining this is the Jupiter AMM adapter SDK for Dr. Fraudsworth.
  </action>
  <verify>
  Run `source "$HOME/.cargo/env" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && cargo check -p drfraudsworth-jupiter-adapter` -- must compile without errors.
  Run `grep "vault-based PROFIT" .planning/REQUIREMENTS.md` -- must match JUP-06 line.
  </verify>
  <done>Crate compiles as workspace member. jupiter-amm-interface 0.5.0 resolves without version conflicts. JUP-06 requirement text is accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Copy pure math functions and create state parsers + address constants</name>
  <files>
    sdk/jupiter-adapter/src/constants.rs
    sdk/jupiter-adapter/src/math/mod.rs
    sdk/jupiter-adapter/src/math/amm_math.rs
    sdk/jupiter-adapter/src/math/tax_math.rs
    sdk/jupiter-adapter/src/math/vault_math.rs
    sdk/jupiter-adapter/src/state/mod.rs
    sdk/jupiter-adapter/src/state/pool_state.rs
    sdk/jupiter-adapter/src/state/epoch_state.rs
    sdk/jupiter-adapter/src/accounts/mod.rs
    sdk/jupiter-adapter/src/accounts/addresses.rs
  </files>
  <action>
  **Math modules -- EXACT COPIES from on-chain code:**

  **amm_math.rs:** Copy `calculate_effective_input` and `calculate_swap_output` EXACTLY from `programs/amm/src/helpers/math.rs`. These are pure u64/u128 functions with zero Solana deps. Add a comment at top: `// Source: programs/amm/src/helpers/math.rs -- MUST stay in sync`. Include the unit tests from the source file (they validate correctness).

  **tax_math.rs:** Copy `calculate_tax` and `split_distribution` EXACTLY from `programs/tax-program/src/helpers/tax_math.rs`. Same pattern -- pure math, no deps. Add source traceability comment. Include unit tests.

  **vault_math.rs:** Create vault conversion math:
  - `compute_vault_output(input_mint: &Pubkey, output_mint: &Pubkey, amount_in: u64) -> Option<u64>` -- mirrors on-chain `compute_output_with_mints` logic but returns Option instead of Result (no anchor errors in SDK).
  - CRIME/FRAUD -> PROFIT: divide by 100 (return None if result is 0)
  - PROFIT -> CRIME/FRAUD: checked_mul by 100
  - CRIME <-> FRAUD: Return None. The on-chain `compute_output_with_mints` returns `InvalidMintPair` for CRIME<->FRAUD. The SDK cannot route a path the on-chain program rejects. Add a comment: `// CRIME<->FRAUD not supported on-chain (InvalidMintPair). Jupiter routes CRIME->PROFIT->FRAUD via multi-hop.`
  - Use the mint address constants from addresses.rs for comparison.
  - CONVERSION_RATE const = 100u64.

  **State parsers -- raw byte extraction (NO anchor-lang dep):**

  **epoch_state.rs:** Create `ParsedEpochState` struct with fields: `crime_buy_tax_bps: u16`, `crime_sell_tax_bps: u16`, `fraud_buy_tax_bps: u16`, `fraud_sell_tax_bps: u16`.
  - `from_bytes(data: &[u8]) -> anyhow::Result<Self>`: Validate minimum length (172 bytes). Validate Anchor discriminator (sha256("account:EpochState")[0..8]) using `sha2` crate. Parse u16 LE fields at offsets: [33..35] crime_buy, [35..37] crime_sell, [37..39] fraud_buy, [39..41] fraud_sell. These offsets are proven in `programs/tax-program/src/state/epoch_state_reader.rs`.
  - `get_tax_bps(&self, is_crime: bool, is_buy: bool) -> u16`: Match on (is_crime, is_buy) to return the correct rate.

  **pool_state.rs:** Create `ParsedPoolState` struct with fields: `mint_a: Pubkey`, `reserve_a: u64`, `reserve_b: u64`, `lp_fee_bps: u16`.
  - `from_bytes(data: &[u8]) -> anyhow::Result<Self>`: Validate minimum length (155 bytes). Parse: [9..41] mint_a (Pubkey), [137..145] reserve_a (u64 LE), [145..153] reserve_b (u64 LE), [153..155] lp_fee_bps (u16 LE). These offsets are proven in `programs/tax-program/src/helpers/pool_reader.rs`.
  - `sol_and_token_reserves(&self) -> (u64, u64)`: Returns (sol_reserve, token_reserve) with is_reversed detection -- if mint_a == NATIVE_MINT, return (reserve_a, reserve_b), else (reserve_b, reserve_a). Use NATIVE_MINT constant from addresses.rs.

  **Address constants:**

  **addresses.rs:** Hardcode ALL mainnet addresses from `deployments/mainnet.json` as `pub const` Pubkey values using `solana_sdk::pubkey!()` macro. Organize into sections:
  - Programs: AMM_PROGRAM_ID, TRANSFER_HOOK_PROGRAM_ID, TAX_PROGRAM_ID, EPOCH_PROGRAM_ID, STAKING_PROGRAM_ID, CONVERSION_VAULT_PROGRAM_ID
  - Mints: CRIME_MINT, FRAUD_MINT, PROFIT_MINT, NATIVE_MINT (So11111111111111111111111111111111111111112)
  - PDAs: EPOCH_STATE_PDA, SWAP_AUTHORITY_PDA, TAX_AUTHORITY_PDA, STAKE_POOL_PDA, ESCROW_VAULT_PDA, CARNAGE_SOL_VAULT_PDA, TREASURY, WSOL_INTERMEDIARY_PDA, VAULT_CONFIG_PDA, VAULT_CRIME, VAULT_FRAUD, VAULT_PROFIT, WHITELIST_AUTHORITY
  - Pools: CRIME_SOL_POOL, CRIME_SOL_VAULT_A, CRIME_SOL_VAULT_B, FRAUD_SOL_POOL, FRAUD_SOL_VAULT_A, FRAUD_SOL_VAULT_B
  - Hook ExtraAccountMetaList PDAs: CRIME_HOOK_META, FRAUD_HOOK_META, PROFIT_HOOK_META
  - Standard Programs: SPL_TOKEN_PROGRAM_ID, TOKEN_2022_PROGRAM_ID, SYSTEM_PROGRAM_ID

  **constants.rs:** Define protocol constants:
  - LP_FEE_BPS: u16 = 100 (1%)
  - CONVERSION_RATE: u64 = 100
  - TOKEN_DECIMALS: u8 = 9
  - EPOCH_STATE_DISCRIMINATOR: [u8; 8] -- compute at const time or document the known value

  **Module files:** Create mod.rs for math/, state/, accounts/ that re-export their submodules.
  </action>
  <verify>
  Run `source "$HOME/.cargo/env" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && cargo test -p drfraudsworth-jupiter-adapter` -- all copied math tests must pass.
  Also run `cargo check -p drfraudsworth-jupiter-adapter` -- everything compiles.
  Manually verify: `calculate_effective_input(1000, 100) == Some(990)` and `calculate_tax(1_000_000_000, 400) == Some(40_000_000)` in the test output -- these are the canonical on-chain values.
  </verify>
  <done>
  Math functions produce identical output to on-chain originals. State parsers extract correct fields from known byte offsets. All mainnet addresses hardcoded. Crate compiles and all unit tests pass.
  </done>
</task>

</tasks>

<verification>
- `cargo check -p drfraudsworth-jupiter-adapter` passes (crate compiles)
- `cargo test -p drfraudsworth-jupiter-adapter` passes (math tests match on-chain)
- Root Cargo.toml includes sdk/jupiter-adapter in workspace members
- `anchor build` still works (SDK not compiled as BPF)
- No anchor-lang dependency in sdk/jupiter-adapter/Cargo.toml
- JUP-06 in REQUIREMENTS.md describes vault-based PROFIT acquisition (not PROFIT AMM pools)
</verification>

<success_criteria>
- Crate skeleton exists and compiles as workspace member
- Pure math functions are byte-for-byte copies of on-chain originals with passing tests
- EpochState parser extracts 4 tax rates from raw bytes at correct offsets
- PoolState parser extracts mint_a, reserves, lp_fee_bps with is_reversed detection
- All mainnet addresses hardcoded as constants (zero network calls)
- jupiter-amm-interface 0.5.0 resolves without version conflicts
- JUP-06 requirement text accurately describes vault-based PROFIT
</success_criteria>

<output>
After completion, create `.planning/phases/107-jupiter-amm-adapter-sdk/107-01-SUMMARY.md`
</output>
