---
phase: 52-smart-swap-routing
plan: 06
type: execute
wave: 5
depends_on: ["52-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "For SOL->PROFIT, the routing engine shows at least 2 routes (via CRIME and via FRAUD) ranked by output"
    - "The route with lower tax (cheap side) ranks first"
    - "For SOL->CRIME (direct pool), the direct route is shown and ranked first"
    - "Smart Routing toggle switches between routed and direct modes"
    - "Selecting a multi-hop route and executing produces the expected token output"
    - "For large SOL->PROFIT orders, a split route is suggested when it beats single-path by >= 0.5%"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 52 success criteria by running the routing engine tests, building the project, and visually verifying the swap UI on the dev server.

Purpose: Final verification that all 5 success criteria from the ROADMAP are met: multi-path evaluation, route UI display, multi-hop execution, direct pool optimization, and split routing for large orders.

Output: Verified Phase 52 is complete
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-smart-swap-routing/52-RESEARCH.md
@.planning/phases/52-smart-swap-routing/52-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all tests and verify build</name>
  <files></files>
  <action>
    1. Run route engine unit tests:
    ```bash
    export PATH="/opt/homebrew/bin:$PATH"
    cd /Users/mlbob/Projects/Dr\ Fraudsworth
    npx vitest run app/lib/swap/__tests__/route-engine.test.ts
    ```
    All tests must pass.

    2. Run split router unit tests:
    ```bash
    npx vitest run app/lib/swap/__tests__/split-router.test.ts
    ```
    All tests must pass.

    3. Run full TypeScript type-check:
    ```bash
    npx tsc --noEmit
    ```
    Zero errors.

    4. Build the project:
    ```bash
    npx next build
    ```
    Build succeeds.

    5. Verify success criteria from route engine tests:
    - SC1: computeRoutes("SOL", "PROFIT", ...) returns >= 2 routes (verified by test case 2)
    - SC4: computeRoutes("SOL", "CRIME", ...) returns 1 direct route ranked first (verified by test case 1)
    - SC5: computeOptimalSplit returns shouldSplit=true for large inputs (verified by split-router test case 2)

    6. Run the existing Rust test suite to confirm no regressions:
    ```bash
    source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH"
    cd /Users/mlbob/Projects/Dr\ Fraudsworth
    cargo test --workspace 2>&1 | tail -20
    ```
    All 299 Rust tests still pass (no on-chain changes in this phase).
  </action>
  <verify>All tests pass. Build succeeds. No regressions.</verify>
  <done>Route engine tests pass, split router tests pass, TypeScript compiles, Next.js builds, 299 Rust tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Smart Swap Routing implementation:
    - Route engine evaluating all viable paths (single-hop, multi-hop, split)
    - Route selection UI with ranked routes, fee/output estimates, and "Best" badge
    - Smart Routing toggle in swap form
    - Multi-hop execution with single wallet approval
    - Split routing for large orders
    - 30-second auto-refresh with countdown timer
    - Partial failure handling with Retry/Keep options
  </what-built>
  <how-to-verify>
    1. Start dev server:
       ```bash
       export PATH="/opt/homebrew/bin:$PATH"
       cd /Users/mlbob/Projects/Dr\ Fraudsworth
       npx next dev
       ```
    2. Open http://localhost:3000 and navigate to the swap page

    3. **SC1 - Multi-path evaluation (SOL->PROFIT):**
       - Select SOL as input, PROFIT as output (new pair!)
       - Enter 0.1 SOL
       - Verify RouteSelector appears with at least 2 routes:
         - SOL -> CRIME -> PROFIT
         - SOL -> FRAUD -> PROFIT
       - Verify routes are ranked by output (best first)
       - Verify the cheap-side route (lower tax) has higher output

    4. **SC2 - Route selection UI:**
       - Verify each route card shows: output amount, LP fee %, tax %, price impact %, hop count
       - Verify "Best" badge on the top route
       - Click "See all routes" to expand alternatives
       - Click a different route -- verify it highlights as selected
       - Verify 30-second countdown timer is visible and animating

    5. **SC3 - Multi-hop execution (if wallet available):**
       - Select SOL -> PROFIT via CRIME route
       - Click Swap -- verify single wallet approval prompt
       - Verify "Executing route..." spinner appears
       - (If wallet connected to devnet with SOL balance, verify transaction confirms)

    6. **SC4 - Direct pool optimization:**
       - Select SOL as input, CRIME as output (direct pool)
       - Verify the direct route shows and ranks first
       - Verify no unnecessary multi-hop alternatives rank higher

    7. **SC5 - Smart Routing toggle:**
       - Toggle Smart Routing OFF
       - Verify RouteSelector disappears
       - Verify the UI looks identical to the pre-Phase-52 swap form
       - Select SOL -> CRIME: works as direct swap
       - Select SOL -> PROFIT: shows "No direct pool" message
       - Toggle Smart Routing back ON: routes reappear

    8. **Backward compatibility:**
       - With Smart Routing OFF, perform a direct SOL -> CRIME swap
       - Verify it works exactly as before (same execution path, same fee display)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 52, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All unit tests pass. Build succeeds. Visual verification confirms all 5 success criteria from ROADMAP.
</verification>

<success_criteria>
- Route engine tests: all pass
- Split router tests: all pass
- TypeScript: zero errors
- Next.js build: succeeds
- Rust tests: 299 pass (no regressions)
- Visual: SOL->PROFIT shows multi-hop routes
- Visual: Route cards show fees, output, price impact, hop count
- Visual: Smart Routing toggle works
- Visual: Direct pools correctly show direct route as best
</success_criteria>

<output>
After completion, create `.planning/phases/52-smart-swap-routing/52-06-SUMMARY.md`
</output>
