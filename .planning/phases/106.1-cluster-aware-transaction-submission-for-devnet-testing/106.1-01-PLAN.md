---
phase: 106.1-cluster-aware-transaction-submission-for-devnet-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/hooks/useProtocolWallet.ts
  - app/lib/swap/multi-hop-builder.ts
autonomous: false

must_haves:
  truths:
    - "Single-hop swaps land on devnet without preflight rejection"
    - "Multi-hop swaps land on devnet without preflight rejection"
    - "Staking transactions land on devnet without preflight rejection"
    - "Mainnet sendTransaction path is byte-for-byte unchanged"
    - "No per-callsite isDevnet/skipPreflight logic remains in multi-hop-builder.ts"
  artifacts:
    - path: "app/hooks/useProtocolWallet.ts"
      provides: "Centralized skipPreflight:true override on devnet branch"
      contains: "skipPreflight: true"
    - path: "app/lib/swap/multi-hop-builder.ts"
      provides: "Simplified executeAtomicRoute without redundant isDevnet check"
  key_links:
    - from: "app/hooks/useProtocolWallet.ts"
      to: "connection.sendRawTransaction"
      via: "devnet branch forces skipPreflight: true"
      pattern: "skipPreflight:\\s*true"
    - from: "app/lib/swap/multi-hop-builder.ts"
      to: "app/hooks/useProtocolWallet.ts"
      via: "wallet.sendTransaction delegates to useProtocolWallet"
      pattern: "wallet\\.sendTransaction"
---

<objective>
Centralize the devnet skipPreflight override in useProtocolWallet so all 6 transaction submission callsites automatically work on devnet, then remove the redundant per-callsite isDevnet check in multi-hop-builder.ts.

Purpose: Unblock Phase 106-04 devnet testing. Currently 3 of 6 callsites fail on devnet because they pass skipPreflight:false (or undefined), which causes v0 transaction preflight simulation to reject with "Blockhash not found". The fix is a single-line change in useProtocolWallet.ts plus cleanup of the now-redundant workaround in multi-hop-builder.ts.

Output: Two modified files, zero new files. All devnet transaction paths work. Mainnet path completely untouched.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106.1-cluster-aware-transaction-submission-for-devnet-testing/106.1-CONTEXT.md
@.planning/phases/106.1-cluster-aware-transaction-submission-for-devnet-testing/106.1-RESEARCH.md

@app/hooks/useProtocolWallet.ts
@app/lib/swap/multi-hop-builder.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize skipPreflight override in useProtocolWallet + clean up multi-hop-builder</name>
  <files>app/hooks/useProtocolWallet.ts, app/lib/swap/multi-hop-builder.ts</files>
  <action>
    **useProtocolWallet.ts** -- In the devnet branch of `wrappedSendTransaction` (line 116), change:
    ```
    skipPreflight: sendOptions.skipPreflight,
    ```
    to:
    ```
    skipPreflight: true, // Forced on devnet: v0 simulation returns "Blockhash not found"
    ```
    This is the ONLY change to this file. Do NOT touch the mainnet branch (line 100). Do NOT add console.warn or extra logging (per CONTEXT.md: "No extra devnet logging"). Do NOT standardize maxRetries (Claude's discretion per CONTEXT.md: leave caller-controlled).

    **multi-hop-builder.ts** -- In `executeAtomicRoute` (around lines 426-432), remove the redundant `isDevnet` computation and simplify the sendTransaction call:
    - Remove: `const isDevnet = process.env.NEXT_PUBLIC_CLUSTER !== "mainnet";`
    - Change `{ skipPreflight: isDevnet, maxRetries: 3 }` to `{ skipPreflight: false, maxRetries: 3 }`
    - The value `false` expresses the mainnet preference (no skip preflight). On devnet, useProtocolWallet overrides this to `true` centrally.
    - Update the code comments above the sendTransaction call to reflect that useProtocolWallet now handles the devnet override. Remove references to "skipPreflight because devnet simulation rejects v0 transactions" since that concern is now in useProtocolWallet. Keep the comment about what each branch (mainnet/devnet) does in the JSDoc at the top of executeAtomicRoute -- just update it to note the override is centralized.

    **What NOT to change:**
    - No changes to useSwap.ts, useStaking.ts, BuyForm.tsx, SellForm.tsx, or RefundPanel.tsx -- they all go through useProtocolWallet and get the fix for free.
    - No changes to confirm-transaction.ts or any polling/error handling.
    - No changes to the mainnet branch of useProtocolWallet (line 100 stays as `return walletSendTransaction(tx, connection, opts)`).
  </action>
  <verify>
    1. `grep -n "skipPreflight: true" app/hooks/useProtocolWallet.ts` -- should match exactly one line in the devnet branch
    2. `grep -n "skipPreflight: sendOptions.skipPreflight" app/hooks/useProtocolWallet.ts` -- should return NO matches (old code removed)
    3. `grep -n "isDevnet" app/lib/swap/multi-hop-builder.ts` -- should return NO matches in executeAtomicRoute (the variable is gone)
    4. `grep -n "skipPreflight: false" app/lib/swap/multi-hop-builder.ts` -- should match one line (callers express mainnet preference)
    5. `grep -n "walletSendTransaction(tx, connection, opts)" app/hooks/useProtocolWallet.ts` -- mainnet line unchanged
    6. `export PATH="/opt/homebrew/bin:$PATH" && cd app && npx next build 2>&1 | tail -5` or equivalent TypeScript check to confirm no type errors
  </verify>
  <done>
    - useProtocolWallet.ts forces skipPreflight:true in the devnet branch (line 116 area)
    - multi-hop-builder.ts has no isDevnet variable and passes skipPreflight:false (mainnet preference)
    - Mainnet path in useProtocolWallet is byte-for-byte identical to before
    - Build passes with no type errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Centralized devnet skipPreflight override in useProtocolWallet.ts. All 6 callsites (useSwap single-hop, useSwap multi-hop, useStaking, BuyForm, SellForm, RefundPanel) now automatically get skipPreflight:true on devnet. Removed redundant isDevnet check from multi-hop-builder.ts.
  </what-built>
  <how-to-verify>
    **Quick devnet smoke test (Railway or localhost with NEXT_PUBLIC_CLUSTER=devnet):**

    1. **Single-hop swap** (previously BROKEN on devnet): Try a small SOL->CRIME swap (~0.003 SOL). It should submit and confirm (or fail with an on-chain error -- NOT a preflight "Blockhash not found" error). A successful submission proves useSwap.ts path works.

    2. **Multi-hop swap** (previously worked via redundant isDevnet): Try SOL->PROFIT multi-hop (~0.01 SOL). Should submit. This proves multi-hop-builder.ts still works after removing the local isDevnet workaround.

    3. **Staking** (previously BROKEN on devnet): Try staking a small amount of PROFIT. Should submit. This proves useStaking.ts path works.

    **What to look for:**
    - Transactions SUBMIT (no "Blockhash not found" errors in the UI)
    - Transactions either succeed or fail with ON-CHAIN errors (which is fine -- this proves the submission layer works; on-chain logic is Phase 106-04's concern)
    - No wallet-level "malicious dApp" warnings (shouldn't happen on devnet but worth noting)

    **Note:** If the devnet vault program hasn't been upgraded yet (106-04 Task 1), vault convert paths will still use the old convert instruction and may fail on-chain. That's expected and is NOT a failure of this phase. The test is: do transactions SUBMIT without preflight rejection?
  </how-to-verify>
  <resume-signal>Type "approved" if transactions submit on devnet, or describe specific submission failures</resume-signal>
</task>

</tasks>

<verification>
1. useProtocolWallet.ts devnet branch forces skipPreflight:true (grep confirms)
2. multi-hop-builder.ts has no isDevnet variable (grep confirms)
3. Mainnet branch in useProtocolWallet is unchanged (grep confirms passthrough)
4. Build passes with no type errors
5. Devnet transactions submit without preflight "Blockhash not found" errors (checkpoint)
</verification>

<success_criteria>
- skipPreflight:true is forced centrally in useProtocolWallet's devnet branch
- Redundant isDevnet check removed from multi-hop-builder.ts
- Mainnet path completely untouched
- At least one swap and one staking transaction submit on devnet without preflight rejection
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/106.1-cluster-aware-transaction-submission-for-devnet-testing/106.1-01-SUMMARY.md`
</output>
