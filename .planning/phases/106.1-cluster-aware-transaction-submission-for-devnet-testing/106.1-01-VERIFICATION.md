---
phase: 106.1-cluster-aware-transaction-submission-for-devnet-testing
verified: 2026-03-26T15:10:44Z
status: passed
score: 5/5 must-haves verified
re_verification: false
---

# Phase 106.1: Cluster-Aware Transaction Submission Verification Report

**Phase Goal:** Centralize the devnet skipPreflight override in useProtocolWallet so all transaction submission callsites automatically work on devnet
**Verified:** 2026-03-26T15:10:44Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | Single-hop swaps land on devnet without preflight rejection | VERIFIED | useSwap.ts calls `wallet.sendTransaction(..., { skipPreflight: false })`. useProtocolWallet devnet path ignores that opt and forces `skipPreflight: true` at line 116 of useProtocolWallet.ts. |
| 2 | Multi-hop swaps land on devnet without preflight rejection | VERIFIED | multi-hop-builder.ts `executeAtomicRoute` calls `wallet.sendTransaction(..., { skipPreflight: false, maxRetries: 3 })`. Same centralized override applies. |
| 3 | Staking transactions land on devnet without preflight rejection | VERIFIED | useStaking.ts calls `wallet.sendTransaction(..., { skipPreflight: false })`. Centralized override applies. |
| 4 | Mainnet sendTransaction path is byte-for-byte unchanged | VERIFIED | Line 100: `return walletSendTransaction(tx, connection, opts)` — pure passthrough, no modification to opts, no skipPreflight injection. |
| 5 | No per-callsite isDevnet/skipPreflight logic remains in multi-hop-builder.ts | VERIFIED | grep for `isDevnet` and `NEXT_PUBLIC_CLUSTER` in multi-hop-builder.ts returns zero matches. The file contains no cluster-detection logic. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---------|---------|--------|---------|
| `app/hooks/useProtocolWallet.ts` | Centralized skipPreflight:true override on devnet branch | VERIFIED | 134 lines. Contains `skipPreflight: true` at line 116 inside `connection.sendRawTransaction()` call within the non-mainnet (devnet) branch. No stubs, no TODOs. Exports `useProtocolWallet` function. |
| `app/lib/swap/multi-hop-builder.ts` | Simplified executeAtomicRoute without redundant isDevnet check | VERIFIED | 469 lines. `executeAtomicRoute` at line 421 passes `{ skipPreflight: false, maxRetries: 3 }` to `wallet.sendTransaction`. No `isDevnet` variable, no `NEXT_PUBLIC_CLUSTER` reference anywhere in file. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `app/hooks/useProtocolWallet.ts` | `connection.sendRawTransaction` | devnet branch forces `skipPreflight: true` | WIRED | Line 115-120: `connection.sendRawTransaction(serialized, { skipPreflight: true, ... })`. Pattern matches `skipPreflight:\s*true`. Value is a literal `true`, not conditional. |
| `app/lib/swap/multi-hop-builder.ts` | `app/hooks/useProtocolWallet.ts` | `wallet.sendTransaction` delegates to useProtocolWallet | WIRED | Line 431: `wallet.sendTransaction(build.transaction, connection, { skipPreflight: false, maxRetries: 3 })`. `wallet` is typed as `ProtocolWallet` from useProtocolWallet. useSwap.ts line 209 and useStaking.ts line 219 both instantiate via `useProtocolWallet()`. |

### Mainnet Safety Check

The mainnet branch (`if (isMainnet)`) at line 97-101 returns `walletSendTransaction(tx, connection, opts)` — a direct pass-through. The devnet `skipPreflight: true` override is in a separate code path that is never reached when `NEXT_PUBLIC_CLUSTER === "mainnet"`. No risk of cross-contamination.

### Callsite Coverage

All 6 TX submission callsites route through useProtocolWallet and receive the override:

| Callsite | Routes via | skipPreflight passed by caller | Effective on devnet |
|----------|-----------|-------------------------------|---------------------|
| `useSwap.ts` (single-hop) | `wallet.sendTransaction` | false | true (override) |
| `multi-hop-builder.ts` (multi-hop) | `wallet.sendTransaction` | false | true (override) |
| `useStaking.ts` | `wallet.sendTransaction` | false | true (override) |
| `BuyForm.tsx` | `sendTransaction` (from useProtocolWallet) | true (already correct) | true (override) |
| `SellForm.tsx` | `sendTransaction` (from useProtocolWallet) | true (already correct) | true (override) |
| `RefundPanel.tsx` | `sendTransaction` (from useProtocolWallet) | not passed (undefined) | true (override) |

BuyForm and SellForm already passed `skipPreflight: true` to the wrapper — that value is passed in opts, but on the devnet path the `connection.sendRawTransaction` call ignores `sendOptions.skipPreflight` entirely and uses the hardcoded `true`. The result is correct regardless.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | — |

No TODOs, FIXMEs, placeholder text, empty returns, or stub patterns found in either modified file.

### Human Verification Required

Per the PLAN checkpoint (Task 2), the SUMMARY records that the human verified:
- SOL->CRIME single-hop swap submitted on devnet
- SOL->PROFIT multi-hop swap submitted on devnet

These are live-transaction tests that cannot be verified programmatically. The SUMMARY documents checkpoint approval. No additional human verification is required for code structure; the automated checks fully confirm the centralization goal.

### Gaps Summary

No gaps. All five must-have truths are verified by direct code inspection:

1. `skipPreflight: true` is hardcoded at line 116 of useProtocolWallet.ts — not derived from caller opts, not conditional, not a TODO.
2. The devnet branch is reached for any cluster that is not literally `"mainnet"` (checked via `process.env.NEXT_PUBLIC_CLUSTER?.toLowerCase() === "mainnet"`).
3. All 6 callsites use `useProtocolWallet`'s `sendTransaction` — none bypass it with direct `sendRawTransaction` calls.
4. The mainnet branch is a pure passthrough to `walletSendTransaction` with zero modifications.
5. multi-hop-builder.ts contains zero cluster-detection logic.

---

_Verified: 2026-03-26T15:10:44Z_
_Verifier: Claude (gsd-verifier)_
