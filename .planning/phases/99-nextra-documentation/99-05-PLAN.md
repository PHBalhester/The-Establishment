---
phase: 99-nextra-documentation
plan: 05
type: execute
wave: 5
depends_on: ["99-04"]
files_modified:
  - docs-site/content/reference/tokenomics.mdx
  - docs-site/content/reference/glossary.mdx
  - Docs/address-placeholders.md
autonomous: false
requirements:
  - DOCS-01
  - DOCS-05

must_haves:
  truths:
    - "Tokenomics page has mainnet vanity mint addresses and accurate supply/distribution data"
    - "Glossary reflects all terminology used in rewritten pages with no banned terms"
    - "Address placeholder tracking document is complete with every placeholder across all 17 pages"
    - "Full docs site builds without errors"
    - "No banned terminology exists anywhere in docs-site/content/"
  artifacts:
    - path: "docs-site/content/reference/tokenomics.mdx"
      provides: "Rewritten Tokenomics page with mainnet addresses"
      min_lines: 40
    - path: "docs-site/content/reference/glossary.mdx"
      provides: "Rewritten Glossary aligned with all rewritten pages"
      min_lines: 40
    - path: "Docs/address-placeholders.md"
      provides: "Complete tracking document with all placeholders from all 17 pages"
      contains: "MAINNET_"
  key_links:
    - from: "Docs/address-placeholders.md"
      to: "docs-site/content/"
      via: "file:line mapping for every placeholder"
      pattern: "MAINNET_"
---

<objective>
Rewrite the Reference section (Tokenomics + Glossary) and perform a full-site sweep to verify all 17 pages are consistent, all placeholders tracked, and the site builds cleanly.

Purpose: The Reference section is where users look up facts (supply, addresses, definitions). The Glossary must align with the terminology decisions applied across all prior pages. The final sweep ensures no stale content, banned terms, or missing placeholders slipped through.

Output: 2 rewritten MDX pages, finalized address tracking document, full-site verification report.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-nextra-documentation/99-CONTEXT.md
@.planning/phases/99-nextra-documentation/99-RESEARCH.md
@.planning/phases/99-nextra-documentation/99-04-SUMMARY.md
@docs-site/content/reference/tokenomics.mdx
@docs-site/content/reference/glossary.mdx
@Docs/address-placeholders.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Reference pages and perform full-site consistency sweep</name>
  <files>
    docs-site/content/reference/tokenomics.mdx
    docs-site/content/reference/glossary.mdx
    Docs/address-placeholders.md
  </files>
  <action>
    **Page 1: `reference/tokenomics.mdx` (Tokenomics)**
    - Fix "game rewards" (x2)
    - Use mainnet vanity mint addresses:
      - CRIME: `cRiMEhAxoDhcEuh3Yf7Z2QkXUXUMKbakhcVqmDsqPXc`
      - FRAUD: `FraUdp6YhtVJYPxC2w255yAbpTsPqd8Bfhy9rC56jau5`
      - PROFIT: `pRoFiTj36haRD5sG2Neqib9KoSrtdYMGrM7SEkZetfR`
    - Accurate supply data:
      - CRIME: 1,000,000,000 (1B)
      - FRAUD: 1,000,000,000 (1B)
      - PROFIT: 20,000,000 (20M)
    - Token decimals: 6 for all three
    - Distribution: bonding curve allocation, AMM pool seeding, staking rewards pool
    - Program addresses: use `<MAINNET_*>` placeholders for all 7 programs
    - Include table of all protocol addresses (mints + programs + key PDAs)
    - Use "rewards" not "yield"
    - No code snippets

    **Page 2: `reference/glossary.mdx` (Glossary)**
    - Fix "game rewards" (x3) and "Game Rewards" heading
    - Align ALL terms with the terminology used in the rewritten pages:
      - Remove any "Game Rewards" entry, replace with "Staking Rewards"
      - Ensure "Epoch", "Carnage", "Soft Peg", "Bonding Curve", "Graduation", "Refund" are all defined
      - Add "Squads Multisig" and "Timelock" definitions (new from governance page)
      - Add "VRF" (Verifiable Random Function) definition
      - Remove any terms that reference "game" framing
    - Alphabetical order
    - Each term: bold name, clear 1-2 sentence definition
    - Cross-reference to relevant pages where applicable

    **Step 3: Full-site consistency sweep**

    Run these checks across ALL 17 pages in `docs-site/content/`:
    1. Banned terms sweep: `grep -rn "yield\|APY\|game reward\|gambl" docs-site/content/`
       - Must return zero matches
    2. Stale tax split: `grep -rn "75.*24.*1\|75/24/1" docs-site/content/`
       - Must return zero matches (should be 71/24/5 everywhere)
    3. Code blocks: `grep -rn '```rust\|```typescript' docs-site/content/`
       - Must return zero matches
    4. Devnet addresses: `grep -rn "8kPzh\|5JsS\|HL3r\|4ugX\|GtxT" docs-site/content/`
       - Must return zero matches (no devnet addresses in docs)
    5. Placeholder tracking: cross-reference every `<MAINNET_` in MDX files against `Docs/address-placeholders.md`
       - Every placeholder in MDX must appear in tracking doc with file:line
    6. Diagram references: `grep -rn "diagrams/" docs-site/content/`
       - Should show 9 diagram references across the pages

    **Step 4: Finalize `Docs/address-placeholders.md`**
    - Complete pass: ensure EVERY `<MAINNET_*>` placeholder across all 17 pages is tracked
    - Update line numbers to be accurate after all rewrites
    - Add summary count at bottom: "Total: N placeholders in M files"

    **Step 5: Build verification**
    - Run `cd docs-site && npm run build` to confirm the site compiles without errors
    - All 17 pages must be present in the build output

    If any issues found during the sweep, fix them in this task.
  </action>
  <verify>
    1. `grep -rn "yield\|APY\|game reward\|gambl" docs-site/content/` returns ZERO matches
    2. `grep -rn "75.*24.*1\|75/24/1" docs-site/content/` returns ZERO matches
    3. `grep -rn '```rust\|```typescript' docs-site/content/` returns ZERO matches
    4. `grep -rn "8kPzh\|5JsS\|HL3r\|4ugX\|GtxT" docs-site/content/` returns ZERO matches
    5. `cd docs-site && npm run build` completes without errors
    6. `grep -c "MAINNET_" Docs/address-placeholders.md` shows all placeholders tracked
    7. Both reference pages exist with substantive content
  </verify>
  <done>
    Reference section rewritten (Tokenomics with mainnet addresses, Glossary aligned with all rewritten pages). Full-site sweep confirms: zero banned terms, zero stale tax splits, zero code blocks, zero devnet addresses, all placeholders tracked. Site builds cleanly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Rewrote Tokenomics and Glossary. Performed full-site consistency sweep across all 17 pages. Finalized address placeholder tracking document. Verified site builds.</what-built>
  <how-to-verify>
    1. Read Tokenomics page -- verify:
       - All 3 mainnet vanity mint addresses present
       - Supply numbers correct (1B/1B/20M)
       - Program addresses use `<MAINNET_*>` placeholders
       - No banned terms
    2. Read Glossary -- verify:
       - No "Game Rewards" entry, has "Staking Rewards" instead
       - New terms added: Squads Multisig, Timelock, VRF
       - Alphabetical order
       - Definitions match terminology in rewritten pages
    3. Review `Docs/address-placeholders.md` -- verify completeness
    4. Optionally: `cd docs-site && npm run dev` to preview the complete docs site
    5. Walk through all 17 pages in sidebar order to spot any remaining issues
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 99, or describe changes needed</resume-signal>
</task>

</tasks>

<verification>
- Tokenomics page has all 3 mainnet vanity addresses and accurate supply data
- Glossary aligned with all terminology decisions from the full rewrite
- Full-site sweep: zero banned terms, zero stale splits, zero code blocks, zero devnet addresses
- Address tracking document complete with all placeholders mapped to file:line
- Docs site builds without errors
- All 17 pages present and substantive
</verification>

<success_criteria>
- Tokenomics: mainnet vanity addresses, accurate supply (1B/1B/20M), all program ID placeholders, no banned terms
- Glossary: aligned with rewritten pages, new governance terms, alphabetical, no "game" framing
- Address tracking: every `<MAINNET_*>` placeholder tracked with file:line, summary count
- Full-site sweep: all 5 checks pass with zero violations
- Site builds: `npm run build` succeeds
- Phase 99 complete: 17 pages production-ready, 9 diagram references placed, address placeholders tracked
</success_criteria>

<output>
After completion, create `.planning/phases/99-nextra-documentation/99-05-SUMMARY.md`
</output>
