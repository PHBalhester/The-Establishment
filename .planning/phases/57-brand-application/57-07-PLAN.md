---
phase: 57-brand-application
plan: 07
type: execute
wave: 3
depends_on: ["57-02", "57-03", "57-04", "57-05", "57-06"]
files_modified:
  - app/app/globals.css
autonomous: false

must_haves:
  truths:
    - "Zero gray/zinc/blue/indigo/emerald class occurrences exist in any active component file"
    - "Every text-on-background combination in the application meets WCAG AA 4.5:1 contrast"
    - "Next.js build succeeds with no TypeScript or CSS errors"
    - "All existing themed components (modal chrome, bolts, close button, toast, scene stations) still look correct after token refinements"
  artifacts:
    - path: "app/app/globals.css"
      provides: "Any final contrast adjustments applied to token values"
  key_links:
    - from: "All component files"
      to: "app/app/globals.css"
      via: "@theme token values driving all component styling"
      pattern: "factory-"
---

<objective>
Run a comprehensive audit: verify zero residual off-palette classes remain, run the Next.js build, and check WCAG AA contrast compliance. Fix any remaining issues. Visual verification checkpoint.

Purpose: This is the quality gate. Plans 01-06 applied systematic changes across 26+ files. This plan catches anything that slipped through and verifies the phase success criteria.

Output: Clean build, zero gray/zinc residuals, passing contrast ratios, human visual verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-brand-application/57-CONTEXT.md
@.planning/phases/57-brand-application/57-RESEARCH.md
@.planning/phases/57-brand-application/57-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive residual scan and build verification</name>
  <files>app/app/globals.css</files>
  <action>
  **Step 1: Full codebase scan for residual off-palette classes**

  Run grep across ALL component files for residual gray/zinc/blue/indigo/emerald classes:
  ```
  grep -rn "bg-gray-\|bg-zinc-\|text-gray-\|text-zinc-\|border-gray-\|border-zinc-\|bg-blue-\|text-blue-\|border-blue-\|bg-indigo-\|text-indigo-\|border-indigo-\|bg-emerald-\|text-emerald-\|placeholder-gray\|placeholder-zinc\|divide-gray\|divide-zinc\|stroke-gray\|stroke-zinc\|ring-gray\|ring-zinc" app/components/ app/app/
  ```

  Exclude:
  - Comments (lines starting with //)
  - Already-themed files known to have no changes (ModalShell, ModalContent, ModalProvider, ModalCloseButton, BigRedButton, ToastProvider, scene components, WalletStation, SettingsStation, DocsStation, page.tsx, layout.tsx)

  For any remaining occurrences found:
  - Fix them immediately using the mapping from research
  - If the occurrence is in a file not covered by Plans 02-06, determine the correct factory-* replacement and apply it

  **Step 2: Build verification**

  Run the Next.js build to ensure no TypeScript errors or CSS issues:
  ```
  export PATH="/opt/homebrew/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && npx next build
  ```

  If the build fails:
  - Read the error output
  - Fix TypeScript errors (likely from changed className string types or missing Tailwind classes)
  - Re-run build until clean

  **Step 3: Contrast spot-check**

  Using the contrast verification script from research, verify the key text/background pairs from Plan 01's contrast matrix are still meeting 4.5:1 after any token adjustments made during Plans 02-06. If any plan's summary mentions a token value was adjusted, re-verify that pair.

  **Step 4: Fix any issues found**

  Apply fixes to globals.css (token adjustments) or component files (missed replacements).
  </action>
  <verify>
  1. The grep scan returns zero results for off-palette classes in active component files
  2. `npx next build` completes successfully
  3. All contrast ratios from the Plan 01 matrix still pass 4.5:1
  </verify>
  <done>Zero off-palette classes remain, build succeeds, contrast ratios verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete steampunk brand application across the entire UI:
  - Refined palette with warmer mahogany backgrounds and polished brass accents
  - New CSS component classes: brass-rimmed inputs, mechanical lever tabs, beveled brass buttons
  - All 26+ component files re-themed from gray/zinc/blue to factory-* palette
  - WCAG AA contrast ratios verified for all text-on-background combinations
  - Faction colors (CRIME=red, FRAUD=amber, PROFIT=green) warm-shifted to match palette
  - Semantic colors (success/error/warning) recognizable but warm-shifted
  </what-built>
  <how-to-verify>
  Start the dev server: `export PATH="/opt/homebrew/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && npx next dev`

  Visit http://localhost:3000 and check each station:

  1. **Swap Station** -- Click the swap machine overlay:
     - Verify swap form inputs have brass-rimmed appearance (inner shadow, warm border)
     - Verify token selector dropdown matches factory palette
     - Verify slippage presets look like mechanical lever switches (raised/pressed states)
     - Verify fee breakdown panel uses warm colors
     - Verify route cards use faction colors for CRIME/FRAUD/PROFIT tokens
     - Check chart background matches the modal dark surface

  2. **Carnage Cauldron** -- Click the cauldron overlay:
     - Verify CarnageCard uses warm reds for CRIME, warm ambers for FRAUD
     - Verify all text is readable against dark backgrounds
     - Verify epoch and tax rate displays match the palette

  3. **Rewards Vat** -- Click the rewards vat overlay:
     - Verify tab buttons (Stake/Unstake/Claim) have lever-switch mechanical appearance
     - Verify amount inputs have brass-rimmed appearance
     - Verify reward amounts display in warm green
     - Verify status messages use semantic colors

  4. **Overall** -- Check across all modals:
     - No jarring gray/blue remnants visible
     - All text readable (no low-contrast text)
     - Modal chrome (brass frame, bolts, close button) still looks correct after palette refinement
     - Toast notifications still styled correctly
     - Existing scene station glow effects still look right

  5. **Legacy page** -- Visit http://localhost:3000/swap:
     - Verify it uses the warm factory palette (not cold gray)

  Report: "approved" if everything looks correct, or describe any specific issues.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Full grep scan: zero off-palette classes in active components
2. `npx next build` succeeds
3. All WCAG AA contrast ratios verified
4. Human visual verification of all 6 station modals
</verification>

<success_criteria>
Phase 57 success criteria met:
1. All existing UI components use steampunk palette classes from @theme tokens -- no residual bg-gray-*, bg-zinc-*, text-gray-* classes remain in active component files
2. All text meets WCAG AA contrast ratio (4.5:1) against steampunk-themed backgrounds, verified with computed ratios and browser visual check
</success_criteria>

<output>
After completion, create `.planning/phases/57-brand-application/57-07-SUMMARY.md`
</output>
