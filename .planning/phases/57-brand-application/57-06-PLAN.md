---
phase: 57-brand-application
plan: 06
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - app/components/chart/ChartControls.tsx
  - app/components/chart/CandlestickChart.tsx
  - app/components/wallet/ConnectModal.tsx
  - app/components/wallet/WalletButton.tsx
  - app/components/wallet/BalanceDisplay.tsx
  - app/app/swap/page.tsx
autonomous: true

must_haves:
  truths:
    - "ChartControls uses factory-accent for active pool selector and range buttons instead of indigo"
    - "CandlestickChart hardcoded JS hex values match the refined factory palette"
    - "ConnectModal (legacy) uses factory-* classes for its standalone modal styling"
    - "WalletButton header button uses factory-* classes"
    - "BalanceDisplay balance cards use factory-* classes"
    - "Legacy swap/page.tsx wrapper uses factory-bg and factory-text"
  artifacts:
    - path: "app/components/chart/CandlestickChart.tsx"
      provides: "Chart with factory palette colors in JS configuration"
    - path: "app/components/chart/ChartControls.tsx"
      provides: "Themed chart controls with factory-accent active states"
  key_links:
    - from: "app/app/globals.css"
      to: "app/components/chart/CandlestickChart.tsx"
      via: "Token hex values hardcoded in JS chart config"
      pattern: "color.*#"
---

<objective>
Re-theme the remaining components: chart controls (with JS hex value updates), wallet components, and the legacy swap page. These are the final component files before the comprehensive verification pass.

Purpose: ChartControls/CandlestickChart require special handling (JS hex values, not just className). Wallet components and legacy page are smaller but still need theming for visual consistency.

Output: 6 fully themed component files covering charts, wallets, and legacy routes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-brand-application/57-CONTEXT.md
@.planning/phases/57-brand-application/57-RESEARCH.md
@.planning/phases/57-brand-application/57-01-SUMMARY.md
@app/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-theme ChartControls and CandlestickChart</name>
  <files>
    app/components/chart/ChartControls.tsx
    app/components/chart/CandlestickChart.tsx
  </files>
  <action>
  **ChartControls.tsx (8 gray/zinc + indigo + green + amber + red):**
  - Pool selector buttons: indigo-600/500 active state -> factory-accent. The active pool button should use bg-factory-accent with appropriate text contrast, inactive buttons use bg-factory-surface
  - Range buttons (1h, 4h, 1d, etc.): indigo-600 active -> factory-accent active, gray inactive -> factory-surface
  - Connection status dot: green-500 -> factory-success (or keep green if it represents connected/disconnected -- semantic)
  - Labels: gray-300/400 -> factory-text-secondary
  - Container: gray/zinc -> factory-surface

  **CandlestickChart.tsx (2 className + hardcoded JS hex - PITFALL 3):**
  - className gray/zinc -> factory-surface, factory-border
  - **JS hex values in chart creation options:** Find the `createChart()` call and its options object. Update:
    - `layout.background.color`: "#0a0a0f" or similar dark -> use the hex value of --color-factory-bg from globals.css
    - `layout.textColor`: gray hex -> use hex value of --color-factory-text-secondary
    - `grid.vertLines.color` and `grid.horzLines.color`: dark gray hex -> use hex value of --color-factory-border-subtle
    - `crosshair.vertLine.color` and `crosshair.horzLine.color`: -> factory-border hex value
    - Candle up/down colors: Keep green/red for candle semantics but warm-shift them to match factory-success/factory-error hex values
    - Volume colors if present: same warm-shifted semantic colors
  - These must be hardcoded hex strings matching the token values (NOT CSS variable reads via getComputedStyle -- per research recommendation, simplicity over dynamism)
  - Add a comment: `// Chart colors: hardcoded hex values matching @theme tokens in globals.css`

  **IMPORTANT:** The chart hex values MUST match the current @theme token hex values from Plan 01. Read globals.css to get the exact hex values before writing them into the chart config.
  </action>
  <verify>
  1. grep -n "gray-\|zinc-\|indigo-" ChartControls.tsx -- zero results
  2. grep -n "gray-\|zinc-" CandlestickChart.tsx -- zero results in className
  3. grep "Chart colors.*hardcoded\|matching.*@theme" CandlestickChart.tsx -- confirms the documentation comment
  4. Verify chart hex values correspond to globals.css token values (manual comparison)
  </verify>
  <done>ChartControls uses factory-accent for active states, CandlestickChart JS hex values match the factory palette tokens</done>
</task>

<task type="auto">
  <name>Task 2: Re-theme wallet components and legacy swap page</name>
  <files>
    app/components/wallet/ConnectModal.tsx
    app/components/wallet/WalletButton.tsx
    app/components/wallet/BalanceDisplay.tsx
    app/app/swap/page.tsx
  </files>
  <action>
  **ConnectModal.tsx (16 gray/zinc):**
  - First check: is this file still imported anywhere active? `grep -rn "ConnectModal" app/` (excluding itself). If only imported by swap/page.tsx (legacy), still theme it for consistency.
  - Modal container: bg-gray-*/bg-zinc-* -> bg-factory-surface
  - Modal border/frame: gray/zinc -> factory-border
  - Wallet option buttons: bg-gray-800/zinc-800 hover states -> bg-factory-surface-elevated, add brass-button class if appropriate
  - Text: white/gray-100 -> factory-text, gray-400 -> factory-text-secondary
  - Close button: gray -> factory-text-muted / hover factory-text
  - Overlay/backdrop: if custom (not using ModalShell), keep functionally equivalent

  **WalletButton.tsx (6 gray/zinc):**
  - Button container: bg-gray-*/bg-zinc-* -> bg-factory-surface-elevated, border -> factory-border
  - Text: gray/zinc -> factory-text
  - Connected indicator: if green dot, -> factory-success
  - Address display: text-gray-300 -> text-factory-text-secondary
  - Consider adding brass-button class for the outer button appearance

  **BalanceDisplay.tsx (10 gray/zinc):**
  - Balance cards: bg-gray-*/bg-zinc-* -> bg-factory-surface
  - Token name labels: text-gray-400 -> text-factory-text-muted
  - Balance values: text-white/gray-100 -> text-factory-text
  - Error state: red-* -> factory-error / factory-error-text
  - Card borders: gray/zinc -> factory-border-subtle
  - Loading skeletons: bg-gray-800 animate-pulse -> bg-factory-surface-elevated animate-pulse

  **swap/page.tsx (3 gray/zinc -- PITFALL 4):**
  - Outer wrapper: bg-gray-950 -> bg-factory-bg
  - Text: text-gray-100 -> text-factory-text
  - Any remaining gray -> factory equivalent
  - Only 3 replacements, minimal effort, prevents jarring mismatch

  **IMPORTANT:** These are lower-priority components (wallet button may be hidden behind scene, ConnectModal may be legacy). Theme them for completeness but don't spend extra time on perfect styling -- just class replacements.
  </action>
  <verify>
  1. grep -n "gray-\|zinc-\|blue-" in all four files -- zero results
  2. grep -c "factory-" in ConnectModal.tsx -- shows meaningful factory class usage
  </verify>
  <done>ConnectModal, WalletButton, BalanceDisplay, and swap/page.tsx use factory-* classes with zero gray/zinc remnants</done>
</task>

</tasks>

<verification>
1. `grep -rn "gray-\|zinc-\|indigo-" app/components/chart/ app/components/wallet/ app/app/swap/page.tsx` returns no results
2. CandlestickChart.tsx JS hex values documented with comment about matching @theme tokens
3. ChartControls active states use factory-accent
4. ConnectModal uses factory-* classes throughout
</verification>

<success_criteria>
All 6 remaining component files use factory-* palette. CandlestickChart JS hex values match the token palette. Zero gray/zinc/blue/indigo/emerald classes remain in the active codebase.
</success_criteria>

<output>
After completion, create `.planning/phases/57-brand-application/57-06-SUMMARY.md`
</output>
