---
phase: 57-brand-application
plan: 05
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - app/components/dashboard/CarnageCard.tsx
  - app/components/dashboard/EpochCard.tsx
  - app/components/dashboard/TaxRatesCard.tsx
  - app/components/dashboard/PoolCard.tsx
  - app/components/dashboard/DashboardGrid.tsx
autonomous: true

must_haves:
  truths:
    - "CarnageCard uses factory-* classes with faction tokens (factory-crime, factory-fraud) for Carnage event indicators"
    - "EpochCard uses factory-* classes with faction tokens for per-token tax rate displays"
    - "TaxRatesCard uses factory-* classes with faction tokens for CRIME/FRAUD rate labels"
    - "PoolCard uses factory-* classes for pool reserve displays with factory-accent for value highlights"
    - "DashboardGrid error banner uses factory-error semantic tokens"
    - "No gray/zinc/blue/emerald classes remain in any dashboard component"
  artifacts:
    - path: "app/components/dashboard/CarnageCard.tsx"
      provides: "Fully themed Carnage dashboard with faction-colored indicators"
    - path: "app/components/dashboard/PoolCard.tsx"
      provides: "Themed pool reserves display"
  key_links:
    - from: "app/app/globals.css"
      to: "app/components/dashboard/CarnageCard.tsx"
      via: "factory-crime and factory-fraud faction tokens"
      pattern: "factory-crime|factory-fraud"
---

<objective>
Re-theme all 5 dashboard card components. CarnageCard is the largest single component with 28 gray/zinc occurrences. These cards render inside CarnageStation and potentially in the trading terminal.

Purpose: Dashboard cards show real-time protocol data (Carnage events, epoch status, tax rates, pool reserves). Faction colors (CRIME=red, FRAUD=amber) are semantically important here -- users associate these colors with specific tokens.

Output: 5 fully themed dashboard component files with faction tokens and zero residual gray/zinc/blue/emerald classes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-brand-application/57-CONTEXT.md
@.planning/phases/57-brand-application/57-RESEARCH.md
@.planning/phases/57-brand-application/57-01-SUMMARY.md
@app/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-theme CarnageCard and EpochCard</name>
  <files>
    app/components/dashboard/CarnageCard.tsx
    app/components/dashboard/EpochCard.tsx
  </files>
  <action>
  **CarnageCard.tsx (28 gray/zinc + faction colors):**
  This is the largest single file to re-theme. Work systematically from top to bottom:
  - Card container: bg-gray-*/bg-zinc-* -> bg-factory-surface, border -> border-factory-border
  - Section headers: text-white/gray-100 -> text-factory-text
  - Section labels: text-gray-400/zinc-400 -> text-factory-text-muted
  - Value displays: text-white/gray-200 -> text-factory-text
  - Carnage fund SOL balance: Keep emphasized (text-factory-accent or text-factory-text with font-bold)
  - Faction colors -- CRITICAL semantic replacement:
    - CRIME indicators: red-400/red-500 -> text-factory-crime / bg-factory-crime
    - FRAUD indicators: amber-400/amber-500 -> text-factory-fraud / bg-factory-fraud
    - Blue generic: blue-400 -> text-factory-accent
    - Emerald generic: emerald-400 -> text-factory-success or text-factory-accent
  - Background pills/badges: bg-red-900/40 -> bg-factory-crime/20, bg-amber-900/40 -> bg-factory-fraud/20
  - Dividers: border-gray-700/zinc-700 -> border-factory-border-subtle
  - Skeleton/loading states: bg-gray-800 animate-pulse -> bg-factory-surface-elevated animate-pulse

  **EpochCard.tsx (10 gray/zinc + faction colors):**
  - Card container: bg-gray-*/bg-zinc-* -> bg-factory-surface, border -> border-factory-border
  - Epoch number/timer: text-white -> text-factory-text
  - Labels: text-gray-400 -> text-factory-text-muted
  - Per-token tax rate displays: Use faction tokens -- CRIME rate in text-factory-crime, FRAUD rate in text-factory-fraud
  - Amber warning indicators: amber-400/600 -> text-factory-warning
  - Progress bars: bg-gray-800 -> bg-factory-surface-elevated for track, fill color -> factory-accent
  - Dividers: gray-700/zinc-700 -> factory-border-subtle

  **IMPORTANT for both:** These cards use faction colors as identity markers, not status indicators. When you see red/amber in the context of CRIME/FRAUD tokens, use factory-crime/factory-fraud. When you see them in the context of good/bad status, use factory-success/factory-error.
  </action>
  <verify>
  1. grep -n "gray-\|zinc-\|blue-\|red-[0-9]\|amber-[0-9]\|emerald-[0-9]\|green-[0-9]" in both files -- zero results
  2. grep "factory-crime\|factory-fraud" CarnageCard.tsx -- confirms faction tokens used
  3. grep "factory-crime\|factory-fraud" EpochCard.tsx -- confirms faction tokens in tax rate displays
  </verify>
  <done>CarnageCard and EpochCard use faction tokens for CRIME/FRAUD identity colors and factory-* for all backgrounds, text, borders</done>
</task>

<task type="auto">
  <name>Task 2: Re-theme TaxRatesCard, PoolCard, and DashboardGrid</name>
  <files>
    app/components/dashboard/TaxRatesCard.tsx
    app/components/dashboard/PoolCard.tsx
    app/components/dashboard/DashboardGrid.tsx
  </files>
  <action>
  **TaxRatesCard.tsx (6 gray/zinc + faction colors):**
  - Card container: bg-gray-*/bg-zinc-* -> bg-factory-surface, border -> border-factory-border
  - CRIME tax rate label/value: red-400 -> text-factory-crime
  - FRAUD tax rate label/value: amber-400 -> text-factory-fraud
  - Generic labels: text-gray-400 -> text-factory-text-muted
  - Values: text-white/gray-200 -> text-factory-text
  - Borders/dividers: gray -> factory-border-subtle

  **PoolCard.tsx (14 gray/zinc + emerald):**
  - Card container: bg-gray-*/bg-zinc-* -> bg-factory-surface, border -> border-factory-border
  - Pool name/header: text-white/gray-100 -> text-factory-text
  - Reserve labels: text-gray-400/zinc-400 -> text-factory-text-muted
  - Reserve values: text-white -> text-factory-text
  - Emerald price/value highlights: emerald-500/400 -> text-factory-accent (pool values are financial data, brass accent fits)
  - Section dividers: gray/zinc -> factory-border-subtle
  - Background sections: bg-gray-800/bg-zinc-800 -> bg-factory-surface-elevated
  - If pool cards display token names (CRIME/FRAUD/PROFIT), use faction tokens for the token name coloring

  **DashboardGrid.tsx (2 gray/zinc):**
  - Error banner: bg-red-900/40 border-red-600 text-red-200 -> bg-factory-error-surface border-factory-error-border text-factory-error-text
  - Container background: if gray -> factory-bg

  **IMPORTANT:** PoolCard may have inline conditionals for different pool types. Preserve the conditional logic, only change the color class values.
  </action>
  <verify>
  1. grep -n "gray-\|zinc-\|blue-\|emerald-[0-9]\|red-[0-9]\|amber-[0-9]" in all three files -- zero results
  2. grep "factory-crime\|factory-fraud" TaxRatesCard.tsx -- confirms faction tokens
  3. grep -c "factory-" PoolCard.tsx -- shows meaningful factory class usage (should be 14+)
  </verify>
  <done>TaxRatesCard uses faction tokens, PoolCard uses factory-accent for value highlights, DashboardGrid error banner uses semantic tokens</done>
</task>

</tasks>

<verification>
1. `grep -rn "gray-\|zinc-\|blue-\|emerald-" app/components/dashboard/` returns no results across all 5 files
2. Faction tokens (factory-crime, factory-fraud) used for CRIME/FRAUD identity colors in CarnageCard, EpochCard, TaxRatesCard
3. Factory-accent used for financial value highlights in PoolCard
4. Semantic error tokens used for DashboardGrid error banner
</verification>

<success_criteria>
All 5 dashboard components use exclusively factory-* palette classes with faction tokens for CRIME/FRAUD identity. Zero gray/zinc/blue/emerald classes remain.
</success_criteria>

<output>
After completion, create `.planning/phases/57-brand-application/57-05-SUMMARY.md`
</output>
