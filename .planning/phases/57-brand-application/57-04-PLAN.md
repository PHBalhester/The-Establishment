---
phase: 57-brand-application
plan: 04
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - app/components/staking/StakingForm.tsx
  - app/components/staking/StakeTab.tsx
  - app/components/staking/UnstakeTab.tsx
  - app/components/staking/ClaimTab.tsx
  - app/components/staking/StakingStats.tsx
  - app/components/staking/StakingStatus.tsx
autonomous: true

must_haves:
  truths:
    - "StakingForm tab buttons use .lever-tab class with data-state attribute for active/inactive mechanical switch appearance"
    - "StakeTab and UnstakeTab use .brass-input for amount fields and .brass-button for Max button"
    - "ClaimTab reward amounts use factory-success or factory-profit color (warm green)"
    - "StakingStats grid uses factory-* for all labels and values"
    - "StakingStatus uses semantic tokens for transaction lifecycle banners"
    - "No gray/zinc/blue classes remain in any staking component"
  artifacts:
    - path: "app/components/staking/StakingForm.tsx"
      provides: "Lever-tab staking interface with mechanical switch tabs"
    - path: "app/components/staking/StakeTab.tsx"
      provides: "Brass-input staking amount field"
  key_links:
    - from: "app/app/globals.css"
      to: "app/components/staking/StakingForm.tsx"
      via: ".lever-tab CSS class with data-state attribute"
      pattern: "lever-tab.*data-state"
---

<objective>
Re-theme all 6 staking components with the steampunk palette. The standout visual change is converting the stake/unstake/claim tabs into mechanical lever switches.

Purpose: The staking interface is the second-most-used feature. Lever-tab styling per CONTEXT.md decisions gives it the immersive factory feel.

Output: 6 fully themed staking component files with lever tabs, brass inputs, and zero residual gray/zinc/blue classes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-brand-application/57-CONTEXT.md
@.planning/phases/57-brand-application/57-RESEARCH.md
@.planning/phases/57-brand-application/57-01-SUMMARY.md
@app/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-theme StakingForm tabs, StakeTab, and UnstakeTab</name>
  <files>
    app/components/staking/StakingForm.tsx
    app/components/staking/StakeTab.tsx
    app/components/staking/UnstakeTab.tsx
  </files>
  <action>
  **StakingForm.tsx (4 gray/zinc + blue tab active states):**
  - Tab container: bg-gray-*/bg-zinc-* -> bg-factory-surface
  - Tab buttons (Stake, Unstake, Claim): Replace current blue-600/20 active + border-blue-500 styling with the .lever-tab CSS class. Add `data-state="active"` when selected and `data-state="inactive"` when not.
    - Currently these likely use conditional className like `tab === 'stake' ? 'bg-blue-600/20 border-blue-500 text-blue-200' : 'bg-gray-800 text-gray-400'`
    - Replace with: `lever-tab ${tab === 'stake' ? '' : ''}` and set `data-state={tab === 'stake' ? 'active' : 'inactive'}` as an attribute. The lever-tab CSS class handles all visual states via the data-state selector.
    - Remove ALL inline color classes from tab buttons -- the lever-tab class handles everything
  - Tab panel container: gray/zinc -> factory-surface or factory-bg
  - Any remaining gray/zinc text -> factory-text-secondary

  **StakeTab.tsx (6 gray/zinc + blue-400 Max button):**
  - Amount input field: Replace bg/border/text classes with `brass-input` class. Remove any inline padding/rounding classes that conflict with brass-input defaults.
  - Max button: Replace blue-400 text with factory-accent. Consider adding `brass-button` class for consistent beveled look.
  - Balance label: gray text -> factory-text-muted
  - Card/container backgrounds: gray/zinc -> factory-surface
  - All text: gray/zinc -> factory-text / factory-text-secondary

  **UnstakeTab.tsx (6 gray/zinc + blue-400 Max + amber warning):**
  - Same input field and Max button treatment as StakeTab
  - Warning message (minimum stake): amber-* -> factory-warning / factory-warning-text / factory-warning-surface
  - If warning uses bg-amber-900/40 border-amber-600 text-amber-200 pattern, replace with bg-factory-warning-surface border-factory-warning-border text-factory-warning-text
  - All remaining gray/zinc -> factory-* equivalents

  **IMPORTANT:** The lever-tab conversion in StakingForm is the key visual change. Ensure the data-state attribute is wired to the existing tab selection state. Do NOT introduce new state -- reuse whatever state variable already controls tab selection.
  </action>
  <verify>
  1. grep -n "gray-\|zinc-\|blue-\|amber-[0-9]" in all three files -- zero results
  2. grep "lever-tab" StakingForm.tsx -- confirms lever tab class usage
  3. grep "data-state" StakingForm.tsx -- confirms data-state attribute on tab buttons
  4. grep "brass-input" StakeTab.tsx UnstakeTab.tsx -- confirms brass input usage
  </verify>
  <done>StakingForm uses lever-tab class with data-state for mechanical switch tabs, StakeTab and UnstakeTab use brass-input for amount fields</done>
</task>

<task type="auto">
  <name>Task 2: Re-theme ClaimTab, StakingStats, and StakingStatus</name>
  <files>
    app/components/staking/ClaimTab.tsx
    app/components/staking/StakingStats.tsx
    app/components/staking/StakingStatus.tsx
  </files>
  <action>
  **ClaimTab.tsx (7 gray/zinc + green-300 rewards):**
  - Reward amount display: green-300 -> text-factory-success (or text-factory-profit -- rewards are PROFIT/SOL, so success semantic is appropriate)
  - Expandable details panel: gray/zinc bg -> factory-surface, border -> factory-border-subtle
  - Labels: gray text -> factory-text-secondary / factory-text-muted
  - Claim button: If it uses a standard button (not BigRedButton), add brass-button class
  - Container: gray/zinc -> factory-surface

  **StakingStats.tsx (5 gray/zinc):**
  - Stats grid container: bg-gray/zinc -> bg-factory-surface
  - Stat labels (APY, Total Staked, etc.): text-gray-400/text-zinc-400 -> text-factory-text-muted
  - Stat values: text-white/gray-100 -> text-factory-text
  - Borders/dividers: gray/zinc -> factory-border-subtle
  - Any accent values (APY percentage): -> text-factory-accent for emphasis

  **StakingStatus.tsx (5 gray/zinc + status colors):**
  - Same semantic banner pattern as SwapStatus:
    - Success: bg-factory-success-surface, border-factory-success-border, text-factory-success-text
    - Error: bg-factory-error-surface, border-factory-error-border, text-factory-error-text
    - Pending/loading: bg-factory-surface, border-factory-border, text-factory-text-secondary
    - Blue confirming: -> factory-accent
  - Container backgrounds: gray/zinc -> factory-surface

  **IMPORTANT:** Do not modify any hook calls, state management, or prop interfaces. className-only changes.
  </action>
  <verify>
  1. grep -n "gray-\|zinc-\|blue-\|green-[0-9]" in all three files -- zero results
  2. grep -c "factory-" in each file -- shows meaningful factory class usage
  </verify>
  <done>ClaimTab uses factory-success for reward amounts, StakingStats uses factory-* throughout, StakingStatus uses semantic banner tokens</done>
</task>

</tasks>

<verification>
1. `grep -rn "gray-\|zinc-\|blue-" app/components/staking/` returns no results across all 6 files
2. StakingForm.tsx has lever-tab class on tab buttons with data-state attributes
3. StakeTab.tsx and UnstakeTab.tsx have brass-input class on amount fields
4. StakingStatus.tsx uses factory-success-surface, factory-error-surface tokens
</verification>

<success_criteria>
All 6 staking components use exclusively factory-* palette classes. Tabs have mechanical lever-switch appearance via .lever-tab. Inputs have brass-rimmed appearance via .brass-input. Zero gray/zinc/blue classes remain.
</success_criteria>

<output>
After completion, create `.planning/phases/57-brand-application/57-04-SUMMARY.md`
</output>
