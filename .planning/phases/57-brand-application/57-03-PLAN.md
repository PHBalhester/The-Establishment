---
phase: 57-brand-application
plan: 03
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - app/components/swap/RouteCard.tsx
  - app/components/swap/RouteSelector.tsx
  - app/components/swap/MultiHopStatus.tsx
  - app/components/station/SwapStation.tsx
  - app/components/station/SwapStatsBar.tsx
autonomous: true

must_haves:
  truths:
    - "RouteCard uses factory-* classes and faction tokens for TOKEN_COLORS object"
    - "RouteSelector uses factory-border for SVG strokes and factory-* for backgrounds"
    - "MultiHopStatus uses semantic tokens for step status indicators"
    - "SwapStation wrapper uses factory-* background"
    - "SwapStatsBar stats grid uses factory-* for all labels, values, and dividers"
  artifacts:
    - path: "app/components/swap/RouteCard.tsx"
      provides: "Themed route cards with faction-colored token indicators"
    - path: "app/components/station/SwapStatsBar.tsx"
      provides: "Themed stats bar with factory palette"
  key_links:
    - from: "app/app/globals.css"
      to: "app/components/swap/RouteCard.tsx"
      via: "factory-crime/fraud/profit token utilities for TOKEN_COLORS"
      pattern: "factory-crime|factory-fraud|factory-profit"
---

<objective>
Re-theme the swap routing components and station wrappers. These handle route display, multi-hop progress, and the swap station layout including the stats bar.

Purpose: Complete the swap station theming (Plan 02 handles form inputs, this plan handles route display and station chrome). TOKEN_COLORS and SVG strokes in these files require special attention beyond simple className swaps.

Output: 5 fully themed routing/station component files with zero residual gray/zinc/blue classes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-brand-application/57-CONTEXT.md
@.planning/phases/57-brand-application/57-RESEARCH.md
@.planning/phases/57-brand-application/57-01-SUMMARY.md
@app/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-theme RouteCard, RouteSelector, and MultiHopStatus</name>
  <files>
    app/components/swap/RouteCard.tsx
    app/components/swap/RouteSelector.tsx
    app/components/swap/MultiHopStatus.tsx
  </files>
  <action>
  **RouteCard.tsx (8 gray/zinc + TOKEN_COLORS object):**
  - Card backgrounds: bg-gray-*/bg-zinc-* -> bg-factory-surface
  - Borders: border-gray-*/border-zinc-* -> border-factory-border, selected state: border-blue-500 -> border-factory-accent
  - Text: gray-*/zinc-* -> factory-text / factory-text-secondary / factory-text-muted
  - **TOKEN_COLORS object (PITFALL 7):** This TypeScript object maps token names to Tailwind classes. Update the values:
    - CRIME-related: text-red-400 -> text-factory-crime
    - FRAUD-related: text-amber-400 -> text-factory-fraud
    - PROFIT-related: text-green-400 -> text-factory-profit
    - SOL/generic: text-green-300 or text-blue-400 -> text-factory-accent
  - Any TOKEN_BADGE_COLORS or similar badge/pill objects: update similarly
  - Green success indicators: green-300/400 -> text-factory-success or text-factory-profit (depending on context)

  **RouteSelector.tsx (4 replacements):**
  - SVG stroke colors: gray-700 or similar -> use CSS variable reference or factory-border token color. If SVG strokes use inline style `stroke="..."`, replace the hex value with the factory-border token value from globals.css. If they use Tailwind `stroke-gray-700`, replace with `stroke-factory-border`.
  - Timer ring: gray background -> factory-surface-elevated or factory-border-subtle
  - Route list container: gray/zinc -> factory-surface
  - Text: gray-* -> factory-text-secondary

  **MultiHopStatus.tsx (3 replacements):**
  - Step status indicators: green-* success -> text-factory-success, red-* error -> text-factory-error, amber-* pending -> text-factory-warning, blue-* in-progress -> text-factory-accent
  - Container: gray/zinc -> factory-surface
  - Connecting lines/dots: gray -> factory-border

  **IMPORTANT:** For TSX objects (TOKEN_COLORS), ensure the string values are valid Tailwind class names that correspond to the new tokens defined in globals.css @theme block. The new utility classes (e.g., `text-factory-crime`) are auto-generated by Tailwind v4 from the @theme tokens.
  </action>
  <verify>
  1. grep -n "gray-\|zinc-\|blue-\|red-[0-9]\|amber-[0-9]\|green-[0-9]" across all three files -- zero results
  2. grep "factory-crime\|factory-fraud\|factory-profit" RouteCard.tsx -- confirms faction tokens in use
  </verify>
  <done>RouteCard TOKEN_COLORS uses faction tokens, RouteSelector SVG strokes use factory-border, MultiHopStatus uses semantic status tokens</done>
</task>

<task type="auto">
  <name>Task 2: Re-theme SwapStation and SwapStatsBar</name>
  <files>
    app/components/station/SwapStation.tsx
    app/components/station/SwapStatsBar.tsx
  </files>
  <action>
  **SwapStation.tsx (1 replacement):**
  - Chart wrapper background: gray/zinc -> bg-factory-surface or bg-factory-bg (match the station-content wrapper darkness)
  - If there's any other non-factory class, replace it

  **SwapStatsBar.tsx (8 replacements):**
  - Stats bar container: bg-gray-*/bg-zinc-* -> bg-factory-surface
  - Dividers between stats: border-gray-*/divide-gray-* -> border-factory-border-subtle or divide-factory-border-subtle
  - Stat labels: text-gray-400/text-zinc-400 -> text-factory-text-muted
  - Stat values: text-white/text-gray-100 -> text-factory-text
  - Price change indicators: if green/red for positive/negative, use factory-success/factory-error
  - Container border: gray/zinc -> factory-border

  Both components should look like they belong in the brass-framed modal chrome, using dark factory surfaces with warm gold text and subtle warm borders.
  </action>
  <verify>
  1. grep -n "gray-\|zinc-\|blue-" in both files -- zero results
  2. grep -c "factory-" in SwapStatsBar.tsx -- shows meaningful factory class usage (should be 8+)
  </verify>
  <done>SwapStation and SwapStatsBar use exclusively factory-* palette classes with zero gray/zinc remnants</done>
</task>

</tasks>

<verification>
1. `grep -rn "gray-\|zinc-\|blue-" app/components/swap/RouteCard.tsx app/components/swap/RouteSelector.tsx app/components/swap/MultiHopStatus.tsx app/components/station/SwapStation.tsx app/components/station/SwapStatsBar.tsx` returns no results
2. TOKEN_COLORS object in RouteCard.tsx contains factory-crime, factory-fraud, factory-profit
3. SVG stroke values in RouteSelector.tsx match factory palette
</verification>

<success_criteria>
All 5 routing and station wrapper components use exclusively factory-* palette classes. TOKEN_COLORS uses faction tokens. SVG strokes match the palette. Zero gray/zinc/blue classes remain.
</success_criteria>

<output>
After completion, create `.planning/phases/57-brand-application/57-03-SUMMARY.md`
</output>
