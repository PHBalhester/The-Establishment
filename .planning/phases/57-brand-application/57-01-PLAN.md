---
phase: 57-brand-application
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/globals.css
autonomous: true

must_haves:
  truths:
    - "All factory-* token values are refined toward warmer mahogany/wood base tones and brighter polished brass accents"
    - "New semantic tokens (success/error/warning surfaces/borders/text) produce Tailwind utility classes"
    - "New interactive tokens (active, active-surface) replace generic blue accent pattern"
    - "New faction tokens (crime/fraud/profit) formalize per-token color identity"
    - "CSS component classes .brass-input, .lever-tab, .brass-button exist with full interactive states"
    - "Every text/background token pair used in the project meets WCAG AA 4.5:1 contrast ratio"
  artifacts:
    - path: "app/app/globals.css"
      provides: "Refined palette tokens, new semantic/interactive/faction tokens, CSS component classes"
      contains: "--color-factory-active"
  key_links:
    - from: "app/app/globals.css"
      to: "All component files"
      via: "@theme token -> Tailwind utility class generation"
      pattern: "--color-factory-"
---

<objective>
Define the complete steampunk palette and CSS component infrastructure that all subsequent re-theming plans consume.

Purpose: This is the foundation plan -- every component re-theming in Plans 02-06 depends on these token values and CSS classes existing. Getting the palette right HERE prevents cascading contrast failures and rework.

Output: Refined globals.css with ~11 new tokens, updated existing token values, 3 CSS component classes (.brass-input, .lever-tab, .brass-button), and a documented contrast verification table as a code comment.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-brand-application/57-CONTEXT.md
@.planning/phases/57-brand-application/57-RESEARCH.md
@app/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine palette tokens and add new tokens</name>
  <files>app/app/globals.css</files>
  <action>
  In the existing @theme block in globals.css, update and extend the color tokens:

  **Refine existing tokens** (warmer/richer per CONTEXT.md):
  - `--color-factory-bg`: Shift from current value toward warmer mahogany/dark wood tone
  - `--color-factory-surface`: Warmer rich dark wood grain tone
  - `--color-factory-surface-elevated`: Slightly lighter warm brown
  - `--color-factory-accent`: Brighter, more saturated polished brass/gold (beyond current #d4a04a)
  - `--color-factory-success`: Recognizable green with warm shift (not muted olive)
  - `--color-factory-error`: Recognizable red with warm shift (not rusty)
  - `--color-factory-warning`: Distinct amber/golden-amber (currently too close to primary)
  - Leave `--color-factory-glow` at or near #f0c050 (already bright)
  - Review `--color-factory-primary`, `--color-factory-secondary`, `--color-factory-text`, `--color-factory-text-secondary`, `--color-factory-text-muted`, `--color-factory-border`, `--color-factory-border-subtle` -- adjust for warmth while maintaining contrast

  **Add new interactive tokens:**
  - `--color-factory-active`: Active tab/selection indicator (replaces blue-600, brass-gold tone)
  - `--color-factory-active-surface`: Active element background (replaces blue-600/20, dark brass tint)

  **Add new semantic surface/border/text tokens:**
  - `--color-factory-success-surface`: Success banner bg (warm dark green)
  - `--color-factory-success-border`: Success banner border (warm medium green)
  - `--color-factory-success-text`: Success banner text (warm light green)
  - `--color-factory-error-surface`: Error banner bg (warm dark red)
  - `--color-factory-error-border`: Error banner border (warm medium red)
  - `--color-factory-error-text`: Error banner text (warm light red)
  - `--color-factory-warning-surface`: Warning banner bg (warm dark amber)
  - `--color-factory-warning-border`: Warning banner border (warm medium amber)
  - `--color-factory-warning-text`: Warning banner text (warm light amber)

  **Add faction identity tokens:**
  - `--color-factory-crime`: CRIME faction color (warm red, replaces text-red-400 in token displays)
  - `--color-factory-fraud`: FRAUD faction color (warm amber, replaces text-amber-400)
  - `--color-factory-profit`: PROFIT token color (warm green, replaces text-green-400)

  **CRITICAL: Contrast verification**
  After defining all values, compute WCAG contrast ratios for every text/background pair using the W3C formula from the research doc. Every text token on its expected background must meet 4.5:1 AA. Add a comment block above the @theme section documenting the contrast matrix with computed ratios.

  Pairs to verify (at minimum):
  - text on bg: factory-text on factory-bg (primary readability)
  - text on surface: factory-text on factory-surface
  - text on elevated: factory-text on factory-surface-elevated
  - secondary text: factory-text-secondary on factory-bg, factory-surface, factory-surface-elevated
  - muted text: factory-text-muted on factory-bg, factory-surface (4.5:1 or document exception for large text)
  - accent text: factory-accent on factory-bg, factory-surface
  - semantic text: factory-success-text on factory-success-surface, factory-error-text on factory-error-surface, factory-warning-text on factory-warning-surface
  - faction text: factory-crime on factory-surface, factory-fraud on factory-surface, factory-profit on factory-surface

  **PITFALL 1 from research:** After changing any token value, visually reason about impact on existing themed components (modal chrome uses factory-accent, toast uses factory-accent, focus ring uses factory-glow). If a value change would break existing visual harmony, adjust carefully.
  </action>
  <verify>
  1. grep for all new token names in globals.css to confirm they exist: `--color-factory-active`, `--color-factory-success-surface`, `--color-factory-crime`, etc.
  2. The contrast verification comment block exists with computed ratios for all pairs
  3. All ratios meet 4.5:1 minimum (or 3:1 for documented large-text exceptions)
  </verify>
  <done>All existing tokens refined to warmer values, all new tokens defined with passing contrast ratios, contrast matrix documented in code comment</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS component classes for steampunk form elements</name>
  <files>app/app/globals.css</files>
  <action>
  Add three new CSS component classes to globals.css (after existing component classes like .big-red-button, .toast-popover, etc.):

  **1. `.brass-input`** -- Steampunk form input (text fields, number inputs, selects)
  Per research code example: factory-surface background, 2px factory-border, inner shadow for recessed gauge look, brass glow on focus, faded on disabled. Include ::placeholder styling with factory-text-muted.

  **2. `.lever-tab`** -- Mechanical lever/switch tab
  Per research code example: gradient surface-elevated to surface, factory-border, raised box-shadow (highlight top, shadow bottom), translateY(0). Hover: brass glow. Active state via `[data-state="active"]`: translateY(2px) pressed-in look with inset shadow and accent border. Disabled: faded/desaturated.

  **3. `.brass-button`** -- Beveled brass secondary button
  Per research code example: gradient surface-elevated to surface, factory-border, beveled box-shadow (highlight top, shadow bottom), brass glow on hover, press-in on active, faded on disabled.

  All three classes MUST:
  - Reference token CSS variables (var(--color-factory-*)), NOT hardcoded hex values
  - Include transition properties for smooth state changes (150ms ease)
  - Include :disabled state with opacity reduction and desaturation
  - Work with the existing dialog :focus-visible gold glow ring (don't override it)

  Add a comment header for the new section: `/* === Brand Application: Component Classes (Phase 57) === */`
  </action>
  <verify>
  1. grep globals.css for `.brass-input`, `.lever-tab`, `.brass-button` -- all three classes exist
  2. grep for `data-state="active"` in the .lever-tab rules
  3. grep for `var(--color-factory-` in all three classes -- no hardcoded hex values in component classes
  4. Run `export PATH="/opt/homebrew/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && npx next build 2>&1 | tail -20` to confirm no CSS syntax errors
  </verify>
  <done>.brass-input, .lever-tab, .brass-button CSS classes exist in globals.css with all interactive states, using only token CSS variables, and Next.js builds without CSS errors</done>
</task>

</tasks>

<verification>
1. All new and refined tokens exist in the @theme block
2. Contrast verification comment block present with 4.5:1+ ratios for all text/background pairs
3. Three CSS component classes (.brass-input, .lever-tab, .brass-button) defined with full interactive states
4. Next.js builds without CSS errors
5. No hardcoded hex values in CSS component classes (only var() references)
</verification>

<success_criteria>
The complete steampunk palette is defined with passing contrast ratios, and CSS component classes are ready for consumption by component re-theming plans 02-06. Running `npx next build` succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/57-brand-application/57-01-SUMMARY.md`
</output>
