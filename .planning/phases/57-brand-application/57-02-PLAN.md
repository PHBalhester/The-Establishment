---
phase: 57-brand-application
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - app/components/swap/SwapForm.tsx
  - app/components/swap/TokenSelector.tsx
  - app/components/swap/SlippageConfig.tsx
  - app/components/swap/FeeBreakdown.tsx
  - app/components/swap/SwapStatus.tsx
  - app/components/swap/RouteBadge.tsx
autonomous: true

must_haves:
  truths:
    - "SwapForm uses .brass-input for all text/number inputs and factory-* classes for all backgrounds, text, and borders"
    - "TokenSelector dropdown uses factory-surface/border/text classes with no gray/zinc remnants"
    - "SlippageConfig uses .lever-tab for preset buttons and .brass-input for custom input"
    - "FeeBreakdown uses factory-* classes with warm-shifted semantic colors for tax indicators"
    - "SwapStatus uses factory-success/error/warning semantic tokens for transaction lifecycle banners"
    - "RouteBadge uses factory-accent instead of blue-600"
  artifacts:
    - path: "app/components/swap/SwapForm.tsx"
      provides: "Fully themed swap form with brass inputs"
    - path: "app/components/swap/SlippageConfig.tsx"
      provides: "Lever-tab slippage presets with brass custom input"
  key_links:
    - from: "app/app/globals.css"
      to: "app/components/swap/SwapForm.tsx"
      via: ".brass-input CSS class + factory-* utility classes"
      pattern: "brass-input|factory-"
---

<objective>
Re-theme the core swap form components -- the primary user-facing trading interface. Replace all gray/zinc/blue classes with factory-* equivalents and apply .brass-input to form fields, .brass-button to secondary buttons.

Purpose: The swap form is the most-used interface in the application. These 6 files contain ~57 gray/zinc/blue occurrences that need systematic replacement.

Output: 6 fully themed swap form component files with zero residual gray/zinc/blue classes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57-brand-application/57-CONTEXT.md
@.planning/phases/57-brand-application/57-RESEARCH.md
@.planning/phases/57-brand-application/57-01-SUMMARY.md
@app/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-theme SwapForm, TokenSelector, and SlippageConfig</name>
  <files>
    app/components/swap/SwapForm.tsx
    app/components/swap/TokenSelector.tsx
    app/components/swap/SlippageConfig.tsx
  </files>
  <action>
  For each file, read the file, identify every gray-*, zinc-*, blue-*, and any other off-palette color class, then replace using the mapping from research:

  **SwapForm.tsx (22+ replacements):**
  - All bg-gray-*/bg-zinc-* -> bg-factory-surface or bg-factory-surface-elevated
  - All text-gray-*/text-zinc-* -> text-factory-text, text-factory-text-secondary, or text-factory-text-muted (based on context)
  - All border-gray-*/border-zinc-* -> border-factory-border or border-factory-border-subtle
  - All blue-* interactive classes -> factory-accent equivalents
  - All text input fields: add `brass-input` class (replace inline bg/border/text classes with the single CSS class)
  - Flip button and other secondary buttons: add `brass-button` class
  - placeholder-gray-* -> remove (handled by .brass-input ::placeholder)
  - Do NOT change any logic, hooks, state, or prop drilling -- className changes ONLY

  **TokenSelector.tsx (8 replacements):**
  - Dropdown container: bg-gray-*/bg-zinc-* -> bg-factory-surface
  - Option items: hover bg -> bg-factory-surface-elevated
  - Selected state: blue-400 -> factory-accent
  - Text colors: gray/zinc -> factory-text / factory-text-secondary
  - Border: gray/zinc -> factory-border

  **SlippageConfig.tsx (12 replacements):**
  - Preset buttons (0.5%, 1%, 3%): Replace current blue-600 active state with .lever-tab class + data-state="active" attribute on the selected preset. Each preset button should have className including `lever-tab` and a `data-state` attribute set to `"active"` when selected and `"inactive"` when not.
  - Custom input: Replace inline styling with .brass-input class
  - Warning text (amber-500): -> text-factory-warning
  - Container backgrounds: gray/zinc -> factory-surface
  - Labels: gray text -> factory-text-secondary

  **IMPORTANT:** Preserve all existing className conditions (e.g., ternary expressions for active/inactive states). Only replace the color class values within those conditionals, not the conditional logic itself.
  </action>
  <verify>
  grep -n "gray-\|zinc-\|blue-" in all three files -- should return zero results (excluding comments)
  </verify>
  <done>SwapForm.tsx, TokenSelector.tsx, and SlippageConfig.tsx use only factory-* classes and CSS component classes, with zero gray/zinc/blue remnants</done>
</task>

<task type="auto">
  <name>Task 2: Re-theme FeeBreakdown, SwapStatus, and RouteBadge</name>
  <files>
    app/components/swap/FeeBreakdown.tsx
    app/components/swap/SwapStatus.tsx
    app/components/swap/RouteBadge.tsx
  </files>
  <action>
  **FeeBreakdown.tsx (10 replacements):**
  - Expandable panel: bg-gray-*/bg-zinc-* -> bg-factory-surface
  - Labels and values: gray text -> factory-text / factory-text-secondary
  - Border: gray/zinc -> factory-border-subtle
  - Tax indicator colors: red-400 -> text-factory-crime (or text-factory-error depending on context -- tax rates use faction identity, errors use semantic). Amber-400 -> text-factory-fraud (or text-factory-warning).
  - Evaluate each red/amber usage: if it represents CRIME/FRAUD faction, use faction tokens. If it represents bad/warning status, use semantic tokens.

  **SwapStatus.tsx (5 replacements):**
  - Status banners must use semantic surface/border/text tokens:
    - Success state: bg-factory-success-surface, border-factory-success-border, text-factory-success-text
    - Error state: bg-factory-error-surface, border-factory-error-border, text-factory-error-text
    - Loading/pending state: bg-factory-surface, border-factory-border, text-factory-text-secondary
    - Blue confirming state: -> factory-accent (or factory-active)
  - Container backgrounds: gray -> factory-surface

  **RouteBadge.tsx (1 replacement):**
  - Badge pill: blue-600 -> bg-factory-accent, ensure text contrast on accent background

  **IMPORTANT:** Do not modify any logic or prop interfaces. className changes only.
  </action>
  <verify>
  grep -n "gray-\|zinc-\|blue-\|red-[0-9]\|amber-[0-9]\|green-[0-9]" in all three files -- should return zero results except for intentional faction/semantic uses documented in code comments
  </verify>
  <done>FeeBreakdown.tsx, SwapStatus.tsx, and RouteBadge.tsx use factory-* classes with appropriate semantic vs faction color assignments</done>
</task>

</tasks>

<verification>
1. `grep -rn "gray-\|zinc-\|blue-" app/components/swap/SwapForm.tsx app/components/swap/TokenSelector.tsx app/components/swap/SlippageConfig.tsx app/components/swap/FeeBreakdown.tsx app/components/swap/SwapStatus.tsx app/components/swap/RouteBadge.tsx` returns no results
2. `grep -c "factory-" app/components/swap/SwapForm.tsx` shows significant factory-* class usage
3. `grep "brass-input" app/components/swap/SwapForm.tsx` confirms brass input class usage
4. `grep "lever-tab" app/components/swap/SlippageConfig.tsx` confirms lever tab class usage
</verification>

<success_criteria>
All 6 swap form components use exclusively factory-* palette classes and CSS component classes. Zero gray/zinc/blue color classes remain. All conditional className logic preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/57-brand-application/57-02-SUMMARY.md`
</output>
