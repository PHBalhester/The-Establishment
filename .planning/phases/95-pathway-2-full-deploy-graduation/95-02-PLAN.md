---
phase: 95-pathway-2-full-deploy-graduation
plan: 02
type: execute
wave: 2
depends_on: ["95-01"]
files_modified:
  - scripts/test/pathway2-verify.ts
  - Docs/pathway2-report.md
requirements:
  - CURVE-07
  - CURVE-08
  - CURVE-09
autonomous: false

must_haves:
  truths:
    - "Both bonding curves filled to capacity (~5.06 SOL each) with mixed buy/sell traffic"
    - "User watched curve filling in real-time via frontend pressure gauges"
    - "Coupled graduation triggered successfully via graduate.ts"
    - "AMM pools seeded with curve proceeds (290M tokens + graduated SOL per pool)"
    - "Conversion vault funded (250M CRIME + 250M FRAUD + 20M PROFIT)"
    - "Tax escrow balances transferred to carnage vault"
    - "Crank can advance at least one epoch with VRF"
    - "Frontend transitioned from launch page to trading interface"
    - "At least one test swap executed via frontend after graduation"
  artifacts:
    - path: "scripts/test/pathway2-verify.ts"
      provides: "Post-graduation verification script"
      min_lines: 100
    - path: "Docs/pathway2-report.md"
      provides: "Formal pathway 2 lifecycle report"
      min_lines: 50
  key_links:
    - from: "scripts/test/pathway2-verify.ts"
      to: "deployments/devnet.json"
      via: "reads addresses for verification checks"
      pattern: "devnet\\.json|deployment"
    - from: "scripts/graduation/graduate.ts"
      to: "deployments/devnet.json"
      via: "reads curve and pool addresses for graduation"
---

<objective>
Fill both bonding curves to capacity, graduate them into AMM pools, verify the full protocol is operational, transition the frontend to live trading mode, and produce a formal lifecycle report.

Purpose: CURVE-07 (fill curves), CURVE-08 (graduate into AMM), and CURVE-09 (post-graduation transition). This is the "it works!" moment -- proving the entire protocol lifecycle from curve launch to live trading.

Output: Both curves graduated, AMM pools seeded, crank running, frontend live, test swap completed, formal report at `Docs/pathway2-report.md`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-pathway-2-full-deploy-graduation/95-CONTEXT.md
@.planning/phases/95-pathway-2-full-deploy-graduation/95-RESEARCH.md
@.planning/phases/95-pathway-2-full-deploy-graduation/95-01-SUMMARY.md
@scripts/graduation/graduate.ts
@scripts/test/pathway2-fill.ts
@deployments/devnet.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Post-Graduation Verification Script</name>
  <files>scripts/test/pathway2-verify.ts</files>
  <action>
Create `scripts/test/pathway2-verify.ts` that runs after graduation to verify the entire protocol is operational.

The script reads addresses from `deployments/devnet.json` and performs these checks, printing PASS/FAIL for each:

1. **Curve status**: Both CRIME and FRAUD curves show `graduated: true` in their on-chain state
2. **AMM pool reserves**: Both CRIME/SOL and FRAUD/SOL pools have ~290M tokens and ~5 SOL each (read PoolState bytes at offset 137/145 like read_pool_reserves pattern). Allow 10% tolerance on SOL amount since sells during filling reduce the total.
3. **Conversion vault funded**: Vault token accounts hold 250M CRIME + 250M FRAUD + 20M PROFIT (read token account balances)
4. **Tax escrow drained**: Both curve tax escrow accounts have only rent-exempt minimum SOL (tax was transferred to carnage vault during graduation)
5. **Crank test**: Attempt to advance one epoch with VRF -- call the epoch program's advance instruction. If it succeeds, the full VRF loop works. If it fails with "epoch not ready" that's also acceptable (means timing constraint, not a broken protocol).
6. **Frontend accessible**: Make an HTTP request to the Railway URL and verify it returns 200

Print a summary table at the end:
```
=== Pathway 2 Verification Report ===
Curve CRIME graduated:     PASS
Curve FRAUD graduated:     PASS
AMM CRIME/SOL reserves:    PASS (289.5M tokens, 4.92 SOL)
AMM FRAUD/SOL reserves:    PASS (290M tokens, 5.01 SOL)
Conversion vault funded:   PASS
Tax escrow drained:        PASS
Crank epoch advance:       PASS (epoch 1 -> 2)
Frontend accessible:       PASS
================================
Result: 8/8 PASSED
```

Follow the patterns from `scripts/deploy/verify.ts` for connection setup, address loading, and on-chain reads.

Skip the crank VRF test if VRF gateway is down (log SKIP with reason, don't fail the whole script).
  </action>
  <verify>
- `cat scripts/test/pathway2-verify.ts | wc -l` shows 100+ lines
- TypeScript compiles without errors
  </verify>
  <done>Post-graduation verification script created that checks curve graduation, AMM pool reserves, conversion vault funding, tax escrow drainage, crank operability, and frontend accessibility.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fill Curves + Graduate + Verify + Transition</name>
  <what-built>
Claude has prepared everything for the full lifecycle test:
- Fresh clean-room deployment (Plan 01)
- Fill script at `scripts/test/pathway2-fill.ts`
- Verification script at `scripts/test/pathway2-verify.ts`
- Frontend polling at 1s for recording

Now the user executes the hands-on lifecycle sequence.
  </what-built>
  <how-to-verify>
**Before starting, ensure:**
- Railway crank is STOPPED (prevents interference with old addresses)
- Railway env vars updated with values from Plan 01
- Railway redeployed with new constants (NEXT_PUBLIC_SITE_MODE=launch)
- Devnet wallet has sufficient SOL

**Step 1: Manual UX Test (2 min)**
1. Visit Railway URL (should show launch page with bonding curves)
2. Connect your wallet
3. Buy 0.1 SOL of CRIME on the curve -- verify price, balance updates, gauge moves
4. Buy 0.1 SOL of FRAUD on the curve -- verify same

**Step 2: Start Screen Recording**

**Step 3: Run Fill Script (~30 seconds)**
```bash
export PATH="/opt/homebrew/bin:$PATH"
cd /Users/mlbob/Projects/Dr\ Fraudsworth
set -a && source .env.devnet && set +a
npx tsx scripts/test/pathway2-fill.ts
```
Watch the frontend gauges fill in real-time. Both should reach 100%.

**Step 4: Verify Curves Full**
Both pressure gauges on the launch page should show 100% filled. The "Graduate" option should appear (or curves show "ready to graduate").

**Step 5: Run Graduation**
```bash
rm -f scripts/graduation/graduation-state.json
set -a && source .env.devnet && set +a
npx tsx scripts/graduation/graduate.ts
```
All 11 steps should complete (coupled graduation, AMM pool seeding, conversion vault, tax escrow distribution).

**Step 6: Run Verification**
```bash
npx tsx scripts/test/pathway2-verify.ts
```
All checks should PASS.

**Step 7: Start Crank on Railway**
Update Railway env vars with new deployment addresses, start the crank service.

**Step 8: Toggle SITE_MODE**
On Railway dashboard: change `NEXT_PUBLIC_SITE_MODE` from `launch` to `live`. Trigger redeploy (not just restart).

**Step 9: Test Swap**
Visit Railway URL (should now show trading interface). Execute one CRIME/SOL buy via the frontend. Verify the swap completes and tax distribution works.

**Step 10: Check /launch page**
Visit `{railway-url}/launch` -- should show graduated banner with curve display as historical record.
  </how-to-verify>
  <resume-signal>Type "approved" with results (or describe any issues encountered). Include: fill script output summary, graduation log, verification results, test swap TX signature.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write Pathway 2 Report + Revert Polling</name>
  <files>Docs/pathway2-report.md, app/hooks/useCurveState.ts</files>
  <action>
**Part A: Write `Docs/pathway2-report.md`**

Create a formal lifecycle report documenting the complete Pathway 2 test. Use `Docs/pathway1-report.md` as a format reference.

The report should include:

1. **Header**: Date, deployer wallet, cluster, deployment summary
2. **Deploy Phase**: All 7 program IDs (from deployment.json), 3 mint addresses, deploy timestamp
3. **Curve Fill Phase**:
   - Fill script configuration (wallet count, SOL amounts, timing)
   - Fill duration (from user's checkpoint results)
   - Total SOL deposited per curve
   - Total tokens distributed
4. **Graduation Phase**:
   - Graduation timestamp
   - AMM pool addresses and reserves (from verify script output)
   - Conversion vault balances
   - Tax escrow final state
5. **Post-Graduation Verification**:
   - Full verification results table (PASS/FAIL for each check)
   - Crank epoch test result
   - Frontend transition confirmation
6. **Test Swap**:
   - TX signature of the post-graduation test swap
   - Confirmation that tax distribution worked
7. **Summary**: Pass/fail checklist mapping to CURVE-06 through CURVE-09

Use data from the user's checkpoint response (Task 2 results) to populate the report with real values.

**Part B: Revert frontend polling**

In `app/hooks/useCurveState.ts`, change the polling interval back from 1_000ms to 5_000ms. Remove the temporary comment about screen recording.
  </action>
  <verify>
- `cat Docs/pathway2-report.md | wc -l` shows 50+ lines
- Report contains all 7 program IDs from deployment.json
- Report contains PASS/FAIL verification results
- `grep -n "5_000\|5000" app/hooks/useCurveState.ts` shows reverted polling interval
- `grep -c "TEMP.*revert\|screen recording" app/hooks/useCurveState.ts` shows 0 (comment removed)
  </verify>
  <done>Formal pathway 2 report written to Docs/pathway2-report.md with deploy addresses, fill data, graduation results, verification checklist, and test swap evidence. Frontend polling reverted to 5s.</done>
</task>

</tasks>

<verification>
- Both bonding curves graduated (on-chain state shows graduated: true)
- AMM pools have correct reserves (~290M tokens + ~5 SOL each)
- Conversion vault funded with correct token amounts
- Tax escrow drained to rent-exempt minimum
- Crank can advance epochs (or gateway acknowledged as temporarily unavailable)
- Frontend serves trading interface at Railway URL
- /launch page shows graduated banner with historical curve data
- Test swap executed successfully with tax distribution
- Formal report captures all evidence
- Frontend polling reverted to production value
</verification>

<success_criteria>
The complete bonding curve lifecycle has been proven end-to-end: deploy from zero, fill curves with organic traffic, graduate into AMM pools, verify protocol operational, transition to live trading, execute test swap. All evidence captured in Docs/pathway2-report.md. Frontend polling restored to production settings.
</success_criteria>

<output>
After completion, create `.planning/phases/95-pathway-2-full-deploy-graduation/95-02-SUMMARY.md`
</output>
