---
phase: 69-devnet-ecosystem-relaunch
plan: 03
type: execute
wave: 3
depends_on: ["69-02"]
files_modified:
  - shared/constants.ts
  - shared/programs.ts
  - app/idl/amm.json
  - app/idl/conversion_vault.json
  - app/idl/epoch_program.json
  - app/idl/staking.json
  - app/idl/tax_program.json
  - app/idl/transfer_hook.json
autonomous: true

must_haves:
  truths:
    - "shared/constants.ts MINTS match the new mint addresses from pda-manifest.json"
    - "shared/constants.ts DEVNET_PDAS match pda-manifest.json PDA addresses"
    - "shared/constants.ts DEVNET_POOLS match pda-manifest.json pool addresses"
    - "shared/constants.ts DEVNET_POOL_CONFIGS match pda-manifest.json pool + vault addresses"
    - "shared/constants.ts DEVNET_PDAS_EXTENDED match pda-manifest.json extended PDAs"
    - "shared/constants.ts TOKEN_PROGRAM_FOR_MINT keys match new mint addresses"
    - "shared/programs.ts DEVNET_ALT matches the new ALT address from alt-address.json"
    - "All 6 IDL files in app/idl/ are synced from target/idl/"
    - "Next.js production build succeeds (npm run build)"
  artifacts:
    - path: "shared/constants.ts"
      provides: "Updated devnet addresses for frontend"
      contains: "MINTS"
    - path: "shared/programs.ts"
      provides: "Updated ALT address"
      contains: "DEVNET_ALT"
    - path: "app/idl/amm.json"
      provides: "Fresh AMM IDL"
    - path: "app/idl/conversion_vault.json"
      provides: "Fresh Conversion Vault IDL"
  key_links:
    - from: "scripts/deploy/pda-manifest.json"
      to: "shared/constants.ts"
      via: "manual address copy"
      pattern: "new PublicKey"
    - from: "scripts/deploy/alt-address.json"
      to: "shared/programs.ts"
      via: "manual address copy"
      pattern: "DEVNET_ALT"
    - from: "target/idl/*.json"
      to: "app/idl/*.json"
      via: "file copy"
      pattern: "address"
---

<objective>
Update all frontend address constants and IDLs to match the fresh devnet deployment, then verify the Next.js production build succeeds.

Purpose: After deployment, the frontend still has old mint/PDA/pool/ALT addresses hardcoded. Every address in shared/constants.ts, shared/programs.ts, and the IDL files must be updated from the deployment artifacts (pda-manifest.json, alt-address.json, target/idl/). Without this, every frontend transaction will fail with "account not found" errors.

Output: Updated shared/constants.ts with ~20 new addresses, updated shared/programs.ts with new ALT, synced IDLs, successful Next.js build.
</objective>

<execution_context>
@./.claude/agents/gsd-executor.md
</execution_context>

<context>
@.planning/phases/69-devnet-ecosystem-relaunch/69-CONTEXT.md
@.planning/phases/69-devnet-ecosystem-relaunch/69-RESEARCH.md
@shared/constants.ts
@shared/programs.ts
@scripts/deploy/pda-manifest.json
@scripts/deploy/alt-address.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update shared/constants.ts and shared/programs.ts from deployment artifacts</name>
  <files>shared/constants.ts, shared/programs.ts</files>
  <action>
    Read `scripts/deploy/pda-manifest.json` to get all new addresses. Read `scripts/deploy/alt-address.json` to get the new ALT address.

    Update EVERY address section in shared/constants.ts systematically. The Research checklist (section "Address Update Checklist") identifies exactly what to update:

    **shared/constants.ts updates (all from pda-manifest.json):**

    1. `MINTS` object — replace CRIME, FRAUD, PROFIT with new mint addresses from `manifest.mints`

    2. `DEVNET_PDAS` object — replace EpochState, CarnageFund, CarnageSolVault with addresses from `manifest.pdas`

    3. `DEVNET_POOLS` object — replace CRIME_SOL.pool and FRAUD_SOL.pool with addresses from `manifest.pools`

    4. `DEVNET_POOL_CONFIGS` object — replace pool, vaultA, vaultB for both CRIME_SOL and FRAUD_SOL with addresses from `manifest.pools` (pool address) and `manifest.vaults` (vault addresses)

    5. `TOKEN_PROGRAM_FOR_MINT` record — the keys are mint address strings. Since MINTS changed, the keys must be updated to match the new mint base58 strings. The values remain the same (TOKEN_2022_PROGRAM_ID for all 3 custom mints, TOKEN_PROGRAM_ID for NATIVE_MINT). The cleanest approach: the existing code already references `MINTS.CRIME.toBase58()` etc. as keys, so if the MINTS object is updated correctly, this map auto-updates. Verify this is the case — if keys are hardcoded strings instead of `MINTS.X.toBase58()`, update them.

    6. `DEVNET_PDAS_EXTENDED` object — replace SwapAuthority, TaxAuthority, StakePool, EscrowVault, StakeVault, WsolIntermediary with addresses from `manifest.pdas` (extended section)

    **shared/programs.ts update:**

    7. `DEVNET_ALT` — replace with the new ALT address from `scripts/deploy/alt-address.json`

    **IMPORTANT:** Do NOT change PROGRAM_IDS — we reused program keypairs, so program IDs are unchanged.

    **IMPORTANT:** Do NOT change any seed constants, fee constants, timing constants, or routing logic — only addresses.

    **Cross-check:** After updating, verify that every PublicKey in shared/constants.ts matches a value in pda-manifest.json (except PROGRAM_IDS which are unchanged, TREASURY_PUBKEY which is the wallet, and NATIVE_MINT which is the system constant).
  </action>
  <verify>
    - Read shared/constants.ts and confirm MINTS, DEVNET_PDAS, DEVNET_POOLS, DEVNET_POOL_CONFIGS, DEVNET_PDAS_EXTENDED all have new addresses
    - Read shared/programs.ts and confirm DEVNET_ALT is updated
    - Confirm PROGRAM_IDS section is UNCHANGED (same 6 program IDs)
    - Confirm TOKEN_PROGRAM_FOR_MINT uses MINTS.X.toBase58() references (auto-updates with MINTS)
    - Cross-reference 3-5 addresses against pda-manifest.json to verify accuracy
  </verify>
  <done>All ~20 devnet addresses in shared/constants.ts and shared/programs.ts updated from pda-manifest.json and alt-address.json. Program IDs unchanged. No seed/fee/routing logic changed.</done>
</task>

<task type="auto">
  <name>Task 2: Sync IDLs and run Next.js production build</name>
  <files>app/idl/amm.json, app/idl/conversion_vault.json, app/idl/epoch_program.json, app/idl/staking.json, app/idl/tax_program.json, app/idl/transfer_hook.json</files>
  <action>
    **Step 1: Copy fresh IDLs from build output to app/idl/**
    ```bash
    cp target/idl/amm.json app/idl/amm.json
    cp target/idl/conversion_vault.json app/idl/conversion_vault.json
    cp target/idl/epoch_program.json app/idl/epoch_program.json
    cp target/idl/staking.json app/idl/staking.json
    cp target/idl/tax_program.json app/idl/tax_program.json
    cp target/idl/transfer_hook.json app/idl/transfer_hook.json
    ```

    Also check if `app/idl/types/` has generated TypeScript types that need regeneration. If so, copy those too.

    **Step 2: Run the Next.js production build**
    ```bash
    export PATH="/opt/homebrew/bin:$PATH"
    npm run build
    ```

    The build must succeed with 0 errors. Warnings are acceptable.

    **Why this matters:** The IDLs contain the program `address` field used by Anchor Program constructors. Stale IDLs = wrong program addresses = transaction failures. The Next.js build validates that all TypeScript imports resolve and no type errors exist with the new addresses.
  </action>
  <verify>
    - `diff target/idl/amm.json app/idl/amm.json` shows no differences (files are identical)
    - `diff target/idl/conversion_vault.json app/idl/conversion_vault.json` shows no differences
    - `npm run build` exits 0
    - `.next/` directory exists with fresh build output
  </verify>
  <done>All 6 IDL files synced from target/idl/ to app/idl/. Next.js production build succeeds with new addresses.</done>
</task>

</tasks>

<verification>
- shared/constants.ts MINTS addresses differ from the old values (F65o4z, 83gSRt, 8y7Mat)
- shared/programs.ts DEVNET_ALT address differs from old value (4rW2yu8)
- All 6 app/idl/*.json files match target/idl/*.json
- `npm run build` exits 0
</verification>

<success_criteria>
Frontend constants fully updated with fresh deployment addresses. IDLs synced. Next.js production build passes. Frontend is ready for Railway deployment.
</success_criteria>

<output>
After completion, create `.planning/phases/69-devnet-ecosystem-relaunch/69-03-SUMMARY.md`
</output>
