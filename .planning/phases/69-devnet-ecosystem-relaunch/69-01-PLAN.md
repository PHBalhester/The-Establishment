---
phase: 69-devnet-ecosystem-relaunch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy/deploy-all.sh
  - .env
autonomous: true

must_haves:
  truths:
    - "deploy-all.sh passes --devnet to build.sh when targeting devnet cluster"
    - ".env SOL_POOL_SEED_SOL_OVERRIDE is 2500000000 (2.5 SOL)"
    - "Old mint keypairs deleted, forcing fresh mint generation on next initialize"
    - "All 6 programs build successfully with --devnet feature flag"
    - "All Rust tests pass (~280+)"
  artifacts:
    - path: "scripts/deploy/deploy-all.sh"
      provides: "--devnet passthrough to build.sh"
      contains: "build.sh --devnet"
    - path: "target/deploy/amm.so"
      provides: "AMM build artifact"
    - path: "target/deploy/transfer_hook.so"
      provides: "Transfer Hook build artifact"
    - path: "target/deploy/tax_program.so"
      provides: "Tax Program build artifact"
    - path: "target/deploy/epoch_program.so"
      provides: "Epoch Program build artifact"
    - path: "target/deploy/staking.so"
      provides: "Staking build artifact"
    - path: "target/deploy/conversion_vault.so"
      provides: "Conversion Vault build artifact"
  key_links:
    - from: "scripts/deploy/deploy-all.sh"
      to: "scripts/deploy/build.sh"
      via: "--devnet flag passthrough"
      pattern: "build\\.sh.*--devnet"
    - from: ".env"
      to: "scripts/deploy/initialize.ts"
      via: "SOL_POOL_SEED_SOL_OVERRIDE env var"
      pattern: "SOL_POOL_SEED_SOL_OVERRIDE=2500000000"
---

<objective>
Pre-flight preparation and full workspace build for the devnet ecosystem relaunch.

Purpose: Ensure all 6 programs compile cleanly with devnet feature flags, fix the deploy-all.sh --devnet pitfall identified in research, update seed liquidity to match CONTEXT.md decisions (2.5 SOL/pool), and delete stale mint keypairs so fresh mints are generated during initialization.

Output: 6 compiled .so artifacts in target/deploy/, updated deploy-all.sh with --devnet passthrough, updated .env with correct seed amounts, clean mint-keypairs directory ready for fresh generation.
</objective>

<execution_context>
@./.claude/agents/gsd-executor.md
</execution_context>

<context>
@.planning/phases/69-devnet-ecosystem-relaunch/69-CONTEXT.md
@.planning/phases/69-devnet-ecosystem-relaunch/69-RESEARCH.md
@scripts/deploy/deploy-all.sh
@scripts/deploy/build.sh
@.env
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — fix deploy-all.sh, update .env, delete stale keypairs</name>
  <files>scripts/deploy/deploy-all.sh, .env</files>
  <action>
    Three changes:

    1. **Fix deploy-all.sh --devnet passthrough** (Research Pitfall 7):
       Line 77 calls `bash scripts/deploy/build.sh` without passing `--devnet`.
       Add devnet auto-detection: if CLUSTER_URL contains "devnet", automatically append `--devnet` to the build.sh invocation.
       Specifically, change the Phase 1 Build section to:
       ```bash
       BUILD_FLAGS=""
       if echo "$CLUSTER_URL" | grep -q "devnet"; then
         BUILD_FLAGS="--devnet"
         echo "  [devnet cluster detected — will build with --devnet flag]"
       fi
       bash scripts/deploy/build.sh $BUILD_FLAGS
       ```

    2. **Update .env seed liquidity** (Research Pitfall 2):
       Change `SOL_POOL_SEED_SOL_OVERRIDE=25000000000` (25 SOL) to `SOL_POOL_SEED_SOL_OVERRIDE=2500000000` (2.5 SOL).
       The token override `SOL_POOL_SEED_TOKEN_OVERRIDE=290000000000000` (290M) is already correct per CONTEXT.md.

    3. **Delete stale mint keypairs and ALT cache**:
       ```bash
       rm -rf scripts/deploy/mint-keypairs/
       rm -f keypairs/carnage-wsol.json
       rm -f scripts/deploy/alt-address.json
       ```
       This forces initialize.ts to generate fresh mint keypairs and alt-helper.ts to create a new ALT.
       Do NOT delete program keypairs (keypairs/*-keypair.json) — we reuse program IDs.
  </action>
  <verify>
    - `grep -q 'devnet' scripts/deploy/deploy-all.sh` finds the --devnet passthrough logic
    - `grep 'SOL_POOL_SEED_SOL_OVERRIDE' .env` shows 2500000000
    - `ls scripts/deploy/mint-keypairs/ 2>/dev/null` returns empty (directory deleted)
    - `ls keypairs/carnage-wsol.json 2>/dev/null` returns nothing
    - `ls scripts/deploy/alt-address.json 2>/dev/null` returns nothing
    - `ls keypairs/amm-keypair.json` still exists (program keypairs preserved)
  </verify>
  <done>deploy-all.sh auto-detects devnet cluster and passes --devnet to build.sh. .env has correct 2.5 SOL seed amount. Stale mint keypairs, carnage-wsol, and ALT cache deleted. Program keypairs preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Build all 6 programs with --devnet and run Rust tests</name>
  <files></files>
  <action>
    Run the full build with devnet feature flags. This must be done from the project root with proper environment sourcing.

    ```bash
    source "$HOME/.cargo/env"
    export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH"
    export PATH="/opt/homebrew/bin:$PATH"

    # Build all 6 programs (step 0 will SKIP patch since mint-keypairs/ was deleted)
    ./scripts/deploy/build.sh --devnet
    ```

    Note: Step [0/3] (patch-mint-addresses) will SKIP because we deleted the mint-keypairs directory. That's correct — the mint keypairs will be generated during initialization (Plan 02), and then we'll need to rebuild with `--devnet` one more time AFTER mints are created. However, the initial build is still needed to:
    - Verify all 6 programs compile successfully
    - Generate the .so artifacts for deployment
    - Verify program ID consistency

    After build, run the Rust test suite to confirm DBS changes haven't broken anything:

    ```bash
    cargo test --workspace 2>&1
    ```

    Expected: ~280+ tests, 0 failures. If any test fails, stop and report — do NOT proceed to deployment.
  </action>
  <verify>
    - All 6 .so files exist in target/deploy/: amm.so, transfer_hook.so, tax_program.so, epoch_program.so, staking.so, conversion_vault.so
    - Build script exits 0 (program ID consistency verified)
    - `cargo test --workspace` exits 0 with 0 failures
    - Output shows ~280+ tests passing
  </verify>
  <done>All 6 programs build cleanly with --devnet flag. All Rust tests pass. Build artifacts ready for deployment.</done>
</task>

</tasks>

<verification>
- `ls target/deploy/*.so | wc -l` returns 6
- `grep 'SOL_POOL_SEED_SOL_OVERRIDE=2500000000' .env` matches
- `grep -c 'devnet' scripts/deploy/deploy-all.sh` shows the --devnet passthrough logic
- `ls scripts/deploy/mint-keypairs/ 2>/dev/null` shows empty/missing (clean slate for fresh mints)
- All Rust tests pass
</verification>

<success_criteria>
6 program .so artifacts compiled with devnet features. deploy-all.sh fixed to auto-pass --devnet. .env updated to 2.5 SOL/pool. Old mint keypairs deleted. All Rust tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/69-devnet-ecosystem-relaunch/69-01-SUMMARY.md`
</output>
