---
phase: 102-full-devnet-lifecycle-redeploy-and-bonding-curve-graduation-test
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Railway devnet service has all fresh program IDs, mint addresses, and ALT from the new deployment"
    - "Railway frontend is accessible and shows the launch page (SITE_MODE=launch)"
    - "Frontend resolves correct devnet addresses via protocol-config.ts"
    - "Pre-launch state survives across sessions (deliberate pause validated)"
  artifacts: []
  key_links:
    - from: "deployments/devnet.json"
      to: "Railway env vars"
      via: "Manual env var update on Railway dashboard"
    - from: "Railway NEXT_PUBLIC_SITE_MODE=launch"
      to: "app/middleware.ts"
      via: "Middleware reads SITE_MODE and redirects to /launch"
---

<objective>
Update Railway devnet service env vars with fresh deployment addresses and verify the frontend shows the launch page correctly.

Purpose: This simulates the real mainnet pre-launch preparation. The Railway frontend must display the launch page (bonding curve UI) with the correct new deployment addresses. This also validates the "Stage 4 to Stage 5 gap" -- proving the pre-deploy state works across sessions.

Output: Railway devnet frontend showing launch page with correct addresses, ready for Stage 5 bonding curve deployment.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-full-devnet-lifecycle-redeploy-and-bonding-curve-graduation-test/102-CONTEXT.md
@.planning/phases/102-full-devnet-lifecycle-redeploy-and-bonding-curve-graduation-test/102-RESEARCH.md
@.planning/phases/102-full-devnet-lifecycle-redeploy-and-bonding-curve-graduation-test/102-01-SUMMARY.md
@app/lib/protocol-config.ts
@app/middleware.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Update Railway devnet env vars with fresh deployment addresses</name>
  <action>
  The user must update the Railway devnet service environment variables with the fresh addresses from the new deployment. Claude cannot access the Railway dashboard.

  **Railway Env Var Checklist -- Stage 4 Update:**

  The exact values were derived from `deployments/devnet.json` in Plan 01 Task 1 Step 7. Update ALL of the following on the Railway devnet service:

  **Program IDs (7):**
  - `AMM_PROGRAM_ID` -- from Plan 01 output
  - `TRANSFER_HOOK_PROGRAM_ID` -- from Plan 01 output
  - `TAX_PROGRAM_ID` -- from Plan 01 output
  - `EPOCH_PROGRAM_ID` -- from Plan 01 output
  - `STAKING_PROGRAM_ID` -- from Plan 01 output
  - `CONVERSION_VAULT_PROGRAM_ID` -- from Plan 01 output
  - `BONDING_CURVE_PROGRAM_ID` -- from Plan 01 output

  **Mints (3):**
  - `CRIME_MINT` -- from Plan 01 output
  - `FRAUD_MINT` -- from Plan 01 output
  - `PROFIT_MINT` -- from Plan 01 output

  **Infrastructure:**
  - `PROTOCOL_ALT` -- from Plan 01 output (new ALT address)
  - `NEXT_PUBLIC_CLUSTER` = `devnet` (verify, should already be set)
  - `NEXT_PUBLIC_SITE_MODE` = `launch` (set to launch page mode)

  **Verify these are UNCHANGED (should already be correct):**
  - `CLUSTER_URL` -- devnet Helius RPC URL
  - `HELIUS_API_KEY` -- unchanged

  **After updating all env vars:** Trigger a Railway redeploy (Railway should auto-redeploy on env var change, but verify).

  **Also update the Helius devnet webhook** with new program IDs if chart data is desired during testing. Use:
  ```bash
  set -a && source .env.devnet && set +a
  export PATH="/opt/homebrew/bin:$PATH"
  npx tsx scripts/deploy/webhook-manage.ts
  ```
  Or manually update via Helius dashboard with new program IDs.
  </action>
  <instructions>
  1. Go to Railway dashboard -> devnet service -> Variables
  2. Update each env var listed above with the fresh values from Plan 01 output
  3. Verify NEXT_PUBLIC_SITE_MODE is set to "launch"
  4. Trigger redeploy if not automatic
  5. Wait for deploy to complete (~1-2 min)
  6. Optionally: re-register Helius devnet webhook with new program IDs
  7. Type "done" when Railway is redeployed with fresh env vars
  </instructions>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Railway frontend shows launch page with correct addresses</name>
  <what-built>
  Railway devnet service updated with fresh deployment addresses and set to launch mode (SITE_MODE=launch).
  </what-built>
  <how-to-verify>
  1. Visit the Railway devnet frontend URL in your browser
  2. Confirm you see the LAUNCH PAGE (bonding curve UI), NOT the trading interface
  3. Connect your Phantom wallet (set to devnet)
  4. Verify the bonding curve page loads without errors (the curves will show as "not yet live" since Stage 5 hasn't deployed the bonding curve program yet)
  5. Check browser console for any address-related errors (AccountNotFound is EXPECTED at this point since BC program isn't deployed yet)
  6. This validates the "Stage 4 to Stage 5 gap" -- the pre-launch state works correctly

  **This is the deliberate pause from CONTEXT.md** -- simulating the real mainnet gap between infrastructure deployment and bonding curve launch. Take as much time as you want here.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Plan 03 (Stage 5: launch bonding curves + multi-person testing), or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Railway devnet service has all 7 program IDs, 3 mints, and ALT from the fresh deployment
- NEXT_PUBLIC_SITE_MODE is set to "launch"
- Frontend renders the launch page (not trading interface)
- No critical errors in browser console (AccountNotFound for BC is expected pre-Stage-5)
</verification>

<success_criteria>
Railway devnet frontend is live with fresh deployment addresses, showing the launch page. The pre-launch state is validated. Ready for Stage 5 bonding curve deployment and multi-person testing.
</success_criteria>

<output>
After completion, create `.planning/phases/102-full-devnet-lifecycle-redeploy-and-bonding-curve-graduation-test/102-02-SUMMARY.md`
</output>
