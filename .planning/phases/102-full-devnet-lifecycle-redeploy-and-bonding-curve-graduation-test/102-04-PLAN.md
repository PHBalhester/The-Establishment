---
phase: 102-full-devnet-lifecycle-redeploy-and-bonding-curve-graduation-test
plan: 04
type: execute
wave: 4
depends_on: ["102-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Graduation completes all 13 steps successfully (transition, withdraw, pools, whitelist, vault, escrow)"
    - "AMM pools are created and seeded with graduated SOL + tokens"
    - "Railway frontend transitions from launch page to trading interface"
    - "Both users can execute swaps on the trading interface post-graduation"
    - "Crank advances at least 1-2 epochs successfully"
    - "Core protocol features work: swaps, staking, conversion vault"
  artifacts: []
  key_links:
    - from: "stage-6-graduation.sh"
      to: "graduate.ts"
      via: "Stage 6 runs the 13-step graduation orchestrator"
    - from: "Railway NEXT_PUBLIC_SITE_MODE=live"
      to: "app/middleware.ts"
      via: "Middleware reads SITE_MODE and allows access to trading interface"
    - from: "scripts/crank/crank-runner.ts"
      to: "epoch program"
      via: "Crank advances epochs with VRF randomness"
---

<objective>
Execute Stage 6 graduation, switch Railway to live mode, start the crank, and perform light post-graduation E2E testing with both users.

Purpose: Complete the lifecycle by graduating both curves into AMM pools, transitioning the frontend from launch to trading mode, and verifying that the core protocol features (swaps, epochs, staking) work post-graduation. This is the final validation that the full mainnet launch sequence works end-to-end.

Output: Fully graduated protocol on devnet with AMM pools trading, crank running, and both users confirmed working on the trading interface.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-full-devnet-lifecycle-redeploy-and-bonding-curve-graduation-test/102-CONTEXT.md
@.planning/phases/102-full-devnet-lifecycle-redeploy-and-bonding-curve-graduation-test/102-RESEARCH.md
@.planning/phases/102-full-devnet-lifecycle-redeploy-and-bonding-curve-graduation-test/102-01-SUMMARY.md
@.planning/phases/102-full-devnet-lifecycle-redeploy-and-bonding-curve-graduation-test/102-02-SUMMARY.md
@.planning/phases/102-full-devnet-lifecycle-redeploy-and-bonding-curve-graduation-test/102-03-SUMMARY.md
@scripts/deploy/stage-6-graduation.sh
@scripts/graduation/graduate.ts
@scripts/crank/crank-runner.ts
@scripts/e2e/smoke-test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute Stage 6 graduation and start local crank</name>
  <files></files>
  <action>
  **Step 1: Run Stage 6 graduation**

  ```bash
  cd /Users/mlbob/Projects/Dr\ Fraudsworth
  ./scripts/deploy/stage-6-graduation.sh devnet
  ```

  This runs graduate.ts which executes 13 steps:
  1. Verify both curves filled
  2. Transition curves to Graduated status
  3. Withdraw SOL from CRIME curve vault
  4. Withdraw SOL from FRAUD curve vault
  5. Close CRIME curve vault
  6. Close FRAUD curve vault
  7. Create CRIME/SOL AMM pool (seeded with graduated SOL + 290M tokens)
  8. Create FRAUD/SOL AMM pool (seeded with graduated SOL + 290M tokens)
  9. Whitelist CRIME pool vault
  10. Whitelist FRAUD pool vault
  11. Seed conversion vault
  12. Distribute tax escrow
  13. Skip whitelist authority burn (retained for Squads)

  Monitor each step. Expected runtime: ~2-5 minutes.

  NOTE: Pitfall 6 -- stage-6 output may incorrectly say "Whitelist authority burned" but graduate.ts actually SKIPS the burn. This is cosmetic, not functional.

  **Step 2: Verify graduation success**

  ```bash
  set -a && source .env.devnet && set +a
  export PATH="/opt/homebrew/bin:$PATH"
  npx tsx scripts/deploy/verify.ts devnet
  ```

  Verify:
  - Both AMM pools exist and have liquidity
  - Pool vaults are whitelisted
  - Conversion vault is seeded
  - Tax escrow distributed

  **Step 3: Start local crank**

  Start the crank runner locally (simpler than Railway for devnet testing):

  ```bash
  set -a && source .env.devnet && set +a
  export PATH="/opt/homebrew/bin:$PATH"
  export CARNAGE_WSOL_PUBKEY=$(solana-keygen pubkey keypairs/carnage-wsol.json)
  npx tsx scripts/crank/crank-runner.ts &
  ```

  Verify crank starts and attempts epoch advancement. First epoch may take a few minutes due to VRF oracle timing on devnet.

  **Step 4: Derive Stage 6 Railway env var updates**

  Present to user for Railway update:
  - `NEXT_PUBLIC_SITE_MODE=live` (switch from launch page to trading interface)
  - `CARNAGE_WSOL_PUBKEY=<value from keypairs/carnage-wsol.json>`
  - Any pool-specific env vars if needed by crank on Railway

  NOTE: For this devnet test, the crank runs locally so CARNAGE_WSOL_PUBKEY on Railway is only needed if we later switch to Railway crank.
  </action>
  <verify>
  - stage-6-graduation.sh exits with code 0
  - All 13 graduation steps completed (check graduate.ts output)
  - verify.ts confirms pools exist, vaults whitelisted, conversion vault seeded
  - Crank is running locally and attempting epoch advancement
  </verify>
  <done>
  Graduation complete. AMM pools created and seeded. Crank running locally. Ready for Railway site mode switch and post-graduation E2E.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Switch Railway to live mode</name>
  <action>
  Update the Railway devnet service to switch from launch page to trading interface.

  **Railway Env Var Update -- Stage 6:**

  1. Set `NEXT_PUBLIC_SITE_MODE` = `live` (was "launch")
  2. Set `CARNAGE_WSOL_PUBKEY` = the value from Plan 01 (even if crank runs locally, good practice)
  3. Trigger Railway redeploy
  4. Wait for deploy to complete (~1-2 min)
  </action>
  <instructions>
  1. Go to Railway dashboard -> devnet service -> Variables
  2. Change NEXT_PUBLIC_SITE_MODE from "launch" to "live"
  3. Add CARNAGE_WSOL_PUBKEY with the value from Plan 01 output
  4. Wait for Railway to redeploy
  5. Type "done" when Railway is redeployed
  </instructions>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Post-graduation light E2E testing with both users</name>
  <what-built>
  Complete graduated protocol on devnet:
  - Both bonding curves graduated into AMM pools
  - Railway frontend switched to trading interface (live mode)
  - Local crank running, advancing epochs
  - All protocol features should be operational
  </what-built>
  <how-to-verify>
  **Both users should test the following on the Railway devnet frontend:**

  **Core trading (both users):**
  1. Visit the frontend -- verify you see the TRADING INTERFACE (not launch page)
  2. Buy CRIME with SOL (small amount: 0.01-0.05 SOL) -- verify swap completes
  3. Sell CRIME for SOL -- verify swap completes and SOL returns
  4. Buy FRAUD with SOL -- verify swap completes
  5. Sell FRAUD for SOL -- verify swap completes

  **Protocol features (mlbob):**
  6. Try converting CRIME to FRAUD (or vice versa) via conversion vault
  7. Check if PROFIT is available and try staking a small amount
  8. Verify epoch info displays on the frontend (epoch number, tax rates)
  9. Wait for 1-2 epoch transitions -- verify the crank advances epochs and tax rates change

  **Frontend quality:**
  10. Pool reserves and prices display correctly
  11. Transaction history or recent trades show up
  12. No critical errors in browser console
  13. The site mode transition was smooth (launch -> live with no stale UI)

  **This is a LIGHT E2E** -- not the exhaustive Phase 96 checklist. Focus on confirming the core user journey works. If any critical issues are found, describe them.
  </how-to-verify>
  <resume-signal>Type "approved" if the full lifecycle test passes, or describe any issues found during testing</resume-signal>
</task>

</tasks>

<verification>
- Stage 6 graduation completed all 13 steps
- AMM pools created with correct liquidity
- Railway transitioned from launch to live mode
- Both users can execute swaps on the trading interface
- Crank advances epochs
- Core protocol features work (swaps, conversion, epoch display)
- No critical frontend errors
</verification>

<success_criteria>
Full devnet lifecycle validated: Stages 0-6 complete, both bonding curves graduated into AMM pools, Railway site mode transitioned from launch to live, crank running, and both users confirmed the trading interface works. The mainnet launch procedure is proven end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/102-full-devnet-lifecycle-redeploy-and-bonding-curve-graduation-test/102-04-SUMMARY.md`
</output>
