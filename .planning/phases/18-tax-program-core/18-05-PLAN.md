---
phase: 18-tax-program-core
plan: 05
type: execute
wave: 3
depends_on:
  - 18-03
  - 18-04
files_modified:
  - programs/tax-program/tests/test_swap_sol_buy.rs
  - programs/tax-program/tests/test_swap_sol_sell.rs
  - programs/amm/src/constants.rs
autonomous: true

must_haves:
  truths:
    - "Integration test proves swap_sol_buy applies buy tax and distributes correctly"
    - "Integration test proves swap_sol_sell applies sell tax and distributes correctly"
    - "Both CRIME and FRAUD pools are tested"
    - "Tax distribution amounts are verified (75/24/1 split)"
    - "Slippage protection works correctly"
  artifacts:
    - path: "programs/tax-program/tests/test_swap_sol_buy.rs"
      provides: "Integration tests for buy swaps"
      min_lines: 100
    - path: "programs/tax-program/tests/test_swap_sol_sell.rs"
      provides: "Integration tests for sell swaps"
      min_lines: 100
  key_links:
    - from: "programs/tax-program/tests/*.rs"
      to: "litesvm test harness"
      via: "test setup helpers"
      pattern: "LiteSVM"
---

<objective>
Create integration tests for swap_sol_buy and swap_sol_sell instructions.

Purpose: Verify the complete CPI chain works: Tax Program -> AMM -> Token transfers. Tests validate tax calculation, distribution splits, slippage protection, and event emission.

Output: Passing integration tests that prove Phase 18 success criteria.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-tax-program-core/18-CONTEXT.md
@.planning/phases/18-tax-program-core/18-03-SUMMARY.md
@.planning/phases/18-tax-program-core/18-04-SUMMARY.md
@programs/amm/tests/test_swap_sol_pool.rs
@programs/amm/tests/test_cpi_access_control.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AMM TAX_PROGRAM_ID to point to real Tax Program</name>
  <files>
    programs/amm/src/constants.rs
  </files>
  <action>
    Update the TAX_PROGRAM_ID constant in programs/amm/src/constants.rs to use the Tax Program's actual program ID (from Anchor.toml, set in Plan 01).

    This change enables the AMM to accept CPI calls from the real Tax Program instead of the mock.

    ```rust
    // Before (mock):
    // pub const TAX_PROGRAM_ID: Pubkey = pubkey!("J5CK3BiYwiQtt7Yfx3PLNrFr7YWCVGrskXiGvtYBqd5W");

    // After (real Tax Program):
    pub const TAX_PROGRAM_ID: Pubkey = pubkey!("<tax-program-id-from-anchor-toml>");
    ```

    Note: Existing AMM tests that use mock-tax-program will fail after this change. That's expected - those tests validated the CPI pattern; now we're using the real Tax Program.

    Consider: Add a comment noting this is the production Tax Program ID.
  </action>
  <verify>
    Run: `source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && anchor build -p amm` compiles with updated constant.
  </verify>
  <done>
    AMM's TAX_PROGRAM_ID points to real Tax Program, enabling CPI acceptance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create integration tests for swap_sol_buy</name>
  <files>
    programs/tax-program/tests/test_swap_sol_buy.rs
  </files>
  <action>
    Create programs/tax-program/tests/test_swap_sol_buy.rs following AMM test patterns:

    Test setup:
    1. Deploy AMM program (use amm.so from target/deploy)
    2. Deploy Tax Program (use tax_program.so from target/deploy)
    3. Initialize AMM AdminConfig
    4. Create WSOL mint (SPL Token) and CRIME mint (Token-2022)
    5. Initialize CRIME/SOL pool
    6. Create user token accounts (WSOL, CRIME)
    7. Fund user with SOL, WSOL, and some CRIME
    8. Create mock staking_escrow, carnage_vault, treasury accounts (just SystemAccounts)

    Test cases:

    **test_buy_crime_with_tax**
    - User swaps 1 SOL for CRIME
    - Expected: 4% buy tax (default mock rate)
    - Tax = 0.04 SOL = 40_000_000 lamports
    - Verify staking_escrow receives 30_000_000 (75%)
    - Verify carnage_vault receives 9_600_000 (24%)
    - Verify treasury receives 400_000 (1%)
    - Verify user's CRIME balance increased
    - Verify AMM pool reserves updated

    **test_buy_fraud_with_tax**
    - Same pattern but with FRAUD pool
    - Validates is_crime=false path

    **test_buy_slippage_protection**
    - Set minimum_output higher than expected output
    - Expect SlippageExceeded error (from AMM)

    **test_buy_zero_amount_fails**
    - amount_in = 0
    - Expect error (ZeroAmount from AMM or similar)

    **test_buy_tax_distribution_rounding**
    - Small amount that tests rounding: e.g., 100 lamports with 4% tax = 4 lamports
    - 75% of 4 = 3, 24% of 4 = 0.96 -> 0, treasury gets remainder = 1
    - Verify: staking=3, carnage=0, treasury=1 (all to staking due to micro-tax?)
    - Actually with micro-tax threshold: 4 < 4 is false, so normal split
    - Let me recalculate: tax=4, staking=3, carnage=0, treasury=1

    Use litesvm test harness following AMM test patterns. Import types from amm and tax_program crates.

  </action>
  <verify>
    Run: `source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && cargo test -p tax-program test_swap_sol_buy` all tests pass.
  </verify>
  <done>
    Integration tests for swap_sol_buy validate tax calculation, distribution, and CPI chain.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create integration tests for swap_sol_sell</name>
  <files>
    programs/tax-program/tests/test_swap_sol_sell.rs
  </files>
  <action>
    Create programs/tax-program/tests/test_swap_sol_sell.rs:

    Test cases:

    **test_sell_crime_with_tax**
    - User sells 1000 CRIME for SOL
    - AMM returns gross SOL output
    - Expected: 14% sell tax (default mock rate)
    - Verify tax calculated on gross output
    - Verify staking_escrow receives 75% of tax
    - Verify carnage_vault receives 24% of tax
    - Verify treasury receives 1% of tax
    - Verify user's WSOL balance increased by net amount
    - Verify user's native SOL decreased by tax amount

    **test_sell_fraud_with_tax**
    - Same pattern but with FRAUD pool

    **test_sell_slippage_after_tax**
    - This is the critical test per RESEARCH.md
    - Set minimum_output to gross_output - tax + 1
    - Expect SlippageExceeded from Tax Program (not AMM)
    - This proves slippage check happens after tax deduction

    **test_sell_slippage_passes**
    - Set minimum_output to gross_output - tax - small_buffer
    - Expect success

    **test_sell_insufficient_sol_for_tax**
    - User has tokens but not enough native SOL for tax
    - Expect system program transfer error

  </action>
  <verify>
    Run: `source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth && cargo test -p tax-program test_swap_sol_sell` all tests pass.
  </verify>
  <done>
    Integration tests for swap_sol_sell validate output-based tax, slippage, and distribution.
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p tax-program` all tests pass
2. Buy tests verify tax deducted from INPUT
3. Sell tests verify tax deducted from OUTPUT
4. Distribution amounts match 75/24/1 split exactly
5. Slippage protection works in both directions
6. Both CRIME and FRAUD pools tested
7. Events are emitted (check test logs)
</verification>

<success_criteria>
- All integration tests pass
- Tax calculation matches spec (basis points formula)
- Distribution matches spec (75/24/1 to staking/carnage/treasury)
- Sell slippage check happens AFTER tax deduction
- CPI chain works: Tax Program -> AMM -> Token transfers
- No regressions in AMM functionality
</success_criteria>

<output>
After completion, create `.planning/phases/18-tax-program-core/18-05-SUMMARY.md`
</output>
