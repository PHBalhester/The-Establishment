---
phase: 91-deploy-config-foundation
plan: 03
type: execute
wave: 2
depends_on: ["91-01"]
files_modified:
  - scripts/deploy/deploy-all.sh
  - scripts/deploy/initialize.ts
requirements: [INFRA-05, INFRA-06, INFRA-07]
autonomous: true

must_haves:
  truths:
    - "deploy-all.sh requires explicit cluster argument (devnet or mainnet), bare invocation exits with usage error"
    - "deploy-all.sh sources .env.{cluster} and cross-validates CLUSTER_URL matches the target cluster"
    - "deploy-all.sh has 7 phases: mint keypairs, build, deploy, initialize, generate-constants, ALT, verify"
    - "initialize.ts hard-errors if pool seed env vars are missing on non-localhost cluster"
    - "BcAdminConfig initialization is automated in the initialize.ts pipeline"
    - "Mainnet deploy path shows confirmation prompt with SOL balance and cost estimate"
  artifacts:
    - path: "scripts/deploy/deploy-all.sh"
      provides: "7-phase deployment orchestrator with cluster safety gates"
      min_lines: 200
    - path: "scripts/deploy/initialize.ts"
      provides: "Extended initialization with env var guards and BcAdminConfig"
  key_links:
    - from: "scripts/deploy/deploy-all.sh"
      to: ".env.devnet"
      via: "source .env.${CLUSTER}"
      pattern: "source.*\\.env\\."
    - from: "scripts/deploy/deploy-all.sh"
      to: "scripts/deploy/generate-constants.ts"
      via: "npx tsx invocation in Phase 4"
      pattern: "generate-constants"
    - from: "scripts/deploy/initialize.ts"
      to: "process.env"
      via: "env var validation at startup"
      pattern: "SOL_POOL_SEED"
---

<objective>
Extend deploy-all.sh from 4 to 7 phases with mainnet safety gates, add env var hard-errors to initialize.ts, and automate BcAdminConfig initialization.

Purpose: The pipeline must be mainnet-safe. Explicit cluster arguments prevent wrong-cluster deploys. Env var guards prevent the Phase 69 disaster (50 SOL lost from missing pool seed vars). BcAdminConfig automation closes DEPLOY-GAP-01.

Output: Extended deploy-all.sh (7 phases), hardened initialize.ts, automated BcAdminConfig init.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-deploy-config-foundation/91-CONTEXT.md
@.planning/phases/91-deploy-config-foundation/91-RESEARCH.md
@.planning/phases/91-deploy-config-foundation/91-01-SUMMARY.md

@scripts/deploy/deploy-all.sh
@scripts/deploy/initialize.ts
@scripts/deploy/create-alt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend deploy-all.sh to 7-phase pipeline with cluster safety gates</name>
  <files>
    scripts/deploy/deploy-all.sh
  </files>
  <action>
  Rewrite deploy-all.sh with these changes (keep existing patterns like `set -e`, toolchain version gate):

  1. **Cluster argument required** (top of script, before anything else):
     - `CLUSTER="${1:-}"` -- if empty or not "devnet"/"mainnet", print usage and `exit 1`
     - No more positional URL argument. No auto-detection from Solana CLI config.
     - Usage message: `Usage: ./scripts/deploy/deploy-all.sh <devnet|mainnet>`

  2. **Cluster-aware .env sourcing** (replace existing .env sourcing):
     - Source `.env.${CLUSTER}` instead of `.env`
     - If `.env.${CLUSTER}` doesn't exist, error with: "ERROR: .env.${CLUSTER} not found. Create it from .env.example."
     - Cross-validate: if CLUSTER=devnet but CLUSTER_URL doesn't contain "devnet", abort. If CLUSTER=mainnet but CLUSTER_URL contains "devnet", abort.

  3. **Mainnet confirmation prompt** (after .env sourcing, before any build/deploy):
     - Only on mainnet: display deployer wallet balance (via `solana balance`), estimated SOL cost (~15 SOL for full deploy), and ask for explicit confirmation
     - Use `read -p "Type 'DEPLOY MAINNET' to continue: " CONFIRM` -- must match exactly

  4. **7 phases** (renumber existing phases):
     - Phase 0: Mint keypairs (existing, unchanged)
     - Phase 1: Build (existing, pass --devnet flag based on CLUSTER not URL grep)
     - Phase 2: Deploy (existing, pass cluster URL from CLUSTER_URL env var instead of positional arg)
     - Phase 3: Initialize (existing `npx tsx scripts/deploy/initialize.ts`)
     - Phase 4: Generate Constants (`npx tsx scripts/deploy/generate-constants.ts $CLUSTER`) -- NEW
     - Phase 5: Create/Extend ALT (`npx tsx scripts/deploy/create-alt.ts`) -- moved from post-deploy note to explicit phase
     - Phase 6: Verify (`npx tsx scripts/deploy/verify.ts`) -- renumbered

  5. **Completion message** (update to reflect new phases):
     - List all artifacts: deployment.json, constants.ts, ALT address, verification report
     - Remove the old "Post-Deploy Note" about manual ALT recreation

  Keep the existing toolchain version gate (Solana CLI v3+ check) unchanged.
  Keep `set -e` for fail-fast behavior.
  Use `export PATH` for toolchains as current script does.
  </action>
  <verify>
  - `bash scripts/deploy/deploy-all.sh` (no args) exits with usage error and exit code 1
  - `bash scripts/deploy/deploy-all.sh invalid` exits with usage error
  - Script sources `.env.devnet` when invoked with `devnet` argument (can verify by reading the script)
  - Script has 7 labeled phases (Phase 0 through Phase 6)
  - Phase 4 calls generate-constants.ts, Phase 5 calls create-alt.ts, Phase 6 calls verify.ts
  - Mainnet path has confirmation prompt
  </verify>
  <done>deploy-all.sh is a 7-phase pipeline requiring explicit cluster argument, with cluster-aware .env sourcing, cross-validation, and mainnet confirmation prompts.</done>
</task>

<task type="auto">
  <name>Task 2: Add env var guards to initialize.ts and automate BcAdminConfig</name>
  <files>
    scripts/deploy/initialize.ts
  </files>
  <action>
  1. **Env var hard-error guard** (add at the VERY TOP of initialize.ts main function, before any initialization logic):
     - Define required vars for non-localhost clusters:
       ```typescript
       const REQUIRED_VARS_NON_LOCALHOST = [
         "SOL_POOL_SEED_SOL_OVERRIDE",
         "SOL_POOL_SEED_TOKEN_OVERRIDE",
         "HELIUS_API_KEY",
         "CLUSTER_URL",
       ];
       ```
     - Check if cluster URL is localhost. If NOT localhost, validate all required vars are set.
     - If ANY are missing, print a clear error listing EVERY missing var and exit(1).
     - Error format:
       ```
       !!! MISSING REQUIRED ENVIRONMENT VARIABLES !!!

         - SOL_POOL_SEED_SOL_OVERRIDE
         - SOL_POOL_SEED_TOKEN_OVERRIDE

       Set these in .env.devnet or .env.mainnet before running.
       Reminder: source the env file with `set -a && source .env.devnet && set +a`
       ```
     - This check runs BEFORE any on-chain operations. The point is to fail fast, not fail halfway through initialization.

  2. **BcAdminConfig initialization** (add as a new step in the initialization sequence, AFTER BondingCurve program deploy is confirmed):
     - Add a step to initialize the BcAdminConfig PDA if it doesn't exist yet
     - The BcAdminConfig is the BondingCurve program's admin configuration account
     - Check if BcAdminConfig PDA account exists (similar to existing idempotent checks for other PDAs)
     - If not exists: call the bonding curve program's `initialize_admin` instruction (or equivalent) to create it
     - If exists: log "BcAdminConfig already initialized, skipping" and continue
     - This closes DEPLOY-GAP-01 from STATE.md pending todos

  3. Ensure existing idempotent behavior is preserved -- re-running initialize.ts should skip already-completed steps without error.
  </action>
  <verify>
  - Setting `CLUSTER_URL=https://api.devnet.solana.com` without pool seed vars causes immediate exit with clear error message listing missing vars
  - Setting `CLUSTER_URL=http://localhost:8899` bypasses the check (localhost is test-only)
  - BcAdminConfig initialization step appears in initialize.ts output (even if skipped as "already initialized")
  - `npx tsc --noEmit scripts/deploy/initialize.ts` compiles without errors
  </verify>
  <done>initialize.ts hard-errors on missing env vars for non-localhost clusters. BcAdminConfig initialization is automated (DEPLOY-GAP-01 closed). All checks are fail-fast at startup.</done>
</task>

</tasks>

<verification>
- `./scripts/deploy/deploy-all.sh` without args exits with usage message
- deploy-all.sh has phases 0-6 (7 phases total)
- initialize.ts rejects missing env vars on devnet with clear error
- BcAdminConfig is part of the automated init sequence
</verification>

<success_criteria>
- Pipeline prevents wrong-cluster deploys via explicit argument + cross-validation
- Pool seed env var disaster (Phase 69) cannot recur -- hard error before any execution
- BcAdminConfig is no longer a manual step (DEPLOY-GAP-01 closed)
- Same script works for devnet and mainnet with cluster argument as only difference
</success_criteria>

<output>
After completion, create `.planning/phases/91-deploy-config-foundation/91-03-SUMMARY.md`
</output>
