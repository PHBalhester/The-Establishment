---
phase: 57.1-helius-rpc-optimization
plan: 01
subsystem: ui, api
tags: [page-visibility, react-hooks, postgres, drizzle, rpc-optimization]

# Dependency graph
requires:
  - phase: 54-modal-system
    provides: ModalProvider context and useModal hook (StationId type)
  - phase: 44-indexer-database
    provides: Drizzle schema with carnageEvents table and db connection
provides:
  - useVisibility hook combining Page Visibility API + modal context awareness
  - /api/carnage-events Postgres-backed REST endpoint (last 5 events)
affects:
  - 57.1-02 (useTokenBalances refactor consumes useVisibility)
  - 57.1-03 (useCarnageEvents refactor consumes both useVisibility and /api/carnage-events)
  - 57.1-04 (useEpochState/usePoolPrices refactors consume useVisibility)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "useVisibility(stationId?) pattern for modal-aware data-fetching gates"
    - "onResume burst-refresh pattern for stale-data recovery on tab return"
    - "Display-ready API response pattern (exclude internal IDs, flatten for client)"

key-files:
  created:
    - app/hooks/useVisibility.ts
    - app/app/api/carnage-events/route.ts
  modified: []

key-decisions:
  - "isActive=true when activeStation===null ensures DashboardGrid hooks run on factory view"
  - "onResume callbacks fire on false->true visibility transition only (not every visible render)"
  - "Carnage events API always returns last 5 with no query parameters (simplicity per CONTEXT.md)"
  - "Auto-increment id excluded from API response (not useful to client)"

patterns-established:
  - "useVisibility(stationId?): modal-aware visibility gating for all data hooks"
  - "Burst-refresh via onResume: register callbacks that fire on tab-return transition"
  - "Postgres-backed API routes replace expensive client-side RPC parsing"

# Metrics
duration: 2min
completed: 2026-02-24
---

# Phase 57.1 Plan 01: Foundation Dependencies Summary

**useVisibility hook (Page Visibility + modal awareness) and /api/carnage-events Postgres endpoint -- zero-dependency foundation for all RPC credit optimization in Wave 2**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-24T11:41:04Z
- **Completed:** 2026-02-24T11:42:54Z
- **Tasks:** 2/2
- **Files created:** 2

## Accomplishments
- useVisibility hook gates data fetching by combining tab visibility + active modal station
- Burst-refresh callback system fires on tab-return for immediate stale-data recovery
- /api/carnage-events serves last 5 events from Postgres (replaces ~900 credits/hr client-side RPC parsing)
- Both files compile cleanly with zero type errors, zero new dependencies

## Task Commits

Each task was committed atomically:

1. **Task 1: Create useVisibility hook** - `06ee81a` (feat)
2. **Task 2: Create /api/carnage-events API route** - `51bf1fd` (feat)

## Files Created/Modified
- `app/hooks/useVisibility.ts` - Page Visibility API + modal context awareness hook with burst-refresh support
- `app/app/api/carnage-events/route.ts` - Postgres-backed REST endpoint returning last 5 carnage events

## Decisions Made
- isActive computation: `tabVisible && (!requiredStation || activeStation === requiredStation || activeStation === null)` -- the null check ensures DashboardGrid hooks run when no modal is open
- Burst-refresh fires on false->true transition only (tracked via prevTabVisibleRef) to prevent duplicate firings
- Carnage events API has no query parameters -- always returns last 5 per CONTEXT.md decision for simplicity
- Response excludes auto-increment `id` field (display-ready format per plan specification)

## Deviations from Plan

None -- plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None -- no external service configuration required.

## Next Phase Readiness
- useVisibility hook ready for consumption by Plans 02-04 (all data hook refactors)
- /api/carnage-events endpoint ready for consumption by Plan 03 (useCarnageEvents refactor)
- Both files follow existing codebase conventions and compile cleanly
- Wave 2 plans (02, 03, 04) can now execute in parallel

---
*Phase: 57.1-helius-rpc-optimization*
*Completed: 2026-02-24*
