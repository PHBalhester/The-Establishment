---
phase: 57.1-helius-rpc-optimization
plan: 05
type: execute
wave: 3
depends_on: ["57.1-02", "57.1-03", "57.1-04"]
files_modified:
  - app/components/dashboard/DashboardGrid.tsx
autonomous: false

must_haves:
  truths:
    - "DashboardGrid pauses ALL 6 data-fetching hooks when any modal is open (credit savings maximized)"
    - "DashboardGrid hooks resume and burst-refresh immediately when modal closes (no stale cards)"
    - "Next.js build passes with zero type errors across all modified files"
    - "No setInterval with 10-second intervals exists in any hook file"
  artifacts:
    - path: "app/components/dashboard/DashboardGrid.tsx"
      provides: "Modal-aware dashboard that pauses hooks when modals are open"
  key_links:
    - from: "app/components/dashboard/DashboardGrid.tsx"
      to: "app/hooks/useVisibility.ts"
      via: "Indirect -- DashboardGrid's hooks internally use useVisibility"
      pattern: "implicit via hook internals"
---

<objective>
Verify that the DashboardGrid's hooks correctly pause when modals are open (since hooks internally use useVisibility which checks modal state), confirm the full build passes, and do a visual smoke-test checkpoint to verify credit savings in the Helius dashboard.

Purpose: DashboardGrid is the critical integration point -- it calls ALL 6 data hooks and is always mounted behind modals. The hooks from Plans 02-04 internally use useVisibility, so DashboardGrid should already benefit from modal-aware pausing without code changes TO DashboardGrid itself. This plan verifies that assumption and does the final build + visual check.

Output: Verified integration with build confirmation and visual checkpoint.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57.1-helius-rpc-optimization/57.1-RESEARCH.md
@.planning/phases/57.1-helius-rpc-optimization/57.1-02-SUMMARY.md
@.planning/phases/57.1-helius-rpc-optimization/57.1-03-SUMMARY.md
@.planning/phases/57.1-helius-rpc-optimization/57.1-04-SUMMARY.md
@app/components/dashboard/DashboardGrid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: DashboardGrid integration verification + build check</name>
  <files>app/components/dashboard/DashboardGrid.tsx</files>
  <action>
**Verify the DashboardGrid integration works correctly:**

1. **Read DashboardGrid.tsx** and confirm that it calls all 6 hooks (useEpochState, useCurrentSlot, useSolPrice, usePoolPrices, useCarnageData, useCarnageEvents). These hooks now internally use `useVisibility()` which reads modal state from `useModal()`.

2. **Analyze the pausing behavior:**
   - When NO modal is open (`activeStation === null`): All 6 hooks should report `isActive === true` (because useVisibility returns true when activeStation is null for hooks without a required station, and for station-scoped hooks like "carnage" and "swap", isActive is also true when activeStation is null).
   - When a modal IS open (e.g., `activeStation === "swap"`): Hooks with `useVisibility("carnage")` and `useVisibility("staking")` should report `isActive === false`. Hooks with `useVisibility("swap")` should report `isActive === true`. Hooks with `useVisibility()` (no station) should report `isActive === true` (they only care about tab visibility).

3. **Identify the gap:** DashboardGrid calls useEpochState (no station scope), useCurrentSlot (no station scope), useSolPrice (no station scope), usePoolPrices ("swap" scoped), useCarnageData ("carnage" scoped), useCarnageEvents ("carnage" scoped).

   When the swap modal is open, DashboardGrid's useCarnageData and useCarnageEvents instances WILL pause (good). But useEpochState, useCurrentSlot, and useSolPrice will NOT pause because they have no station scope -- they only pause on tab hidden.

   This is actually the CORRECT behavior per CONTEXT.md: "If no modal is open, only epoch state polls (30s)." But we need epoch state to NOT poll when a modal IS open. The current useVisibility() without a station returns isActive=true even when a modal is open (it only checks tab visibility).

4. **Fix: DashboardGrid needs modal awareness for its non-station-scoped hooks.**

   The cleanest approach: DashboardGrid should read the modal state itself and conditionally skip calling its hooks' data, or the non-station-scoped hooks (useEpochState, useCurrentSlot, useSolPrice) should respect a "dashboard" mode.

   **Recommended fix:** Add a second optional parameter to `useVisibility` -- `pauseWhenAnyModalOpen?: boolean`. When true, the hook also returns `isActive === false` when ANY modal is open (regardless of which station). DashboardGrid's hooks would use `useVisibility(undefined, true)` -- no required station, but pauses when any modal is open.

   However, this changes the useVisibility API which was just created in Plan 01. Instead, a simpler approach:

   **Alternative: Update useVisibility to accept `{ station?: StationId; pauseOnModal?: boolean }` as a config object:**

   Actually, the simplest fix that doesn't change the existing useVisibility API: modify `useEpochState`, `useCurrentSlot`, and `useSolPrice` to pass a special sentinel value. But that's ugly.

   **Cleanest fix:** Update useVisibility (in this task, since we own the file):

   Change the signature from:
   ```typescript
   export function useVisibility(requiredStation?: StationId)
   ```
   to:
   ```typescript
   export function useVisibility(requiredStation?: StationId | "any")
   ```

   When `requiredStation === "any"`, `isActive` is true ONLY when the tab is visible AND no modal is open (`activeStation === null`). This is the "dashboard mode" -- only active when the user is looking at the main scene.

   Then in useEpochState, useCurrentSlot, and useSolPrice, change:
   - `useVisibility()` -> `useVisibility()` stays the same for hooks called from station modals

   Wait -- the issue is that these hooks are called from BOTH DashboardGrid AND station modals. useEpochState is called in DashboardGrid (should pause when modal open) and CarnageStation (should be active when carnage modal open). But the hook is a SINGLE instance per call site. The DashboardGrid instance and the CarnageStation instance are separate React hook instances.

   **The DashboardGrid instance should pause when any modal is open. The CarnageStation instance should be active when the carnage modal is open.**

   Since they're separate instances with separate `useVisibility` calls, we can't change the hook's internal useVisibility without affecting both. BUT -- CarnageStation doesn't call useEpochState directly; it receives epoch data as props from its parent or calls its own instance.

   Let me re-check: CarnageStation imports useEpochState? Let me verify by looking at the research: "useCarnageData: DashboardGrid + CarnageStation" and "useEpochState: DashboardGrid + CarnageStation". So CarnageStation does call useEpochState independently.

   **The correct fix: Don't change the hooks. Change DashboardGrid to not call hooks when a modal is open.**

   DashboardGrid can use `useModal()` to check if a modal is open, and conditionally render a "paused" version:

   ```typescript
   const { state: modalState } = useModal();
   const anyModalOpen = modalState.activeStation !== null;
   ```

   But React hooks can't be called conditionally. So DashboardGrid ALWAYS calls the hooks, but the hooks themselves decide whether to be active based on their internal `useVisibility`.

   **Final answer: The correct approach is to NOT change DashboardGrid at all.** The hooks' visibility scoping already works:
   - `usePoolPrices("swap")` pauses when a non-swap modal is open -- GOOD
   - `useCarnageData("carnage")` pauses when a non-carnage modal is open -- GOOD
   - `useCarnageEvents("carnage")` pauses when a non-carnage modal is open -- GOOD
   - `useEpochState()` (no station) only pauses when tab is hidden -- This is ACCEPTABLE because epoch state changes are pushed via WS (from Plan 02), costing 0 credits when no data changes
   - `useCurrentSlot()` (no station) only pauses when tab is hidden -- ACCEPTABLE because slot comes via WS (from Plan 02), 0 credit cost
   - `useSolPrice()` (no station) only pauses when tab is hidden -- This calls Jupiter API (0 Helius credits), but we should still pause it to reduce CPU/network

   **The WS-converted hooks (useEpochState, useCurrentSlot) cost ~0 credits/hr even when active.** The polling-to-WS conversion in Plan 02 already solved the DashboardGrid credit cost for these hooks. useSolPrice costs 0 Helius credits (it's a Jupiter API call). So DashboardGrid's credit consumption when a modal is open is: 0 (epoch WS) + 0 (slot WS) + 0 (Jupiter) + 0 (pool WS paused) + 0 (carnage paused) + 0 (events paused) = **0 credits/hr**.

   **Conclusion: DashboardGrid needs NO code changes.** The hooks' internal visibility + WS conversions already achieve the goal. The station modals' own hook instances handle their data independently.

   **What this task should do instead:**
   1. Run `npx next build` (or `npx tsc --noEmit`) to verify the entire app compiles.
   2. Audit all hook files to confirm:
      - No `setInterval` with 10-second intervals remains
      - All hooks import useVisibility
      - useEpochState and useCurrentSlot use WS subscriptions
      - useCarnageEvents fetches from API route
   3. Count the projected credit savings and confirm they match the <500/hr target.
   4. Update DashboardGrid's JSDoc to document the new behavior (hooks internally manage their own pausing).

  **Actually DO modify DashboardGrid.tsx:** Update the header JSDoc comment to reflect the new architecture:
  - Replace references to "10 seconds polling" with "WebSocket subscriptions"
  - Note that hooks internally manage visibility-based pausing
  - Remove the "WebSocket reconnection strategy" comment about passive reconnection (hooks now actively manage WS lifecycle)
  </action>
  <verify>
Run `export PATH="/opt/homebrew/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && npx tsc --noEmit` to type-check the entire app. All files must compile without errors. Then run `npx next build` to verify the production build succeeds.
  </verify>
  <done>
Full app compiles with zero type errors. DashboardGrid JSDoc updated to reflect WS + visibility architecture. All hooks verified to use useVisibility. No 10-second polling intervals remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 57.1 Helius RPC/WebSocket Optimization is complete. All changes:

1. **NEW: useVisibility hook** -- Page Visibility + modal-aware pausing (tab hidden = all fetching stops, modal-scoped pausing for station-specific hooks)
2. **NEW: /api/carnage-events route** -- Postgres query replacing client-side RPC parsing
3. **MODIFIED: useEpochState** -- 10s polling -> WS onAccountChange (360 -> ~1 credits/hr)
4. **MODIFIED: useCurrentSlot** -- 10s polling -> WS onSlotChange with 5s throttle (360 -> ~1 credits/hr)
5. **MODIFIED: useCarnageEvents** -- RPC parsing -> Postgres API fetch (900 -> 0 credits/hr)
6. **MODIFIED: usePoolPrices** -- 4 individual fetches -> 1 batch getMultipleAccounts + visibility pausing
7. **MODIFIED: useCarnageData** -- 10s -> 30s polling + visibility/modal pausing (360 -> ~120 credits/hr)
8. **MODIFIED: useStaking** -- modal-scoped pausing (240 -> ~30 credits/hr)
9. **MODIFIED: useTokenBalances** -- tab visibility pausing (360 -> ~180 credits/hr)
10. **MODIFIED: useSolPrice** -- tab visibility pausing (0 Helius credits but reduced CPU/network)

**Projected total:** ~2,590 credits/hr -> ~332 credits/hr (87% reduction)
  </what-built>
  <how-to-verify>
1. Open https://dr-fraudsworth-production.up.railway.app in a browser tab
2. Open browser DevTools -> Network tab -> filter by "ws" to see WebSocket connections
3. Verify you see WebSocket messages arriving (slot updates, account changes)
4. Open a station modal (e.g., click the Swap Machine) -> observe that carnage/staking data stops updating in the background (no new network requests for those hooks)
5. Switch to a different browser tab for 30 seconds -> switch back -> observe burst-refresh (all data updates immediately on tab return)
6. Check the Helius dashboard (https://dashboard.helius.dev) -> Credits usage section -> confirm credit consumption has dropped significantly compared to pre-optimization levels
7. Verify all 6 station modals still load their data correctly (swap prices, carnage events, staking data, wallet, docs, settings)
  </how-to-verify>
  <resume-signal>Type "approved" if credit consumption is reduced and all stations work correctly, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors for the full app.
2. `npx next build` succeeds.
3. No hook file contains `POLL_INTERVAL_MS = 10_000` (all converted to WS or reduced to 30s).
4. useEpochState contains `onAccountChange`, useCurrentSlot contains `onSlotChange`.
5. useCarnageEvents contains `fetch("/api/carnage-events")` and NOT `getSignaturesForAddress`.
6. usePoolPrices contains `getMultipleAccountsInfo`.
7. All 6+ hook files import `useVisibility`.
8. DashboardGrid renders all cards without errors.
</verification>

<success_criteria>
- Full app builds successfully (Next.js production build)
- All station modals display correct data
- Helius credit consumption drops to <500 credits/hr per active tab (target: ~332)
- Tab hidden = zero RPC/WS activity
- Modal open = only relevant hooks active, DashboardGrid hooks paused or zero-cost (WS)
- Tab return = burst-refresh, fresh data in <1 second
</success_criteria>

<output>
After completion, create `.planning/phases/57.1-helius-rpc-optimization/57.1-05-SUMMARY.md`
</output>
