---
phase: 60-design-tokens-component-kit
plan: 03
type: execute
wave: 2
depends_on: ["60-01", "60-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Frame WebP assets exist in app/public/frames/ for both ornate-paper and riveted-paper variants"
    - "Each frame variant has both normal and active state assets (4 files total minimum)"
    - "Assets are WebP format at 2x retina size (600x600px) with transparent outside frame borders"
  artifacts:
    - path: "app/public/frames/ornate-paper.webp"
      provides: "Ornate Victorian brass frame (normal state)"
    - path: "app/public/frames/ornate-paper-active.webp"
      provides: "Ornate Victorian brass frame (active/pressed state)"
    - path: "app/public/frames/riveted-paper.webp"
      provides: "Functional riveted metal frame (normal state)"
    - path: "app/public/frames/riveted-paper-active.webp"
      provides: "Functional riveted metal frame (active/pressed state)"
  key_links:
    - from: "docs/v1.1-asset-spec.md"
      to: "app/public/frames/"
      via: "Asset spec guides Photoshop creation"
      pattern: "ornate-paper|riveted-paper"
---

<objective>
Pause execution so the user can create frame WebP assets in Photoshop using the asset specification document produced by Plan 60-01.

Purpose: The ROADMAP requires "Frame assets (WebP) in public/frames/ -- at least 2 variants (paper, dark)" as a Phase 60 success criterion. The user creates these manually in Photoshop from AI-generated references. Plans 60-04 and 60-05 (structural and interactive components) can then reference real frame assets instead of CSS-only fallbacks.

Output: 4 WebP frame assets in `app/public/frames/`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.1/ROADMAP.md
@.planning/phases/60-design-tokens-component-kit/60-CONTEXT.md
@docs/v1.1-asset-spec.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Plan 60-01 produced the comprehensive asset specification document at docs/v1.1-asset-spec.md. Plan 60-02 built the design tokens and kit.css foundation with CSS classes that reference frame assets at /frames/ornate-paper.webp etc. The CSS fallback (solid border) works without assets, but the ROADMAP requires real WebP frame assets.</what-built>
  <how-to-verify>
Create the following 4 frame WebP assets in Photoshop using the asset spec document (docs/v1.1-asset-spec.md) as your guide:

**Required assets (minimum for Phase 60):**

1. `ornate-paper.webp` -- Ornate Victorian brass frame with decorative scrollwork and filigree corners, parchment texture fill in center, transparent outside borders
2. `ornate-paper-active.webp` -- Same frame with active/pressed state (slightly recessed, darker brass, subtle shadow shift)
3. `riveted-paper.webp` -- Functional riveted/bolted metal frame, parchment texture fill, transparent outside borders
4. `riveted-paper-active.webp` -- Same frame with active/pressed state

**Specifications per asset spec:**
- Native size: 600x600px (2x for retina, renders at 300px CSS)
- 9-slice anatomy: 80px corners for ornate, 40px for riveted
- Format: WebP (lossy body + lossless alpha, quality 90/100)
- Parchment texture in center region
- Transparent pixels outside frame borders
- Blend polished brass (Image 2 reference) with weathered patina (Image 3 reference)

**Place completed assets at:**
```
app/public/frames/ornate-paper.webp
app/public/frames/ornate-paper-active.webp
app/public/frames/riveted-paper.webp
app/public/frames/riveted-paper-active.webp
```

**Quick check after placing files:**
```bash
ls -la app/public/frames/*.webp
```
Should show 4 files, each roughly 20-100KB.
  </how-to-verify>
  <resume-signal>Type "approved" when frame assets are in app/public/frames/</resume-signal>
</task>

</tasks>

<verification>
- 4 WebP files exist in app/public/frames/
- File names match exactly: ornate-paper.webp, ornate-paper-active.webp, riveted-paper.webp, riveted-paper-active.webp
- Files are non-zero size
</verification>

<success_criteria>
Frame WebP assets are in place at app/public/frames/, ready for the Frame component (Plan 60-04) to reference via border-image-source CSS rules in kit.css.
</success_criteria>

<output>
After completion, create `.planning/phases/60-design-tokens-component-kit/60-03-SUMMARY.md`
</output>
