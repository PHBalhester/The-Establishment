---
phase: 104-open-source-release-and-ottersec-verification
plan: 05
type: execute
wave: 3
depends_on: ["104-01", "104-02", "104-03", "104-04"]
files_modified:
  - /tmp/drfraudsworth-public/SECURITY_AUDIT_SUMMARY.md
  - /tmp/drfraudsworth-public/README.md
  - /tmp/drfraudsworth-public/LICENSE
autonomous: false

must_haves:
  truths:
    - "Public GitHub repo MetalLegBob/drfraudsworth exists and is publicly accessible"
    - "The repo has exactly one commit with the complete curated codebase"
    - "No secrets exist in the pushed commit (verified by prior 5-pass sanitization)"
    - "README.md is visible on the repo landing page"
    - "The repo is cloneable by anyone (git clone https://github.com/MetalLegBob/drfraudsworth)"
  artifacts:
    - path: "https://github.com/MetalLegBob/drfraudsworth"
      provides: "Public GitHub repository"
  key_links:
    - from: "Public repo"
      to: "OtterSec verification"
      via: "OtterSec needs public repo URL + commit hash"
      pattern: "github.com/MetalLegBob/drfraudsworth"
---

<objective>
Assemble the final staging directory with outputs from Plans 01-04, apply the documentation fixes from Plan 03 (which were made in the private repo), create the GitHub repo, and push the initial commit.

Purpose: This is the moment the protocol goes public. The initial commit must contain everything from the sanitized staging directory plus the doc fixes and audit summary. One commit, one push, zero secrets.
Output: A live public GitHub repo at MetalLegBob/drfraudsworth with the complete curated codebase.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-open-source-release-and-ottersec-verification/104-CONTEXT.md
@.planning/phases/104-open-source-release-and-ottersec-verification/104-01-SUMMARY.md
@.planning/phases/104-open-source-release-and-ottersec-verification/104-02-SUMMARY.md
@.planning/phases/104-open-source-release-and-ottersec-verification/104-03-SUMMARY.md
@.planning/phases/104-open-source-release-and-ottersec-verification/104-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble final staging directory and sync doc fixes</name>
  <files>
    /tmp/drfraudsworth-public/ (multiple files)
  </files>
  <action>
    The staging directory was created by Plan 01 and verified by Plan 04. Now we need to incorporate changes from Plans 02 and 03:

    1. **Sync documentation fixes from Plan 03**: Plan 03 made fixes to docs in the PRIVATE repo (Docs/*.md, docs-site/app/layout.tsx). Re-sync these specific files from the private repo to the staging directory:
       ```bash
       # Copy updated doc files from private repo to staging
       # Only copy files that Plan 03 modified
       cp "/Users/mlbob/Projects/Dr Fraudsworth/Docs/operational-runbook.md" /tmp/drfraudsworth-public/Docs/
       cp "/Users/mlbob/Projects/Dr Fraudsworth/Docs/deployment-sequence.md" /tmp/drfraudsworth-public/Docs/
       cp "/Users/mlbob/Projects/Dr Fraudsworth/Docs/mainnet-deploy-checklist.md" /tmp/drfraudsworth-public/Docs/
       cp "/Users/mlbob/Projects/Dr Fraudsworth/Docs/security-model.md" /tmp/drfraudsworth-public/Docs/
       cp "/Users/mlbob/Projects/Dr Fraudsworth/Docs/token-economics-model.md" /tmp/drfraudsworth-public/Docs/
       cp "/Users/mlbob/Projects/Dr Fraudsworth/Docs/architecture.md" /tmp/drfraudsworth-public/Docs/
       cp "/Users/mlbob/Projects/Dr Fraudsworth/docs-site/app/layout.tsx" /tmp/drfraudsworth-public/docs-site/app/
       # Also copy any other files Plan 03 modified (check 03-SUMMARY for full list)
       ```

    2. **Verify SECURITY_AUDIT_SUMMARY.md from Plan 02**: Check if Plan 02 wrote the file to the staging directory or to the private repo root. If at the private repo root, copy it:
       ```bash
       # Check staging directory first
       ls /tmp/drfraudsworth-public/SECURITY_AUDIT_SUMMARY.md 2>/dev/null
       # If not found, copy from private repo
       cp "/Users/mlbob/Projects/Dr Fraudsworth/SECURITY_AUDIT_SUMMARY.md" /tmp/drfraudsworth-public/ 2>/dev/null
       ```

    3. **Run one final sanitization pass** on the updated staging directory (same 4 checks from Plan 04) to verify the synced files didn't introduce any secrets:
       ```bash
       # Check A: Gitleaks (if installed)
       gitleaks detect --source /tmp/drfraudsworth-public --no-git -v --config /tmp/drfraudsworth-public/.gitleaks.toml 2>/dev/null
       # Check B: Keypair arrays
       find /tmp/drfraudsworth-public -name "*.json" -exec grep -l '\[.*[0-9].*,' {} \; | head -5
       # Check C: API keys
       grep -r 'helius-rpc.*api-key=[a-f0-9]' /tmp/drfraudsworth-public/ 2>/dev/null
       grep -r 'SOLANA_PRIVATE_KEY' /tmp/drfraudsworth-public/ 2>/dev/null
       # Check D: Sensitive files
       test ! -d /tmp/drfraudsworth-public/keypairs && echo "OK" || echo "FAIL"
       ```

    4. **Verify all key files are present**:
       ```bash
       # Must exist:
       ls /tmp/drfraudsworth-public/README.md
       ls /tmp/drfraudsworth-public/LICENSE
       ls /tmp/drfraudsworth-public/SECURITY_AUDIT_SUMMARY.md
       ls /tmp/drfraudsworth-public/.gitignore
       ls /tmp/drfraudsworth-public/.env.example
       ls /tmp/drfraudsworth-public/Cargo.toml
       ls /tmp/drfraudsworth-public/Cargo.lock
       ls /tmp/drfraudsworth-public/Anchor.toml
       ls /tmp/drfraudsworth-public/package.json
       ls /tmp/drfraudsworth-public/rust-toolchain.toml
       ls /tmp/drfraudsworth-public/programs/amm/src/lib.rs
       ls /tmp/drfraudsworth-public/deployments/mainnet.json
       ```
  </action>
  <verify>
    All key files present. Final sanitization pass clean. Documentation fixes synced from private repo.
  </verify>
  <done>Final staging directory assembled with all outputs from Plans 01-04 integrated and one final clean sanitization pass confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete staging directory at /tmp/drfraudsworth-public ready for public GitHub push. Contains: sanitized codebase (zero secrets across 5+ sanitization passes), comprehensive README.md, MIT LICENSE, SECURITY_AUDIT_SUMMARY.md, all documentation verified against mainnet state, docs-site GitHub link updated.</what-built>
  <how-to-verify>
    1. Review README.md: `cat /tmp/drfraudsworth-public/README.md` -- does it represent the project well?
    2. Review SECURITY_AUDIT_SUMMARY.md: `cat /tmp/drfraudsworth-public/SECURITY_AUDIT_SUMMARY.md` -- is the audit disclosure honest and complete?
    3. Spot-check governance doc: `cat /tmp/drfraudsworth-public/Docs/mainnet-governance.md` -- are operational details properly stripped?
    4. Verify no secrets: `grep -r "SOLANA_PRIVATE_KEY\|helius-rpc.*api-key=[a-f0-9]" /tmp/drfraudsworth-public/ 2>/dev/null` -- should return nothing
    5. Check directory size: `du -sh /tmp/drfraudsworth-public/` -- should be reasonable (no 21MB BOK results)
    6. Verify critical exclusions: `ls /tmp/drfraudsworth-public/keypairs/ 2>&1` -- should fail
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with GitHub repo creation and push, or describe any issues to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create GitHub repo and push initial commit</name>
  <files>
    (GitHub repo creation -- no local files modified)
  </files>
  <action>
    1. **Create the public GitHub repo**:
       ```bash
       export PATH="/opt/homebrew/bin:$PATH"
       gh repo create MetalLegBob/drfraudsworth --public --description "Dr. Fraudsworth's Finance Factory - A Solana DeFi protocol with tax-driven yield mechanics" --license mit
       ```
       Note: If `gh` auth fails, this becomes a checkpoint for the user to authenticate. If the repo already exists (from a prior attempt), skip creation.

    2. **Initialize git in the staging directory and push**:
       ```bash
       cd /tmp/drfraudsworth-public
       git init
       git checkout -b main

       # Add everything
       git add -A

       # Verify what's being committed (spot check for secrets)
       git status | head -50
       git diff --cached --stat | tail -20

       # Create the single initial commit
       git commit -m "Initial open source release - Dr. Fraudsworth's Finance Factory

    Complete protocol source code: 7 Anchor programs, Next.js frontend, Nextra docs site,
    deploy infrastructure, crank runner, and test suites.

    Includes 104 phases of AI-assisted build documentation, 3 internal security audits
    (SOS, Bulwark, BOK formal verification), and curated audit summary.

    All 6 active mainnet programs submitted for OtterSec verified build badges.

    Live at: https://fraudsworth.fun
    Docs: https://fraudsworth.fun/docs"

       # Add remote and push
       git remote add origin https://github.com/MetalLegBob/drfraudsworth.git
       git push -u origin main
       ```

    3. **Verify the repo is publicly accessible**:
       ```bash
       # Clone to a temp dir to verify
       git clone https://github.com/MetalLegBob/drfraudsworth.git /tmp/drfraudsworth-verify
       ls /tmp/drfraudsworth-verify/README.md
       ls /tmp/drfraudsworth-verify/programs/amm/src/lib.rs
       # Clean up verify dir
       rm -rf /tmp/drfraudsworth-verify
       ```

    4. **Record the commit hash** -- needed for OtterSec verification in Plan 06:
       ```bash
       cd /tmp/drfraudsworth-public
       COMMIT_HASH=$(git rev-parse HEAD)
       echo "Initial commit hash: $COMMIT_HASH"
       echo "$COMMIT_HASH" > /tmp/drfraudsworth-commit-hash.txt
       ```
  </action>
  <verify>
    ```bash
    # Repo exists and is public
    gh repo view MetalLegBob/drfraudsworth --json visibility
    # Should show "PUBLIC"

    # Commit hash recorded
    cat /tmp/drfraudsworth-commit-hash.txt
    # Should contain a 40-char hex hash

    # Repo is cloneable
    git ls-remote https://github.com/MetalLegBob/drfraudsworth.git
    # Should list refs
    ```
  </verify>
  <done>Public GitHub repo MetalLegBob/drfraudsworth created with single initial commit. Commit hash recorded for OtterSec verification. Repo is publicly accessible and cloneable.</done>
</task>

</tasks>

<verification>
1. `gh repo view MetalLegBob/drfraudsworth --json visibility` returns PUBLIC
2. Repo has exactly 1 commit
3. README.md is visible on the GitHub repo landing page
4. Commit hash is recorded at /tmp/drfraudsworth-commit-hash.txt
5. `git clone https://github.com/MetalLegBob/drfraudsworth.git` succeeds from any location
</verification>

<success_criteria>
- Public GitHub repo exists at MetalLegBob/drfraudsworth
- Single initial commit with clean, curated codebase
- User approved the staging directory before push
- Commit hash recorded for OtterSec verification
- Repo is publicly cloneable
</success_criteria>

<output>
After completion, create `.planning/phases/104-open-source-release-and-ottersec-verification/104-05-SUMMARY.md`
</output>
