---
phase: 97-squads-governance
plan: 01
type: execute
wave: 1
depends_on: []
requirements: []
files_modified:
  - programs/amm/src/instructions/transfer_admin.rs
  - programs/amm/src/instructions/mod.rs
  - programs/amm/src/lib.rs
  - programs/transfer-hook/src/instructions/transfer_authority.rs
  - programs/transfer-hook/src/instructions/mod.rs
  - programs/transfer-hook/src/lib.rs
  - programs/bonding_curve/src/instructions/transfer_bc_admin.rs
  - programs/bonding_curve/src/instructions/mod.rs
  - programs/bonding_curve/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "AMM has a transfer_admin instruction that changes AdminConfig.admin to a new pubkey"
    - "Transfer Hook has a transfer_authority instruction that changes WhitelistAuthority.authority to a new pubkey"
    - "Bonding Curve has a transfer_bc_admin instruction that changes BcAdminConfig.authority to a new pubkey"
    - "All 3 programs compile, deploy to devnet, and existing state (pools, mints, PDAs) is unaffected"
  artifacts:
    - path: "programs/amm/src/instructions/transfer_admin.rs"
      provides: "AMM admin transfer instruction"
      contains: "has_one = admin"
    - path: "programs/transfer-hook/src/instructions/transfer_authority.rs"
      provides: "Hook authority transfer instruction"
      contains: "authority"
    - path: "programs/bonding_curve/src/instructions/transfer_bc_admin.rs"
      provides: "BC admin transfer instruction"
      contains: "has_one = authority"
  key_links:
    - from: "programs/amm/src/lib.rs"
      to: "programs/amm/src/instructions/transfer_admin.rs"
      via: "pub fn transfer_admin"
      pattern: "transfer_admin"
    - from: "programs/transfer-hook/src/lib.rs"
      to: "programs/transfer-hook/src/instructions/transfer_authority.rs"
      via: "pub fn transfer_authority"
      pattern: "transfer_authority"
    - from: "programs/bonding_curve/src/lib.rs"
      to: "programs/bonding_curve/src/instructions/transfer_bc_admin.rs"
      via: "pub fn transfer_bc_admin"
      pattern: "transfer_bc_admin"
---

<objective>
Add transfer authority instructions to AMM, Transfer Hook, and Bonding Curve programs so admin PDA authorities can be transferred to a Squads vault (or any new pubkey).

Purpose: The 3 admin PDAs currently only have initialize + burn instructions. Without a transfer instruction, there is no way to move admin authority to the Squads multisig vault PDA. This is a hard blocker for GOV-03.

Output: Three new Anchor instructions added, programs rebuilt with build.sh, redeployed to devnet. Existing on-chain state (pools, mints, curves) unaffected because adding instructions does NOT change account layouts or PDA seeds.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-squads-governance/97-CONTEXT.md
@.planning/phases/97-squads-governance/97-RESEARCH.md

# Existing admin instruction patterns (use as templates):
@programs/amm/src/instructions/initialize_admin.rs
@programs/amm/src/instructions/burn_admin.rs
@programs/transfer-hook/src/instructions/initialize_authority.rs
@programs/transfer-hook/src/instructions/burn_authority.rs
@programs/bonding_curve/src/instructions/initialize_bc_admin.rs
@programs/bonding_curve/src/instructions/burn_bc_admin.rs

# Build pipeline:
@scripts/deploy/build.sh

# Deployment config:
@deployments/devnet.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add transfer_admin instructions to all 3 programs</name>
  <files>
    programs/amm/src/instructions/transfer_admin.rs
    programs/amm/src/instructions/mod.rs
    programs/amm/src/lib.rs
    programs/transfer-hook/src/instructions/transfer_authority.rs
    programs/transfer-hook/src/instructions/mod.rs
    programs/transfer-hook/src/lib.rs
    programs/bonding_curve/src/instructions/transfer_bc_admin.rs
    programs/bonding_curve/src/instructions/mod.rs
    programs/bonding_curve/src/lib.rs
  </files>
  <action>
    Create three new instruction files, each following the existing initialize/burn patterns in their respective programs:

    **1. AMM `transfer_admin`** (`programs/amm/src/instructions/transfer_admin.rs`):
    - Accounts struct: `admin` (Signer), `admin_config` (Account, mut, has_one = admin, seeds = [b"admin"], bump)
    - Instruction arg: `new_admin: Pubkey`
    - Handler: `ctx.accounts.admin_config.admin = new_admin;`
    - Emit a log: `msg!("Admin transferred from {} to {}", ctx.accounts.admin.key(), new_admin);`
    - Add `require!(new_admin != Pubkey::default(), AmmError::InvalidAuthority);` to prevent accidental burn via transfer (burn_admin exists for that)
    - Wire into `mod.rs` and `lib.rs` following the exact pattern of initialize_admin/burn_admin

    **2. Transfer Hook `transfer_authority`** (`programs/transfer-hook/src/instructions/transfer_authority.rs`):
    - Accounts struct: `authority` (Signer), `whitelist_authority` (Account, mut) with constraint that `whitelist_authority.authority == Some(authority.key())`
    - Instruction arg: `new_authority: Pubkey`
    - Handler: `ctx.accounts.whitelist_authority.authority = Some(new_authority);`
    - Add `require!(new_authority != Pubkey::default(), ...);` guard
    - Wire into mod.rs and lib.rs following initialize_authority/burn_authority pattern

    **3. Bonding Curve `transfer_bc_admin`** (`programs/bonding_curve/src/instructions/transfer_bc_admin.rs`):
    - Accounts struct: `authority` (Signer), `admin_config` (Account, mut, has_one = authority, seeds = [b"bc_admin_config"], bump)
    - Instruction arg: `new_authority: Pubkey`
    - Handler: `ctx.accounts.admin_config.authority = new_authority;`
    - Add `require!(new_authority != Pubkey::default(), ...);` guard
    - Wire into mod.rs and lib.rs following initialize_bc_admin/burn_bc_admin pattern

    IMPORTANT: Look at the existing instruction files in each program to match the exact struct field names, error enum variants, and account constraint syntax. Each program may use slightly different conventions. Match them exactly.
  </action>
  <verify>
    Run `source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH" && cd "/Users/mlbob/Projects/Dr Fraudsworth" && cargo check --manifest-path programs/amm/Cargo.toml && cargo check --manifest-path programs/transfer-hook/Cargo.toml && cargo check --manifest-path programs/bonding_curve/Cargo.toml` — all three must compile without errors.
  </verify>
  <done>
    Three new transfer authority instructions exist, each with has_one constraint on current admin, Pubkey::default() guard, and log message. All 3 programs pass `cargo check`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild and redeploy all 3 programs to devnet</name>
  <files>
    (no new files — uses existing build.sh and deploy.sh pipeline)
  </files>
  <action>
    1. Run `build.sh --devnet` to rebuild all programs (this handles the feature-flagged two-pass build).
    2. Deploy the 3 modified programs to devnet using their existing program keypairs (programs IDs stay the same):
       ```
       solana program deploy target/deploy/amm.so --program-id keypairs/amm-keypair.json --url devnet --keypair keypairs/devnet-wallet.json
       solana program deploy target/deploy/transfer_hook.so --program-id keypairs/transfer-hook-keypair.json --url devnet --keypair keypairs/devnet-wallet.json
       solana program deploy target/deploy/bonding_curve.so --program-id keypairs/bonding-curve-keypair.json --url devnet --keypair keypairs/devnet-wallet.json
       ```
       NOTE: Project path contains a space ("Dr Fraudsworth"). Use proper quoting on all paths. If CLI has issues, use absolute paths with quotes.
    3. After deploy, run `verify.ts` to confirm all programs are still executable and existing state (pools, mints, PDAs) is intact.
    4. Quick smoke test: call the new transfer_admin instruction on AMM to transfer admin to a temporary keypair, then transfer back to the deployer. This confirms the instruction works on-chain. (Create a small throwaway script or inline test for this.)

    IMPORTANT: Do NOT redeploy programs that were NOT modified (tax, epoch, staking, vault). Only AMM, Transfer Hook, and Bonding Curve.
    IMPORTANT: Remember to source env before any Solana commands: `source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH"`
    IMPORTANT: Check deployer wallet balance before deploying — need enough SOL for 3 program deploys.
  </action>
  <verify>
    1. `solana program show <AMM_PROGRAM_ID> --url devnet` shows "Upgradeable" status
    2. `solana program show <HOOK_PROGRAM_ID> --url devnet` shows "Upgradeable" status
    3. `solana program show <BC_PROGRAM_ID> --url devnet` shows "Upgradeable" status
    4. Smoke test of transfer_admin round-trip succeeds (transfer to temp key, transfer back)
    5. verify.ts passes with no regressions
  </verify>
  <done>
    All 3 programs redeployed to devnet with new transfer instructions. Existing state intact. Transfer instruction confirmed working via smoke test.
  </done>
</task>

</tasks>

<verification>
- `cargo check` passes for all 3 modified programs
- All 3 programs deployed to devnet with same program IDs
- verify.ts shows no regressions
- Transfer instruction smoke test proves on-chain functionality
</verification>

<success_criteria>
AMM, Transfer Hook, and Bonding Curve programs each have a working transfer authority instruction on devnet, ready for use by transfer-authority.ts in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/97-squads-governance/97-01-SUMMARY.md`
</output>
