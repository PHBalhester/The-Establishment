---
phase: 50-program-maintenance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - programs/epoch-program/src/events.rs
  - programs/epoch-program/src/errors.rs
  - programs/epoch-program/src/instructions/consume_randomness.rs
  - scripts/e2e/lib/carnage-flow.ts
  - scripts/vrf/lib/vrf-flow.ts
  - scripts/vrf/devnet-vrf-validation.ts
autonomous: true

must_haves:
  truths:
    - "No references to 'byte 3' remain in events.rs, errors.rs, consume_randomness.rs, carnage-flow.ts, vrf-flow.ts, or devnet-vrf-validation.ts"
    - "InsufficientRandomness error message says 'need 8' not 'need 6'"
    - "consume_randomness.rs comments reference MIN_VRF_BYTES = 8, not 6"
    - "vrf-flow.ts byte mapping comments are correct (byte 5 = carnageTrigger)"
    - "All VRF byte references in TypeScript files say byte 5 for carnage trigger"
  artifacts:
    - path: "programs/epoch-program/src/events.rs"
      provides: "Corrected CarnageNotTriggered event comments"
    - path: "programs/epoch-program/src/errors.rs"
      provides: "Corrected InsufficientRandomness error message"
    - path: "programs/epoch-program/src/instructions/consume_randomness.rs"
      provides: "Corrected MIN_VRF_BYTES comments"
    - path: "scripts/e2e/lib/carnage-flow.ts"
      provides: "Corrected VRF byte references"
    - path: "scripts/vrf/lib/vrf-flow.ts"
      provides: "Corrected VRF byte mapping and trigger references"
    - path: "scripts/vrf/devnet-vrf-validation.ts"
      provides: "Corrected VRF byte reference"
  key_links: []
---

<objective>
Sweep all stale VRF byte position comments and error messages across the codebase. Update 18 locations (across 6 files) where pre-Phase 37 byte positions are referenced.

Purpose: Accurate documentation prevents confusion during future maintenance and audit. Stale "byte 3" references could mislead developers into checking the wrong VRF byte for Carnage triggers.
Output: All VRF byte references updated to post-Phase 37 layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-program-maintenance/50-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale byte references in Rust files (events.rs, errors.rs, consume_randomness.rs)</name>
  <files>programs/epoch-program/src/events.rs, programs/epoch-program/src/errors.rs, programs/epoch-program/src/instructions/consume_randomness.rs</files>
  <action>
  Three Rust files to update:

  **A. events.rs** (around lines 178-186):
  1. Line ~179: Change "VRF byte 3 did not meet" -> "VRF byte 5 did not meet"
  2. Line ~185: Change "VRF byte 3 value" -> "VRF byte 5 value"

  **B. errors.rs** (around lines 53-54):
  1. Change the InsufficientRandomness error message from `"Insufficient randomness bytes (need 6)"` to `"Insufficient randomness bytes (need 8)"`.
  Note: The `#[msg(...)]` attribute string is what appears in error messages.

  **C. consume_randomness.rs** (around lines 90 and 106):
  1. Line ~90: If comment says "MIN_VRF_BYTES = 6", change to "MIN_VRF_BYTES = 8"
  2. Line ~106: If comment says "less than 6 bytes revealed", change to "less than 8 bytes"
  3. Line ~97: "Check if VRF byte 5 < 11" -- this one is already CORRECT, leave it alone

  **VRF byte layout reference (post-Phase 37):**
  - Byte 0: coinFlip (tax high/low)
  - Byte 1: crimeLow (CRIME low tax)
  - Byte 2: crimeHigh (CRIME high tax)
  - Byte 3: fraudLow (FRAUD low tax)
  - Byte 4: fraudHigh (FRAUD high tax)
  - Byte 5: carnageTrigger (< 11 = Carnage)
  - Byte 6: carnageAction (< 5 = sell)
  - Byte 7: carnageCoin (< 128 = CRIME, >= 128 = FRAUD)
  </action>
  <verify>
  Run: `source "$HOME/.cargo/env" && cd "/Users/mlbob/Projects/Dr Fraudsworth" && grep -rn 'byte 3' programs/epoch-program/src/events.rs programs/epoch-program/src/errors.rs programs/epoch-program/src/instructions/consume_randomness.rs`
  Should return NO results.
  Run: `grep -rn 'need 6' programs/epoch-program/src/errors.rs`
  Should return NO results.
  Run: `cargo test -p epoch-program` -- all tests pass.
  </verify>
  <done>All Rust files reference correct post-Phase 37 byte positions. InsufficientRandomness says "need 8". consume_randomness comments reference MIN_VRF_BYTES = 8.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale byte references in TypeScript files</name>
  <files>scripts/e2e/lib/carnage-flow.ts, scripts/vrf/lib/vrf-flow.ts, scripts/vrf/devnet-vrf-validation.ts</files>
  <action>
  Three TypeScript files to update:

  **A. carnage-flow.ts** -- 5 locations (around lines 6, 64, 472, 658, 843):
  All instances of "VRF byte 3 < 11" -> "VRF byte 5 < 11"
  These are comment strings explaining the Carnage trigger condition. Use find-and-replace across the entire file: every occurrence of "byte 3 < 11" should become "byte 5 < 11".

  **B. vrf-flow.ts** -- 2 locations:
  1. Line ~78: "VRF byte 3 < 11" -> "VRF byte 5 < 11"
  2. Lines ~720-723: Byte mapping comments are wrong. The current mapping says something like:
     ```
     // byte 0: coinFlip
     // byte 1: crimeLow
     // byte 2: crimeHigh
     // byte 3: carnage trigger
     ```
     Fix to the correct post-Phase 37 layout:
     ```
     // byte 0: coinFlip
     // byte 1: crimeLow
     // byte 2: crimeHigh
     // byte 3: fraudLow
     // byte 4: fraudHigh
     // byte 5: carnageTrigger
     // byte 6: carnageAction
     // byte 7: carnageCoin
     ```

  **C. devnet-vrf-validation.ts** -- 1 location:
  Line ~287: "VRF byte 3 < 11" -> "VRF byte 5 < 11"
  </action>
  <verify>
  Run: `grep -rn 'byte 3' scripts/e2e/lib/carnage-flow.ts scripts/vrf/lib/vrf-flow.ts scripts/vrf/devnet-vrf-validation.ts`
  Should return NO results (except if "byte 3" legitimately appears in other contexts like "byte 3: fraudLow" in the corrected mapping -- which is fine).
  Actually, the corrected mapping WILL have "byte 3: fraudLow", so grep for "byte 3 <" or "byte 3:" combined with "carnage" to verify no stale carnage-byte-3 references remain.
  Run: `grep -rn 'byte 3.*carnage\|byte 3 <' scripts/e2e/lib/carnage-flow.ts scripts/vrf/lib/vrf-flow.ts scripts/vrf/devnet-vrf-validation.ts`
  Should return NO results.
  </verify>
  <done>All TypeScript files reference correct post-Phase 37 byte positions. Carnage trigger is byte 5. VRF byte mapping in vrf-flow.ts shows full 8-byte layout.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'byte 3 <\|byte 3.*carnage\|byte 3.*trigger' programs/epoch-program/src/ scripts/e2e/ scripts/vrf/` returns NO results
2. `grep -rn 'need 6' programs/epoch-program/src/errors.rs` returns NO results
3. `grep -rn 'byte 4 < 5' programs/epoch-program/src/constants.rs` returns NO results (fixed in Plan 01)
4. `grep -rn 'byte 5.*11\|byte 5.*carnage' programs/epoch-program/src/events.rs` returns matches (correct)
5. `source "$HOME/.cargo/env" && cargo test -p epoch-program` -- all tests pass
</verification>

<success_criteria>
- Zero occurrences of stale "byte 3" referencing Carnage trigger in any program or script file
- InsufficientRandomness error says "need 8"
- consume_randomness.rs MIN_VRF_BYTES comments say 8
- vrf-flow.ts has correct 8-byte VRF layout mapping
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/50-program-maintenance/50-03-SUMMARY.md`
</output>
