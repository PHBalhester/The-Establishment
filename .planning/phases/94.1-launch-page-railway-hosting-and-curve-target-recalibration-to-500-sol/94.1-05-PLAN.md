---
phase: 94.1-launch-page-railway-hosting-and-curve-target-recalibration-to-500-sol
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/launch/page.tsx
autonomous: false

must_haves:
  truths:
    - "LaunchWalletButton is visible when a curve has failed (refund state)"
    - "LaunchWalletButton is visible during active/filled curve states"
    - "LaunchWalletButton is visible during graduation overlay"
    - "RefundPanel still renders correctly when a curve fails"
    - "User can connect wallet from refund state via the floating button"
  artifacts:
    - path: "app/app/launch/page.tsx"
      provides: "LaunchWalletButton rendered outside StateMachineWrapper"
      contains: "LaunchWalletButton"
  key_links:
    - from: "app/app/launch/page.tsx"
      to: "app/components/launch/LaunchWalletButton.tsx"
      via: "JSX render outside StateMachineWrapper"
      pattern: "<LaunchWalletButton"
---

<objective>
Fix LaunchWalletButton disappearing when a bonding curve fails.

Purpose: When StateMachineWrapper detects a failed curve, it returns only `<RefundPanel>` and discards all `children` -- including the floating `<LaunchWalletButton />`. This means users in refund state have no visible way to connect their wallet (the RefundPanel's inline connect button exists but is reportedly not showing). Moving LaunchWalletButton outside StateMachineWrapper guarantees it renders in ALL states.

Output: Updated page.tsx with LaunchWalletButton rendered independently of StateMachineWrapper.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/app/launch/page.tsx
@app/components/launch/StateMachineWrapper.tsx
@app/components/launch/LaunchWalletButton.tsx
@app/components/launch/RefundPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move LaunchWalletButton outside StateMachineWrapper</name>
  <files>app/app/launch/page.tsx</files>
  <action>
In `app/app/launch/page.tsx`, move `<LaunchWalletButton />` from inside `<StateMachineWrapper>` (currently line 254, the last child before the closing tag) to OUTSIDE and AFTER `<StateMachineWrapper>`, but still inside `<DocsModalProvider>`.

Current structure (lines 109-256):
```
<DocsModalProvider>
  <StateMachineWrapper crime={crime} fraud={fraud}>
    {/* desktop layout */}
    {/* mobile layout */}
    <LaunchWalletButton />       <-- MOVE THIS
  </StateMachineWrapper>
</DocsModalProvider>
```

Target structure:
```
<DocsModalProvider>
  <StateMachineWrapper crime={crime} fraud={fraud}>
    {/* desktop layout */}
    {/* mobile layout */}
  </StateMachineWrapper>
  <LaunchWalletButton />         <-- NOW OUTSIDE, always renders
</DocsModalProvider>
```

This is a one-line move. LaunchWalletButton uses `position: fixed` with `z-50`, so it renders correctly regardless of its position in the component tree. It just needs to be inside the React tree (for hooks) and outside StateMachineWrapper (so it's not discarded in refund/graduation states).

Do NOT modify StateMachineWrapper.tsx, RefundPanel.tsx, or LaunchWalletButton.tsx -- the bug is purely a rendering tree issue in page.tsx.
  </action>
  <verify>
1. Run `cd /Users/mlbob/Projects/Dr\ Fraudsworth && export PATH="/opt/homebrew/bin:$PATH" && npx next lint app/app/launch/page.tsx` -- no errors
2. Run `export PATH="/opt/homebrew/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && npx tsc --noEmit --pretty` -- no type errors
3. Visually inspect the diff: only the `<LaunchWalletButton />` line should have moved, no other changes
  </verify>
  <done>LaunchWalletButton JSX element is a sibling of StateMachineWrapper (not a child), both wrapped by DocsModalProvider</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Moved LaunchWalletButton outside StateMachineWrapper so it renders in all curve states including the refund state.</what-built>
  <how-to-verify>
1. Open the launch page in browser
2. If you can simulate a failed curve state (or have one on devnet), verify:
   - The floating wallet button (top-right corner) is visible on the refund panel
   - Clicking "Connect Wallet" opens the wallet modal
   - After connecting, the address shows in the floating button
3. Also verify normal (active) state still shows the wallet button correctly
4. Check that the RefundPanel's own inline connect/disconnect (inside the "CURVE FAILED" header) also works if visible
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `<LaunchWalletButton />` appears outside `<StateMachineWrapper>` in page.tsx JSX tree
- TypeScript compilation passes with no errors
- No changes to any other files
</verification>

<success_criteria>
- LaunchWalletButton renders in ALL curve states: active, filled, failed (refund), graduated
- The fix is a minimal one-line move with no side effects
- User has visually confirmed the fix works
</success_criteria>

<output>
After completion, create `.planning/phases/94.1-launch-page-railway-hosting-and-curve-target-recalibration-to-500-sol/94.1-05-SUMMARY.md`
</output>
