---
phase: 94.1-launch-page-railway-hosting-and-curve-target-recalibration-to-500-sol
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - programs/bonding_curve/src/constants.rs
  - app/lib/curve/curve-constants.ts
  - scripts/deploy/generate-constants.ts
  - shared/constants.ts
autonomous: true

must_haves:
  truths:
    - "Mainnet/localnet P_START is 450 (was 900)"
    - "Mainnet/localnet P_END is 1725 (was 3450)"
    - "Mainnet/localnet TARGET_SOL is 500,000,000,000 lamports (500 SOL)"
    - "Devnet constants are UNCHANGED (P_START=5, P_END=17, TARGET_SOL=5 SOL)"
    - "Client-side constants match on-chain constants exactly"
    - "shared/constants.ts is regenerated with 500 SOL value"
    - "Program compiles successfully with both devnet and default features"
  artifacts:
    - path: "programs/bonding_curve/src/constants.rs"
      provides: "On-chain source of truth for curve parameters"
      contains: "P_START: u128 = 450"
    - path: "app/lib/curve/curve-constants.ts"
      provides: "Client-side BigInt mirror of on-chain constants"
      contains: "450n"
    - path: "scripts/deploy/generate-constants.ts"
      provides: "Auto-generator for shared/constants.ts"
      contains: "500_000_000_000"
    - path: "shared/constants.ts"
      provides: "Auto-generated shared constants consumed by scripts"
      contains: "CURVE_TARGET_SOL"
  key_links:
    - from: "programs/bonding_curve/src/constants.rs"
      to: "app/lib/curve/curve-constants.ts"
      via: "manual mirror (must stay in sync)"
      pattern: "P_START.*450"
    - from: "scripts/deploy/generate-constants.ts"
      to: "shared/constants.ts"
      via: "code generation (auto-written)"
      pattern: "CURVE_TARGET_SOL"
---

<objective>
Update all bonding curve constants from 1000 SOL target to 500 SOL target across the on-chain program, client-side TypeScript, and auto-generated shared constants.

Purpose: Market conditions dictate a lower fundraise target. 500 SOL per curve (1000 SOL total) is more achievable while preserving the same 3.83x price appreciation curve shape.

Output: Updated constants in Rust, client TS, generate-constants.ts, and regenerated shared/constants.ts
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/94.1-launch-page-railway-hosting-and-curve-target-recalibration-to-500-sol/94.1-CONTEXT.md
@.planning/phases/94.1-launch-page-railway-hosting-and-curve-target-recalibration-to-500-sol/94.1-RESEARCH.md
@programs/bonding_curve/src/constants.rs
@app/lib/curve/curve-constants.ts
@scripts/deploy/generate-constants.ts
@shared/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update on-chain Rust constants</name>
  <files>programs/bonding_curve/src/constants.rs</files>
  <action>
Update `programs/bonding_curve/src/constants.rs` -- ONLY the localnet and mainnet (default) values. Devnet stays unchanged.

Changes:
1. **P_START localnet:** 900 -> 450
2. **P_START mainnet (default):** 900 -> 450
3. **P_END localnet:** 3_450 -> 1_725
4. **P_END mainnet (default):** 3_450 -> 1_725
5. **TARGET_SOL localnet:** 1_000_000_000_000 -> 500_000_000_000
6. **TARGET_SOL mainnet (default):** 1_000_000_000_000 -> 500_000_000_000

Update all doc comments that reference the old values:
- "Mainnet: P_START=900, P_END=3450 -> ... (~1000 SOL)" becomes "Mainnet: P_START=450, P_END=1725 -> (450+1725)/2 * 460M = 500,250,000,000 lam (~500 SOL)"
- "~3.4-3.8x ratio" -- ratio is exactly 3.833x, this comment is fine
- "Target SOL raised" comments: "Mainnet: 1000 SOL per curve" becomes "Mainnet: 500 SOL per curve"

DO NOT touch devnet values (P_START=5, P_END=17, TARGET_SOL=5_000_000_000).

After editing, verify the compile-time assertion `P_END > P_START` still holds (1725 > 450, obviously true).

Then verify the program compiles:
```bash
source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH"
cd "/Users/mlbob/Projects/Dr Fraudsworth"
anchor build -p bonding_curve -- --features devnet
```
Also verify it compiles without devnet feature (for the localnet constants):
```bash
anchor build -p bonding_curve -- --features localnet
```
  </action>
  <verify>
Both `anchor build -p bonding_curve -- --features devnet` and `anchor build -p bonding_curve -- --features localnet` succeed without errors.
  </verify>
  <done>On-chain constants updated: P_START=450, P_END=1725, TARGET_SOL=500 SOL for localnet/mainnet. Devnet unchanged. Both feature-flag builds succeed.</done>
</task>

<task type="auto">
  <name>Task 2: Update client-side constants and regenerate shared</name>
  <files>app/lib/curve/curve-constants.ts, scripts/deploy/generate-constants.ts, shared/constants.ts</files>
  <action>
Update client-side constants to mirror the new on-chain values:

**1. `app/lib/curve/curve-constants.ts`:**
- `P_START`: `isDevnet ? 5n : 900n` -> `isDevnet ? 5n : 450n`
- `P_END`: `isDevnet ? 17n : 3_450n` -> `isDevnet ? 17n : 1_725n`
- `TARGET_SOL`: `isDevnet ? 5_000_000_000n : 1_000_000_000_000n` -> `isDevnet ? 5_000_000_000n : 500_000_000_000n`
- Update doc comments: "Mainnet: 900 (1000 SOL target)" -> "Mainnet: 450 (500 SOL target)" etc.

**2. `scripts/deploy/generate-constants.ts`:**
- Line 261: `CURVE_TARGET_SOL = 1_000_000_000_000` -> `CURVE_TARGET_SOL = 500_000_000_000`

**3. Regenerate `shared/constants.ts`:**
Run generate-constants.ts to regenerate shared/constants.ts:
```bash
export PATH="/opt/homebrew/bin:$PATH"
cd "/Users/mlbob/Projects/Dr Fraudsworth"
npx tsx scripts/deploy/generate-constants.ts
```

Verify shared/constants.ts now contains 500_000_000_000 (not 1_000_000_000_000).

DO NOT manually edit shared/constants.ts -- it is auto-generated. Only edit generate-constants.ts and re-run it.
  </action>
  <verify>
1. `grep "450n" app/lib/curve/curve-constants.ts` shows the new P_START value
2. `grep "1_725n" app/lib/curve/curve-constants.ts` shows the new P_END value
3. `grep "500_000_000_000" app/lib/curve/curve-constants.ts` shows new TARGET_SOL
4. `grep "500_000_000_000" shared/constants.ts` confirms regenerated value
5. `grep "1_000_000_000_000" shared/constants.ts` returns NO matches (old value gone)
  </verify>
  <done>Client-side curve-constants.ts updated with 500 SOL values. generate-constants.ts updated. shared/constants.ts regenerated with correct values. All three files in sync with on-chain constants.</done>
</task>

</tasks>

<verification>
1. `grep -c "450" programs/bonding_curve/src/constants.rs` shows P_START occurrences
2. `grep -c "1_725" programs/bonding_curve/src/constants.rs` shows P_END occurrences
3. `grep -c "500_000_000_000" programs/bonding_curve/src/constants.rs` shows TARGET_SOL
4. Devnet values unchanged: `grep "P_START: u128 = 5" programs/bonding_curve/src/constants.rs` still present
5. Client mirrors on-chain: P_START, P_END, TARGET_SOL match across files
6. shared/constants.ts was auto-generated (not manually edited)
7. Both `--features devnet` and `--features localnet` builds succeed
</verification>

<success_criteria>
- Mainnet/localnet: P_START=450, P_END=1725, TARGET_SOL=500 SOL
- Devnet: P_START=5, P_END=17, TARGET_SOL=5 SOL (UNCHANGED)
- Client-side constants exactly mirror on-chain values
- shared/constants.ts regenerated with correct values
- Bonding curve program compiles with both feature flags
</success_criteria>

<output>
After completion, create `.planning/phases/94.1-launch-page-railway-hosting-and-curve-target-recalibration-to-500-sol/94.1-02-SUMMARY.md`
</output>
