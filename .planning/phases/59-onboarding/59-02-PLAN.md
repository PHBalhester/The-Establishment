---
phase: 59-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - app/providers/providers.tsx
autonomous: false

must_haves:
  truths:
    - "First-time visitors (no localStorage flag) see the welcome modal on page load"
    - "After clicking Enter the Factory, the modal closes with animation and does not appear on refresh"
    - "After clicking Connect Wallet, the modal closes and Privy wallet picker opens and is interactive"
    - "Returning visitors (localStorage flag present) never see the welcome modal"
    - "Escape key dismisses the modal and it does not reappear on refresh"
    - "Welcome modal renders with steampunk chrome matching station modals"
    - "Mobile: welcome modal is fullscreen with centered content"
    - "Desktop: welcome modal is centered card with iris-like scale entrance"
    - "Build passes with zero errors"
  artifacts:
    - path: "app/providers/providers.tsx"
      provides: "WelcomeModal mounted as sibling of ModalRoot inside PrivyProvider"
      contains: "WelcomeModal"
  key_links:
    - from: "app/providers/providers.tsx"
      to: "app/components/onboarding/WelcomeModal.tsx"
      via: "import and render as sibling of ModalRoot"
      pattern: "<WelcomeModal"
    - from: "app/components/onboarding/WelcomeModal.tsx"
      to: "@privy-io/react-auth"
      via: "useConnectWallet accessed because WelcomeModal is inside PrivyProvider"
      pattern: "PrivyProvider.*ModalProvider.*WelcomeModal"
---

<objective>
Wire the WelcomeModal into the app's provider tree and verify the complete onboarding flow works end-to-end. This is the integration plan: mounting the component created in Plan 01 and confirming it functions correctly on both desktop and mobile.

Purpose: The welcome modal must actually render in the app for users to see it. Without this wiring step, the component exists but is never mounted.

Output: Updated providers.tsx with WelcomeModal mounted. Successful production build. Human-verified visual and functional behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-onboarding/59-CONTEXT.md
@.planning/phases/59-onboarding/59-01-SUMMARY.md

# Files to modify:
@app/providers/providers.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire WelcomeModal into providers and verify build</name>
  <files>app/providers/providers.tsx</files>
  <action>
**Mount WelcomeModal in providers.tsx:**

1. Add import at top of file:
   ```
   import { WelcomeModal } from '@/components/onboarding/WelcomeModal';
   ```

2. Render `<WelcomeModal />` as a sibling of `<ModalRoot />` inside the provider tree. Place it AFTER ModalRoot in JSX order. The WelcomeModal needs to be inside:
   - `<PrivyProvider>` (for useConnectWallet hook access)
   - `<ModalProvider>` (not strictly needed, but it's the natural nesting position)
   - `<ToastProvider>` (not strictly needed, but consistent placement)

   Updated JSX:
   ```tsx
   <ModalProvider>
     <ToastProvider>
       {children}
       <ModalRoot />
       <WelcomeModal />
       <ToastContainer />
       <WalletConnectionToast />
       <PrivyTopLayerFix />
     </ToastProvider>
   </ModalProvider>
   ```

3. Add a comment explaining the welcome modal's role:
   ```
   {/* WelcomeModal: standalone dialog for first-time visitors.
       Independent from ModalShell -- has its own <dialog> element.
       Shows once per browser via localStorage, then never again. */}
   ```

**Build verification:**

Run production build to confirm zero errors:
```bash
export PATH="/opt/homebrew/bin:$PATH"
cd /Users/mlbob/Projects/Dr\ Fraudsworth/app
npx next build
```

The build must complete with exit code 0. If there are TypeScript or CSS errors, fix them before proceeding to the checkpoint.

**Pre-checkpoint smoke test:**

Start dev server and verify basic rendering:
```bash
export PATH="/opt/homebrew/bin:$PATH"
cd /Users/mlbob/Projects/Dr\ Fraudsworth/app
npx next dev --turbopack &
```

Open browser to localhost, clear localStorage (`localStorage.removeItem('dr-fraudsworth-welcomed')`), and refresh. The welcome modal should appear. This is a quick sanity check before the human-verify checkpoint.
  </action>
  <verify>
  1. `app/providers/providers.tsx` imports WelcomeModal from '@/components/onboarding/WelcomeModal'
  2. `<WelcomeModal />` renders inside PrivyProvider tree
  3. Production build completes with exit code 0: `export PATH="/opt/homebrew/bin:$PATH" && cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && npx next build`
  4. Dev server starts without errors on the root page
  </verify>
  <done>
  WelcomeModal is mounted in the provider tree. Production build passes. Dev server renders the welcome modal for first-time visitors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete onboarding welcome modal for first-time visitors. A standalone steampunk-chromed dialog with "Welcome to Dr. Fraudsworth's Finance Factory" title, protocol description, and two action buttons ("Enter the Factory" / "Connect Wallet"). Persists dismissal in localStorage so returning visitors never see it again.
  </what-built>
  <how-to-verify>
  **Setup:** Open the dev server URL (likely http://localhost:3000). Clear localStorage first: open DevTools console and run `localStorage.removeItem('dr-fraudsworth-welcomed')`, then refresh.

  **Desktop verification (viewport >= 1024px):**
  1. On page load, the welcome modal should appear as a centered card with steampunk brass chrome (gradient background, brass border, corner bolts, decorative emblem)
  2. The factory scene should be visible but blurred behind the modal backdrop
  3. Title reads "Welcome to Dr. Fraudsworth's Finance Factory" in Cinzel serif font
  4. Body has 2-3 sentences explaining the protocol (three tokens, taxes fund yield, staking earns SOL)
  5. Token names (CRIME, FRAUD, PROFIT) should be in monospace font
  6. Two buttons visible: "Enter the Factory" (primary/prominent) and "Connect Wallet" (secondary)
  7. Click "Enter the Factory" -- modal should fade out smoothly, factory scene revealed
  8. Refresh -- modal should NOT reappear (localStorage flag persists)
  9. Clear localStorage again, refresh -- modal appears again
  10. This time press Escape -- modal should animate closed and not reappear on refresh
  11. Clear localStorage again, refresh -- click "Connect Wallet" -- modal should close and Privy wallet picker should appear and be fully interactive (buttons clickable, not inert)

  **Mobile verification (viewport < 1024px, or use DevTools responsive mode):**
  12. Clear localStorage, resize to mobile viewport (e.g., 375px width)
  13. Welcome modal should be fullscreen (100dvh), content centered vertically
  14. Buttons should be stacked vertically with adequate touch targets
  15. "Enter the Factory" dismisses to mobile navigation view
  16. Back on mobile, verify "Connect Wallet" path works (Privy dialog functional)

  **Edge cases:**
  17. Tab key should cycle within the modal (focus trap from showModal)
  18. No double modals -- while welcome is open, clicking behind it should not open a station modal
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the welcome modal appearance, behavior, or content</resume-signal>
</task>

</tasks>

<verification>
1. Production build passes (zero errors)
2. Welcome modal appears for first-time visitors (no localStorage flag)
3. Welcome modal does NOT appear for returning visitors (localStorage flag present)
4. "Enter the Factory" closes modal with animation
5. "Connect Wallet" closes modal and Privy wallet picker is interactive
6. Escape key dismisses and persists
7. Mobile fullscreen layout works
8. Desktop centered card layout works
9. Steampunk chrome matches station modals visually
</verification>

<success_criteria>
- First-time visitors see welcome modal with protocol introduction and two action paths
- Modal never reappears after dismissal (localStorage persistence verified across refresh)
- Privy wallet flow is functional after Connect Wallet (no inert dialog trap)
- Build passes, no TypeScript errors, no CSS errors
- Human-verified visual quality on both desktop and mobile
</success_criteria>

<output>
After completion, create `.planning/phases/59-onboarding/59-02-SUMMARY.md`
</output>
