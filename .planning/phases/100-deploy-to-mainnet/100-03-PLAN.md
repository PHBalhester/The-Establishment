---
phase: 100-deploy-to-mainnet
plan: 03
type: execute
wave: 3
depends_on: ["100-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Bonding curve program deployed to mainnet at launch moment"
    - "Both CRIME and FRAUD curves initialized and accepting buys"
    - "Frontend launch page shows live curves with pressure gauges"
    - "After both curves fill, graduation creates AMM pools with curve proceeds"
    - "Crank is running on Railway mainnet and advancing epochs"
    - "Frontend transitions from launch page to trading interface after graduation"
  artifacts:
    - path: "deployments/mainnet.json"
      provides: "Complete mainnet address registry including bonding curve + pool addresses"
      contains: "bondingCurve"
  key_links:
    - from: "scripts/deploy/stage-5-launch.sh"
      to: "mainnet-beta"
      via: "bonding curve deploy + curve init"
      pattern: "LAUNCH"
    - from: "scripts/graduation/graduate.ts"
      to: "mainnet-beta"
      via: "coupled graduation into AMM pools"
      pattern: "GRADUATE"
---

<objective>
Execute Stage 5 (launch) and Stage 6 (graduation) of the mainnet deployment. Stage 5 deploys the bonding curve program and initializes curves at the chosen launch moment. After community fills both curves (up to 48hr), Stage 6 graduates them into AMM pools and starts the crank.

Purpose: This is the PUBLIC LAUNCH of Dr. Fraudsworth's Finance Factory. Stage 5 makes the protocol live. Stage 6 transitions from bonding curve phase to trading phase. The fill period between them is community-driven and may take up to 48 hours.

Output: Bonding curves live and filled, graduated into AMM pools, crank running, protocol fully operational.
</objective>

<phase102_prerequisite>
⚠️ PHASE 102 DEVNET REDEPLOY (2026-03-20) CHANGED PROGRAM KEYPAIRS

Before executing this plan, you MUST complete the mainnet restoration checklist
in Docs/mainnet-deploy-checklist.md Appendix E. Summary:

1. Restore mainnet program keypairs from keypairs/mainnet-*-program.json backups
2. Run sync-program-ids to update declare_id! macros back to mainnet IDs
3. Rebuild WITHOUT --devnet flag (anchor build, NOT anchor build --features devnet)
4. Verify deployments/mainnet.json programs match restored keypair pubkeys
5. Verify mainnet vanity mint keypairs are intact

DO NOT proceed with Stage 5 until all 5 steps are verified.
</phase102_prerequisite>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-deploy-to-mainnet/100-CONTEXT.md
@.planning/phases/100-deploy-to-mainnet/100-RESEARCH.md
@.planning/phases/100-deploy-to-mainnet/100-02-SUMMARY.md
@Docs/mainnet-deploy-checklist.md
@scripts/deploy/stage-5-launch.sh
@scripts/deploy/stage-6-graduation.sh
@scripts/graduation/graduate.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Launch timing: When to execute Stage 5 and deploy bonding curves to mainnet</decision>
  <context>
    Per CONTEXT.md: "Exact launch day/time is a game-day decision -- not locked in advance."

    Before proceeding, confirm:
    1. Joint review from Plan 02 passed (all Stage 0-4 artifacts verified)
    2. Marketing/community coordination is ready (out of scope for this plan, but launch should be announced)
    3. Priority fee levels are acceptable for launch-day deploy:
       ```bash
       solana fees --url mainnet-beta
       ```
    4. Deployer has sufficient SOL remaining (~14 SOL, need ~4.72 for Stage 5)
    5. UptimeRobot monitoring is active (Phase 98.1)
    6. Claude session on standby for emergency response


    **Stage 5 takes ~5 minutes:** deploy bonding curve program, whitelist curve PDAs, init both curves. After this, curves are LIVE and publicly accessible.
  </context>
  <options>
    <option id="launch-now">
      <name>Execute Stage 5 now</name>
      <pros>Protocol goes live immediately</pros>
      <cons>Irreversible -- curves go live</cons>
    </option>
    <option id="wait">
      <name>Wait for better timing</name>
      <pros>More preparation, coordination</pros>
      <cons>Delays launch</cons>
    </option>
  </options>
  <resume-signal>Select: launch-now or wait (with reason)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute Stage 5 (Launch -- Deploy Bonding Curve + Init Curves)</name>
  <files></files>
  <action>
    Execute Stage 5 on mainnet. This is the PUBLIC LAUNCH MOMENT.

    **CRITICAL: Check priority fees before launch:**
    ```bash
    source "$HOME/.cargo/env" && export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"
    solana fees --url mainnet-beta
    ```

    If fees are elevated, consider increasing `--with-compute-unit-price` in stage-5-launch.sh from 1 to 50000 (micro-lamports per CU) to ensure the bonding curve deploy lands quickly. This is the one time-critical deploy -- snipers may be watching.

    **Execute:**
    ```bash
    set -a && source .env.mainnet && set +a
    ./scripts/deploy/stage-5-launch.sh mainnet
    ```

    The script will:
    1. Deploy bonding curve program (anti-sniper: withheld from Stage 2)
    2. Whitelist bonding curve PDAs in transfer hook
    3. Initialize both CRIME and FRAUD curves with 48hr deadline
    4. Require typing "LAUNCH" to confirm

    **After Stage 5 completes, verify:**
    1. Bonding curve program is executable on mainnet
    2. Both curves initialized with correct parameters (500 SOL target per curve)
    3. Frontend launch page shows live curves with pressure gauges
    4. A test buy works (small amount, e.g. 0.01 SOL) to confirm curves accept purchases

    **Then enter fill period monitoring:**
    - Monitor via frontend launch page gauges
    - UptimeRobot alerts for site downtime
    - No custom alerting scripts (CONTEXT.md decision)
    - Fill period may take up to 48 hours
    - If both curves don't fill within 48hr, refund path activates (protocol does not proceed)
  </action>
  <verify>
    1. Bonding curve program is executable: `solana program show <BC_PROGRAM_ID> --url mainnet-beta`
    2. Both curves show on frontend with buy/sell functionality
    3. deployments/mainnet.json updated with bonding curve program ID and curve PDAs
  </verify>
  <done>Stage 5 complete: bonding curve deployed, both curves live and accepting purchases, frontend showing live pressure gauges</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Both bonding curves are live on mainnet. Community is buying tokens. Need to confirm both curves have filled before running graduation.</what-built>
  <how-to-verify>
    **IMPORTANT: Multi-session wait expected.** The fill period is community-driven and may take up to 48 hours. This checkpoint will likely span multiple Claude sessions. When resuming:
    1. Re-read this plan (100-03-PLAN.md) to restore context
    2. Re-source .env.mainnet: `set -a && source .env.mainnet && set +a`
    3. Check curve status below, then proceed to Task 4 if both filled

    **Pre-graduation verification (CONTEXT.md: "Pre-graduation on-chain verification required"):**

    1. Visit frontend launch page -- both pressure gauges should show 100% filled
    2. Verify on-chain that both curves are genuinely filled. Read each curve PDA account data directly:
       ```bash
       source "$HOME/.cargo/env" && export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"
       set -a && source .env.mainnet && set +a
       # Get curve PDAs from deployments/mainnet.json
       CRIME_CURVE=$(cat deployments/mainnet.json | jq -r '.bondingCurve.crimeCurve')
       FRAUD_CURVE=$(cat deployments/mainnet.json | jq -r '.bondingCurve.fraudCurve')
       # Check each curve's total_raised vs target on-chain
       solana account $CRIME_CURVE --url mainnet-beta --output json
       solana account $FRAUD_CURVE --url mainnet-beta --output json
       ```
       Verify `total_raised >= target` (500 SOL per curve) in the decoded account data.
    3. Confirm deployer has enough SOL for graduation (~0.05 SOL needed)

    **If curves NOT filled after 48hr:** The refund path activates automatically. Do NOT run graduation. Phase 100 ends here with a different outcome.

    **If BOTH curves filled:** Proceed to graduation.
  </how-to-verify>
  <resume-signal>Type "both-filled" to proceed with graduation, "refund-path" if 48hr expired without filling, or "still-filling" to wait longer</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Execute Stage 6 (Graduation -- Create AMM Pools + Start Crank)</name>
  <files></files>
  <action>
    Execute Stage 6 on mainnet. This graduates both bonding curves into AMM pools.

    **Execute:**
    ```bash
    set -a && source .env.mainnet && set +a
    ./scripts/deploy/stage-6-graduation.sh mainnet
    ```

    The script will:
    1. Run graduate.ts with checkpoint/resume (idempotent)
    2. Create 4 AMM pools (CRIME/SOL, FRAUD/SOL, CRIME/PROFIT, FRAUD/PROFIT)
    3. Seed pools with curve proceeds (dynamic balance-delta tracking)
    4. Whitelist all pool vault PDAs in transfer hook
    5. Retain whitelist authority (NOT burned -- transferred to Squads multisig at Stage 7 for future flexibility)
    6. Require typing "GRADUATE" to confirm

    **After graduation, verify:**
    1. All 4 pools initialized with correct liquidity
    2. Trading works via frontend (test a small swap)
    3. Tax distribution is working (75/24/1 split on SOL pools)

    **Start crank on Railway:**
    1. Deploy crank service to Railway mainnet environment
    2. Set env vars from deployments/mainnet.json:
       - CARNAGE_WSOL_PUBKEY
       - PDA_MANIFEST (full JSON string from scripts/deploy/pda-manifest.json)
       - WALLET_KEYPAIR (deployer secret key JSON array)
       - CLUSTER_URL (mainnet Helius RPC)
    3. Verify crank advances first epoch
    4. Confirm epoch transitions on frontend

    **Switch frontend from launch mode to live mode:**
    - Verify `NEXT_PUBLIC_CLUSTER=mainnet` is set on Railway mainnet service (CRITICAL — drives
      address resolution, program IDs, and ALT. Without this, frontend uses devnet addresses.)
    - Set NEXT_PUBLIC_SITE_MODE=live on Railway
    - Verify `HELIUS_RPC_URL` is set to mainnet Helius (server-side, no fallback — app will throw
      if missing, preventing silent cross-cluster routing)
    - Verify DBS migration env vars are set on Railway (should be from Stage 4.5):
      - `WS_SUBSCRIBER_ENABLED=true` (enables server-side SSE pipeline — without this, hooks fall back to RPC polling)
      - `TOKEN_SUPPLY_POLL_INTERVAL_MS=60000`
      - `STAKER_COUNT_POLL_INTERVAL_MS=30000`
      - `SLOT_BROADCAST_INTERVAL_MS=5000`
    - Redeploy frontend
    - Verify frontend shows trading interface (not launch page)

    **Register mainnet Helius webhook:**
    ```bash
    npx tsx scripts/webhook-manage.ts register mainnet
    ```
  </action>
  <verify>
    1. All 4 pools show on frontend with correct reserves
    2. A test swap completes successfully
    3. Crank is running (Railway service logs show epoch advancement)
    4. Frontend is in "live" mode showing trading interface
    5. `npx tsx scripts/deploy/verify.ts mainnet` -- all checks pass (including pools)
  </verify>
  <done>Stage 6 complete: both curves graduated into AMM pools, crank running on Railway, frontend in live mode, protocol fully operational for trading</done>
</task>

</tasks>

<verification>
- Bonding curve deployed and curves filled by community
- Graduation successful -- 4 AMM pools created with correct liquidity
- Crank running on Railway, epochs advancing
- Frontend in live mode, trading functional
- verify.ts all checks pass
</verification>

<success_criteria>
- Protocol is LIVE on mainnet with active trading
- Crank is advancing epochs with VRF randomness
- Frontend displays real mainnet data
- Tax distribution working correctly
</success_criteria>

<output>
After completion, create `.planning/phases/100-deploy-to-mainnet/100-03-SUMMARY.md`
</output>
