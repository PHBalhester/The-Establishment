---
phase: 51-program-rebuild-devnet-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - programs/amm/tests/test_swap_sol_pool.rs
  - programs/amm/tests/test_swap_profit_pool.rs
autonomous: true

must_haves:
  truths:
    - "All 7 test_swap_sol_pool tests pass (previously AccountNotSigner on swap_authority)"
    - "All 12 test_swap_profit_pool tests pass (previously AccountNotSigner on swap_authority)"
  artifacts:
    - path: "programs/amm/tests/test_swap_sol_pool.rs"
      provides: "Updated SOL pool swap test harness with swap_authority PDA"
    - path: "programs/amm/tests/test_swap_profit_pool.rs"
      provides: "Updated profit pool swap test harness with swap_authority PDA"
  key_links:
    - from: "programs/amm/tests/test_swap_sol_pool.rs"
      to: "programs/amm/src/instructions/swap_sol_pool.rs"
      via: "swap_authority PDA derived from Tax Program ID"
      pattern: "swap_authority"
---

<objective>
Fix all 19 AMM LiteSVM integration test failures caused by Phase 46's swap_authority PDA requirement.

Purpose: The AMM swap instructions now require `swap_authority` to be a PDA signed by the Tax Program (Phase 46 security hardening). The LiteSVM test harnesses still call AMM swap directly without proper PDA setup, causing AccountNotSigner (error 3010) on every swap test. The on-chain code is authoritative -- tests must adapt.

Output: All 19 AMM swap tests passing (`cargo test -p amm --tests`)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-program-rebuild-devnet-deploy/51-RESEARCH.md

Key context from RESEARCH.md:
- All 19 failures are `AccountNotSigner` (error 3010) on `swap_authority`
- 7 in test_swap_sol_pool.rs, 12 in test_swap_profit_pool.rs
- swap_authority is derived with seeds `[b"swap_authority"]` from the Tax Program ID
- Tests call AMM swap directly (not via Tax Program CPI)
- LiteSVM supports loading multiple programs -- check if we can restructure tests to CPI through Tax Program, OR find how the test harness can mark the PDA as a signer in LiteSVM (LiteSVM may have a `set_signer` or similar mechanism)

Key files to reference:
@programs/amm/src/instructions/swap_sol_pool.rs (the swap instruction struct -- see swap_authority account)
@programs/amm/src/instructions/swap_profit_pool.rs (same pattern)
@programs/amm/tests/test_swap_sol_pool.rs (failing tests)
@programs/amm/tests/test_swap_profit_pool.rs (failing tests)
@programs/tax-program/src/lib.rs (Tax Program CPI that normally signs for swap_authority)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test_swap_sol_pool.rs (7 failures)</name>
  <files>programs/amm/tests/test_swap_sol_pool.rs</files>
  <action>
Investigate the AMM's swap_authority account constraint to understand exactly what the tests need to provide. The swap_authority is a PDA with seeds `[b"swap_authority"]` derived from TAX_PROGRAM_ID. In LiteSVM integration tests, the Tax Program is not loaded (tests only load the AMM), so the PDA cannot sign via CPI.

**Research first:** Check how the test harness currently creates/passes swap_authority. Check if LiteSVM has a mechanism to set an arbitrary account as a signer (some test frameworks allow this). Check how other test files in the repo (e.g., test_cpi_access_control.rs) handle this.

**Fix approach (determine which works):**
Option A: If LiteSVM supports adding arbitrary signers for PDAs, derive the swap_authority PDA from Tax Program ID and add it as a signer in the test transaction context.
Option B: If LiteSVM supports loading multiple programs, load both AMM and Tax Program, then invoke via Tax Program's CPI path (more realistic but heavier restructure).
Option C: If neither works, the test may need to use the mock-tax-program (which exists at `programs/mock-tax-program/`) to simulate the CPI signing.

Whichever approach works, apply it consistently to all 7 failing tests in this file. Do NOT modify the on-chain AMM code -- the code is authoritative. Only modify test harness code.

Also check for any new accounts that the swap instruction expects post-Phase 46-50 (e.g., additional accounts in remaining_accounts) and ensure the test harness provides them.
  </action>
  <verify>
Run `cargo test -p amm --test test_swap_sol_pool` (from project root with env sourced).
All 7 previously-failing tests must pass. Zero new failures.
  </verify>
  <done>7/7 test_swap_sol_pool tests pass where previously all 7 failed with AccountNotSigner</done>
</task>

<task type="auto">
  <name>Task 2: Fix test_swap_profit_pool.rs (12 failures)</name>
  <files>programs/amm/tests/test_swap_profit_pool.rs</files>
  <action>
Apply the same swap_authority fix pattern discovered in Task 1 to test_swap_profit_pool.rs. The root cause is identical (AccountNotSigner on swap_authority, error 3010).

The profit pool swap instruction has the same swap_authority PDA requirement as the SOL pool swap. Use the exact same approach from Task 1.

Key difference: profit pool swaps may have different remaining_accounts requirements (dual hook accounts for Token-2022 input/output mints). Verify the account setup matches what the on-chain instruction expects.

Apply the fix to all 12 failing tests. If any helper functions are shared between the two test files (e.g., a common test setup module), extract the swap_authority fix into the shared helper.
  </action>
  <verify>
Run `cargo test -p amm --test test_swap_profit_pool` (from project root with env sourced).
All 12 previously-failing tests must pass. Zero new failures.
Then run `cargo test -p amm --tests` to confirm all AMM tests pass (both files plus other test files like test_pool_initialization, test_cpi_access_control, test_transfer_routing).
  </verify>
  <done>12/12 test_swap_profit_pool tests pass, AND full AMM test suite passes (including the 7 from Task 1 + all other AMM tests)</done>
</task>

</tasks>

<verification>
Run `cargo test -p amm --tests` from project root with environment sourced:
```bash
source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH" && cargo test -p amm --tests
```
Expected: 19 previously-failing tests now pass. All other AMM tests continue to pass. Zero test failures.
</verification>

<success_criteria>
- `cargo test -p amm --tests` shows 0 failures
- All 19 previously-failing tests (7 sol_pool + 12 profit_pool) now pass
- No on-chain code was modified (only test harness files)
</success_criteria>

<output>
After completion, create `.planning/phases/51-program-rebuild-devnet-deploy/51-01-SUMMARY.md`
</output>
