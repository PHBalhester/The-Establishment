---
phase: 51-program-rebuild-devnet-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - programs/tax-program/tests/test_swap_sol_buy.rs
  - programs/tax-program/tests/test_swap_sol_sell.rs
autonomous: true

must_haves:
  truths:
    - "All 5 test_swap_sol_buy tests pass (previously InvalidAccountData on pool_vault_b)"
    - "All 5 test_swap_sol_sell tests pass (previously InvalidAccountData on pool_vault_b + missing wsol_intermediary)"
  artifacts:
    - path: "programs/tax-program/tests/test_swap_sol_buy.rs"
      provides: "Updated buy test harness with Token-2022 vaults"
    - path: "programs/tax-program/tests/test_swap_sol_sell.rs"
      provides: "Updated sell test harness with Token-2022 vaults + wsol_intermediary account"
  key_links:
    - from: "programs/tax-program/tests/test_swap_sol_sell.rs"
      to: "programs/tax-program/src/instructions/swap_sol_sell.rs"
      via: "wsol_intermediary PDA account matching Phase 48 on-chain changes"
      pattern: "wsol_intermediary"
---

<objective>
Fix all 10 Tax Program LiteSVM integration test failures caused by Phase 46-48 changes: Token-2022 InterfaceAccount vaults and the new wsol_intermediary account.

Purpose: Two root causes: (1) Pool vaults are now `InterfaceAccount<TokenAccount>` requiring Token-2022 program data, but test harnesses create vaults under SPL Token -- causes InvalidAccountData (error 17179869184). (2) The sell handler now requires the `wsol_intermediary` account (Phase 48), which tests don't provide. The on-chain code is authoritative -- tests must adapt.

Output: All 10 Tax Program swap tests passing (`cargo test -p tax-program --test test_swap_sol_buy --test test_swap_sol_sell`)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-program-rebuild-devnet-deploy/51-RESEARCH.md

Key context from RESEARCH.md:
- 5 test_swap_sol_buy failures: InvalidAccountData on pool_vault_b (Token-2022 vs SPL Token)
- 5 test_swap_sol_sell failures: Same InvalidAccountData + missing wsol_intermediary account
- Test helpers use `spl_token::instruction::initialize_account` -- need Token-2022 equivalents
- wsol_intermediary is a PDA with seeds from Phase 48 (check tax-program constants.rs for WSOL_INTERMEDIARY_SEED)

Key files to reference:
@programs/tax-program/src/instructions/swap_sol_buy.rs (buy instruction struct)
@programs/tax-program/src/instructions/swap_sol_sell.rs (sell instruction struct -- has wsol_intermediary)
@programs/tax-program/src/constants.rs (WSOL_INTERMEDIARY_SEED)
@programs/tax-program/tests/test_swap_sol_buy.rs (failing tests)
@programs/tax-program/tests/test_swap_sol_sell.rs (failing tests)
@programs/amm/src/state/pool.rs (PoolState -- InterfaceAccount<TokenAccount> for vaults)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test_swap_sol_buy.rs (5 failures)</name>
  <files>programs/tax-program/tests/test_swap_sol_buy.rs</files>
  <action>
The 5 buy test failures are all `InvalidAccountData` on `pool_vault_b`. The on-chain code expects vaults to be Token-2022 `InterfaceAccount<TokenAccount>`, but the test harness creates vaults using the old SPL Token program.

**Fix approach:**
1. Find the test helper function that creates pool vault accounts (likely a shared helper in the tests directory or inline in each test file).
2. Change vault creation to use `spl_token_2022::instruction::initialize_account3` (or the appropriate Token-2022 instruction) instead of `spl_token::instruction::initialize_account`.
3. Ensure the vault account's owner is set to `TOKEN_2022_PROGRAM_ID` (not `TOKEN_PROGRAM_ID`).
4. The mint itself may still be WSOL (SPL Token native mint) for SOL pools. Check if `InterfaceAccount` requires the vault AND mint to be from the same token program, or if it accepts cross-program combinations. If it's flexible (Interface means both are accepted), just the vault program ID needs to change.
5. Also check if the buy instruction has any new accounts added in Phases 46-50 that the test harness doesn't provide yet (beyond just the vault fix).

Apply the fix to all 5 failing buy tests. Do NOT modify on-chain code.
  </action>
  <verify>
Run `cargo test -p tax-program --test test_swap_sol_buy` (from project root with env sourced).
All 5 previously-failing tests must pass. Zero new failures.
  </verify>
  <done>5/5 test_swap_sol_buy tests pass where previously all 5 failed with InvalidAccountData</done>
</task>

<task type="auto">
  <name>Task 2: Fix test_swap_sol_sell.rs (5 failures)</name>
  <files>programs/tax-program/tests/test_swap_sol_sell.rs</files>
  <action>
The 5 sell test failures have TWO root causes:
1. Same InvalidAccountData on pool_vault_b (Token-2022 vault fix -- same as Task 1)
2. Missing `wsol_intermediary` account (Phase 48 addition)

**Fix approach:**
1. Apply the same Token-2022 vault fix from Task 1.
2. Create the `wsol_intermediary` account in the test setup:
   - Derive the PDA using the seed from `programs/tax-program/src/constants.rs` (WSOL_INTERMEDIARY_SEED)
   - The wsol_intermediary is a Token account (WSOL) owned by a PDA. Check the on-chain constraint to understand:
     - What seeds derive the PDA
     - What token program it uses (SPL Token for WSOL)
     - What mint it holds (NATIVE_MINT / WSOL)
   - Initialize it in the test harness before calling the sell instruction
3. If the sell instruction has other new accounts from Phases 46-50, add those too.
4. The sell path now does transfer-close-distribute-reinit on the wsol_intermediary (Phase 48). Ensure the test setup gives it the right initial state (it should be a valid WSOL token account with some rent-exempt balance if needed, or the instruction may initialize it).

Apply the fix to all 5 failing sell tests. If helper functions are shared between buy and sell test files, extract the Token-2022 vault fix into a shared helper to avoid duplication.
  </action>
  <verify>
Run `cargo test -p tax-program --test test_swap_sol_sell` (from project root with env sourced).
All 5 previously-failing tests must pass. Zero new failures.
Then run `cargo test -p tax-program --tests` to confirm ALL Tax Program tests pass (both files + other test files like test_swap_exempt, test_carnage_signer_pda, test_swap_profit_buy, test_swap_profit_sell).
  </verify>
  <done>5/5 test_swap_sol_sell tests pass, AND full Tax Program test suite passes (including the 5 buy tests from Task 1 + all other Tax tests)</done>
</task>

</tasks>

<verification>
Run `cargo test -p tax-program --tests` from project root with environment sourced:
```bash
source "$HOME/.cargo/env" && export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH" && cargo test -p tax-program --tests
```
Expected: 10 previously-failing tests now pass. All other Tax tests continue to pass. Zero test failures.
</verification>

<success_criteria>
- `cargo test -p tax-program --tests` shows 0 failures
- All 10 previously-failing tests (5 buy + 5 sell) now pass
- No on-chain code was modified (only test harness files)
- wsol_intermediary account properly set up in sell test harness
</success_criteria>

<output>
After completion, create `.planning/phases/51-program-rebuild-devnet-deploy/51-02-SUMMARY.md`
</output>
