---
phase: 63-station-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/station/CarnageStation.tsx
  - app/components/modal/ModalShell.tsx
autonomous: false

must_haves:
  truths:
    - "Carnage Cauldron modal opens with riveted-paper 9-slice frame (same as Swap Machine)"
    - "Vault balance, lifetime stats, and recent events display correctly with kit components"
    - "Text is legible on parchment background (dark ink, not light-on-light)"
    - "Mobile viewport shows fullscreen slide-up without outer frame"
  artifacts:
    - path: "app/components/station/CarnageStation.tsx"
      provides: "Self-contained Carnage station with inlined display logic"
      min_lines: 200
    - path: "app/components/modal/ModalShell.tsx"
      provides: "chromeVariant: 'kit-frame' for carnage station"
      contains: "chromeVariant: 'kit-frame'"
  key_links:
    - from: "app/components/station/CarnageStation.tsx"
      to: "@/components/kit"
      via: "import { Card, Divider, Scrollbar } from '@/components/kit'"
      pattern: "import.*from.*@/components/kit"
    - from: "app/components/modal/ModalShell.tsx"
      to: "carnage station"
      via: "STATION_META.carnage.chromeVariant = 'kit-frame'"
      pattern: "carnage.*kit-frame"
    - from: "app/components/station/CarnageStation.tsx"
      to: "no dashboard dependency"
      via: "CarnageCard import removed, display logic inlined"
      pattern: "^(?!.*@/components/dashboard)"
---

<objective>
Polish the Carnage Cauldron station modal with the Phase 60 component kit.

Purpose: Replace the dark-themed dashboard card styling with kit-frame riveted parchment treatment, making Carnage visually consistent with the polished Swap Machine station. Also resolves the CarnageCard import dependency so Wave 3 can safely delete the dashboard directory.

Output: Self-contained CarnageStation.tsx with inlined display logic using kit Card, Divider, and Scrollbar components. ModalShell flipped to kit-frame for carnage station.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/63-station-polish/63-CONTEXT.md
@.planning/phases/63-station-polish/63-RESEARCH.md

# Key source files to read before starting:
@app/components/station/CarnageStation.tsx
@app/components/dashboard/CarnageCard.tsx
@app/components/modal/ModalShell.tsx
@app/components/kit/Card.tsx
@app/components/kit/Divider.tsx
@app/components/kit/Scrollbar.tsx
@app/components/kit/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CarnageStation with inlined display logic and kit components</name>
  <files>app/components/station/CarnageStation.tsx</files>
  <action>
Rewrite CarnageStation.tsx to be self-contained (no import from @/components/dashboard/CarnageCard).

1. **Remove** the import: `import { CarnageCard } from '@/components/dashboard/CarnageCard';`

2. **Add kit imports**: `import { Card, Divider, Scrollbar } from '@/components/kit';`

3. **Inline all format helpers** from CarnageCard.tsx into CarnageStation.tsx:
   - `const TOKEN_DECIMALS = 6;`
   - `formatTokenBurned(baseUnits: number): string`
   - `formatSol(lamports: number): string`
   - `formatEventDate(timestamp: string): string`
   - `import type { CarnageEvent } from '@/hooks/useCarnageEvents';`
   - `import { carnageActionLabel } from '@/hooks/useCarnageEvents';`
   - `import { solscanTxUrl } from '@/lib/solscan';`

4. **Keep** the three existing hook calls exactly as they are:
   ```tsx
   const { carnageData, loading: carnageLoading, error: carnageError } = useCarnageData();
   const { events: carnageEvents, loading: eventsLoading } = useCarnageEvents();
   const { epochState } = useEpochState();
   ```

5. **Keep** the existing prop derivation pattern (nullish coalescing to pass data through).

6. **Inline the CarnageCard display logic** directly in the return JSX, replacing old CSS patterns with kit components:

   - **Outer container**: Replace `<div className="bg-factory-surface border border-factory-border rounded-lg p-6">` with `<Card header="Carnage Fund">`. Since we're under kit-frame chromeVariant, use `frame="css"` (the outer modal frame already provides the riveted parchment via border-image; inner cards use CSS mode for rounded sections).

   - **Error banner**: Keep the error `<p>` tag, but place it inside the Card body.

   - **Vault Balance section**: Keep the existing vault balance display structure (loading skeleton, formatted SOL amount, fallback). No Card wrapping needed -- it's a top-level metric inside the main Card.

   - **Lifetime Stats section**: Replace `<div className="border-t border-factory-border-subtle pt-3 space-y-2">` with `<Divider className="my-3" />` followed by the stats content. Keep the 5 stat rows (CRIME burned, FRAUD burned, SOL spent, Total Triggers, Last Trigger) with their existing flex layout and loading skeletons.

   - **Recent Events section**: Replace `<div className="border-t border-factory-border-subtle pt-3 mt-3">` with `<Divider className="my-3" />`. Wrap the events list in `<Scrollbar className="max-h-64">` (cap at ~256px to prevent the event list from dominating the modal). Each event card: replace `<div className="bg-factory-surface-elevated/50 border border-factory-border-subtle/50 rounded p-2">` with `<Card className="p-2">` (small inner cards for each event).

7. **Text color handling on parchment**: The kit-frame chromeVariant remaps `--color-factory-text` from light to dark ink. Most text classes (`text-factory-text`, `text-factory-text-secondary`, `text-factory-text-muted`) will auto-inherit the dark ink. However, loading skeletons (`bg-factory-surface-elevated`) and event cards with dark backgrounds need local CSS variable overrides. Add a wrapper class or inline style `style={{ '--color-factory-text': 'var(--color-dark-ink, #2a1f0e)' } as React.CSSProperties}` if needed -- or simply use `<Card>` which handles its own background.

   **Key insight from Phase 62**: Under `modal-chrome-kit`, the `station-content` wrapper goes transparent. Inner `<Card>` components with CSS mode provide their own subtle background that works on parchment. Do NOT use `bg-factory-surface` directly -- use kit Card which has its own theming.

8. **Compute lastTriggerText** exactly as in CarnageCard lines 98-111 (epochs ago logic).

9. **Preserve all existing Tailwind utility classes** for layout (flex, space-y, text-xs, font-mono, etc.) -- only replace structural/surface classes with kit components.

  </action>
  <verify>
- `grep -r "from.*@/components/dashboard" app/components/station/CarnageStation.tsx` returns 0 hits
- `grep "from.*@/components/kit" app/components/station/CarnageStation.tsx` returns a hit
- File compiles: `cd /Users/mlbob/Projects/Dr\ Fraudsworth/app && npx tsc --noEmit 2>&1 | grep -i "CarnageStation" | head -5` returns no errors
  </verify>
  <done>CarnageStation.tsx is self-contained with zero dashboard imports. All format helpers inlined. Display uses Card, Divider, Scrollbar from kit. Hook calls unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Flip chromeVariant to kit-frame for carnage station</name>
  <files>app/components/modal/ModalShell.tsx</files>
  <action>
In ModalShell.tsx, change the `carnage` entry in STATION_META (line 48) from:

```typescript
carnage: { title: 'Carnage Cauldron', maxWidth: '700px', chromeVariant: 'classic' },
```

to:

```typescript
carnage: { title: 'Carnage Cauldron', maxWidth: '700px', chromeVariant: 'kit-frame' },
```

This single-line change activates the 9-slice riveted-paper border-image frame, removes the classic header (title + close button), shows the floating close button outside the frame, and remaps station-content text to dark ink on parchment.

Do NOT modify any other station entries -- staking, wallet, docs, settings stay 'classic' for now.
  </action>
  <verify>
- `grep "carnage.*kit-frame" app/components/modal/ModalShell.tsx` returns a match
- `grep "staking.*classic" app/components/modal/ModalShell.tsx` returns a match (unchanged)
  </verify>
  <done>Carnage station uses kit-frame chromeVariant. Other stations remain classic.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Carnage Cauldron station polished with riveted-paper frame and kit components. CarnageCard display logic inlined into CarnageStation (dashboard import removed). All vault balance, lifetime stats, and recent events display with kit Card, Divider, and Scrollbar components.</what-built>
  <how-to-verify>
1. Open the app (Railway or local dev server)
2. Click the Carnage Cauldron machine in the factory scene
3. Verify: Modal opens with riveted-paper parchment frame (same style as Swap Machine)
4. Verify: No classic header (title bar) -- floating close button in top-right corner
5. Verify: Vault balance displays correctly (number + "SOL" label)
6. Verify: Lifetime stats section shows 5 rows (CRIME Burned, FRAUD Burned, SOL Spent, Total Triggers, Last Trigger)
7. Verify: Recent events section displays event cards (or "No Carnage events recorded yet" if empty)
8. Verify: All text is legible -- dark ink on parchment, no invisible text
9. Verify: Dividers separate the sections cleanly
10. Resize browser to mobile width (<1024px): verify fullscreen slide-up, no outer frame, content still readable
11. Check browser console: no errors
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 2, or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `grep -r "from.*@/components/dashboard" app/components/station/` returns 0 hits (CarnageCard import removed)
- `grep "carnage.*kit-frame" app/components/modal/ModalShell.tsx` returns a match
- `npm run build` completes without errors
- Visual: Carnage Cauldron modal shows riveted-paper frame with legible parchment-themed content
</verification>

<success_criteria>
- CarnageStation.tsx is self-contained (no dashboard imports)
- Carnage modal uses kit-frame chromeVariant
- Kit components (Card, Divider, Scrollbar) replace raw Tailwind surface/border classes
- All data (vault balance, stats, events) displays correctly
- Text legible on parchment at both desktop and mobile viewports
</success_criteria>

<output>
After completion, create `.planning/phases/63-station-polish/63-01-SUMMARY.md`
</output>
