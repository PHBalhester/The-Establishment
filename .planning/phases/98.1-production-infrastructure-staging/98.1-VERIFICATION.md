---
phase: 98.1-production-infrastructure-staging
verified: 2026-03-15T19:00:00Z
status: gaps_found
score: 7/8 must-haves verified
gaps:
  - truth: "UptimeRobot monitors active for frontend, API health, and crank health"
    status: partial
    reason: "Monitors were created but left in paused state. ROADMAP success criterion 8 and Plan 03 Output explicitly require 'active' monitors. Monitors created paused serve no monitoring function until launch."
    artifacts: []
    missing:
      - "UptimeRobot monitors unpaused/activated (external dashboard action)"
      - "Or: explicit phase goal amendment confirming 'paused until launch' is acceptable for this phase's done criteria"
  - truth: "REQUIREMENTS.md tracking entries updated for INFRA-09 and INFRA-12"
    status: failed
    reason: "Both INFRA-09 and INFRA-12 remain marked '[ ] Pending' in REQUIREMENTS.md, and requirements-completed: [] in all three plan SUMMARYs. The work was done but requirement tracking was not closed."
    artifacts:
      - path: ".planning/REQUIREMENTS.md"
        issue: "Line 20: '- [ ] **INFRA-09**' — still unchecked despite Helius mainnet keys provisioned and verified"
      - path: ".planning/REQUIREMENTS.md"
        issue: "Line 23: '- [ ] **INFRA-12**' — still unchecked despite Railway production services created"
    missing:
      - "Mark INFRA-09 as [x] in REQUIREMENTS.md"
      - "Mark INFRA-12 as [x] in REQUIREMENTS.md"
      - "Update phase mapping table rows at lines 134 and 137 to show 'Done' status"
---

# Phase 98.1: Production Infrastructure Staging Verification Report

**Phase Goal:** All production infrastructure provisioned before mainnet launch day -- Cloudflare DNS with CNAME flattening for apex domain, Railway mainnet services (web + crank + Postgres), Helius mainnet RPC keys + webhook, environment-driven CSP, and UptimeRobot monitoring

**Verified:** 2026-03-15T19:00:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CSP dynamically includes cluster-appropriate Helius URLs based on NEXT_PUBLIC_CLUSTER | VERIFIED | `app/next.config.ts` lines 10-17: cluster detection + heliusRpcDomain conditional. Both `mainnet.helius-rpc.com` (4 occurrences) and `devnet.helius-rpc.com` (4 occurrences) present. NEXT_PUBLIC_CLUSTER read at line 10. |
| 2 | webhook-manage.ts uses correct API base and webhook type per cluster | VERIFIED | `scripts/webhook-manage.ts` lines 39-50: CLUSTER detection, HELIUS_API_BASE conditional (api-mainnet.helius-rpc.com vs api.helius.xyz), WEBHOOK_TYPE ternary. `rawDevnet` appears only in the ternary (line 50), not hardcoded in create/update. |
| 3 | Cloudflare manages DNS for fraudsworth.fun with CNAME flattening to Railway | VERIFIED | `dig NS fraudsworth.fun +short` returns `barbara.ns.cloudflare.com` and `anirban.ns.cloudflare.com`. `dig A fraudsworth.fun +short` returns Cloudflare IPs `172.67.139.219` and `104.21.65.23`. STATE.md records CNAME targets: apex -> auupp37g.up.railway.app (proxied). |
| 4 | Railway mainnet-web, mainnet-crank, mainnet-postgres services exist with correct env var templates | VERIFIED | 98.1-02-SUMMARY.md documents creation with 12 and 9 env vars respectively. `app/.env.mainnet` (4005 bytes) and `.env.mainnet` (4818 bytes) provide the reference templates with NEXT_PUBLIC_CLUSTER=mainnet. HELIUS_WEBHOOK_SECRET populated in .env.mainnet. |
| 5 | Helius mainnet API keys generated and verified working | VERIFIED | 98.1-02-SUMMARY.md documents both primary ([REDACTED-MAINNET-KEY]) and fallback ([REDACTED-FALLBACK-KEY]) keys verified via `getHealth` returning `"result":"ok"`. Keys are separate from devnet key ([REDACTED-DEVNET-KEY] in .env.devnet). |
| 6 | fraudsworth.fun resolves to Railway mainnet-web with valid SSL | VERIFIED | `curl -s -o /dev/null -w "%{http_code} %{ssl_verify_result}" https://fraudsworth.fun` returns `404 0` (404 = no app deployed yet, expected; ssl_verify_result=0 = SSL OK). Response headers confirm `server: cloudflare` and `cf-ray` header present. |
| 7 | Mainnet Helius webhook registered pointing to fraudsworth.fun | VERIFIED | 98.1-03-SUMMARY.md documents webhook ID `7ec4fa52-171b-4100-a613-3bc52e321369`, type=raw, URL=https://fraudsworth.fun/api/webhooks/helius, auth header set, monitoring Tax (43fZ) + Epoch (4Heq) programs. |
| 8 | UptimeRobot monitors active for frontend, API health, and crank health | PARTIAL | 3 monitors were created but left paused. ROADMAP criterion 8 states "active". Plan 03 Output states "3 UptimeRobot monitors active". The 03-SUMMARY key-decisions records "UptimeRobot monitors created paused -- will activate at launch" as a deliberate decision but this deviates from the stated goal. |

**Score: 7/8 truths verified**

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/next.config.ts` | Environment-driven CSP with cluster-aware Helius URLs | VERIFIED | 122 lines, no stubs. Exports `nextConfig`. Cluster detection at lines 10-11. `isMainnet` drives 3 conditional blocks (RPC domain, API sources, docs frame sources). CSP template literal interpolates all three at lines 41-43. |
| `scripts/webhook-manage.ts` | Cluster-aware webhook management (API base + webhook type) | VERIFIED | 255 lines, no stubs. Full CRUD implementation (list/create/update/delete). Cluster detection lines 39-42. Both `HELIUS_API_BASE` and `WEBHOOK_TYPE` are cluster-conditional. `WEBHOOK_TYPE` used in both `createWebhook()` (line 148) and `updateWebhook()` (line 180). Startup logging at lines 52-55. |
| `app/.env.mainnet` | Mainnet frontend env var reference template | VERIFIED | Exists (4005 bytes). Contains NEXT_PUBLIC_CLUSTER=mainnet, HELIUS_RPC_URL pointing to mainnet.helius-rpc.com, HELIUS_WEBHOOK_SECRET placeholder. Note: missing NEXT_PUBLIC_SITE_MODE (set in Railway per SUMMARY but not in template — minor template incompleteness). |
| `.env.mainnet` | Mainnet crank/deploy env file | VERIFIED | Exists (4818 bytes). Contains HELIUS_API_KEY, CLUSTER_URL (mainnet), HELIUS_WEBHOOK_SECRET (populated: [REDACTED-WEBHOOK-SECRET]...). CHANGE_ME_MAINNET placeholders for runtime secrets (keypair, treasury) as expected at this stage. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `app/next.config.ts` | `process.env.NEXT_PUBLIC_CLUSTER` | build-time env var | VERIFIED | Line 10: `const cluster = process.env.NEXT_PUBLIC_CLUSTER \|\| "devnet"`. Cluster drives all three CSP conditional blocks. Defaults to devnet (safe). |
| `scripts/webhook-manage.ts` | `process.env.CLUSTER` | runtime env var | VERIFIED | Lines 39-41: `(process.env.CLUSTER \|\| process.env.NEXT_PUBLIC_CLUSTER \|\| "devnet").toLowerCase()`. Both vars checked, devnet default. |
| `webhook-manage.ts` WEBHOOK_TYPE | `createWebhook()` body | WEBHOOK_TYPE constant | VERIFIED | Line 148: `webhookType: WEBHOOK_TYPE`. Not hardcoded. |
| `webhook-manage.ts` WEBHOOK_TYPE | `updateWebhook()` body | WEBHOOK_TYPE constant | VERIFIED | Line 180: `webhookType: WEBHOOK_TYPE`. Not hardcoded. |
| GoDaddy NS records | Cloudflare nameservers | NS delegation | VERIFIED | `dig NS fraudsworth.fun +short` returns Cloudflare NS. |
| Cloudflare CNAME | Railway mainnet-web | CNAME flattening | VERIFIED | Cloudflare IPs confirmed via `dig A`. `curl -I https://fraudsworth.fun` returns `server: cloudflare` with Railway fallback headers. |
| www.fraudsworth.fun | fraudsworth.fun | 301 redirect | VERIFIED | `curl -w "%{redirect_url} %{http_code}"` returns `https://fraudsworth.fun/ 301`. |

---

### Requirements Coverage

| Requirement | Definition | Status | Notes |
|-------------|-----------|--------|-------|
| INFRA-09 | Fresh Helius mainnet API key provisioned, separate from devnet key | SATISFIED (untracked) | Primary ([REDACTED-MAINNET-KEY]) and fallback ([REDACTED-FALLBACK-KEY]) keys created, both verified via `getHealth`. Separate from devnet key ([REDACTED-DEVNET-KEY]). Work done; REQUIREMENTS.md checkbox still unchecked. |
| INFRA-12 | Railway production environment configured separately from devnet | SATISFIED (untracked) | Three services (mainnet-web, mainnet-crank, mainnet-postgres) created with NEXT_PUBLIC_CLUSTER=mainnet and separate Postgres instance. Work done; REQUIREMENTS.md checkbox still unchecked. |

**Critical finding:** Neither INFRA-09 nor INFRA-12 has been marked complete in REQUIREMENTS.md (both remain `- [ ]` with status "Pending"). The `requirements-completed: []` field in all three plan SUMMARYs confirms no requirement was explicitly closed during this phase. This is a tracking gap that blocks milestone closure.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.planning/REQUIREMENTS.md` | 20, 23, 134, 137 | INFRA-09 and INFRA-12 unchecked despite work complete | Warning | Milestone tracking shows these requirements open; will cause confusion at Phase 100 checklist review |
| `app/.env.mainnet` | — | Missing `NEXT_PUBLIC_SITE_MODE` entry | Info | SITE_MODE was set in Railway per 02-SUMMARY but omitted from template file; future deploys using template as checklist may miss this variable |

No blocker anti-patterns found in code files (`app/next.config.ts`, `scripts/webhook-manage.ts`).

---

### Human Verification Required

#### 1. UptimeRobot Monitor Status

**Test:** Log into UptimeRobot account and confirm whether monitors are paused or active.
**Expected per ROADMAP criterion 8:** Monitors should be active (green, checking on 5-min intervals).
**Why human:** UptimeRobot is an external service; cannot verify monitor state programmatically.
**Decision needed:** Either activate all 3 monitors now (monitoring a paused Railway 404 is fine — alerts just won't fire until launch), or explicitly accept that "paused until launch" is the correct state for this phase.

#### 2. Railway Mainnet Service Env Var Completeness

**Test:** In Railway Dashboard, verify mainnet-web service contains NEXT_PUBLIC_SITE_MODE=launch (noted in 02-SUMMARY as set, but absent from app/.env.mainnet template).
**Expected:** `NEXT_PUBLIC_SITE_MODE=launch` present in Railway mainnet-web variables.
**Why human:** Railway dashboard is not accessible programmatically; cannot verify env var contents directly.

#### 3. Helius Webhook Registration Confirmation

**Test:** Run `CLUSTER=mainnet HELIUS_API_KEY=<key> WEBHOOK_URL=https://fraudsworth.fun/api/webhooks/helius npx tsx scripts/webhook-manage.ts list` and confirm webhook 7ec4fa52 appears.
**Expected:** Webhook listed with type=raw, URL=fraudsworth.fun, accounts=[43fZ..., 4Heq...].
**Why human:** Requires live Helius API key credentials not present in repo. Verifies the registered webhook persists and wasn't inadvertently deleted.

---

### Gaps Summary

Two gaps found:

**Gap 1 — UptimeRobot monitors paused (partial):** The ROADMAP success criterion 8 and Plan 03 Output both say monitors should be "active". The 03-SUMMARY records a deliberate decision to leave them paused until launch. This is a reasonable operational decision (no point alerting on a 404 before the app is deployed) but it technically does not satisfy the stated criterion. Resolution: either unpause the monitors now, or accept this deviation and amend the phase success criteria.

**Gap 2 — REQUIREMENTS.md tracking not closed:** INFRA-09 and INFRA-12 remain marked Pending despite both being fully satisfied by this phase's work. The requirements table at lines 134 and 137 still maps them to "Phase 92" with no Done status. This does not block the infrastructure from working, but it will cause confusion at milestone review. Resolution: mark both requirements [x] and update the mapping table.

Neither gap blocks the infrastructure from functioning. The code artifacts (CSP, webhook-manage.ts), DNS, domain binding, Helius keys, Railway services, and mainnet webhook are all verified. The gaps are an operational decision (monitors paused) and a tracking omission (requirement checkboxes unchecked).

---

*Verified: 2026-03-15T19:00:00Z*
*Verifier: Claude (gsd-verifier)*
