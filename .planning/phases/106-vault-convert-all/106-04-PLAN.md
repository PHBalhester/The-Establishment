---
phase: 106-vault-convert-all
plan: 04
type: execute
wave: 3
depends_on: ["106-01", "106-02", "106-03"]
files_modified: []
autonomous: false
requirements: [VAULT-05]

must_haves:
  truths:
    - "Vault program upgraded on devnet in-place (not fresh deploy)"
    - "Direct vault convert (CRIME->PROFIT, PROFIT->FRAUD) works via frontend"
    - "2-hop SOL->PROFIT multi-hop completes without intermediate token leakage"
    - "4-step split SOL->PROFIT route completes without intermediate token leakage"
    - "40+ SOL multi-hop swap simulates cleanly in Phantom without Blowfish warnings"
    - "Old convert instruction still works on upgraded program (backwards compatible)"
  artifacts: []
  key_links:
    - from: "app/lib/swap/swap-builders.ts"
      to: "programs/conversion-vault (deployed)"
      via: "on-chain convertV2 instruction"
      pattern: "convertV2"
    - from: "app/lib/swap/multi-hop-builder.ts"
      to: "programs/conversion-vault (deployed)"
      via: "amount_in=0 convert-all mode"
      pattern: "effectiveAmountIn.*0"
---

<objective>
Deploy the upgraded vault program to devnet (in-place upgrade, not fresh deploy) and run the complete wallet simulation test matrix across all affected routes, sizes, and wallets.

Purpose: This is the final verification gate before mainnet. The user requires "full wallet simulation before ANY mainnet upgrade." We must prove that: (1) the upgrade works in-place (mirrors mainnet path), (2) all 6 affected multi-hop routes work without intermediate token leakage, (3) large swaps (40+ SOL) simulate cleanly in Phantom/Backpack without Blowfish "malicious" warnings, and (4) the old convert instruction still works (backwards compatibility).

Output: Verified devnet deployment + wallet simulation results documenting all route/size/wallet combinations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-vault-convert-all/106-CONTEXT.md
@.planning/phases/106-vault-convert-all/106-RESEARCH.md
@.planning/phases/106-vault-convert-all/106-01-SUMMARY.md
@.planning/phases/106-vault-convert-all/106-02-SUMMARY.md
@.planning/phases/106-vault-convert-all/106-03-SUMMARY.md

@Docs/vault-convert-all-proposal.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build with devnet feature flag and deploy in-place upgrade to devnet</name>
  <files></files>
  <action>
    **CRITICAL**: This is an IN-PLACE upgrade of the existing devnet vault program, NOT a fresh deploy. This mirrors the real mainnet upgrade path.

    1. **Build with devnet feature**:
       ```bash
       source "$HOME/.cargo/env"
       export PATH="/Users/mlbob/.local/share/solana/install/active_release/bin:$PATH"

       # Step 0: Sync program IDs from keypairs (build.sh does this)
       # Step 1: Build all programs (needed for IDL)
       anchor build
       # Step 2: Build vault with devnet feature (overrides mint addresses)
       anchor build -p conversion_vault -- --features devnet
       ```

    2. **Verify the built binary has convert_v2**:
       Check IDL: `cat target/idl/conversion_vault.json | grep convertV2`

    3. **Deploy in-place upgrade** using the existing vault keypair:
       ```bash
       solana program deploy \
         --program-id keypairs/vault-keypair.json \
         --keypair keypairs/devnet-wallet.json \
         --url devnet \
         target/deploy/conversion_vault.so
       ```
       The program ID must remain `5uawA6ehYTu69Ggvm3LSK84qFawPKxbWgfngwj15NRJ` (or whatever the Phase 95 clean deploy address is -- check deployments/devnet.json or PROGRAM_IDS.VAULT).

    4. **Sync IDL to app**:
       ```bash
       export PATH="/opt/homebrew/bin:$PATH"
       node scripts/sync-idl.mjs
       ```

    5. **Verify the upgrade**:
       ```bash
       solana program show --url devnet $(solana-keygen pubkey keypairs/vault-keypair.json)
       ```
       Confirm program is deployed and the data length has changed (new instruction adds to program size).

    **IMPORTANT**: If the deployer wallet has insufficient SOL for the upgrade buffer, airdrop or fund it first. Check with `solana balance keypairs/devnet-wallet.json --url devnet`.

    **PITFALL**: Per MEMORY.md "Feature Flag Build Order" -- building without `--features devnet` uses mainnet mint addresses. The two-step build (anchor build, then anchor build -p conversion_vault -- --features devnet) is the correct sequence.
  </action>
  <verify>
    1. `solana program show --url devnet $(solana-keygen pubkey keypairs/vault-keypair.json)` shows deployed program
    2. Program ID matches the existing vault program ID (no new program created)
    3. IDL in app/idl/ contains both `convert` and `convertV2` instructions
  </verify>
  <done>
    - Vault program upgraded in-place on devnet with convert_v2 instruction
    - Program ID unchanged from pre-upgrade
    - IDL synced to app with both convert and convertV2
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Vault program upgraded on devnet with convert_v2 instruction. Client updated to use convertV2 for all vault convert paths with convert-all mode (amount_in=0) for multi-hop steps. SOS audit cleared.
  </what-built>
  <how-to-verify>
    **Test matrix to verify on the running frontend (Railway or localhost):**

    Before testing, ensure the frontend is running with the updated code and synced IDL.

    **A. Direct vault converts (1-hop, exact amount):**
    1. CRIME -> PROFIT: Convert ~1000 CRIME to PROFIT. Expect exactly 10 PROFIT received.
    2. PROFIT -> FRAUD: Convert ~10 PROFIT to FRAUD. Expect exactly 1000 FRAUD received.
    3. Verify wallet preview shows clean simulation (no warnings).

    **B. Multi-hop 2-hop routes (convert-all mode):**
    4. SOL -> CRIME -> PROFIT (0.05 SOL): Should complete. Check wallet -- ZERO leftover CRIME.
    5. SOL -> FRAUD -> PROFIT (0.05 SOL): Should complete. Check wallet -- ZERO leftover FRAUD.
    6. PROFIT -> CRIME -> SOL (small amount): Should complete. Check wallet -- ZERO leftover CRIME.
    7. PROFIT -> FRAUD -> SOL (small amount): Should complete. Check wallet -- ZERO leftover FRAUD.

    **C. Multi-hop split routes (4-step, convert-all mode):**
    8. SOL -> PROFIT split (0.5 SOL): Both legs should complete. ZERO leftover CRIME or FRAUD.
    9. PROFIT -> SOL split (small amount): Both legs should complete. ZERO leftover CRIME or FRAUD.

    **D. Large swap wallet simulation (the primary UX fix):**
    10. SOL -> PROFIT (4+ SOL if devnet balance allows, or largest available):
        - In Phantom: Does the wallet preview show ONLY "-SOL, +PROFIT" (no CRIME/FRAUD)?
        - Does Blowfish allow the transaction (no "malicious" or "unfair trade" warning)?
    11. Repeat in Backpack if available.

    **E. Backwards compatibility:**
    12. If possible, test that old convert instruction still works by sending a raw transaction with the old instruction discriminator (or verify that the program didn't remove it by checking IDL still has `convert`).

    **What to look for:**
    - ZERO intermediate CRIME or FRAUD tokens remaining in wallet after ANY multi-hop swap
    - Wallet previews show clean token balance changes (only input/output, no intermediaries)
    - No Blowfish warnings on any swap size
    - All routes complete successfully (no Custom error codes)
  </how-to-verify>
  <resume-signal>Type "approved" with any notes on routes tested, or describe specific failures to fix</resume-signal>
</task>

</tasks>

<verification>
1. Vault program upgraded in-place on devnet (same program ID)
2. All 6 affected multi-hop routes tested (A+B+C above)
3. Large swap wallet simulation passed in at least Phantom (D above)
4. Zero intermediate token leakage observed across all routes
5. No Blowfish "malicious" or "unfair trade" warnings
6. Old convert instruction still exists in IDL (backwards compatible)
</verification>

<success_criteria>
- Devnet vault program upgraded in-place with convert_v2
- At least 6 multi-hop routes tested without intermediate token leakage
- Large swap (4+ SOL) simulates cleanly in Phantom (no Blowfish warning)
- Zero intermediate CRIME/FRAUD tokens left in wallet after any multi-hop swap
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/106-vault-convert-all/106-04-SUMMARY.md`
</output>
