[build]
builder = "NIXPACKS"
buildCommand = "cd docs-site && npm install && npm run build"

[deploy]
startCommand = "cd docs-site && npm start"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
