# H003: Block all npm lifecycle scripts by default to prevent supply chain attacks.
# npm packages can execute arbitrary code during install via preinstall/postinstall.
# If a package legitimately needs scripts (e.g., native modules), run:
#   npm rebuild <package>
ignore-scripts=true
